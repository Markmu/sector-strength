# plan-03 前端双视图页面 - 验收报告

- 功能 ID：plan-03（前端双视图页面）
- 维度：frontend
- 当前状态：review
- 验收日期：2026-07-23
- 验收方式：纯代码审查（未修改任何代码）
- 验收结论：**未通过（E2E 质量证据缺失，需用户裁决是否豁免）**

## 总览结论

代码实现质量高，文件交付完整、契约对齐、type-check/build 全绿、实现规格符合度好，无正确性/运行时 blocker。**唯一阻断项是维度 6（E2E red/green 证据）缺失**：plan-03 §6 明确要求 `e2e/sector-fund-flow.spec.ts`，但仓库中无该 spec、无任何覆盖 13 期 plan-03 的 red/green 证据文件。

按 task-review skill 规则：前端用户可观察功能通常不豁免 E2E。故本次报告为「未通过」，是否豁免由用户决定。

## 各维度结论

| 维度 | 结论 | 说明 |
| --- | --- | --- |
| 1. 文件交付完整性 | 通过 | 5 个清单文件全部交付 + 4 个合理拆分的子组件 |
| 2. Task 列表完成度 | 通过 | 9/9 Task 全 done |
| 3. AC 覆盖审查 | 通过（代码层） | AC-01~AC-10/AC-12 交互均在代码实现；checklist 未勾选（文档未回填） |
| 4. 实现规格符合度 | 通过 | 泛型/endpoint/解包/双视图/状态/导航/类型 全符合 |
| 5. 验证命令执行 | 通过 | type-check src/ 零错误；next build 成功 |
| 6. E2E 质量证据 | **未通过** | 无 sector-fund-flow.spec.ts，无 13 期 red/green 证据 |
| 7. 边界与风险 | 通过 | 6/6 边界场景全 done |
| 8. 契约对齐 | 通过 | 类型/字段/泛型与后端 _dict_to_camel 输出一致 |
| 9. 代码审查 | 通过 | 无 blocker；2 个建议项 |
| 10. 执行验证 | N/A | 本功能无 task handler |

## 维度 1：文件交付完整性 — 通过

清单 5 个文件全部交付且非空：
- create `web/src/types/fundFlowTypes.ts`（88 行，camelCase 类型）
- create `web/src/hooks/useSectorFundFlow.ts`（143 行，3 个 SWR hook）
- create `web/src/app/dashboard/sector-fund-flow/page.tsx`（26 行路由壳，'use client'）
- modify `web/src/lib/api.ts`（1096-1143 行新增 sectorFundFlowApi）
- modify `web/src/components/dashboard/DashboardLayout.tsx`（27-31 行新增导航项）

额外新增子组件（合理拆分，未在清单但属同一功能模块，非风险项）：
- `web/src/components/sector-fund-flow/SectorFundFlowPage.tsx`（主页面逻辑）
- `web/src/components/sector-fund-flow/FundFlowRankingTable.tsx`（排行表格）
- `web/src/components/sector-fund-flow/FundFlowTimeseriesChart.tsx`（变化曲线）
- `web/src/components/sector-fund-flow/helpers.ts`（金额/百分比/色标格式化）

注：page.tsx 为薄路由壳，业务全部在 SectorFundFlowPage 组件中（单文件 20KB 行数合理，拆分得当）。

## 维度 2：Task 列表完成度 — 通过

plan-03 §4 Task 列表 9/9 全 done（api/类型/hooks/页面/导航/排行视图/曲线视图/三态/验证）。无 todo、无非法状态、无未带原因的 waived。

## 维度 3：AC 覆盖审查 — 通过（代码层）

逐条核对代码实现：
- AC-01 排行默认态：`useState<SectorType>('industry')` + `sortBy:'net_inflow'` + `order:'desc'`（SectorFundFlowPage.tsx:54/58/59），默认即净额降序排行 ✅
- AC-02 维度切换：`handleSectorTypeChange` 重置 page + 清空已选板块（:111-117）✅
- AC-03 排序：COLUMNS 中仅 inflow/outflow/netInflow `sortable:true`，`handleSortClick` 切换升降序+箭头（FundFlowRankingTable.tsx:42-98），不可排序列无 onClick ✅
- AC-04 日期切换：date input + `handleDateChange`/`handleResetDate`，空数据显示空态 ✅
- AC-05 变化视图未选引导态：`selectedSectors.length===0` → 引导态「请选择要对比的板块」不画坐标系（:460-471）✅
- AC-06 板块叠加+图例+移除：chip 可移除 + 候选清单多选 + ECharts legend + connectNulls ✅
- AC-07 刷新延长：`handleRefresh` 调 `mutateTimeseries()`（:173-179），刷新按钮 spin 动画 ✅
- AC-08 空态/历史回看：`!timeseries?.hasData || series.length===0` → 空态「暂无盘中采样数据」+ 日期切换回看 ✅
- AC-09 失败重试两视图独立：排行 `fund-flow-ranking-retry`→mutateRankings，曲线 `fund-flow-timeseries-retry`→mutateTimeseries ✅
- AC-10 跳转：`item.sectorId !== null` 才渲染 button + `router.push('/dashboard/sector-analysis/${sectorId}')`（FundFlowRankingTable.tsx:111-114/228-250）✅
- AC-12 分页+翻页滚动顶部：Pagination + `scrollIntoView`（:100-108）✅
- AC-11（管理员触发）属 plan-01/02，不在 plan-03 范围 ✅

注意：plan-03 §5 checklist 项均为未勾选状态（`[ ]`），属文档未回填，非代码缺失。

## 维度 4：实现规格符合度 — 通过

- **sectorFundFlowApi 泛型**：三个方法泛型均写 `{ success: boolean; data: 业务对象 }`（api.ts:1106/1124/1137），与 dev-plan-check 阻断项要求一致，dev-plan-check 提到的「T 直接写业务对象」风险已规避 ✅
- **endpoint 不带 /api/v1**：`/sector-fund-flow/rankings|timeseries|latest-date`，baseURL 已含 `/api/v1`，无双前缀 ✅
- **query snake_case**：sector_type/trade_date/sort_by/order/page/page_size/sector_names 全 snake_case ✅
- **useSectorFundFlow 解包**：`.then(res => res.data as unknown as {success, data: Biz})`，与 useFundCrowdAnalysis 完全一致范式；SWR key 均为数组（`['fundFlowRankings', params]` 等）✅
- **双视图**：排行视图（表格+排序表头+分页+色标红涨绿跌+跳转 sectorId 非 null）+ 变化曲线视图（多选 chip+叠加图+图例+零轴 markLine+刷新 mutate+未选引导态+无采样空态）✅
- **视图/维度切换保留状态**：currentView 与 sectorType 独立 useState，切换视图不重置维度；维度切换清空已选板块 ✅
- **日期切换**：date input + latestDate 默认填充 ✅
- **加载/失败/空三态**：排行（骨架/重试/空态）、曲线（加载文案/重试/引导态/空态）各自独立 ✅
- **DashboardLayout 导航**：「板块资金流」插入在「板块分析」之后（DashboardLayout.tsx:27-31），href 正确 ✅
- **类型 camelCase**：fundFlowTypes.ts 全 camelCase，与后端 `_dict_to_camel` 输出一致 ✅

## 维度 5：验证命令执行 — 通过

注：`pnpm type-check` / `pnpm build` 被 pnpm 前置 install 门拦截（`ERR_PNPM_IGNORED_BUILDS`，与 plan-03 无关的环境问题）。改用直接调用本地工具：

- `node_modules/.bin/tsc --noEmit`：**src/ 下 0 错误**（tests/ 下有大量预存类型错误，与本功能无关，按要求忽略）。已用 `grep -E "^src/"` 过滤确认 src/ 零错误。
- `node_modules/.bin/next build`：**成功**。`✓ Compiled successfully`、`Finished TypeScript`、`Generating static pages (26/26)`。路由表含 `○ /dashboard/sector-fund-flow`。

## 维度 6：E2E 质量证据 — 未通过

plan-03 §6 明确要求：
- `pnpm e2e -- e2e/sector-fund-flow.spec.ts`
- 「实现前先写 red spec（预期失败），完成后转 green」

实际检查：
- `web/tests/e2e/` 下**无 sector-fund-flow.spec.ts**（仅有 fund-crowd-analysis / fund-list / shareholder 等其它功能 spec）
- `grep -r "sector-fund-flow|fund-flow-view|板块资金流" web/tests/` **零命中**
- `docs/e2e/evidence/` 下所有 plan-03-* 证据文件均为 **历史期（05/08/09/10 期）**，最新日期 2026-07-07，与 13 期板块资金流无关
- 无任何覆盖 feat_id=plan-03（13 期）的 red/green 证据文件

结论：缺 red 和 green 两类证据。属「前端用户可观察功能」，task-review skill 规定通常不豁免。**是否豁免由用户裁决**。

## 维度 7：边界与风险 — 通过

plan-03 §8 边界场景表 6/6 全 done：排行无数据/曲线无采样/曲线未选/板块名不匹配/加载失败/维度切换清空。代码均有对应实现。

## 维度 8：契约对齐 — 通过

- 后端 `sector_fund_flow_service.get_rankings` 返回 snake_case（rank/sector_name/sector_id/change_percent/inflow/outflow/net_inflow/company_count/leading_stock/leading_stock_change/current_price），路由 `_dict_to_camel` 转 camelCase，与 `FundFlowRankingItem` 字段逐一对应 ✅
- `get_timeseries` 返回 `series[].data[].sample_time/net_inflow` → `sampleTime/netInflow`，与 `FundFlowSeriesPoint` 对应 ✅
- `latest_date` → `latestDate`，与 `FundFlowLatestDateData` 对应 ✅
- 响应外层 `{success, data}` 包裹，前端泛型与 hook 解包链路一致 ✅
- 下游消费方：无（最终用户功能）✅

## 维度 9：代码审查 — 通过（无 blocker）

blocker：无。泛型解包正确（`res.data` 取 body，非业务对象），无运行时 undefined 风险。跳转路径 `/dashboard/sector-analysis/${sectorId}` 正确。

建议项（非阻塞）：
1. **死代码**（SectorFundFlowPage.tsx:315）：`{hasAnyData === false && !isLatestLoading && !isLatestError ? null : null}` 是恒为 null 的三元，无任何作用，建议删除或实现真正的全局空态。
2. **plan-03 §3 规定维度为 `'industry' | 'concept'`**，代码用全局 `SECTOR_TYPES`（含 `region`）。后端 `is_valid_sector_type` 容错了 region 且 backend Query 默认文档写「industry/concept/region」，故非 blocker，但与 plan 文档字面略有出入。

## 维度 10：执行验证 — N/A

本功能为前端页面，无 task handler / 异步任务处理器，不适用。

## blocker 汇总

1. **【维度 6】E2E red/green 证据缺失**：无 `sector-fund-flow.spec.ts`，无 13 期 plan-03 E2E 证据。需用户决定是否豁免。

## 建议项

1. 删除 SectorFundFlowPage.tsx:315 死代码三元。
2. 回填 plan-03 §5 checklist 勾选状态（当前全 `[ ]` 未勾）。

## 状态变更记录

- 未变更。plan-03 frontmatter 维持 `status: review`。
- 待用户裁决 E2E 豁免：若豁免 → 可补写豁免原因至 §8 后重跑 review 转 done；若不豁免 → 需先经 test-e2e skill 补 red spec + 实现后 green 证据。
