---
feat_id: plan-05
phase: red
date: 2026-07-29
title: ETF 监控页面（EtfMonitorPage 双视图 + 排行表 + 趋势图）— Playwright E2E red 阶段证据（14 期）
source_architecture: docs/14-ETF份额与资金流监控/14-1-架构文档-ETF份额与资金流监控.md
source_plan: docs/14-ETF份额与资金流监控/14-2-实现计划-ETF份额与资金流监控/plan-05-ETF监控页面.md
---

# plan-05 E2E Red 证据：ETF 监控页面（14 期）

## 结论

**预期失败，测试有效。**

- plan-05「ETF 监控页面」是用户可观察的最终功能（指数排行视图 + 历史趋势视图双视图协调），用 **Playwright E2E red 测试**。前置 plan-01~04 均已 done。
- plan-05 范围的 **12 个业务交互用例（TC-5.1~5.12）全部失败**，失败原因均为「业务组件尚未实现」：
  - 7 个用例（TC-5.1~5.6、TC-5.12）→ `element(s) not found`（10.2s）：排行表 / 展开标记 / 错误态 / 分页控件等 `data-testid` 不存在（`etf-index-ranking-table` / `etf-expand-*` / `etf-ranking-error` / `etf-pagination` 等）
  - 5 个用例（TC-5.7~5.11，趋势视图组）→ `locator.click: Test timeout of 30000ms exceeded`：趋势视图切换按钮 / 对象选择器等 testid 不存在，`click()` 永不满足
- spec 文件可被 Playwright 正常收集（18 tests，6 passed + 12 failed，0 skipped），无编译错误、无 import 错误——失败完全来自被测功能缺失（page.tsx 仍是 plan-04 占位空壳，渲染「ETF 监控（建设中）」），符合 red 关键原则。
- **环境已就绪**：dev server（3100）返回 200，后端（8000）运行中（测试用 mock 不依赖真实后端数据）。**plan-04 的 6 个用例（TC-4.1~4.6）全部 green**（导航项存在、路由可达、占位页渲染、降级回归正常），证明认证 fixture、页面加载、断言基建均正常——12 个失败只能归因于 plan-05 待实现（排行表/趋势图业务组件）。

## 关联文档

- 功能文件：`docs/14-ETF份额与资金流监控/14-2-实现计划-ETF份额与资金流监控/plan-05-ETF监控页面.md`（§3 实现规格：EtfMonitorPage/EtfIndexRankingTable/EtfTrendChart/helpers；§5 验收标准 AC-01~11/13；§6 验证命令）
- 架构文档：`docs/14-ETF份额与资金流监控/14-1-架构文档-ETF份额与资金流监控.md`（§3.1/3.3 用户流程与状态、§3.3 业务规则、§7.2 输出 Schema、§8.2 降级回归）
- 测试文件：`web/tests/e2e/etf-monitor.spec.ts`
- Mock helper（本阶段新增）：`web/tests/e2e/helpers/mock-etf-monitor-api.ts`（`installEtfMonitorMocks` + 4 端点 mock + test data factory）
- 先例参照：`web/tests/e2e/sector-fund-flow.spec.ts` + `web/tests/e2e/helpers/mock-sector-fund-flow-api.ts`（installFullFundFlowMocks 范式）、`web/tests/e2e/broker-recommend-analysis.spec.ts`（authedPage 认证 fixture 范式）

## 失败原因

plan-05 待实现的业务组件当前均不存在，page.tsx 仍是 plan-04 占位空壳（`<div>ETF 监控（建设中）</div>`），导致 12 个用例无法走到业务断言：

| 模块 | 文件 | 待实现（plan-05 §3 Task） | red 表现 |
| --- | --- | --- | --- |
| 页面主组件 | `web/src/components/etf-monitor/EtfMonitorPage.tsx` | 新建：双视图协调 + 状态管理 + 控制栏（Task #4） | 不存在，page.tsx 未接入，仍渲染占位文本 |
| 排行表格 | `web/src/components/etf-monitor/EtfIndexRankingTable.tsx` | 新建：原生 table 四态 + 展开明细 + 趋势入口 + 排序（Task #2） | `etf-index-ranking-table` / `etf-expand-*` / `etf-trend-entry-*` / `etf-sort-*` / `etf-ranking-*` / `etf-detail-row-*` / `etf-pagination` 等 testid 全部不存在 |
| 趋势曲线 | `web/src/components/etf-monitor/EtfTrendChart.tsx` | 新建：dynamic echarts + 对象/指标/区间切换 + 四态（Task #3） | `etf-trend-chart` / `etf-trend-placeholder` / `etf-view-trend` / `etf-trend-target-*` / `etf-trend-metric-*` / `etf-trend-days-*` 等 testid 全部不存在 |
| helpers | `web/src/components/etf-monitor/helpers.ts` | 新建：格式化/色标纯函数（Task #1） | 不存在（不直接断言，但表格色标文案依赖） |
| page.tsx 接入 | `web/src/app/dashboard/etf-monitor/page.tsx` | 修改：替换占位为 `<EtfMonitorPage />`（Task #5） | 仍为占位 `<div>ETF 监控（建设中）</div>`（plan-04 §3 #4） |

预检证据（red 前 dev server 探活）：`curl http://localhost:3100/` → `200`（dev server 在线）。

## 执行结果

- 命令：`cd web && npx playwright test tests/e2e/etf-monitor.spec.ts --reporter=list`
- 通过 / 失败：**6 passed / 12 failed**（共 18 tests，0 skipped）
- 失败截图：`web/test-results/etf-monitor-plan-05：*-admin/test-failed-1.png`（每个失败用例一份）
- HTML 报告：`web/playwright-report/index.html`

### 用例明细

| # | 用例 | 类型 | 结果 | 失败原因（摘要） |
| --- | --- | --- | --- | --- |
| 1 | TC-4.1 侧边栏「ETF 监控」导航项 | plan-04 | passed | — |
| 2 | TC-4.2 导航项位于「板块资金流」之后 | plan-04 | passed | — |
| 3 | TC-4.3 点击导航跳转 /dashboard/etf-monitor | plan-04 | passed | — |
| 4 | TC-4.4 页面空载渲染占位内容 | plan-04 | passed | — |
| 5 | TC-4.5 页面套用 DashboardLayout | plan-04 | passed | — |
| 6 | TC-4.6 现有导航项与 active 不受影响 | plan-04 | passed | — |
| 7 | TC-5.1 默认宽基指数排行（AC-01） | plan-05 | failed | `etf-index-ranking-table` not found（10.2s） |
| 8 | TC-5.2 宽基/行业维度切换（AC-02） | plan-05 | failed | 排行表行 `toContainText` not found（10.2s） |
| 9 | TC-5.3 净流入额/份额/份额变化排序（AC-03） | plan-05 | failed | 排行表行 `toHaveCount` 不符（10.2s） |
| 10 | TC-5.4 切换日期历史排行（AC-05） | plan-05 | failed | 排行表行 `toHaveCount` 不符（10.2s） |
| 11 | TC-5.5 分页浏览（AC-13） | plan-05 | failed | `etf-pagination` not found（10.2s） |
| 12 | TC-5.6 展开指数看 ETF 明细（AC-04） | plan-05 | failed | `etf-expand-沪深300` not found（10.2s） |
| 13 | TC-5.7 切趋势视图未选对象引导态（AC-06） | plan-05 | failed | `etf-view-trend` click 超时（30s） |
| 14 | TC-5.8 份额/净流入额曲线+区间切换（AC-07） | plan-05 | failed | `etf-view-trend` click 超时（30s） |
| 15 | TC-5.9 下钻单只 ETF（AC-08） | plan-05 | failed | `etf-view-trend` click 超时（30s） |
| 16 | TC-5.10 历史不足区间（AC-09） | plan-05 | failed | `etf-view-trend` click 超时（30s） |
| 17 | TC-5.11 趋势入口跳转（AC-11） | plan-05 | failed | `etf-trend-entry-沪深300` not found（10.2s） |
| 18 | TC-5.12 加载失败重试（AC-10） | plan-05 | failed | `etf-ranking-error` not found（10.2s） |

> 趋势视图组（TC-5.7~5.11）因测试步骤以 `click()` 先于 `expect` 失败，命中 30s 测试级 timeout；根因相同——趋势视图 testid 不存在。均为「业务组件未实现」类失败，非测试 bug / 环境错误。

## 本阶段产出（red-e2e）

1. **新增 mock helper**：`web/tests/e2e/helpers/mock-etf-monitor-api.ts`
   - 仿 `mock-sector-fund-flow-api.ts` 范式（URL.pathname 精确匹配 + test data factory + `page.route` 函数匹配）
   - `installEtfMonitorMocks(page)`：一键 mock etfMonitorApi 4 个端点（index-rankings / index-detail / trend / latest-date），返回符合 `etfMonitorTypes.ts` 的 camelCase 数据
   - 单端点 mock：`mockEtfIndexRankings` / `mockEtfIndexRankingsError` / `mockEtfIndexRankingsEmpty` / `mockEtfIndexDetail` / `mockEtfTrend` / `mockEtfTrendError` / `mockEtfLatestDate`
   - test data factory：`createTestEtfIndexRankings`（宽基 沪深300/中证500/创业板指 + 行业 半导体/新能源车，netInflow 正负色标）/ `createTestEtfIndexDetail`（沪深300 下 510300.SH/510310.SH，含 changePercent null 容错）/ `createTestEtfTrend`（share/netInflow 序列 + 历史不足 shortHistory + 空态 empty + 单只 ETF 量级缩小）
   - handler 内按 query（category/sort_by/order/page/trade_date/index_name/target_type/target_code/metric/days）模拟后端排序/过滤/分页/差异化标签；query 参数 snake_case、响应字段 camelCase；sort_by 与 metric 值用 camelCase（架构 §7.6 特例）

2. **完善 spec（etf-monitor.spec.ts）**：
   - 移除 plan-05 三处 `test.skip(true, ...)`，启用 TC-5.1~5.12
   - 每个 TC 用 `installEtfMonitorMocks` / 单端点 mock 注入数据，与时钟/真实后端解耦
   - testid 断言与 plan-05 §3 实现规格一致：`etf-index-ranking-table` / `etf-category-{broad|industry}` / `etf-sort-{netInflow|shareChange|share}` / `etf-trade-date` / `etf-ranking-empty` / `etf-ranking-error` / `etf-ranking-retry` / `etf-pagination` / `etf-page-info` / `etf-expand-{indexName}` / `etf-detail-row-{tsCode}` / `etf-view-{ranking|trend}` / `etf-trend-chart` / `etf-trend-placeholder` / `etf-trend-empty` / `etf-trend-target-type-{index|etf}` / `etf-trend-target-select` / `etf-trend-metric-{share|netInflow}` / `etf-trend-days-{7|30|90}` / `etf-trend-entry-{indexName}`
   - 同步更新文件头注释与 plan-04 describe 注释（去掉「red-e2e 阶段」/ skip 相关措辞）

## 测试设计要点

- **mock 数据让 AC 断言有意义**：
  - 宽基排行 3 行（沪深300 +12亿 / 创业板指 +0.8亿 / 中证500 -3.5亿），netInflow 正负覆盖色标断言（`+12` 红涨 / `-3.5` 绿跌）
  - 行业维度标签差异化（半导体/新能源车），AC-02 切换前后标签变化可断言
  - 明细 changePercent 一个有值（0.85）一个 null，验证 plan-01 §6 风险备注的容错展示
  - 趋势 netInflow 正负交替（验证零轴基线 + 正负段色标）；share 单调全正；单只 ETF scale=0.1（AC-08 单只 < 汇总）
  - 历史不足用 `__short__` 标记的 target_code 触发 mock 返回 3 天 < 7/30/90 天（AC-09 正常绘制已有部分）
- **与时钟/真实后端解耦**：全部用 `page.route` mock，dev server（3100）仅需可达，后端（8000）数据无关
- **testid 命名约定**（供 plan-05 实现者参照）：见上「testid 断言」清单，全部前缀 `etf-`，与 sector-fund-flow 的 `fund-flow-` 前缀风格一致

## 下一步

plan-05 实现者（implementer）以本 red 证据为前置条件，按 plan-05 §3 Task 顺序实现：helpers.ts → EtfIndexRankingTable.tsx → EtfTrendChart.tsx → EtfMonitorPage.tsx → page.tsx 接入（替换占位），重跑 `npx playwright test tests/e2e/etf-monitor.spec.ts` 应使 TC-5.1~5.12 转 green（覆盖 AC-01~11/13）。实现时需在组件暴露上述 testid。

## 阻塞项

无。本 red 阶段产出齐全（mock helper + 完善 spec + red 证据），失败原因明确（业务组件未实现），可推进至 implementer 阶段。
