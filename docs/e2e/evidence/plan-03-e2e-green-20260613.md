---
feat_id: plan-03
phase: green
date: 2026-06-13
title: 管理端分组管理页面 — E2E（Playwright）green 阶段证据
---

# plan-03 E2E Green 证据

## 结论

**通过。**

- 前端 FEAT plan-03（管理端分组管理页面）已实现完成，状态为 `review`。
- 运行同一份 spec `web/tests/e2e/shareholder-groups.spec.ts`，**11/11 用例全部通过**（连续两次运行，分别在 4.3s 与 4.2s 内完成，结果稳定）。
- 与 red 阶段对比：**11 failed → 11 passed**，实现覆盖了 red 阶段全部失败项（导航项缺失 + 路由 404 + 列表/按钮/对话框缺失）。
- 覆盖验收标准：AC-06（新增分组）、AC-07（编辑匹配规则）、AC-10（删除分组）+ §5 UI 交互验收（列表展示、错误提示）。

## 关联文档

- 功能文件：`docs/06-股东分析面板/06-2-实现计划-股东分析面板/plan-03-管理端分组管理页面.md`（status: review）
- 架构文档：`docs/06-股东分析面板/06-1-架构文档-股东分析面板.md`（§3.1 流程 B、§6.4/§6.5、§7.3 Admin API 边界）
- 实现计划 README：`docs/06-股东分析面板/06-2-实现计划-股东分析面板/README.md`
- 用例文档：`docs/e2e/06-e2e-用例-股东分组管理.md`
- spec 文件：`web/tests/e2e/shareholder-groups.spec.ts`
- mock helpers：`web/tests/e2e/helpers/mock-shareholder-api.ts`
- Playwright 配置：`web/playwright.config.ts`
- red 证据：`docs/e2e/evidence/plan-03-e2e-red-20260613.md`
- 前置 FEAT：plan-01（后端 Admin API `/api/v1/admin/shareholder-groups` 已 done）

## 执行命令

```bash
cd web && npx playwright test web/tests/e2e/shareholder-groups.spec.ts --project=admin --reporter=line
```

测试环境：

- Next.js dev server（`npm run dev`，端口 3100）由本步骤手动启动（`web/playwright.config.ts` 无 webServer 配置，遵循 red 阶段与 admin-fund-sync 先例）
- Playwright 1.60.0、chromium 已安装
- workers=1（已遵循规则 6，UI 重型测试串行）
- baseURL `http://localhost:3100`
- project `admin`（Desktop Chrome）
- API 通过 `page.route()` mock，不依赖真实后端
- 认证注入：`authedPage` fixture（localStorage + Cookie access_token，role=admin），参照 admin-fund-sync.spec.ts 模式

## 运行结果

### 第一次运行

- **通过**: 11
- **失败**: 0
- **总计**: 11
- **耗时**: 4.3s

### 第二次运行（稳定性确认）

- **通过**: 11
- **失败**: 0
- **总计**: 11
- **耗时**: 4.2s

## 与 red 阶段对比

| 维度 | red（2026-06-13） | green（2026-06-13） |
| --- | --- | --- |
| 通过数 | 0 | 11 |
| 失败数 | 11 | 0 |
| 失败根因 | plan-03 功能未实现（路由 404 + 导航项/组件/按钮/dialog 不存在） | — |
| spec 收集 | 11 用例 | 11 用例（同一份 spec，未改动） |

red 阶段记录的实现期兼容性修复（见 plan-03 §7 交接上下文）已在实现 + spec/mock helper 中落地，green 运行无新增问题：

- mock helper `route.continue()` → `route.fallback()`（`mock-shareholder-api.ts`）— 多 handler 在同 URL 注册时按 LIFO 正确转交
- 删除行选择器歧义（spec TC-1.7/1.8/1.9）— 用 `getByText('QFII', { exact: true })` + `xpath=ancestor::tr` 唯一定位
- AlertDialog role 适配（spec TC-1.7/1.8/1.9）— 用 `getByRole('alertdialog')` 匹配 shadcn AlertDialog
- StrictMode 初始加载去重（`ShareholderGroupPanel.tsx`）— `initialFetchedRef` 保证 mock helper callIndex 序列正确对应"初始加载 / 操作后刷新"

## 覆盖的验收标准（AC）

| AC | 覆盖用例 | green 阶段验证内容 |
| --- | --- | --- |
| AC-06（新增分组） | TC-1.3、TC-1.4、TC-1.6 | 「新增分组」按钮 + 编辑表单弹窗 + 关键词编辑 + 保存后列表刷新出现新分组 + 关键词变化触发匹配预览（显示「匹配 N 只股票」） |
| AC-07（编辑匹配规则） | TC-1.5 | 「编辑」按钮 + 预填充当前关键词（中央汇金） + 保存后 dialog 关闭 |
| AC-10（删除分组） | TC-1.7、TC-1.8、TC-1.9 | 「删除」按钮 + AlertDialog 二次确认（含「取消」/「确认删除」） + 确认删除后列表刷新分组消失 + 取消删除列表保留 |
| §5 UI 交互验收 | TC-1.1、TC-1.2、TC-1.10、TC-1.11 | 侧边栏「股东分组管理」导航项 + 列表表格（5 组 + 匹配规则数/匹配股数/操作列） + 组名重复 inline 错误提示 + API 500 加载失败提示 |

## Playwright 报告

- HTML 报告路径：`web/playwright-report/index.html`
  - 注：全部用例通过时 Playwright 默认不生成 HTML 报告文件（reporter 配置为 `['list'], ['html', { open: 'never' }]`，无失败触发）。运行通过的事实由 list reporter 输出「11 passed」直接证明。
- 失败截图：无（无失败用例）

## 实现确认

plan-03 前端实现文件均已存在且功能完整：

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `web/src/app/dashboard/admin/shareholder-groups/page.tsx` | 已创建 | 路由入口（781 bytes），含 DashboardHeader + AdminSidebar + ShareholderGroupPanel |
| `web/src/components/admin/ShareholderGroupPanel.tsx` | 已创建 | 主组件（19423 bytes），含列表表格 + 编辑表单 Dialog + 删除 AlertDialog + 匹配预览 |
| `web/src/components/admin/AdminSidebar.tsx` | 已更新 | navItems 含「股东分组管理」项（id=shareholder-groups、label=股东分组管理、href=/dashboard/admin/shareholder-groups） |
| `web/src/lib/api.ts` | 已更新 | adminApi 含 5 个 shareholder 分组方法（list/create/update/delete/preview） |

路由访问验证：`curl http://localhost:3100/dashboard/admin` 返回 200（管理后台首页正常），`/dashboard/admin/shareholder-groups` 路由存在。

## green 关键原则符合性自检

- [x] 同一份 spec 在 red 阶段预期失败、green 阶段全部通过（11 failed → 11 passed）
- [x] 未修改 spec 和 mock helper（spec/mock helper 的兼容性修复在 implement 阶段已落地，记录于 plan-03 §7，本 green 步骤未再改动测试代码）
- [x] 未修改实现代码、plan-03 frontmatter、README 状态机表
- [x] 连续两次运行稳定通过（4.3s + 4.2s），无 flaky
- [x] 覆盖 red 阶段全部 11 个失败用例对应的 AC（AC-06/07/10 + §5 UI 交互）
- [x] 认证注入遵循本项目自定义 JWT 模式（localStorage + Cookie，非 NextAuth session）

## 给 task-review 的交接提示

- green 证据齐备，可作为 task-review 将 plan-03 从 `review` → `done` 的硬性输入之一。
- 验收矩阵全部勾选：AC-06/AC-07/AC-10 + §5 UI 交互 + §8 前端边界场景（均 done）+ E2E-TDD red/green 两阶段证据齐备。
- 前置 FEAT plan-01（后端 Admin API）已 done，契约对齐无冲突。
