---
feat_id: plan-04
phase: red
created: 2026-07-07
source_architecture: docs/12-股票数据独立建表/12-1-架构文档-股票数据独立建表.md
source_plan: docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-04-测试修复与全量回归.md
---

# plan-04 E2E Red 证据：测试修复与全量回归

## 1. 摘要

本证据在 plan-04 实现前记录全量 pytest 的 13 个失败清单，并对每个失败做根因分类：
- 哪些失败由"股票数据独立建表"（plan-01/02/03 拆表改造）引入
- 哪些是既有的、与本次改造无关的失败

**结论：预期失败，测试有效**。实现 plan-04 后，"新表切换导致"的那部分失败将转为通过；其余"既有失败"非本期范围（已在 green 证据中明确）。

证据收集方式：`pytest --tb=no -q` 全量运行（含生产代码 plan-01/02/03 改造），并通过 `git stash` 切回改造前 baseline 做对照。

## 2. 现状 Baseline（实现前）

### 2.1 全量 pytest 结果（改造后，含 plan-01/02/03 生产代码）

```
cd server && pytest --tb=no -q
...
13 failed, 1054 passed
```

13 个失败清单：

| # | 测试 | 失败根因 | 分类 |
|---|------|---------|------|
| 1 | `test_classification_cache.py::TestCachePerformance::test_cache_get_performance` | 缓存性能断言 `104ms < 50ms` 失败（环境性能波动） | **既有失败** |
| 2 | `test_fund_sync_filter.py::TestSyncFundBasicDelistFilter::test_skip_fund_with_delist_date` | `skipped==4` 期望 `2`（基金退市过滤逻辑，与拆表无关） | **既有失败** |
| 3 | `test_fund_sync_filter.py::...::test_skip_fund_with_status_E` | 同上（基金状态过滤） | **既有失败** |
| 4 | `test_fund_sync_filter.py::...::test_skip_count_aggregates_both_conditions` | 同上 | **既有失败** |
| 5 | `test_fund_sync_filter.py::...::test_cleanup_removes_existing_delisted_funds` | 同上 | **既有失败** |
| 6 | **`test_hk_stock_sync.py::TestSetDiffCleanup::test_cleanup_deletes_disappeared_with_cascade`** | fixture 向旧表 `DailyMarketData(entity_type='stock')` 插衍生数据并断言级联清理，但生产 `_cascade_delete_stock_data` 已改清新三表（`StockDailyMarketData` 等） | **新表切换导致** |
| 7 | `test_scheduler_service.py::TestJobManager::test_register_jobs` | `daily_data_update` job 未注册（调度器注册逻辑） | **既有失败** |
| 8 | `test_scheduler_service.py::...::test_daily_data_update_job` | 同上 | **既有失败** |
| 9 | `test_scheduler_service.py::...::test_data_quality_check_job` | 同上 | **既有失败** |
| 10 | `test_scheduler_service.py::...::test_cache_cleanup_job` | 同上 | **既有失败** |
| 11 | `test_scheduler_service.py::...::test_get_jobs` | 同上 | **既有失败** |
| 12 | `test_scheduler_service.py::...::test_trigger_job_success` | 同上 | **既有失败** |
| 13 | `test_sector_classification_data_init_atdd.py::TestAC02_DailyIncrementalUpdate::test_ac02_02_scheduled_task_at_16_00` | 每日板块分类更新任务未注册（调度器注册逻辑） | **既有失败** |

### 2.2 对照 baseline（切回改造前生产代码）

为确认 12 个"非 hk_stock_sync"失败是否预先存在，执行：

```bash
git stash push server/src/      # 临时还原生产代码到改造前
pytest --tb=no -q               # 全量跑
git stash pop                   # 恢复
```

baseline 结果：

```
12 failed, 1055 passed
```

baseline 的 12 个失败与改造后清单中 #1~#5、#7~#13 完全一致（test_classification_cache、test_fund_sync_filter×4、test_scheduler_service×6、test_sector_classification_data_init_atdd×1）。

**关键差异**：baseline（改造前）中 `test_hk_stock_sync.py::test_cleanup_deletes_disappeared_with_cascade` **通过**（因为旧生产代码 `_cascade_delete_stock_data` 清的是旧表，测试 fixture 也写旧表，自洽）；改造后该测试**失败**（生产代码改清新表，fixture 仍写旧表）。这精确确认 #6 是本次拆表改造引入的、本期需修复的唯一失败。

### 2.3 plan-04 §2 文件清单核查

逐个运行 plan-04 §2 列出的 15 个测试文件：

```bash
pytest tests/conftest.py tests/test_data_init.py tests/test_data_update.py \
       tests/test_data_updater.py tests/test_strength_services.py \
       tests/test_strength_score_model.py tests/test_strength_api.py \
       tests/test_calculation_engine.py tests/test_ma_system_calculator.py \
       tests/test_hk_stock_sync.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_strength_api.py tests/test_api/test_rankings_heatmap_api.py \
       tests/services/test_strength_snapshot_service.py tests/test_models.py
```

结果：**1 failed, 277 passed**。唯一失败即 #6（test_hk_stock_sync）。

这证明 15 个清单文件中其余 14 个文件全部通过（其中大量测试用旧 `StrengthScore(entity_type='stock')` 直接构造模型，因旧 `StrengthScore` 模型/表仍存在并继续承载 sector+stock 共表逻辑，与生产读路径解耦——这些测试不触发生产写入路径，故不受拆表影响）。

## 3. 待修复范围与预期

### 3.1 需修复（新表切换导致）

| 测试 | 文件 | 修复策略（plan-04 §3 分类） |
|------|------|----------------------------|
| `test_hk_stock_sync.py::test_cleanup_deletes_disappeared_with_cascade` | tests/test_hk_stock_sync.py | **A 类**：fixture 衍生数据从 `DailyMarketData(entity_type='stock', entity_id=...)` 改写为新表 `StockDailyMarketData(stock_id=..., symbol=..., date=...)`；断言改为查 `StockDailyMarketData`。生产 `_cascade_delete_stock_data`（data_init.py:701-735）已正确按 `stock_id` 清新三表，无需改生产代码 |

### 3.2 不修复（既有失败，非本期范围）

12 个既有失败（#1~#5、#7~#13）分布在以下文件，**均不在 plan-04 §2 的 15 文件清单内**：

- `tests/test_classification_cache.py` — 缓存性能基准（环境波动）
- `tests/test_fund_sync_filter.py` — 基金退市/状态过滤逻辑
- `tests/test_scheduler_service.py` — 调度器 job 注册
- `tests/test_sector_classification_data_init_atdd.py` — 板块分类调度任务

这些失败根因与"股票数据独立建表"无关，属于既有历史问题，本期不修复，但会在 green 证据中明确列出（避免被误认为改造引入）。

## 4. API 响应契约核对基线（AC-06）

plan-04 §3.2 要求对比改造前后个股 API 响应字段。本期采用"代码静态核对 + 测试断言验证"方式：
- 个股 API 测试（test_strength_api.py / test_api/test_stocks_api.py / test_api/test_strength_api.py）全部通过，证明响应字段名/类型/结构与生产 schema 一致
- 响应中 `entity_type` 字段需继续返回 `"stock"`（StockStrengthResponse schema 硬填 `period='all'` / `change_rate_5d=None`，plan-03 已实现）

详细的契约 diff 核对在 green 证据中给出。

## 5. 结论

**预期失败，测试有效**。

- 全量 pytest 13 个失败中，**1 个由本次拆表改造引入**（test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade），属 plan-04 §3.A 类（fixture 写旧表），预期 implement 后转为通过
- 其余 **12 个为既有失败**，已用 git stash baseline 对照确认预先存在，非本期范围
- plan-04 §2 的 15 文件清单中仅 test_hk_stock_sync 需修复，其余 14 文件现状已全部通过
