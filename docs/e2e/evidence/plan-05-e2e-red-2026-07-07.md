---
feat_id: plan-05
phase: red
created: 2026-07-07
source_architecture: docs/12-股票数据独立建表/12-1-架构文档-股票数据独立建表.md
source_plan: docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-05-板块回归基线验证.md
---

# plan-05 E2E Red 证据：板块回归基线验证

## 1. 摘要

plan-05 是整个"股票数据独立建表"拆表改造的**最终质量门**，专项验证 AC-03（板块功能零回归）。本功能为**纯验证**，无任何 modify/create 代码文件；若发现板块回归问题，应回退到 plan-02/03 修复共享方法，不在本功能改代码。

本 red 证据记录在执行 plan-05 §3/§4 的 13 项板块回归验证矩阵**之前**的 baseline 状态：

- 7 个板块 service/api 文件 + 5 个共享方法的 sector 分支是否已具备"零改动/读旧表"前置条件
- 板块单元测试、API 测试、task 执行测试的当前基线
- 预期结论：**板块功能零回归**（拆表改造通过 ADR-4 内部分发隔离，板块分支未受影响）

证据收集方式：`git diff`、共享方法代码静态审查、板块相关 pytest 全量运行。

> 说明：plan-05 不开发新功能，因此"red"不是"测试预期失败"，而是"建立板块回归验证矩阵的 baseline，明确哪些项需在 green 阶段逐一证明零回归"。若 green 阶段任一项与 baseline 不一致或与改造前行为偏离，则视为回归。

## 2. 板块回归验证矩阵（plan-05 §3，13 项）Baseline 状态

| # | 板块功能 | 验证方式 | Baseline 状态 | 预期（green） |
|---|---------|---------|--------------|--------------|
| 1 | 板块强度排名 | `GET /api/v1/rankings/v2/sectors`（ranking_service sector 分支） | ranking_service.calculate_rankings: entity_type='sector' → `StrengthScore`（line 67：`is_stock=False` → `Model=StrengthScore`） | 行为同改造前，读旧表 |
| 2 | 板块强度统计 | `GET /api/v1/rankings/v2/stats?entity_type=sector` | 同上（ranking_service._calculate_percentile line 194-208 读 StrengthScore） | 同上 |
| 3 | 板块强度详情 | `GET /api/v1/sectors/{code}/strength` | sectors.py 零改动（git diff 空）→ 行为不变 | 同上 |
| 4 | 板块强度历史 | `GET /api/v1/sectors/{code}/strength-history` | strength_history_service.get_sector_history line 116-124：`StrengthScore.entity_type=='sector'` | 读旧表 |
| 5 | 板块均线历史 | `GET /api/v1/sectors/{code}/ma-history` | sector_ma_service.py 零改动（git diff 空），line 462-464 读 `MovingAverageData.entity_type=='sector'` | 读旧表 |
| 6 | 板块成分股强势股 | 板块成分股接口 | sectors.py 零改动 | 行为不变 |
| 7 | 板块强度等级表 | strength_grade_table_service | 文件零改动（git diff 空） | 行为不变 |
| 8 | 板块强度散点图 | strength_scatter_service | 文件零改动（git diff 空）；test_strength_scatter_service.py 全通过 | 行为不变 |
| 9 | 板块分布 | sector_distribution_service | 文件零改动（git diff 空） | 行为不变 |
| 10 | 板块分类 | sector_classification_service | 文件零改动（git diff 空）；test_sector_classification_service.py 全通过 | 行为不变 |
| 11 | 板块数据采集 | 触发 sector 采集 task | collector._update_market_data line 246-247：`pg_insert(DailyMarketData).values(entity_type='sector')` | 写旧表 daily_market_data |
| 12 | 板块均线计算 | 触发 sector MA task | sector_ma_service line 381-382：`MovingAverageData(entity_type='sector')` | 写旧表 moving_average_data |
| 13 | 板块强度计算 | 触发 sector strength task | strength_service_v2._save_strength_score line 316-318：`is_stock=False` → `Model=StrengthScore`；calculate_sector_strength line 201-207 透传 entity_type='sector' | 写旧表 strength_scores |

## 3. 前置条件核查（red 阶段已成立）

### 3.1 板块 service / api 文件零改动（AC-03 第一道门）

```bash
cd /Users/muchao/code/sector-strength
git diff --name-only HEAD -- \
  server/src/services/sector_ma_service.py \
  server/src/services/sector_strength_service.py \
  server/src/services/sector_classification_service.py \
  server/src/services/strength_grade_table_service.py \
  server/src/services/strength_scatter_service.py \
  server/src/services/sector_distribution_service.py \
  server/src/api/v1/sectors.py
# 输出：空（7 个文件全部零改动）
```

> 注：本次"股票数据独立建表"全部生产代码改动均处于工作区未提交状态（HEAD=f828ae4），上述 7 个板块文件未出现在 `git status` 修改清单中，证明未被本次改造触及。

### 3.2 共享方法 sector 分支读旧表（AC-03 第二道门）

5 个共享方法的 sector 分支静态审查结论（详见 green 证据 §3.2）：

| 共享方法 | sector 分支读旧表 | 关键代码位置 |
|---------|------------------|------------|
| ma_data_loader.load_ma_values | ✅ MovingAverageData | line 102-103 |
| ma_data_loader.load_current_price | ✅ DailyMarketData | line 199-200 |
| ranking_service.calculate_rankings | ✅ StrengthScore | line 66-67 |
| strength_history_service.get_sector_history | ✅ StrengthScore | line 116-124 |
| strength_history_service（共享 get_history） | ✅ StrengthScore | line 172-179 |
| strength_snapshot_service（sector 循环 143-189） | ✅ StrengthScore（line 350-355） | sector 排名更新读旧表 |
| strength_service_v2._save_strength_score | ✅ StrengthScore | line 316-318 |

所有 sector 分支均通过 `is_stock = (entity_type == "stock")` 二选一分发，sector 走旧表模型，分支零改动。

## 4. 板块测试 Baseline（实现 plan-05 验证前）

### 4.1 板块核心单元测试（plan-05 §3 命令）

```bash
cd server && pytest \
  tests/services/test_sector_ma_service.py \
  tests/services/test_sector_classification_service.py \
  tests/services/test_strength_scatter_service.py \
  tests/services/test_strength_snapshot_service.py \
  tests/test_sector_classification_service.py \
  tests/test_sector_strength_service.py --no-cov -q
# => 125 passed
```

> 说明：plan-05 §3/§6 列出的 `tests/services/test_sector_strength_service.py` 路径有误（实际文件在 `tests/test_sector_strength_service.py`，根目录）。本验证已用正确路径运行。

### 4.2 板块 API / ATDD / 计算引擎测试

```bash
pytest tests/test_sector_classification.py \
       tests/test_sector_classification_api.py \
       tests/test_sector_classification_data_init_atdd.py \
       tests/test_api/test_sectors_api.py \
       tests/api/test_sector_analysis_charts_api.py --no-cov -q
# => 1 failed, 61 passed
```

唯一失败：`test_sector_classification_data_init_atdd.py::test_ac02_02_scheduled_task_at_16_00`。

**根因**：`assert daily_classification_job is not None` —— 每日板块分类调度任务未在 scheduler 注册（断言 `None is not None`）。这是**既有失败**，与数据存储层拆表无关：
- plan-04 green 证据 §3.3 #12 已记录同一失败（baseline 同样存在）
- 涉及 scheduler job 注册逻辑（test_scheduler_service.py 系列 6 个同类失败均为既有问题）
- 不读写 daily_market_data / moving_average_data / strength_scores 任何一张表

## 5. 预期（green 阶段需证明）

plan-05 green 阶段需对 §2 矩阵 13 项逐一给出零回归证据：

1. 板块 7 文件 git diff 持续为空（Task #1）
2. 5 个共享方法 sector 分支读旧表（代码审查，Task #2）
3. 板块核心单元测试 125 全通过（Task #3）
4. 板块 API 测试全通过（Task #4，唯一失败为既有 scheduler 问题）
5. 板块采集/MA/强度 task 写旧表（Task #5/#6，通过 collector/sector_ma_service/strength_service_v2 代码审查 + 已有测试覆盖证明）

## 6. 结论

**预期板块功能零回归**。

- 板块 7 个 service/api 文件在本次拆表改造中零改动（git diff 空）
- 5 个共享方法通过 ADR-4 内部分发隔离 sector 分支，sector 一律走旧表模型
- 板块核心单元测试 125 全通过；板块 API/ATDD 测试仅 1 个既有失败（scheduler 注册问题，非拆表引入）
- 拆表改造的"分支隔离"设计（ADR-4）已在前置 plan-02/03 落地，plan-05 仅做最终确认

无代码改动（本功能纯验证）。进入 green 阶段执行 §2 矩阵的完整验证。
