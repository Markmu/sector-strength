---
feat_id: plan-04
phase: green
date: 2026-07-29
title: 前端基础设施（etfMonitorApi + 类型 + SWR hooks + 路由壳 + 导航入口）— Playwright E2E green 阶段证据（14 期）
source_architecture: docs/14-ETF份额与资金流监控/14-1-架构文档-ETF份额与资金流监控.md
source_plan: docs/14-ETF份额与资金流监控/14-2-实现计划-ETF份额与资金流监控/plan-04-前端基础设施.md
---

# plan-04 E2E Green 证据：ETF 监控前端基础设施（14 期）

## 结论

**通过（TC-4.1~4.6，6 passed / 12 skipped）。**

- plan-04「前端基础设施」是用户可观察功能（导航入口可点击、路由可访问），用 **Playwright E2E green 测试**（非 pytest）。后端 plan-01/02/03 均已 done，plan-04 implement 阶段已完成。
- plan-04 范围的 **6 个导航/路由用例全部通过**（TC-4.1~4.6），实现同一份 red spec（未改断言）后从 5 failed/1 passed 反转为 6 passed：
  - TC-4.1/4.2 → 侧边栏存在"ETF 监控"导航项（`getByRole('link', { name: 'ETF 监控' })` 可见、href=/dashboard/etf-monitor、顺序在板块资金流之后）
  - TC-4.3 → 点击导航项跳转 `/dashboard/etf-monitor`（`toHaveURL(/\/dashboard\/etf-monitor/)`）
  - TC-4.4 → 路由页空载渲染占位（`getByText('ETF 监控（建设中）')` 可见）
  - TC-4.5 → 路由页套用 DashboardLayout（侧边栏"板块资金流"链接可见）
  - TC-4.6 → 降级回归（现有导航项 + active href 正常，架构 §8.2）
- plan-05 范围的 12 个业务交互用例（TC-5.1~5.12）保持 `test.skip`（依赖 plan-05 的 EtfMonitorPage 业务组件，本期页面为占位空壳），符合 red 阶段声明的分层策略。
- **环境就绪**：dev server（3100）+ 后端（8000）均运行中。

## 关联文档

- 功能文件：`docs/14-ETF份额与资金流监控/14-2-实现计划-ETF份额与资金流监控/plan-04-前端基础设施.md`（§3 实现规格 #4 路由壳 page.tsx、#5 导航菜单 DashboardLayout.tsx；§5 验收标准）
- 架构文档：`docs/14-ETF份额与资金流监控/14-1-架构文档-ETF份额与资金流监控.md`（§8.2 降级回归）
- 测试文件：`web/tests/e2e/etf-monitor.spec.ts`
- red 证据：`docs/e2e/evidence/plan-04-e2e-red-2026-07-29.md`

## 测试 harness 修复说明（非放宽断言）

implement 阶段对 red spec 做了两处 **测试 harness 修复**，属稳定性/可观察性修复，未放宽任何核心断言：

| 用例 | 修复 | 性质 | 核心断言 |
| --- | --- | --- | --- |
| TC-4.3 | 增加 `installFullFundFlowMocks(page)` | **harness 稳定性修复**。源页 sector-fund-flow 用 mock token 发 SWR 请求会被后端 401 拒绝，apiClient 401 分支触发 `handleUnauthorizedRedirect` 跳 /login，导航 `<Link>` 在点击前被卸载（click 永不满足稳定性检查）。装全量 mock 让源页稳定后再点击导航项——与 sector-fund-flow.spec.ts 同款处理。 | **保留**：`expect(page).toHaveURL(/\/dashboard\/etf-monitor/)` |
| TC-4.4 | 文本匹配从模糊 `getByText('ETF 监控')` 收紧为精确 `getByText('ETF 监控（建设中）')` | **断言收紧（非放宽）**。模糊子串 "ETF 监控" 在 strict mode 下与侧边栏导航项 "ETF 监控" 冲突产生歧义，改为精确占位文本 "ETF 监控（建设中）" 消除歧义——断言更严格。 | **保留并收紧**：`toHaveURL(/\/dashboard\/etf-monitor/)` + 精确占位文本可见 |

两处修复均未触及 red 阶段的核心断言（导航项存在 `getByRole('link', { name })`、跳转 `toHaveURL(/\/dashboard\/etf-monitor/)`、页面渲染占位、DashboardLayout 侧边栏可见）。red→green 的反转由 plan-04 的实现（导航项新增 + 路由壳建立）驱动，而非由断言放宽驱动。

## 执行命令

```bash
cd /Users/muchao/code/sector-strength/web
npx playwright test tests/e2e/etf-monitor.spec.ts --project=admin --reporter=list
# 结果：6 passed, 12 skipped
```

> 运行前确认 dev server（`npm run dev`，端口 3100）与后端（端口 8000）已运行。Playwright config `baseURL: http://localhost:3100`，project `admin`。

## 执行结果

```
Running 18 tests using 1 worker

  ✓  1 [admin] TC-4.1 侧边栏存在"ETF 监控"导航项 (228ms)
  ✓  2 [admin] TC-4.2 ETF 监控导航项位于"板块资金流"之后 (220ms)
  ✓  3 [admin] TC-4.3 点击"ETF 监控"导航跳转到 /dashboard/etf-monitor (326ms)
  ✓  4 [admin] TC-4.4 ETF 监控页空载渲染占位内容 (170ms)
  ✓  5 [admin] TC-4.5 ETF 监控页套用 DashboardLayout（侧边栏可见） (172ms)
  ✓  6 [admin] TC-4.6 现有导航项与 active 高亮不受影响 (208ms)
  -  7~18 [admin] TC-5.1~5.12 (plan-05，skipped)

  12 skipped
  6 passed (1.6s)
```

HTML 报告：`web/playwright-report/index.html`
失败截图/trace：无（全绿）。

## 阻塞项

无。plan-04 范围 6 个用例全部通过，12 个 plan-05 用例按分层策略保持 skip。

## 后续步骤

- 进入 `task-review` 阶段对 plan-04 做功能验收（文件清单 / Task / 验收标准 / 契约 / 代码审查）。
- plan-05 实现业务组件（EtfMonitorPage）后，把 TC-5.1~5.12 从 skip 转为 run 并补充 mock helper，green 证据写入 `docs/e2e/evidence/plan-05-e2e-green-*.md`。
