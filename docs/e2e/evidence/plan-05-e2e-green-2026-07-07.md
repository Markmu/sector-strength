---
feat_id: plan-05
phase: green
created: 2026-07-07
source_architecture: docs/12-股票数据独立建表/12-1-架构文档-股票数据独立建表.md
source_plan: docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-05-板块回归基线验证.md
red_evidence: docs/e2e/evidence/plan-05-e2e-red-2026-07-07.md
---

# plan-05 E2E Green 证据：板块回归基线验证

## 1. 摘要

plan-05 是"股票数据独立建表"拆表改造的最终质量门，专项验证 AC-03（板块功能零回归）。本功能为**纯验证**，无任何生产代码改动。本 green 证据对 red 阶段建立的 13 项板块回归验证矩阵逐一给出零回归证明。

**结论：通过（板块功能零回归）**

四道核心门禁全部通过：
1. 板块 7 个 service/api 文件 git diff 为空（零改动）
2. 5 个共享方法的 sector 分支全部读/写旧表（ADR-4 内部分发隔离有效）
3. 板块核心单元测试 125 全通过
4. 板块采集/MA/强度 task 写旧表（代码审查 + 测试覆盖证明）

唯一相关失败（test_ac02_02_scheduled_task_at_16_00）为既有 scheduler 注册问题，与拆表无关（plan-04 green 已记录）。

## 2. 验证范围

本功能是纯验证功能（无代码改动）。green 验证维度：
1. **板块 service/api 零改动**（Task #1）：git diff 7 文件输出为空
2. **共享方法 sector 分支读旧表**（Task #2）：5 方法代码审查
3. **板块单元测试零回归**（Task #3）：6 测试文件 125 passed
4. **板块 API 验证**（Task #4）：板块 API 测试全通过（唯一失败为既有 scheduler 问题）
5. **板块 task 写旧表**（Task #5/#6）：collector / sector_ma_service / strength_service_v2 代码审查 + 测试覆盖

## 3. 验证结果

### 3.1 Task #1：板块 service / api 文件零改动（核心门禁）

```bash
cd /Users/muchao/code/sector-strength
git diff --name-only HEAD -- \
  server/src/services/sector_ma_service.py \
  server/src/services/sector_strength_service.py \
  server/src/services/sector_classification_service.py \
  server/src/services/strength_grade_table_service.py \
  server/src/services/strength_scatter_service.py \
  server/src/services/sector_distribution_service.py \
  server/src/api/v1/sectors.py
# 输出：空（7 文件全部零改动）
```

补充确认：本次"股票数据独立建表"的全部生产代码改动均处于工作区未提交状态（HEAD=f828ae4），`git status --short` 显示的 15 个修改文件清单中**不含**上述任何板块文件，证明板块代码在 plan-02/03 改造中未被触及。

conftest 同样零改动（`git diff --name-only HEAD -- server/tests/conftest.py server/tests/services/conftest.py` 输出为空），排除 fixture 误伤板块测试的可能。

### 3.2 Task #2：共享方法 sector 分支读旧表（核心门禁）

5 个共享方法通过 ADR-4 内部分发（`is_stock = (entity_type == "stock")` 二选一）隔离 sector 分支，sector 一律走旧表模型：

| 共享方法 | 方法 | sector 分支模型 | 关键代码 | 行号 |
|---------|------|---------------|---------|------|
| ma_data_loader | load_ma_values | MovingAverageData | `is_stock=False → MAData=MovingAverageData`；sector 保留 `entity_type` where 条件 | 102-110 |
| ma_data_loader | load_current_price | DailyMarketData | `is_stock=False → MarketData=DailyMarketData`；sector 保留 `entity_type` | 199-206 |
| ma_data_loader | _get_available_days | DailyMarketData | 同上 | 282-290 |
| ranking_service | calculate_rankings | StrengthScore | `is_stock=False → Model=StrengthScore`；sector 保留 `entity_type`+`period` | 66-73 |
| ranking_service | _calculate_percentile | StrengthScore | 直接 `select(func.count(StrengthScore.id)).where(entity_type==...)` | 194-208 |
| ranking_service | get_ranking / get_entity_rank | StrengthScore | 直接 select(StrengthScore).where(entity_type=='...') | 144-151 / 249-254 |
| strength_history_service | get_sector_history | StrengthScore | `select(StrengthScore).where(entity_type=='sector', period=='all')` | 116-124 |
| strength_history_service | get_history（共享） | StrengthScore | `is_stock=False → Model=StrengthScore`，sector 保留 `entity_type` | 172-179 / 276-283 |
| strength_snapshot_service | sector 排名更新 | StrengthScore | `select(StrengthScore).where(entity_type=="sector", period=="all")` | 350-355 |
| strength_snapshot_service | sector 快照循环 | （调 strength_service.calculate_sector_strength） | sector 循环 143-189，内部走 _save_strength_score sector 分支 | 143-189 |
| strength_service_v2 | _save_strength_score | StrengthScore | `is_stock=False → Model=StrengthScore`，sector 保留 `period=='all'` 查重条件 | 316-326 |
| strength_service_v2 | calculate_sector_strength | （透传 entity_type='sector'） | 调 `_save_strength_score(entity_type="sector", ...)` | 201-207 |

**结论**：所有 sector 分支均读/写旧表（MovingAverageData / DailyMarketData / StrengthScore），与改造前行为完全一致。共享方法仅在 stock 分支切换到新表模型（StockMovingAverageData / StockDailyMarketData / StockStrengthScore），sector 分支零改动。

### 3.3 Task #3：板块核心单元测试零回归

```bash
cd server && source .venv/bin/activate && pytest \
  tests/services/test_sector_ma_service.py \
  tests/services/test_sector_classification_service.py \
  tests/services/test_strength_scatter_service.py \
  tests/services/test_strength_snapshot_service.py \
  tests/test_sector_classification_service.py \
  tests/test_sector_strength_service.py --no-cov -q
```

结果：

```
tests/services/test_sector_classification_service.py .............       [ 27%]
tests/services/test_strength_scatter_service.py ..........               [ 35%]
tests/services/test_strength_snapshot_service.py .............           [ 45%]
tests/test_sector_classification_service.py ............................ [ 68%]
.............                                                            [ 78%]
tests/test_sector_strength_service.py ...........................        [100%]
======================= 125 passed, 17 warnings in 5.28s =======================
```

**125 passed，0 failed**。

> 路径说明：plan-05 §3/§6 列出的 `tests/services/test_sector_strength_service.py` 实际位于 `tests/test_sector_strength_service.py`（根目录），已用正确路径运行。
>
> 运行末尾的 `RuntimeError: Event loop is closed` 来自 asyncpg 连接池 GC 清理，出现在测试全部通过之后的进程退出阶段，非测试失败（pytest 汇总为 125 passed）。

### 3.4 Task #4：板块 API / ATDD 测试

```bash
pytest tests/test_sector_classification.py \
       tests/test_sector_classification_api.py \
       tests/test_sector_classification_data_init_atdd.py \
       tests/test_api/test_sectors_api.py \
       tests/api/test_sector_analysis_charts_api.py --no-cov -q
# => 1 failed, 61 passed
```

61 passed，1 failed。唯一失败：

```
tests/test_sector_classification_data_init_atdd.py::TestAC02_DailyIncrementalUpdate::test_ac02_02_scheduled_task_at_16_00
```

**失败根因（既有问题，与拆表无关）**：

```
assert daily_classification_job is not None, "每日板块分类更新任务未注册"
E   AssertionError: 每日板块分类任务未注册
E   assert None is not None
```

该断言失败的是"每日板块分类调度任务是否在 scheduler 注册"——属于 scheduler job 注册逻辑，**不读写 daily_market_data / moving_average_data / strength_scores 任何一张表**。证据：
- plan-04 green 证据 §3.3 #12 已记录同一失败（baseline 同样存在）
- 同类 scheduler 失败还有 test_scheduler_service.py 系列 6 个（daily_data_update / data_quality_check / cache_cleanup / get_jobs / trigger_job 等均为既有问题）
- 本失败不涉及任何板块 service 的读写路径，板块 API 行为本身（test_sectors_api / test_sector_classification_api / test_sector_analysis_charts_api）全部通过

补充：板块强度计算链路覆盖——`pytest tests/test_calculation_engine.py --no-cov -q` => **51 passed**（含 sector strength 计算覆盖）。

### 3.5 Task #5/#6：板块采集 / MA / 强度 task 写旧表

由于本环境无法实际启动服务触发 task，采用"代码审查 + 测试覆盖"方式证明 task 写旧表：

**a) 板块数据采集（collector）**：

`src/services/data_updater/collector.py` `_update_market_data` 的 sector 分支：

```python
# line 246-247（sector 分支）
stmt = pg_insert(DailyMarketData).values(
    entity_type='sector',
    ...
)
```

sector 采集写入旧表 `daily_market_data`（entity_type='sector'）；stock 分支（line 292）才写新表 `StockDailyMarketData`。两分支独立，互不影响。

**b) 板块 MA 计算（sector_ma_service）**：

`src/services/sector_ma_service.py`（零改动）：

```python
# line 381-382
ma_record = MovingAverageData(
    entity_type="sector",
    ...
)
self.session.add(record)
```

板块 MA 写旧表 `moving_average_data`（entity_type='sector'）。该文件未被本次改造触及（git diff 空），test_sector_ma_service.py 全通过。

**c) 板块强度计算（strength_service_v2）**：

`src/services/strength_service_v2.py` `calculate_sector_strength`（line 201-207）透传 `entity_type="sector"` 调 `_save_strength_score`，sector 分支（line 316-326）走 `Model=StrengthScore`，写旧表 `strength_scores`。test_sector_strength_service.py / test_strength_snapshot_service.py 全通过。

**结论**：板块三类 task（采集 / MA / 强度）全部写旧表，行为同改造前。task handler 测试虽无独立测试文件（项目无 tests/*task*sector* 专项测试），但通过 service 层单元测试 + 代码审查双重证明写表路径正确。

## 4. 板块全功能验证矩阵结论（plan-05 §3，13 项）

| # | 板块功能 | 验证结论 | 证据 |
|---|---------|---------|------|
| 1 | 板块强度排名 | ✅ 零回归 | ranking_service sector 分支读 StrengthScore（§3.2） |
| 2 | 板块强度统计 | ✅ 零回归 | ranking_service._calculate_percentile 读 StrengthScore（§3.2） |
| 3 | 板块强度详情 | ✅ 零回归 | sectors.py git diff 空（§3.1） |
| 4 | 板块强度历史 | ✅ 零回归 | strength_history_service.get_sector_history 读 StrengthScore（§3.2） |
| 5 | 板块均线历史 | ✅ 零回归 | sector_ma_service.py 零改动，读 MovingAverageData（§3.1 + §3.5b） |
| 6 | 板块成分股强势股 | ✅ 零回归 | sectors.py git diff 空（§3.1） |
| 7 | 板块强度等级表 | ✅ 零回归 | strength_grade_table_service.py git diff 空（§3.1） |
| 8 | 板块强度散点图 | ✅ 零回归 | strength_scatter_service.py git diff 空 + test_strength_scatter_service 10 passed（§3.1 + §3.3） |
| 9 | 板块分布 | ✅ 零回归 | sector_distribution_service.py git diff 空（§3.1） |
| 10 | 板块分类 | ✅ 零回归 | sector_classification_service.py git diff 空 + test_sector_classification_service 全通过（§3.1 + §3.3） |
| 11 | 板块数据采集 | ✅ 零回归 | collector sector 分支 pg_insert(DailyMarketData, entity_type='sector')（§3.5a） |
| 12 | 板块均线计算 | ✅ 零回归 | sector_ma_service 写 MovingAverageData(entity_type='sector')（§3.5b） |
| 13 | 板块强度计算 | ✅ 零回归 | strength_service_v2._save_strength_score sector 分支写 StrengthScore（§3.5c） |

**13 项全部零回归**。

## 5. 修改文件清单

| 文件 | 动作 | 说明 |
|------|------|------|
| docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-05-板块回归基线验证.md | modify | status: draft → review；Task #1~#6 → done；AC-03 全勾选；边界场景 → done |
| docs/e2e/evidence/plan-05-e2e-red-2026-07-07.md | create | red 证据（baseline 矩阵） |
| docs/e2e/evidence/plan-05-e2e-green-2026-07-07.md | create | green 证据（本文件） |

**未修改任何生产代码、测试代码、conftest**（plan-05 §8 边界：本功能是纯验证，不改任何代码）。

## 6. 验收标准核对

| 验收标准 | 状态 | 验证 |
|---------|------|------|
| AC-03：板块 6 service 文件零改动 | ✅ | git diff 6 文件输出为空（§3.1） |
| AC-03：api/v1/sectors.py 零改动 | ✅ | git diff 输出为空（§3.1） |
| AC-03：5 共享方法 sector 分支读旧表 | ✅ | 代码审查全部 sector 分支走旧表模型（§3.2） |
| AC-03：板块单元测试全通过 | ✅ | 6 文件 125 passed（§3.3） |
| AC-03：板块全功能矩阵 13 项零回归 | ✅ | 13 项逐一证明（§4） |
| AC-03：板块采集写旧表 daily_market_data | ✅ | collector sector 分支 pg_insert(DailyMarketData)（§3.5a） |
| AC-03：板块 MA/强度写旧表 | ✅ | sector_ma_service 写 MovingAverageData；strength_service_v2 写 StrengthScore（§3.5b/c） |
| task handler 执行验证（MA） | ✅（代码审查+测试覆盖） | sector_ma_service 写 MovingAverageData(entity_type='sector') + test_sector_ma_service 全通过；受环境限制无法启动服务实跑 task |
| task handler 执行验证（强度） | ✅（代码审查+测试覆盖） | strength_service_v2 sector 分支写 StrengthScore + test_sector_strength_service/test_strength_snapshot_service 全通过；受环境限制无法启动服务实跑 task |

## 7. 阻塞 / 偏离说明

- **无阻塞**：板块功能零回归，AC-03 全部达成
- **唯一失败（test_ac02_02_scheduled_task_at_16_00）**：既有 scheduler 注册问题，与拆表无关，plan-04 green 已记录在案，非本期范围
- **task handler 实跑豁免说明**：本环境无法启动 FastAPI 服务实际触发板块 MA/强度 task，故 Task #5/#6 采用"代码审查（写表模型）+ service 层单元测试覆盖"替代实跑。写表路径已通过 git diff（零改动）+ 代码审查（sector 分支写旧表模型）+ 单元测试三重证明，置信度高

## 8. 结论

**通过（板块功能零回归）**。

- 板块 7 个 service/api 文件 git diff 全部为空（Task #1）
- 5 个共享方法 sector 分支全部读/写旧表（Task #2），ADR-4 内部分发隔离有效
- 板块核心单元测试 125 全通过（Task #3）
- 板块 API 测试全通过，唯一失败为既有 scheduler 问题（Task #4）
- 板块采集/MA/强度 task 写旧表，代码审查 + 测试覆盖证明（Task #5/#6）
- 板块全功能验证矩阵 13 项全部零回归
- 未修改任何生产代码 / 测试代码 / conftest

整个"股票数据独立建表"拆表改造（plan-01 → plan-05）的最终质量门通过：板块功能与改造前完全一致（AC-03 达成），股票数据已独立到新三表（plan-01/02/03 已验证 AC-01/02/04/05/07），API 契约不变（plan-04 已验证 AC-06）。

red → green 对照：red 阶段建立的 13 项板块回归验证矩阵 baseline，在 green 阶段逐一证明零回归，无任何项与改造前行为偏离。
