---
feat_id: plan-07
phase: green
date: 2026-08-14
title: 首页融资融券面板（MarginPanel 卡片 + 双 Y 轴曲线 + 范围切换 + 空缺口错误态）— Playwright E2E green 阶段证据（第 17 期）
source_spec: docs/FEAT-0002-market-margin.md（REQ-7 首页面板 / AC-6）
source_plan: docs/17-融资融券数据同步与首页曲线图/17-2-实现计划-融资融券数据同步与首页曲线图/plan-07-首页融资融券面板.md
red_evidence: docs/e2e/evidence/plan-07-e2e-red-2026-08-14.md
note: 文件名日期用连字符格式（与 red 证据一致）：紧凑格式 plan-07-e2e-green-20260814.md 已被 16 期 market-metrics plan-07 的同名证据占用，为不覆盖历史证据故避让
---

# plan-07 E2E Green 证据：首页融资融券面板（第 17 期）

## 结论

**5/5 用例全部通过（TC-7.1~7.5），red → green 闭环完成。**

- red 阶段（2026-08-14）失败原因全部为「MarginPanel 组件未实现 + dashboard 未插入」，见 [red 证据](./plan-07-e2e-red-2026-08-14.md)。
- 实现阶段按 plan-07 Task 顺序完成 5 项前端交付（类型 → API 客户端 → 组件 → 挂载 → jest 组件测试），未修改 spec / mock helper / 用例文档（契约冻结零改动）。
- 实现后重跑 `pnpm test:e2e -- tests/e2e/margin-panel.spec.ts` → **5 passed（4.4s）**，与 red 阶段同命令同 spec 逐用例对照转绿。
- 16 期回归 `pnpm test:e2e -- tests/e2e/market-metrics-panel.spec.ts` → **8 passed（2.9s）**（dashboard/page.tsx 同文件编辑未破坏既有面板）。

## 实现交付物（green 转绿的代码变更）

| 文件 | 动作 | 说明 |
| --- | --- | --- |
| `web/src/types/marginTypes.ts` | create | MarginPoint / MarginTrendData / MarginRange / MarginDateResult / MarginTaskResult（四类一次写全 + plan-08 消费两类；rqmcl 注释「不入图，仅保留数据契约」） |
| `web/src/lib/api.ts` | modify | marketMetricsApi 之后新增 `marginApi.getTrend`（apiClient GET，泛型完整业务包 `{success, data}`；endpoint `/margin/trend?range=`，baseURL 已含 /api/v1 无双前缀） |
| `web/src/components/market-margin/MarginPanel.tsx` | create | SWR `['marginTrend', range]`（dedupingInterval:0）+ dynamic echarts ssr:false 单实例 + 4 卡片 ÷1e8 亿元 + 双 Y 轴（左 rzye+rzrqye / 右 rqye+rzmre，均 scale:true）+ legend 4 项 + connectNulls:false + 缺口/空态（管理员链接）/错误重试态 + `__echartsInst__` 测试钩子（onChartReady 挂 margin-chart 容器） |
| `web/src/app/dashboard/page.tsx` | modify | L69 `{!isLoading && <MarketMetricsPanel />}` 之后并列插入 `{!isLoading && <MarginPanel />}`（非管理员分支；管理员分支 IndexMonitorPage 未动） |
| `web/tests/margin/MarginPanel.test.tsx` | create | jest 组件测试 10 用例（加载/错误重试 mutate/空态双角色/卡片亿元换算/缺口提示/范围切换 aria-pressed） |

用例文档要求的 13 个 data-testid 全部暴露：`margin-panel` / `margin-latest-date` / `margin-card-{rzye,rqye,rzrqye,rzmre}` / `margin-range-{30,90,250}`（含 aria-pressed）/ `margin-chart` / `margin-missing-hint` / `margin-empty` / `margin-empty-admin-link` / `margin-error` / `margin-retry`，外加 `__echartsInst__` 测试钩子。

## 执行结果

- 命令：`cd web && pnpm test:e2e -- tests/e2e/margin-panel.spec.ts`（plan-07 §验证命令 3）
- 通过 / 失败：**5 passed / 0 failed**（0 skipped）
- 预检：`curl http://localhost:3100/` → `200`（dev server 在线，复用未重启）
- HTML 报告：`web/playwright-report/index.html`

### 用例明细（red → green 对照）

| # | 用例 | 类型 | red 表现 | green 表现 |
| --- | --- | --- | --- | --- |
| 1 | TC-7.1 面板渲染：market-metrics 之后 + 4 卡片（亿元）+ 双 Y 轴 legend（AC-6） | Happy | `margin-panel` not found（10s） | ✓ 1.2s：DOM 顺序（market-metrics 后、市场强度前）+ 最近结果日 2026-08-13 + 4 卡片 fixture÷1e8（zh-CN 2 位小数）+ 亿元标签 + legend 恰 4 项 + 4 series 全 line/connectNulls:false + 左轴 0=融资余额+两融合计余额、右轴 1=融券余额+融资买入额 + aria-pressed |
| 2 | TC-7.2 范围切换 30/90/250：发起对应 range 请求且无整页刷新（AC-6） | Happy | `margin-panel` not found | ✓ 369ms：waitForRequest 命中 range=90/250 + 初始 range=30 + URL 保持 /dashboard + window 标记仍 alive |
| 3 | TC-7.3 缺口断线：缺失日六指标 null 非 0 + 提示可见（AC-5/AC-6） | Edge | `margin-panel` not found | ✓ 1.1s：`margin-missing-hint`「部分日期无数据」可见 + 图表仍渲染 + 缺口索引 [2,4] 全 null 非 0 + 全程无 0 值点 |
| 4 | TC-7.4 空态：latest=null 纯文案，普通用户无数据管理链接 | Edge | `margin-panel` not found | ✓ 280ms：`margin-empty` 含「尚未同步/暂无」文案 + `margin-empty-admin-link` 计数 0 |
| 5 | TC-7.5 加载失败重试：仅重发 margin trend，不影响相邻 market-metrics 面板 | Error | `margin-error` not found | ✓ 1.1s：错误框 + 重试按钮 → 点击后 margin trend 计数 +1、/market-metrics/trend 计数不变（expect.poll）+ 重试后图表恢复渲染 |

> `__echartsInst__` 测试钩子按用例文档约定在 `onChartReady` 挂载，TC-7.1/7.3 的 legend 与 series null 断言均经 `getOption()` 读取成功。

## 配套验证（plan-07 §验证命令全量）

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | 无新增错误 | 现存错误均位于历史测试文件（tests/auth.test.tsx、tests/dashboard/SectorAnalysisPage.test.tsx、tests/hooks/useSectorScatterData.test.ts），与本期改动无关（与 red 证据记录一致） |
| `pnpm run lint` | 新增/修改文件 0 问题 | 仓库基线存在 194 个既有 lint 错误（含母本 MarketMetricsPanel.tsx/MarketMetricsPanel.test.tsx），本期新文件全部零告警（eslint 单独跑 4 个新/改文件无输出） |
| `pnpm build` | 通过 | Next.js 生产构建成功，/dashboard 正常产出 |
| `pnpm test -- tests/margin/MarginPanel.test.tsx` | 10/10 passed | jest 组件测试 |
| `pnpm test:e2e -- tests/e2e/margin-panel.spec.ts` | 5/5 passed | 本功能 E2E（green） |
| `pnpm test:e2e -- tests/e2e/market-metrics-panel.spec.ts` | 8/8 passed | 16 期面板回归（dashboard 同文件编辑无破坏） |

## 阻塞项

无。plan-07 全部 6 个 Task 完成，验收标准（AC-6 面板渲染/范围切换/缺口/空态/错误态/单位/布局/性能）均经 E2E + jest 覆盖通过。
