---
feat_id: plan-03
phase: red
created: 2026-06-24
plan_file: docs/08-基金扎堆股票分析/08-2-实现计划-基金扎堆股票分析/plan-03-前端下钻反查复用04.md
architecture_file: docs/08-基金扎堆股票分析/08-1-架构文档-基金扎堆股票分析.md
use_case_doc: docs/e2e/08-e2e-用例-基金扎堆股票分析.md
---

# plan-03 E2E red 证据：前端下钻反查复用 04

## 1. 范围

- **FEAT**：plan-03「前端下钻反查复用 04」
- **覆盖 AC**：AC-05（下钻反查 + 返回口径/页码/搜索词/滚动位置保持）
- **场景数**：5 个（TC-3.1 ~ TC-3.5；04 反查页侧 3 + 扎堆分析页侧 2）
- **测试方式**：Playwright E2E（前端 FEAT，参照 plan-02 范式：authedPage fixture + 全量 mock 安装）
- **复用声明**：完全复用 04 反查页核心逻辑（`ReverseLookupContent` + `useReverseLookup` + `ReverseLookupTable`）+ 04 `/api/v1/funds/reverse-lookup` 端点（stk_mkv_ratio ≥1% 口径，ADR-4），后端零改动；spec 侧仅复用现有 mock helper，无新增

## 2. 修改文件清单

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `web/tests/e2e/fund-reverse-lookup.spec.ts` | 追加 | AC-05 test.describe 块（3 场景）：TC-3.1 `from=fund-crowd` 差异提示 + 返回扎堆分析入口 / TC-3.2 无 from 原生入口零影响回归 / TC-3.3 点击返回扎堆分析跳转。复用现有 `authedPage` fixture + `REVERSE_LOOKUP_PAGE` + `mockReverseLookup` / `createTestReverseLookup`（`mock-fund-api.ts`） |
| `web/tests/e2e/fund-crowd-analysis.spec.ts` | 追加 | AC-05 test.describe 块（2 场景）：TC-3.4 反查按钮点击跳转 04 反查页 / TC-3.5 返回后口径/页码/搜索/scroll 恢复。复用现有 `authedPage` fixture + `installFullMocks` + `FUND_CROWD_ANALYSIS_PAGE` |
| `docs/e2e/08-e2e-用例-基金扎堆股票分析.md` | 追加 | FEAT-03 章节（5 场景表 + 汇总 + red/green 证据路径 + data-testid 清单 + E2E 编写规则对照），在 plan-02 既有文档末尾追加 |

### 未修改文件（复用确认）

- `web/tests/e2e/helpers/mock-fund-api.ts` — **无需新增 mock helper**。现有 `mockReverseLookup` + `createTestReverseLookup`（line 250-297 / 426-447）直接复用：mock 只关心 API 响应，不关心 query 参数；spec 侧 `page.goto` 带 `from=fund-crowd` query 即可触发 04 反查页条件渲染
- `web/tests/e2e/helpers/mock-fund-crowd-api.ts` — plan-02 已实现的 `mockCrowdRankings` / `mockCrowdIndustryDistribution` / `createTestCrowdRankings` / `createTestCrowdIndustryDistribution` 直接复用

## 3. 执行命令

```bash
cd web && npx playwright test tests/e2e/fund-reverse-lookup.spec.ts tests/e2e/fund-crowd-analysis.spec.ts
```

dev server：Next.js `npm run dev` 端口 3100（手动后台启动，`playwright.config.ts` 无 webServer 配置，与 06 / plan-02 范式一致）。

## 4. 运行结果

- **总结果**：24 tests（2 files），**5 failed / 19 passed**
  - 其中 **plan-03 AC-05 场景**：5 个 → **4 failed / 1 passed**（TC-3.1/3.3/3.4/3.5 失败，TC-3.2 通过）
  - **plan-02 TC-2.x 场景**：7 个 → 全部 passed（plan-02 已实现，回归正常）
  - **04 AC-04 既有场景**：12 个 → **11 passed / 1 failed**（1 个预先存在失败，与本 red 无关，见 §6 说明）
- **plan-03 red 结论**：预期失败，测试有效

### plan-03 AC-05 失败场景摘要

| # | 用例 | AC | 失败原因（根因：plan-03 功能未实现） |
| --- | --- | --- | --- |
| TC-3.1 | `from=fund-crowd` 时顶部展示差异提示与返回扎堆分析入口 | AC-05 | `getByTestId('fund-crowd-drilldown-hint')` `toBeVisible()` 失败（10s timeout，element not found）—— 04 反查页 `ReverseLookupContent` 未解析 `from` 参数、未条件渲染差异提示（plan-03 Task 6 未实现） |
| TC-3.3 | 点击返回扎堆分析跳转到扎堆分析页 | AC-05 | `getByTestId('back-to-fund-crowd').click()` 失败（30s timeout，locator not found）—— 返回扎堆分析入口未渲染（plan-03 Task 6 未实现） |
| TC-3.4 | 点击反查按钮跳转 04 反查页（带 symbol + from=fund-crowd） | AC-05 | `expect(page).toHaveURL(...)` 失败（URL 仍为扎堆分析页）—— `FundCrowdAnalysisPage.handleReverseLookup` 为 plan-02 占位（仅 `console.warn`，未 `router.push`，未写 sessionStorage）（plan-03 Task 4 未实现） |
| TC-3.5 | 返回后恢复原口径/页码/搜索/滚动位置 | AC-05 | `expect(Math.abs(scrollY - 200)).toBeLessThan(50)` 失败（Received: 200，即 scrollY=0 未恢复）—— `FundCrowdAnalysisPage` useEffect 返回状态恢复中 **scroll 恢复未实现**（plan-02 §3 line 59 注释「plan-03 会在此追加 scroll 恢复」；plan-03 Task 5 未实现）。注：scope/page/search/sessionStorage 消费 4 项断言均已通过（plan-02 基础恢复工作正常） |

### plan-03 AC-05 通过场景

| # | 用例 | 通过原因（合理，非测试漏洞） |
| --- | --- | --- |
| TC-3.2 | 无 `from` 参数（04 原生入口）不渲染差异提示与返回扎堆分析 | 04 原生入口本就无 `from` 参数 → `fund-crowd-drilldown-hint` / `back-to-fund-crowd` testid 在 red 阶段确实不存在（count=0 断言成立）+ 原「返回基金分析」入口可见（04 原生行为）。这是 04 原生零影响的回归保护用例，red 阶段通过是预期且合理的（与 green 阶段断言完全一致，不会因 plan-03 实现而改变） |

### 失败根因（统一：plan-03 功能未实现）

4 个失败场景的根因完全一致，均为 **plan-03 实现规格（§2 文件清单 Task 4-7）尚未落地**：

1. **Task 4（`FundCrowdAnalysisPage.handleReverseLookup` 实现）未落地** → TC-3.4 反查按钮点击无路由跳转（plan-02 占位仅 `console.warn`）
2. **Task 5（`FundCrowdAnalysisPage` 返回状态恢复追加 scroll 恢复）未落地** → TC-3.5 scroll 未恢复（plan-02 已实现 scope/page/search/sessionStorage 消费，仅缺 scroll）
3. **Task 6（04 反查页 `from` 参数解析 + 条件渲染差异提示 + 返回扎堆分析入口）未落地** → TC-3.1 / TC-3.3 缺 `fund-crowd-drilldown-hint` / `back-to-fund-crowd` 元素
4. **Task 7（04 反查页 `syncUrl` 保留 `from` 参数）未落地**（本 red 阶段 spec 未直接断言 syncUrl，但 green 阶段需覆盖筛选/翻页后 from 不丢失；如需追加可由 implementer 在 green 阶段补强）

这是 plan-03 §4 Task 列表全部 `todo` 状态的预期表现。spec 语法正确（`--list` 验证 24 个用例被收集，含 5 个 TC-3.x），mock helper 复用现有实现且能正确注册路由拦截，断言未放宽。

## 5. 证据完整性

- **非测试自身错误**：spec 编译通过（TypeScript 无报错），5 个 plan-03 用例 + 19 个既有用例全部被 Playwright 收集（`--list` 输出 `Total: 24 tests in 2 files`）
- **非环境错误**：dev server 正常运行（`curl http://localhost:3100/` 返回 200，Ready in 340ms）；authedPage fixture 注入正常；mock 路由拦截正常（TC-2.x + AC-04 既有 11 个用例通过证明 mock + 路由层工作正常）
- **断言强度未放宽**：
  - TC-3.1 保留「差异提示可见 + 含特定文案 + 返回扎堆分析入口可见 + 原返回基金分析 count=0」四断言
  - TC-3.3 保留「点击返回 → toHaveURL 匹配扎堆分析页」断言
  - TC-3.4 保留「URL 精确匹配 `symbol=600519&from=fund-crowd`」断言（正则全匹配，未放宽为「URL 含 reverse-lookup」）
  - TC-3.5 保留「scope-all aria-pressed=true + search 值=600 + sessionStorage 已清空 null + scrollY 误差<50」四断言（**MEMORY 提醒（`前端 E2E implementer 改 red spec 把关`）：scrollY 误差<50 不应被 green 阶段放宽为「不验证 scroll」；scope-all 恢复断言不应被放宽为「口径非空」**）

## 6. 关于预先存在的 AC-04 失败（与本 red 无关）

运行结果中 `fund-reverse-lookup.spec.ts:153` `AC-04：反查页 › 无 symbol 参数 › 直接访问无 symbol 参数时展示"请输入股票代码"` 失败（`getByText('请输入股票代码')` `toBeVisible()` 失败）。这是 plan brief 已知的**预先存在失败**（AC-04 placeholder 文案），与 plan-03 的 AC-05 实现完全无关：

- 该用例在 plan-03 red 之前就处于失败状态（04 反查页 symbol 缺失空状态文案与 spec 断言不一致）
- plan-03 不修改 04 反查页的 symbol 缺失空状态分支（plan-03 §3 #3.3 明确「`if (!symbol)` 分支原样保留」）
- green 阶段不应修复此用例（不在 plan-03 范围），如需修复应作为独立 FEAT

**plan-03 red 失败判定**：仅计 TC-3.1 / TC-3.3 / TC-3.4 / TC-3.5 共 4 个场景（plan-03 功能未实现），TC-3.2 作为回归保护通过合理。AC-04 既有失败不计入 plan-03 red 判定。

## 7. 截图路径

- `web/test-results/fund-reverse-lookup-AC-05：-8edc0-nd-crowd-时顶部展示差异提示与返回扎堆分析入口-admin/test-failed-1.png`（TC-3.1）
- `web/test-results/fund-reverse-lookup-AC-05：-2a116-wd）-TC-3-3-点击返回扎堆分析跳转到扎堆分析页-admin/test-failed-1.png`（TC-3.3）
- `web/test-results/fund-crowd-analysis-AC-05：-f25e0-页（带-symbol-from-fund-crowd）-admin/test-failed-1.png`（TC-3.4）
- `web/test-results/fund-crowd-analysis-AC-05：-554a5--TC-3-5-返回后恢复原口径-页码-搜索-滚动位置-admin/test-failed-1.png`（TC-3.5）

TC-3.1 / TC-3.3 截图应展示 04 反查页正常渲染（表格可见）但顶部无差异提示与返回扎堆分析入口（plan-03 Task 6 未实现）。TC-3.4 截图应展示扎堆分析页（URL 未跳转）。TC-3.5 截图应展示扎堆分析页 scope=全部基金 / 搜索=600 已恢复（plan-02 基础恢复工作），但 scrollY=0（scroll 恢复未实现）。

## 8. HTML 报告

`web/playwright-report/index.html`

## 9. 下一步

将 plan-03 状态推进到 `implement`，由 implementer 落地 plan-03 §4 Task 列表的 Task 4-7（共 4 个实现任务）：

1. **Task 4**：`web/src/components/fund-crowd-analysis/FundCrowdAnalysisPage.tsx` — 实现 `handleReverseLookup`（替换 plan-02 占位）：记录 sessionStorage（scope/page/search/scrollX/scrollY）+ `router.push` 到 `/dashboard/funds/reverse-lookup?symbol=...&from=fund-crowd`
2. **Task 5**：增强 `FundCrowdAnalysisPage` useEffect 返回状态恢复 —— 追加 scroll 恢复（`requestAnimationFrame` + `setTimeout(0)` 等 SWR 加载完成；必要时改 `useEffect` 监听 `rankings` 变化）
3. **Task 6**：修改 `web/src/app/dashboard/funds/reverse-lookup/page.tsx` 的 `ReverseLookupContent` —— 解析 `from` 参数 + 条件渲染差异提示（`data-testid="fund-crowd-drilldown-hint"` + `role="note"`）+ 返回扎堆分析入口（`data-testid="back-to-fund-crowd"`，仅 `from=fund-crowd` 时渲染，替代原「返回基金分析」按钮）
4. **Task 7**：修改 04 反查页 `syncUrl`（line 77-86）—— 保留 `from` 参数（`useCallback` 依赖数组追加 `fromFundCrowd`），避免筛选/翻页时 `router.replace` 丢失 `from=fund-crowd` 标识

实现完成后跑同一套 spec 验证 TC-3.1~3.5 全部通过（TC-3.2 应继续保持通过），green 证据存 `docs/e2e/evidence/plan-03-08-e2e-green-{date}.md`。

### implementer 必须 land 的 data-testid（spec 选择器依赖，plan-03 §3 已约定）

- `back-to-fund-crowd`（04 反查页返回扎堆分析按钮，仅 `from=fund-crowd` 时渲染）
- `fund-crowd-drilldown-hint`（04 反查页差异提示容器 + `role="note"`，仅 `from=fund-crowd` 时渲染）
- plan-02 已落地复用：`crowd-reverse-lookup-{stockSymbol}` / `crowd-scope-all` / `crowd-search-input` / `crowd-ranking-table`

### 回归验证（green 阶段）

- 现有 04 AC-04 既有用例（除预先存在的 symbol 缺失失败外）不破坏
- plan-02 TC-2.1~2.7 全部通过
- 04 原生入口（无 from）体验零影响：TC-3.2 继续 pass

### MEMORY 提醒（`前端 E2E implementer 改 red spec 把关`）

green 阶段 implementer 改 red spec 是常态，主 agent 必须核查断言未放宽。重点关注：

- TC-3.4 的「URL 精确匹配 `symbol=600519&from=fund-crowd`」断言不应被放宽为「URL 含 reverse-lookup」
- TC-3.5 的「scrollY 误差 < 50px」断言不应被放宽为「不验证 scroll」或「scrollY > 0」
- TC-3.5 的「scope-all aria-pressed=true」断言不应被放宽为「口径非空」
- TC-3.5 的「sessionStorage 已清空 null」断言不应被删除

## 10. 阻塞项

无阻塞项。plan-03 red 阶段全部完成：

- 5 个场景已写入 2 个 spec 文件（追加，非新建）
- AC-05 章节已追加到用例文档
- 4 个核心场景（TC-3.1/3.3/3.4/3.5）预期失败，根因均为 plan-03 功能未实现（非测试错误 / 非环境错误）
- TC-3.2 作为回归保护通过合理（无 from 时本就不应有 plan-03 新增元素）
- dev server 启动 / 运行 / kill 流程正常
