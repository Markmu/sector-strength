---
feat_id: plan-08
phase: green
date: 2026-08-14
title: 数据管理融资融券同步面板（MarginSyncPanel：Tab 接入 + 创建/轮询/校验/互斥/dateResults/记录分页）— Playwright E2E green 阶段证据（17 期）
source_spec: docs/FEAT-0002-market-margin.md
source_plan: docs/17-融资融券数据同步与首页曲线图/17-2-实现计划-融资融券数据同步与首页曲线图/plan-08-数据管理融资融券同步面板.md
red_evidence: docs/e2e/evidence/plan-08-e2e-red-2026-08-14.md
---

# plan-08 E2E Green 证据：数据管理融资融券同步面板（17 期）

## 结论

**通过。** red 阶段的 9 个用例（TC-8.1~8.9）在实现后全部转 green：**9 passed / 0 failed（9.5s）**，覆盖 AC-7（同步面板：进度/明细/历史记录）+ 前端校验拦截 + 互斥交互。

## 实现摘要（对应 red 证据「失败原因」表的逐项转绿）

| 模块 | 文件 | 实现（Task） | green 表现 |
| --- | --- | --- | --- |
| 数据管理页 Tab | `web/src/app/dashboard/admin/data/page.tsx` | DataTab 追加 `'market-margin'` + nav 按钮 `data-testid="tab-market-margin"`（文案「融资融券」，市场量价之后）+ 内容区渲染 `<MarginSyncPanel />`（Task 6） | TC-8.1 tab 可见/可点，面板渲染，默认近 1 年（start=今天−364、end=今天）断言通过 |
| 同步面板组件 | `web/src/components/market-margin/MarginSyncPanel.tsx`（新建） | 创建区（默认近 1 年 + 前端校验倒置/未来/超 10 年）/进度区/终态三类计数/dateResults 展开（状态 + 截断 100 字符 reason，无四类计数）/unprocessedDates 提示/记录分页/互斥交互，暴露全部 14 个 data-testid（Task 3/4/5） | TC-8.1~8.9 全部断言通过 |
| API 客户端 | `web/src/lib/api.ts` | adminApi 追加 `initMargin(start_date, end_date)` POST `/admin/init/margin`，body snake_case（Task 2） | TC-8.2 POST body `toEqual({start_date, end_date})` 通过 |
| Hook 扩展 | `web/src/hooks/useTaskStatus.ts` | TaskData 泛型化 `TaskData<TResult = MarketMetricsTaskResult>`，result 字段类型参数化；MarginSyncPanel 以 `useTaskStatus<MarginTaskResult>` 直消费 camelCase result（Task 1；默认参数保持 16 期既有消费方类型不变，未放宽 any、无转换层） | TC-8.2 轮询序列 running→completed 终态三类计数显示 fixture 值 |

## 执行结果

### 1. E2E（green 核心）

- 命令：`cd web && pnpm test:e2e -- tests/e2e/margin-sync.spec.ts --reporter=list`（dev server 复用 3100 已有进程）
- 结果：**9 passed / 0 failed**（workers=1）

```text
Running 9 tests using 1 worker

  ✓  1 TC-8.1 数据管理页新增「融资融券」Tab，点击后渲染面板且默认近 1 年 (375ms)
  ✓  2 TC-8.2 合法创建（body snake_case）→ 轮询进度先现后隐 → 终态三类计数 → 记录列表出现该任务 (6.7s)
  ✓  3 TC-8.3 起止倒置：按钮禁用且不发请求 (286ms)
  ✓  4 TC-8.4 未来结束日：按钮禁用且不发请求 (280ms)
  ✓  5 TC-8.5 跨度超 10 年：按钮禁用且不发请求 (297ms)
  ✓  6 TC-8.6 点开某失败日期显示状态与截断原因；unprocessedDates 非空时独立提示块可见 (328ms)
  ✓  7 TC-8.7 已有同类任务运行时创建按钮禁用并提示 (291ms)
  ✓  8 TC-8.8 后端互斥拒绝时展示返回 message (313ms)
  ✓  9 TC-8.9 记录列表按 createdAt 倒序、分页可用、状态列渲染五态 (287ms)

  9 passed (9.5s)
```

关键断言落点（与用例文档 18 号逐条对应）：

- TC-8.1：`tab-market-margin` 文案含「融资融券」；`margin-sync-panel` + `aria-label="开始日期"/"结束日期"` + `margin-sync-start-button` 可见；默认值 startDate=今天−364、endDate=今天（近 1 年，区别于 16 期 30 日）
- TC-8.2：POST `/api/v1/admin/init/margin` body `toEqual({ start_date: '2026-07-15', end_date: '2026-08-13' })`（snake_case）；`margin-sync-progress` 先 `toBeVisible` 后 `toBeHidden`（mock 序列 3 次 running 后 completed）；`margin-sync-success/skipped/failed-count` 与 fixture 一致（2/8/1）；终态回调 refreshRecords 后 `margin-sync-records` 出现该 taskId（前 8 位）
- TC-8.3/8.4/8.5：三类非法输入 → `margin-sync-start-button` 禁用 + `margin-sync-validation-error` 可见 + `expect.poll(initCalls).toBe(0)`（网络零调用）
- TC-8.6：失败日期行展开含「失败」+ reason 前 50 字符、不含末尾 20 字符（截断 100 字符）；面板不含「应参与」「全天停牌」（两融无四类计数）；`margin-sync-unprocessed-dates` 提示块含未处理日期
- TC-8.7：记录含 running 同类任务 → 按钮禁用 + `margin-sync-mutex-hint` 可见
- TC-8.8：POST init `success=false`（HTTP 200）→ `margin-sync-validation-error` 包含后端互斥 message（AdminApiClient 抛错携带 message）
- TC-8.9：`margin-sync-records` 按 createdAt 倒序、状态徽章（已完成/失败/已取消）、`margin-sync-records-next-page` 可见（total=25 > page_size=20）

### 2. jest 组件测试（Task 7）

- 命令：`cd web && pnpm test -- tests/margin/MarginSyncPanel.test.tsx`
- 结果：**1 suite passed，14/14 tests passed**

覆盖：默认近 1 年渲染 / 三类校验拦截 / initMargin 位置参数调用 / 后端互斥拒绝 message / 记录互斥禁用 / 终态三类计数 / 失败日展开（状态 + 截断 reason + 反向断言无四类计数）/ unprocessedDates 提示 / 本地 running 隐藏历史结果（S-1 占位）/ 终态恢复 / 记录徽章 + 分页 / 空态。

### 3. 类型 / lint（基线不新增）

- `pnpm exec tsc --noEmit`：8 个 error，全部位于既有历史测试文件（tests/auth、tests/dashboard/SectorAnalysisPage、tests/hooks/useSectorScatterData）——与实现前基线完全一致，**src/ 与新增文件零错误**
- `pnpm run lint`：260 problems（186 errors / 74 warnings）——与实现前基线完全一致，**新增/修改文件零新增告警**（新测试文件用显式类型替代 any）

### 4. 16 期母本回归（同文件编辑不破坏既有面板）

- `pnpm test:e2e -- tests/e2e/market-metrics-sync.spec.ts`：**10 passed / 0 failed**（useTaskStatus 泛型默认参数保持 MarketMetricsSyncPanel 类型不变；data/page.tsx 三处对称扩展未动 16 期行）
- `pnpm test -- tests/margin/ tests/market-metrics/`：**4 suites / 50 tests passed**（margin-panel（plan-07）与 market-metrics 组件测试均未受影响）

## 本阶段产出（implementer）

| 动作 | 文件 |
| --- | --- |
| create | `web/src/components/market-margin/MarginSyncPanel.tsx`（同步面板组件，Task 3/4/5） |
| create | `web/tests/margin/MarginSyncPanel.test.tsx`（jest 组件测试 14 例，Task 7） |
| modify | `web/src/hooks/useTaskStatus.ts`（TaskData/UseTaskStatusOptions/UseTaskStatusResult 泛型参数化，默认 MarketMetricsTaskResult 兼容既有消费方，Task 1） |
| modify | `web/src/lib/api.ts`（adminApi.initMargin，Task 2） |
| modify | `web/src/app/dashboard/admin/data/page.tsx`（DataTab 联合类型 + tab 按钮 + 挂载，Task 6） |

未动：后端、`marginTypes.ts`（plan-07 交付，直消费）、`margin-panel.spec.ts` / `mock-margin-api.ts`（plan-07）、`market-metrics-sync.spec.ts` / `mock-market-metrics-sync-api.ts`（16 期）、README §7.2 状态机表、`margin-sync.spec.ts` / `mock-margin-sync-api.ts`（red 阶段契约原样使用，零修改）。

## 阻塞项

无。plan-08 全部 8 个 Task 完成，E2E red→green 闭环（red 证据：`plan-08-e2e-red-2026-08-14.md`，0/9 → 9/9）。
