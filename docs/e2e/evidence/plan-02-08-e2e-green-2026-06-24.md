---
feat_id: plan-02
phase: green
created: 2026-06-24
plan_file: docs/08-基金扎堆股票分析/08-2-实现计划-基金扎堆股票分析/plan-02-前端扎堆分析页.md
architecture_file: docs/08-基金扎堆股票分析/08-1-架构文档-基金扎堆股票分析.md
use_case_doc: docs/e2e/08-e2e-用例-基金扎堆股票分析.md
red_evidence: docs/e2e/evidence/plan-02-08-e2e-red-2026-06-24.md
spec_file: web/tests/e2e/fund-crowd-analysis.spec.ts
---

# plan-02 E2E green 证据：前端扎堆分析页

## 1. 范围

- **FEAT**：plan-02「前端扎堆分析页」（status: review）
- **覆盖 AC**：AC-01 / AC-02 / AC-03 / AC-04 / AC-06 / AC-07 / AC-08（共 7 条；AC-05 由 plan-03 独立承接）
- **场景数**：7 个（TC-2.1 ~ TC-2.7）
- **测试方式**：Playwright E2E（前端 FEAT）
- **结论**：**通过**（7 passed / 7 总数）

## 2. 执行命令

```bash
cd web && npx playwright test tests/e2e/fund-crowd-analysis.spec.ts
```

dev server：Next.js `npm run dev` 端口 3100（手动后台启动；`playwright.config.ts` 无 `webServer` 配置，与 06 范式一致）。本次执行前启动 dev server，`curl http://localhost:3100/dashboard/fund-crowd-analysis` 返回 200，证明路由入口已落地。

## 3. 目标 spec 结果

```
Running 7 tests using 1 worker

  ✓  1 TC-2.1 进入页面默认展示仅主动基金扎堆度排行榜 (235ms)        [AC-01]
  ✓  2 TC-2.2 切换口径为全部基金后排行榜重新计算 (320ms)             [AC-02]
  ✓  3 TC-2.3 环比变化列展示升降数值与新进标识 (220ms)               [AC-03]
  ✓  4 TC-2.4 行业分布按扎堆股数量占比展示 (215ms)                   [AC-04]
  ✓  5 TC-2.5 上期数据缺失时环比列显示占位符 (214ms)                 [AC-06]
  ✓  6 TC-2.6 持仓数据未同步展示整页空状态 (217ms)                   [AC-07]
  ✓  7 TC-2.7 搜索过滤、无结果提示与清空恢复 (2.3s)                   [AC-08]

  7 passed (4.0s)
```

### 场景-AC 对应矩阵

| # | 用例 | AC | 关键呈现验证点 | 结果 |
| --- | --- | --- | --- | --- |
| TC-2.1 | 进入页面默认展示仅主动基金扎堆度排行榜 | AC-01 | 默认 `crowd-scope-active` `aria-pressed=true`；表格 3 行按基金数降序（286 > 198 > 45） | ✓ |
| TC-2.2 | 切换口径为全部基金后排行榜重新计算 | AC-02 | 切换 `crowd-scope-all` → `aria-pressed=true`；首行基金数 286→572（mock 翻倍）；切回 active → 286 恢复 | ✓ |
| TC-2.3 | 环比变化列展示升降数值与新进标识 | AC-03 | 600519 基金 +12（绿色 ↑）；300750 基金 -8（红色 ↓）；688981 `crowd-new-badge-688981` 新进标识可见 | ✓ |
| TC-2.4 | 行业分布按扎堆股数量占比展示 | AC-04 | `crowd-industry-distribution` 可见；`crowd-industry-bar-食品饮料` 渲染且 label 含 `16.0%`；电力设备/银行标签可见 | ✓ |
| TC-2.5 | 上期数据缺失时环比列显示占位符 | AC-06 | `hasPrevPeriod=false` 时 3 行 `crowd-change-cell-*` 均含「—」；`crowd-new-badge-688981` count=0（无新进误显） | ✓ |
| TC-2.6 | 持仓数据未同步展示整页空状态 | AC-07 | `crowd-empty-portfolio` 可见 + 文案「暂无基金持仓数据」可见；`crowd-ranking-table` count=0 + `crowd-industry-distribution` count=0（不渲染业务区） | ✓ |
| TC-2.7 | 搜索过滤、无结果提示与清空恢复 | AC-08 | 「600」前缀→1 行命中 600519；「茅台」名称包含→1 行；「不存在的股票」→「未找到匹配股票」；点 `crowd-search-clear` → 恢复 3 行 | ✓ |

## 4. 关键呈现验证点（实现已落地的 data-testid）

red 证据 §7 列出的 data-testid 清单全部在源码中验证存在并被 spec 命中：

- **口径切换**：`crowd-scope-selector` / `crowd-scope-active` / `crowd-scope-all`（`aria-pressed` 三态正确切换）— TC-2.1/2.2 命中
- **行业分布双轨标签**：`crowd-industry-distribution` 容器 + `crowd-industry-bar-{industry}` 可点击 DOM button（与 06 IndustryDistribution 同范式，规避 ECharts canvas 点击不稳定）— TC-2.4 命中
- **排行榜表格**：`crowd-ranking-table` 根容器 + `tbody tr` 按 `.nth(idx)` 定位 — TC-2.1/2.2/2.3/2.5/2.7 命中
- **搜索框**：`crowd-search-input` + `crowd-search-clear` — TC-2.7 命中
- **环比列三态**：
  - `crowd-new-badge-{stockSymbol}`（新进标识，AC-03）— TC-2.3 命中可见 / TC-2.5 命中 count=0
  - `crowd-change-cell-{stockSymbol}`（占位符「—」/ 数值 + 箭头）— TC-2.5 命中「—」断言
- **空状态**：`crowd-empty-portfolio`（持仓未同步整页空状态，AC-07）— TC-2.6 命中

### 关键降级路径覆盖

| 降级等级 | 场景 | 验证用例 | 结果 |
| --- | --- | --- | --- |
| L5（无持仓数据） | `hasData=false` 整页空状态 | TC-2.6（AC-07） | ✓ |
| L4（无上期数据） | `hasPrevPeriod=false` 环比列统一「—」 | TC-2.5（AC-06） | ✓ |
| AC-03 新进标识 | `isNew=true` 显示 ★ 新进 | TC-2.3 | ✓ |
| 口径切换重算 | scope 切换 → rankings + industry-distribution 联动重发 | TC-2.2（AC-02） | ✓ |
| 搜索三态 | 命中（代码前缀 + 名称包含）→ 无匹配 → 清空恢复 | TC-2.7（AC-08） | ✓ |

## 5. 断言强度核查（MEMORY `前端 E2E implementer 改 red spec 把关`）

green 阶段 spec 与 red 阶段一致，**未放宽断言**：

- **TC-2.5（AC-06）**：保留「3 行环比列均含『—』」+「新进标识 count=0」双断言；通过循环逐行断言 `crowd-change-cell-*` `toContainText('—')`，未放宽为「部分行」
- **TC-2.7（AC-08）**：保留「600 前缀→1 行」「茅台→1 行」「无匹配→未找到匹配股票」「清空→恢复 3 条」四阶段断言；`toHaveCount(3)` 精确数量断言未放宽为「恢复非空」
- **TC-2.6（AC-07）**：保留「空状态可见 + 排行榜 count=0 + 行业分布 count=0」三断言
- **TC-2.2（AC-02）**：保留口径切换前后双向断言（切到 all 验证 572，切回 active 验证 286 恢复）

## 6. 回归验证

### 现有股东分析 spec（plan-04 / 必须不破坏）

```bash
cd web && npx playwright test tests/e2e/shareholder-analysis.spec.ts
```

```
Running 11 tests using 1 worker
  ✓  1-11（TC-4.1 ~ TC-4.10 全部通过）
  11 passed (8.5s)
```

**结论**：plan-02 对 `DashboardLayout.tsx` 的侧边栏改造（追加「基金扎堆分析」导航项 + import `UsersRound`）未破坏 plan-04 股东分析页的 11 个场景，全部通过。

### fund-reverse-lookup.spec.ts（plan-03 范围，参考性回归）

```bash
cd web && npx playwright test tests/e2e/fund-reverse-lookup.spec.ts
```

```
Running 12 tests using 1 worker
  ✓  11 passed
  ✗  1 failed：AC-04「无 symbol 参数」场景
       - locator: main.getByText('请在基金分析页面输入股票代码进行反查')
       - 失败原因：反查页空状态文案未渲染（plan-03 范围，与 plan-02 完全无关）
  11 passed, 1 failed (19.7s)
```

**结论**：fund-reverse-lookup.spec.ts 有 **1 个预先存在的 failed**，属于 **plan-03 范围**（反查页 placeholder 文案「请在基金分析页面输入股票代码进行反查」未渲染），与 plan-02 前端扎堆分析页无任何代码依赖关系。plan-02 不负责修复此 spec，plan-03 落地后由其 green 阶段处理。其余 11 个场景全部通过，证明 plan-02 未影响反查页主体功能。

## 7. HTML 报告与截图

- **HTML 报告**：`web/playwright-report/index.html`
- **失败截图**：本次 green 阶段无失败（7/7 通过），无 `test-results/fund-crowd-analysis-*/test-failed-*.png` 产物

## 8. 实现文件清单（已落地，对应 plan-02 §2）

| 文件 | 动作 | 验证 |
| --- | --- | --- |
| `web/src/lib/api.ts` | modify | `fundCrowdAnalysisApi` 对象 + 5 个 TS 类型已加 |
| `web/src/hooks/useFundCrowdAnalysis.ts` | create | 2 个 SWR hooks（rankings + industry-distribution） |
| `web/src/hooks/index.ts` | modify | 导出已注册 |
| `web/src/components/fund-crowd-analysis/FundCrowdAnalysisPage.tsx` | create | 主页面（scope/page/search 状态 + 空状态分支） |
| `web/src/components/fund-crowd-analysis/CrowdScopeSelector.tsx` | create | 口径切换按钮组 |
| `web/src/components/fund-crowd-analysis/CrowdIndustryDistribution.tsx` | create | ECharts 双轨标签条形图 |
| `web/src/components/fund-crowd-analysis/CrowdRankingTable.tsx` | create | 排行榜表格（搜索 + 环比列 + 反查按钮 + 分页） |
| `web/src/app/dashboard/fund-crowd-analysis/page.tsx` | create | 路由入口（`'use client'`） |
| `web/src/components/dashboard/DashboardLayout.tsx` | modify | 侧边栏追加「基金扎堆分析」 |
| `web/tests/e2e/helpers/mock-fund-crowd-api.ts` | create | mock helper + 工厂 |
| `web/tests/e2e/fund-crowd-analysis.spec.ts` | create | 7 场景 spec |

## 9. 阻塞项

**无**。plan-02 green 已通过，可推进至 task-review。

下游说明：fund-reverse-lookup.spec.ts 的 1 个 failed 属 plan-03 范围，不应阻塞 plan-02 的 review→done 流程。
