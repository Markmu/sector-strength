---
feat_id: plan-08
phase: red
date: 2026-08-14
title: 数据管理融资融券同步面板（MarginSyncPanel：Tab 接入 + 创建/轮询/校验/互斥/dateResults/记录分页）— Playwright E2E red 阶段证据（17 期）
source_spec: docs/FEAT-0002-market-margin.md
source_plan: docs/17-融资融券数据同步与首页曲线图/17-2-实现计划-融资融券数据同步与首页曲线图/plan-08-数据管理融资融券同步面板.md
---

# plan-08 E2E Red 证据：数据管理融资融券同步面板（17 期）

## 结论

**预期失败，测试有效。**

- plan-08「数据管理融资融券同步面板」是用户可观察的前端功能（数据管理页新增「融资融券」Tab + 新建 `MarginSyncPanel`），按 README §9.2 走完整 E2E-TDD 红绿循环，本证据为 **red 阶段**。前置 plan-05/07 已 done（后端 POST `/api/v1/admin/init/margin` 契约已上线；`marginTypes.ts` 的 `MarginTaskResult`/`MarginDateResult` 已由 plan-07 写全）。
- plan-08 范围的 **9 个用例（TC-8.1~8.9）全部失败**，失败原因均为「融资融券 Tab 与 MarginSyncPanel 组件尚未实现」：`tab-market-margin` 不存在（TC-8.1 首断言 `toBeVisible` element(s) not found，10s）；其余 8 个用例首个动作 `openMarginTab` 点击 `tab-market-margin` 超时（30s，等待该元素出现）。
- spec 文件可被 Playwright 正常收集（9 tests，0 passed + 9 failed，0 skipped），无编译错误、无 import 错误——失败完全来自被测功能缺失，符合 red 关键原则。
- **环境已就绪且宿主页稳定**：dev server（3100，复用已有进程，未新起）`/` 与 `/dashboard/admin/data` 均返回 200。admin 认证 fixture（localStorage user.role=admin + access_token cookie）生效；Playwright error-context 页面快照证实：标题「数据管理 / 数据初始化和计算管理」、既有七个 Tab（数据状态[默认选中]/数据初始化/板块均线计算/板块强度计算/券商金股同步/指数数据/市场量价）齐全、默认「数据状态」Tab 三类状态卡片正常（`mockDataStatusNormal` 生效）、admin 侧边栏完整、TestAdmin 已登录，**无 /login 重定向、无错误态**——9 个失败只能归因于 plan-08 待实现（`tab-market-margin` Tab 按钮 + `MarginSyncPanel` 组件）。

## 关联文档

- 功能文件：`docs/17-融资融券数据同步与首页曲线图/17-2-实现计划-融资融券数据同步与首页曲线图/plan-08-数据管理融资融券同步面板.md`（§实现规格-5 的 4 条 Given/When/Then 场景；§验收标准 AC-7；Task 8）
- 实现计划 README：`docs/17-融资融券数据同步与首页曲线图/17-2-实现计划-融资融券数据同步与首页曲线图/README.md`（§7.2 状态机 plan-08 当前步骤 red-e2e）
- 用例文档（本阶段新增）：`docs/e2e/18-e2e-用例-融资融券同步面板.md`
  - 编号裁定：plan-08 文件清单原写 `17-e2e-用例-融资融券同步面板.md`，但 17 号已被 plan-07 的 `17-e2e-用例-融资融券面板.md` 占用，沿用 docs/e2e/ 既有递增惯例定为 **18 号**
- 测试文件（本阶段新增）：`web/tests/e2e/margin-sync.spec.ts`
- Mock helper（本阶段新增）：`web/tests/e2e/helpers/mock-margin-sync-api.ts`（创建成功/互斥拒绝/校验拒绝 + 轮询序列 running→completed + result fixture + 记录列表 + test data factory）
- 范式母本：`web/tests/e2e/market-metrics-sync.spec.ts` + `web/tests/e2e/helpers/mock-market-metrics-sync-api.ts`（16 期 plan-08，helper 范式逐段照抄，仅按两融契约裁剪）
- 未动的既有资产（回归安全）：`web/tests/e2e/margin-panel.spec.ts`、`web/tests/e2e/helpers/mock-margin-api.ts`（plan-07 已 green）、`web/src/lib/api.ts`、`web/src/app/dashboard/admin/data/page.tsx`、README §7.2 状态机表

## 失败原因

plan-08 待实现的「融资融券」Tab 按钮 + `MarginSyncPanel` 组件当前均不存在，导致 9 个用例无法走到业务断言：

| 模块 | 文件 | 待实现（plan-08 Task） | red 表现 |
| --- | --- | --- | --- |
| 数据管理页 Tab | `web/src/app/dashboard/admin/data/page.tsx` | DataTab 联合类型追加 `'market-margin'` + nav 按钮 `data-testid="tab-market-margin"`（文案「融资融券」，市场量价之后）+ 内容区渲染 `<MarginSyncPanel />`（Task 6） | `tab-market-margin` 不存在（TC-8.1 首断言 element(s) not found 10s；TC-8.2~8.9 点击超时 30s） |
| 同步面板组件 | `web/src/components/market-margin/MarginSyncPanel.tsx` | 新建：创建区（默认近 1 年 + 前端校验）/进度区/终态三类计数/dateResults 展开（状态 + 截断 reason，无四类计数）/unprocessedDates 提示/记录分页/互斥交互（Task 3/4/5） | `margin-sync-panel` 及内部 testid（start-button/progress/success-skipped-failed-count/date-result-list/date-result-{tradeDate}/unprocessed-dates/mutex-hint/validation-error/records/records-next-page）全部不存在 |
| API 客户端 | `web/src/lib/api.ts` | adminApi 追加 `initMargin`（Task 2，snake_case body；red 不改该文件） | 不存在（spec 用 mock 拦截，red 不依赖） |
| Hook 扩展 | `web/src/hooks/useTaskStatus.ts` | TaskData 增加 `result?: MarginTaskResult \| null`（Task 1） | 不存在（mock 响应已含 camelCase result，消费侧未建） |

预检证据（red 运行前后 dev server 探活）：`curl http://localhost:3100/` → `200`；`curl http://localhost:3100/dashboard/admin/data` → `200`（复用已在跑的 dev server，未新起进程）。

## 执行结果

- 命令：`cd web && pnpm test:e2e -- tests/e2e/margin-sync.spec.ts --reporter=list`（等价于 plan-08 §6 验证命令 3 `pnpm test:e2e -- tests/e2e/margin-sync.spec.ts`）
- 通过 / 失败：**0 passed / 9 failed**（共 9 tests，0 skipped）
- 失败截图：`web/test-results/margin-sync-plan-0-*/test-failed-1.png`（每个失败用例一份）
- 错误上下文：`web/test-results/margin-sync-plan-0-*/error-context.md`
- HTML 报告：`web/playwright-report/index.html`

### 用例明细

| # | 用例 | 类型 | 结果 | 失败原因（摘要） |
| --- | --- | --- | --- | --- |
| 1 | TC-8.1 数据管理页新增「融资融券」Tab，点击后渲染面板且默认近 1 年 | Happy | failed | `getByTestId('tab-market-margin').toBeVisible` element(s) not found（10s） |
| 2 | TC-8.2 合法创建（body snake_case）→ 轮询进度先现后隐 → 终态三类计数 → 记录列表出现该任务（AC-7） | Happy | failed | `openMarginTab` 点击 `tab-market-margin` 超时（30s） |
| 3 | TC-8.3 起止倒置：按钮禁用且不发请求 | Edge | failed | 同上，`tab-market-margin` 不存在（30s） |
| 4 | TC-8.4 未来结束日：按钮禁用且不发请求 | Edge | failed | 同上（30s） |
| 5 | TC-8.5 跨度超 10 年：按钮禁用且不发请求 | Edge | failed | 同上（30s） |
| 6 | TC-8.6 点开某失败日期显示状态与截断原因；unprocessedDates 非空时独立提示块可见（AC-7） | Happy | failed | 同上（30s） |
| 7 | TC-8.7 已有同类任务运行时创建按钮禁用并提示 | Edge | failed | 同上（30s） |
| 8 | TC-8.8 后端互斥拒绝时展示返回 message | Error | failed | 同上（30s） |
| 9 | TC-8.9 记录列表按 createdAt 倒序、分页可用、状态列渲染五态 | Happy | failed | 同上（30s） |

> 注：TC-8.1 首断言为 `tab-market-margin` 可见（10s 超时，element(s) not found）；TC-8.2~8.9 首个动作为 `openMarginTab()` 点击该 Tab（30s 超时等待元素出现）。所有失败均为「业务 Tab/组件未实现」类失败，非测试 bug / 环境错误。

### 失败输出摘录

```text
1) [admin] › tests/e2e/margin-sync.spec.ts:98:3 › plan-08：数据管理融资融券同步面板 ›
   TC-8.1 数据管理页新增「融资融券」Tab，点击后渲染面板且默认近 1 年

   Error: expect(locator).toBeVisible() failed
   Locator: getByTestId('tab-market-margin')
   Expected: visible
   Timeout: 10000ms
   Error: element(s) not found

2) [admin] › tests/e2e/margin-sync.spec.ts:127:3 › ... › TC-8.2 合法创建（body snake_case）→ ...

   Error: locator.click: Test timeout of 30000ms exceeded.
   Call log:
     - waiting for getByTestId('tab-market-margin')
   > await page.getByTestId('tab-market-margin').click()
        at openMarginTab (tests/e2e/margin-sync.spec.ts:78:47)

（TC-8.3 ~ TC-8.9 同 TC-8.2：waiting for getByTestId('tab-market-margin') 超时）

9 failed, 0 passed
```

### 页面快照佐证（宿主页稳定，来自 error-context.md）

- admin 侧边栏完整（管理控制台：仪表板/数据管理/任务监控/基金同步/…/用户管理 + 退出登录）——TestAdmin 已登录（admin role fixture 生效），**无 /login 重定向**
- 主区：标题「数据管理 / 数据初始化和计算管理」；既有七个 Tab 齐全（数据状态[默认选中]/数据初始化/板块均线计算/板块强度计算/券商金股同步/指数数据/市场量价）
- 默认「数据状态」Tab 内容区三类状态卡片正常（「正常 最新日期：2026-05-26」——`mockDataStatusNormal` 生效）
- 快照确认宿主页结构完整、用户已认证、默认 Tab mock 生效，失败唯一原因是 `tab-market-margin` 缺失（plan-08 Task 6 待实现）

## 本阶段产出（red-e2e）

1. **新增用例文档**：`docs/e2e/18-e2e-用例-融资融券同步面板.md`
   - 9 个用例覆盖 plan-08 §实现规格-5 全部 4 条 Given/When/Then 场景 + 验收标准扩展：Tab 进入与默认近 1 年（TC-8.1，场景 1）、合法创建 snake_case body → 2s 轮询进度先现后隐 → 终态三类计数 → 记录列表出现该任务（TC-8.2，场景 2 / AC-7）、前端校验三类拦截-倒置/未来/超10年 + 网络零调用（TC-8.3/8.4/8.5，场景 3）、失败日展开（状态 + 截断 reason，反向断言无四类计数）+ unprocessedDates 提示（TC-8.6，场景 4 / AC-7）、互斥禁用（TC-8.7）、后端互斥拒绝展示 message（TC-8.8）、记录列表倒序/分页/五态（TC-8.9）
   - 列出供 implementer 暴露的 14 个 data-testid 清单（tab-market-margin / margin-sync-panel / start-button / progress / success-skipped-failed-count / date-result-list / date-result-{tradeDate} / unprocessed-dates / mutex-hint / validation-error / records / records-next-page + 两个 aria-label 日期输入）
   - 单列「与 16 期母本的差异」节防照抄错位：默认近 1 年（非 30 日）、dateResults 仅状态+截断 reason（无 expected/daily/suspended/final）、testid 前缀 `margin-sync-*`、任务类型 `sync_market_margin`

2. **新增 mock helper**：`web/tests/e2e/helpers/mock-margin-sync-api.ts`
   - 范式逐段照抄 16 期 `mock-market-metrics-sync-api.ts`（`matchApiPath` pathname 精确匹配 + `parseQuery` + test data factory + `page.route` 函数匹配 + `route.fallback()` 放行非目标方法）
   - test data factory：`createTestMarginResult`（dateResults 仅 `{tradeDate, status, reason?}`，failed 自带 >100 字符 `FAILED_DAY_REASON` 验证截断；unprocessedDates 可注入）；`createTestSyncRecord`（taskType=sync_market_margin，携带 camelCase result）
   - 创建入口 mock：`mockMarginSyncSuccess`（POST 成功 `{success:true,data:{task_id}}`）/ `mockMarginSyncMutex`（互斥拒绝 `{success:false,data:null,message}`，HTTP 200）/ `mockMarginSyncValidationReject`（校验拒绝）
   - 轮询 mock：`mockMarginTaskPollingSequence`（闭包计数器 running→completed 状态推进 + result fixture；新增可选 `onTerminal` 回调供 spec 联动状态化记录列表）/ `mockMarginTaskCompletedWithResult`（固定 completed）
   - 记录列表 mock：`mockMarginSyncRecords`（GET `/api/v1/admin/tasks`，按 query `task_types=sync_market_margin` 匹配，返回 `{tasks,total,page}`）
   - 一键安装 `installMarginSyncMocks`；宿主页稳定 mock 复用 `mock-api.ts` 的 `mockDataStatusNormal`

3. **新增 spec**：`web/tests/e2e/margin-sync.spec.ts`
   - admin auth fixture（沿用 16 期 market-metrics-sync 范式：localStorage user.role=admin + access_token cookie）
   - beforeEach 装 `mockDataStatusNormal` 让宿主页默认 Tab 稳定
   - TC-8.2 POST body 断言：`page.waitForRequest` 捕获 POST `/api/v1/admin/init/margin`，`toEqual({start_date, end_date})` 验证 snake_case；轮询序列 runningCalls=3（约 6s 窗口）断言 `margin-sync-progress` 先 `toBeVisible` 后 `toBeHidden`；状态化记录列表（终态前空 → `onTerminal` 后含该任务）验证终态回调 refreshRecords 后列表出现该条任务
   - 前端校验零调用验证：`page.on('request')` 计数 POST init（pathname 精确匹配，区分 market-metrics），填非法日期后断言 `toBeDisabled` + `expect.poll` 计数为 0
   - TC-8.6 截断验证：断言展开行含 reason 前 50 字符、不含末尾 20 字符（截断 100 字符）；反向断言面板不含「应参与」「全天停牌」（两融无四类计数）
   - TC-8.9 倒序断言：以三条记录不同 params.start_date 在 `innerText` 中的先后位置验证 createdAt 倒序

## 测试设计要点

- **red 失败原因落在「Tab/组件未实现」**：TC-8.1 首断言为 `tab-market-margin` 可见（10s element(s) not found）；TC-8.2~8.9 首动作为点击该 Tab（30s 超时）。组件不存在即失败，避免长链路误判
- **宿主页稳定（沿用 16 期经验）**：数据管理页默认 Tab「数据状态」请求 `/api/v1/admin/data/status`，mock token 打真实后端会 401 → handleUnauthorizedRedirect 跳 /login。beforeEach 装 `mockDataStatusNormal` 消除该重定向，使 9 个失败全部落在「Tab 缺失」而非环境错误（error-context 快照佐证：七 Tab 齐全 + 默认 Tab 卡片正常 + 无 /login）
- **与时钟/真实后端解耦**：全部用 `page.route` mock，dev server（3100，复用已有进程）仅需可达，后端（8000）数据无关；默认近 1 年断言用测试进程本地时区与组件 `formatLocalDate` 同口径计算
- **testid 命名约定**（供 plan-08 实现者参照）：见用例文档 data-testid 清单，前缀 `margin-sync-*`（Tab 为 `tab-market-margin`），日期输入沿用 IndexSyncPanel 的 `aria-label` 约定

## 下一步

plan-08 实现者（implementer）以本 red 证据为前置条件，按 plan-08 Task 顺序实现：`useTaskStatus.ts` TaskData.result（Task 1）→ `api.ts` `adminApi.initMargin`（Task 2，snake_case body）→ `MarginSyncPanel.tsx`（Task 3/4/5 创建/轮询/进度/三类计数/dateResults 展开/记录/互斥，暴露上述 testid）→ `data/page.tsx` Tab 接入（Task 6 `tab-market-margin`）→ jest 组件测试（Task 7），重跑 `pnpm test:e2e -- tests/e2e/margin-sync.spec.ts` 应使 TC-8.1~8.9 转 green（覆盖 AC-7），并跑 §6 回归（`tests/margin/`、`tests/market-metrics/`、`market-metrics-sync.spec.ts`）确认母本面板未破坏。

## 阻塞项

无。本 red 阶段产出齐全（用例文档 + mock helper + spec + red 证据），失败原因明确（`tab-market-margin` Tab 按钮 + `MarginSyncPanel` 组件未实现），可推进至 implementer 阶段。
