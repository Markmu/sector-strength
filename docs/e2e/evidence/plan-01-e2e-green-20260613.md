---
feat_id: plan-01
phase: green
date: 2026-06-13
title: 后端数据层与管理 Admin API — E2E（pytest API 集成测试）green 阶段证据
---

# plan-01 E2E Green 证据

## 结论

**通过。**

- 后端 FEAT plan-01 已由 implementer 推进至 `review`，5 个 Admin API 端点全部实现并注册路由。
- 运行同一份 red 阶段测试文件（`tests/test_shareholder_group_admin_api.py`），**18/18 用例全部通过**（`18 passed, 9 warnings in 4.95s`）。
- 与 red 阶段对比：`17 failed, 1 passed` → `0 failed, 18 passed`，红灯全部转绿。
- 特别地，`test_delete_nonexistent_returns_404` 由 red 阶段的「无害重叠」（端点不存在 → 任意 id 都 404）转为真正通过（端点存在 + 资源不存在 → 404），与 red 证据「给 implementer 的交接提示」预测一致。

## 关联文档

- 功能文件：`docs/06-股东分析面板/06-2-实现计划-股东分析面板/plan-01-后端数据层与管理API.md`（§5 验收标准 / §3 实现规格 / §7 交接上下文 / §8 边界场景）
- 架构文档：`docs/06-股东分析面板/06-1-架构文档-股东分析面板.md`
- 实现计划 README：`docs/06-股东分析面板/06-2-实现计划-股东分析面板/README.md`
- 测试文件：`server/tests/test_shareholder_group_admin_api.py`
- red 证据：`docs/e2e/evidence/plan-01-e2e-red-20260613.md`
- 先例参照：`server/tests/test_fund_admin_api.py`、`server/tests/conftest.py`

## 执行命令

```bash
cd server && ENVIRONMENT=test python -m pytest tests/test_shareholder_group_admin_api.py -v --no-cov -p no:cacheprovider
```

测试环境：PostgreSQL via docker-compose（`sector-strength-postgres-1` Up 7 days healthy）；`test_session` fixture 通过 `CREATE SCHEMA` 建独立 schema + `Base.metadata.create_all`，未触发 Alembic。E2E 形态为 pytest API 集成测试（参照 `test_fund_admin_api.py` 先例），非 Playwright。

## 运行输出摘要

```
============================= test session starts ==============================
platform darwin -- Python 3.12.13, pytest-9.0.3, pluggy-1.6.0
rootdir: /Users/muchao/code/sector-strength/server
configfile: pytest.ini
plugins: langsmith-0.8.6, cov-7.1.0, asyncio-1.1.0, anyio-4.13.0
asyncio: mode=Mode.AUTO, debug=False
collecting ... collected 18 items

tests/test_shareholder_group_admin_api.py::TestListShareholderGroups::test_list_returns_predefined_groups        PASSED [  5%]
tests/test_shareholder_group_admin_api.py::TestListShareholderGroups::test_list_item_camel_case_fields           PASSED [ 11%]
tests/test_shareholder_group_admin_api.py::TestListShareholderGroups::test_list_requires_admin                   PASSED [ 16%]
tests/test_shareholder_group_admin_api.py::TestListShareholderGroups::test_list_requires_auth                    PASSED [ 22%]
tests/test_shareholder_group_admin_api.py::TestCreateShareholderGroup::test_create_group_success                PASSED [ 27%]
tests/test_shareholder_group_admin_api.py::TestCreateShareholderGroup::test_create_group_duplicate_name_rejected PASSED [ 33%]
tests/test_shareholder_group_admin_api.py::TestCreateShareholderGroup::test_create_group_requires_admin          PASSED [ 38%]
tests/test_shareholder_group_admin_api.py::TestCreateShareholderGroup::test_create_group_requires_auth           PASSED [ 44%]
tests/test_shareholder_group_admin_api.py::TestUpdateShareholderGroup::test_update_group_keywords_success       PASSED [ 50%]
tests/test_shareholder_group_admin_api.py::TestUpdateShareholderGroup::test_update_group_requires_admin         PASSED [ 55%]
tests/test_shareholder_group_admin_api.py::TestUpdateShareholderGroup::test_update_group_requires_auth          PASSED [ 61%]
tests/test_shareholder_group_admin_api.py::TestDeleteShareholderGroup::test_delete_group_success                PASSED [ 66%]
tests/test_shareholder_group_admin_api.py::TestDeleteShareholderGroup::test_delete_nonexistent_returns_404      PASSED [ 72%]
tests/test_shareholder_group_admin_api.py::TestDeleteShareholderGroup::test_delete_group_requires_admin         PASSED [ 77%]
tests/test_shareholder_group_admin_api.py::TestDeleteShareholderGroup::test_delete_group_requires_auth          PASSED [ 83%]
tests/test_shareholder_group_admin_api.py::TestPreviewShareholderGroups::test_preview_returns_matched_count     PASSED [ 88%]
tests/test_shareholder_group_admin_api.py::TestPreviewShareholderGroups::test_preview_requires_admin            PASSED [ 94%]
tests/test_shareholder_group_admin_api.py::TestPreviewShareholderGroups::test_preview_requires_auth             PASSED [100%]

======================== 18 passed, 9 warnings in 4.95s ========================
```

## 与 red 阶段对比

| 指标 | red（2026-06-13） | green（2026-06-13） |
| --- | --- | --- |
| 总用例数 | 18 | 18 |
| 通过 | 1（`test_delete_nonexistent_returns_404`，无害重叠） | **18（全部通过）** |
| 失败 | 17（端点 404） | **0** |
| 失败原因 | 端点未实现 → 404 Not Found | — |
| `test_delete_nonexistent_returns_404` 性质 | 端点不存在，任意 id 都 404（巧合通过） | 端点存在 + 资源不存在 → 404（真正通过） |

红灯转绿完全来自实现侧：路由 `server/src/api/admin/shareholder_groups.py`（`prefix="/shareholder-groups"`）在 `server/src/api/admin/__init__.py` 中 `include_router`，最终命中 `/api/v1/admin/shareholder-groups/*`，与 red 证据「给 implementer 的交接提示」一致。

## 覆盖的验收标准（AC）

| AC | 覆盖用例 | 验证内容 |
| --- | --- | --- |
| AC-06（新增分组） | `test_create_group_success`、`test_create_group_duplicate_name_rejected` | 管理员 POST 新增成功（200）；name 重复返回 400/409 |
| AC-07（编辑关键词） | `test_update_group_keywords_success` | 管理员 PATCH 编辑关键词（200），列表中 keywords 更新可见 |
| AC-10（删除分组） | `test_delete_group_success`、`test_delete_nonexistent_returns_404` | 管理员 DELETE 成功（200），列表中不再出现；不存在 id 返回 404 |
| §5 预览与列表 | `test_list_returns_predefined_groups`、`test_list_item_camel_case_fields`、`test_preview_returns_matched_count` | 列表返回 5 个预定义组（国家队/外资投行/社保基金/保险公司/私募基金）；GroupListItem 字段 camelCase（isSystem/ruleCount/matchedStockCount/keywords）；preview 返回 matchedStockCount（int） |
| 权限/认证（横切） | `test_*_requires_admin`（5）、`test_*_requires_auth`（4） | normal_client → 401/403；未认证 → 401；列表/新增/编辑/删除/预览 五端点全覆盖 |

## green 关键原则符合性自检

- [x] 同一份 spec 在 red / green 阶段运行，未修改测试代码与断言。
- [x] 所有用例均为真实 HTTP 链路（`admin_client` / `normal_client` / `client` fixtures），通过 `Base.metadata.create_all` 建表 + dependency_overrides 注入认证，未 mock 任何业务模块。
- [x] 18/18 全部通过，无 skipped、无 xfail、无 error。
- [x] 覆盖 plan-01 §5 全部 AC（AC-06/07/10）+ §5 预览与列表验收 + §8 边界场景（重复 name / 不存在 id）。
- [x] 权限矩阵覆盖完整：5 端点 × 2 角色 × 认证态 = 14 个权限/认证用例全绿。

## 给 task-review 的交接提示

- green 证据齐备，可作为 `review → done` 的硬性输入之一。
- 与 red 证据配合完整：red 17 failed（端点未实现）→ green 18 passed（实现完毕），TDD 红绿闭环成立。
- 实现侧路由顺序（`/preview` 声明在 `/{id}` 之前）已生效——`test_preview_returns_matched_count` 与 `test_update_group_keywords_success` 同时通过，证明路径未被错误吞掉。
