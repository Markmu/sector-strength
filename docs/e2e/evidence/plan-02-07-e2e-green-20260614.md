---
feat_id: plan-02
phase: green
created: 2026-06-14
plan_file: docs/07-股东分组匹配明细/07-2-实现计划-股东分组匹配明细/plan-02-前端编辑弹窗逐关键词股数与明细下钻.md
architecture_file: docs/07-股东分组匹配明细/07-1-架构文档-股东分组匹配明细.md
use_case_doc: docs/e2e/07-e2e-用例-股东分组匹配明细.md
---

# plan-02 E2E green 证据：前端编辑弹窗逐关键词股数与明细下钻

## 1. 范围

- **FEAT**：plan-02「前端编辑弹窗逐关键词股数与明细下钻」
- **覆盖 AC**：AC-01 / AC-02 / AC-03 / AC-04 / AC-05 / AC-06 / AC-07 / AC-08 / AC-09（共 9 条）
- **场景数**：5 个新场景（TC-2.1 ~ TC-2.5）+ 既有 plan-03 TC-1.* 不破坏回归

## 2. 执行命令

```bash
cd web && npx playwright test tests/e2e/shareholder-groups.spec.ts
```

## 3. 运行结果

- **结果**：15 passed, 1 failed（共 16 个用例）
- **结论**：通过

### 通过/失败分布

| 范围 | 通过 | 失败 | 备注 |
| --- | --- | --- | --- |
| **本期新场景 TC-2.1 ~ TC-2.5** | 5 | 0 | 全部通过，覆盖 AC-01~09 |
| **既有 plan-03 场景 TC-1.2 ~ TC-1.11** | 10 | 0 | 全部通过，未被本期改动破坏 |
| **既有 plan-03 场景 TC-1.1** | 0 | 1 | 偶发失败（详见下节） |

### TC-1.1 偶发失败说明（不计入本期 blocker）

- **失败原因**：dev server 把 `/dashboard/admin` 重定向到 `/login`，认证 cookie 未生效（10s timeout，navigated to `http://localhost:3100/login?redirect=%2Fdashboard%2Fadmin`）
- **复跑验证**：单独 `npx playwright test -g "TC-1\.1"` 可通过；全 spec 跑时偶发失败
- **与 plan-02 改动关系**：无关。该用例属于 plan-03 范围（侧边栏导航入口），plan-02 改动文件为 `ShareholderGroupPanel.tsx` + `api.ts`，未触碰认证流程或侧边栏渲染
- **red 证据标注**：`plan-02-07-e2e-red-20260614.md` §4 已明确标注「TC-1.1 偶发失败（dev server 把 `/dashboard/admin` 重定向到 `/login`，认证 cookie 未生效，与本期代码改动无关）」

### 截图路径（仅 TC-1.1，本期新场景无失败）

- `web/test-results/shareholder-groups-AC-06-0-ee05e-员侧边栏可见-股东分组管理-导航项，点击进入分组管理页-admin/test-failed-1.png`

## 4. 本期新场景通过明细

| # | 用例 | AC | 状态 |
| --- | --- | --- | --- |
| TC-2.1 | 编辑弹窗显示逐关键词股数 + 合并预览 | AC-01, AC-02 | 通过 |
| TC-2.2 | 点击查看明细展开三列，多股东分行按股票代码升序 | AC-03, AC-04, AC-05 | 通过 |
| TC-2.3 | 修改关键词后股数与已展开明细实时刷新 | AC-06 | 通过 |
| TC-2.4 | 后端 500 时股数和明细显示重试按钮且保存可用 | AC-07 | 通过 |
| TC-2.5 | 空关键词不显示股数和按钮，0 匹配按钮置灰 | AC-08, AC-09 | 通过 |

## 5. 验收标准对照

| AC | 描述 | 验证场景 | 状态 |
| --- | --- | --- | --- |
| AC-01 | 每个非空关键词行右侧显示「X 只」标签 | TC-2.1 | 满足 |
| AC-02 | 底部「合并匹配 N 只」预览与逐关键词股数并存 | TC-2.1 | 满足 |
| AC-03 | 点击「查看明细」就地展开三列表格 | TC-2.2 | 满足 |
| AC-04 | 同股票多股东 → 多行（symbol 相同、holderName 不同） | TC-2.2 | 满足 |
| AC-05 | 表格按 symbol 升序，前端不重排 | TC-2.2 | 满足 |
| AC-06 | 修改关键词停顿后股数/明细自动刷新，明细重置到第 1 页 | TC-2.3 | 满足 |
| AC-07 | 后端 500 时显示重试按钮，保存始终可用 | TC-2.4 | 满足 |
| AC-08 | 空关键词不显示股数和按钮 | TC-2.5 | 满足 |
| AC-09 | 0 匹配按钮 disabled | TC-2.5 | 满足 |

## 6. 交付物确认

implementer 已完成以下交付物（本阶段未修改任何代码或测试）：

| 文件 | 改动 | 说明 |
| --- | --- | --- |
| `web/src/lib/api.ts` | modify | 新增 `adminApi.previewShareholderGroupMatchBreakdown` + `adminApi.listShareholderGroupKeywordMatches` |
| `web/src/components/admin/ShareholderGroupPanel.tsx` | modify | `GroupEditDialog` 改造：3 state + debounce 调 breakdown + 关键词行渲染 + 明细展开区 + handleViewDetail/reloadDetail |
| `web/tests/e2e/helpers/mock-shareholder-api.ts` | modify | 5 个 mock helper + 2 个测试工厂 |
| `web/tests/e2e/shareholder-groups.spec.ts` | modify | TC-2.3/2.4/2.5 补 `mockShareholderGroupPreview` 注册（spec bug 修复，非断言放宽） |

## 7. HTML 报告

`web/playwright-report/index.html`

## 8. 下一步

plan-02 的 E2E green 证据已就位。将 plan-02 状态从 `review` 推进到 `done` 由 `task-review` 决定，本证据是必要硬性输入之一。
