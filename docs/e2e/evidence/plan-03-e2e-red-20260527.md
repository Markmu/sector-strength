---
feat_id: plan-03
phase: red
date: 2026-05-27
---

# plan-03 E2E Red 证据

## 基本信息

- **feat_id**: plan-03（前端数据状态标签页）
- **phase**: red
- **关联架构文档**: `docs/02-数据状态总览与一键补齐/02-1-架构文档-数据状态总览与一键补齐.md`
- **关联 FEAT 文件**: `docs/02-数据状态总览与一键补齐/02-2-实现计划-数据状态总览与一键补齐/plan-03-前端数据状态标签页.md`
- **用例文档**: `docs/e2e/02-e2e-用例-数据状态标签页.md`
- **spec 文件**: `web/tests/e2e/data-status.spec.ts`
- **mock helpers**: `web/tests/e2e/helpers/mock-api.ts`
- **Playwright 配置**: `web/playwright.config.ts`

## 执行信息

- **执行命令**: `cd web && npx playwright test --project=admin tests/e2e/data-status.spec.ts --timeout 5000 --retries 0 --reporter=line`
- **执行日期**: 2026-05-27

## 执行结果

- **通过**: 0
- **失败**: 13
- **总计**: 13

### 失败用例详情

| # | 用例 ID | 失败原因 | 根因 |
|---|---------|---------|------|
| 1 | TC-3.1 | `getByTestId('tab-data-status')` element not found | "数据状态"标签按钮不存在（page.tsx 未新增 tab） |
| 2 | TC-3.2 | `getByTestId('data-type-card-history')` element not found | DataTypeCard 组件不存在 |
| 3 | TC-3.3 | `getByTestId('data-type-card-history')` element not found | DataTypeCard 组件不存在 |
| 4 | TC-3.4 | `getByTestId('data-type-card-history')` element not found | DataTypeCard 组件不存在 |
| 5 | TC-3.5 | `getByTestId('backfill-button')` element not found | 补齐按钮不存在 |
| 6 | TC-3.6 | `getByTestId('data-type-card-strength')` element not found | DataTypeCard 组件不存在 |
| 7 | TC-3.7 | `getByTestId('data-status-panel')` element not found | DataStatusPanel 组件不存在 |
| 8 | TC-3.8 | `getByTestId('error-state')` element not found | 错误态 UI 不存在 |
| 9 | TC-3.9 | `getByTestId('backfill-button')` element not found | 补齐按钮不存在 |
| 10 | TC-3.10 | `getByTestId('backfill-button')` element not found | 补齐按钮不存在 |
| 11 | TC-3.11 | `getByTestId('data-type-card-ma')` element not found | DataTypeCard 组件不存在 |
| 12 | TC-3.12 | `getByTestId('error-state')` element not found | 错误态 UI 不存在 |
| 13 | TC-3.13 | `getByTestId('retry-backfill-button')` element not found | 重新补齐按钮不存在 |

### 失败根因分析

所有 13 个用例失败原因一致：功能尚未实现。需要创建的组件/文件均不存在：

1. `web/src/hooks/useDataStatus.ts` -- 不存在
2. `web/src/components/admin/DataTypeCard.tsx` -- 不存在
3. `web/src/components/admin/DataStatusPanel.tsx` -- 不存在
4. `web/src/app/dashboard/admin/data/page.tsx` -- 未新增 "data-status" 标签页

失败原因是功能尚未实现，不是测试代码错误、环境错误或选择器错误。

## 截图路径

失败截图保存在 `web/test-results/` 目录下，包括：

- `test-results/data-status-plan-03*admin/test-failed-1.png`（每个失败用例一张）

## Playwright HTML 报告

- 路径: `web/playwright-report/index.html`

## 结论

**预期失败，测试有效。**

所有 13 个 E2E 用例均因功能尚未实现而失败，符合 red 阶段预期。失败原因是目标组件（DataStatusPanel、DataTypeCard）和标签页入口尚未创建，不是测试代码或环境问题。测试覆盖了架构文档中定义的全部核心用户路径：

- AC-01: 默认标签页展示 + 三张卡片状态展示（TC-3.1, TC-3.2, TC-3.4）
- AC-02: 缺失状态卡片展示（TC-3.3）
- AC-03: 补齐按钮触发（TC-3.5）
- AC-04: 进度条展示（TC-3.6）
- AC-05: 补齐失败处理（TC-3.11, TC-3.13）
- AC-06: 状态获取失败处理（TC-3.8, TC-3.12）
- 错误场景（TC-3.9, TC-3.10）
- 标签页切换（TC-3.7）

implementer 可基于此 red 证据开始实现 plan-03。
