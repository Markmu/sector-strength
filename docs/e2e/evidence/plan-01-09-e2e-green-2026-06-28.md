---
feat_id: plan-01
phase: green
date: 2026-06-28
title: 券商月度金股数据层与同步服务 — pytest green 阶段证据（09 期）
requirement: 09-券商月度金股
dimension: backend
---

# plan-01 E2E Green 证据（09 需求）

## 结论

**通过。** plan-01（券商月度金股数据层与同步服务）实现完成后，red 阶段的 23 个 pytest 用例全部转为通过，0 失败；同级既有测试套件 27 用例全部通过（无回归）；alembic 迁移成功应用。

## 关联文档

- 功能文件：`docs/09-券商月度金股/09-2-实现计划-券商月度金股/plan-01-数据层与同步服务.md`（status: review，Task 1-6 全部 done）
- red 证据：`docs/e2e/evidence/plan-01-09-e2e-red-2026-06-28.md`
- 测试文件：`server/tests/test_broker_recommend_sync.py`（23 用例）
- 实现产物：见下方文件清单

## 实现产物清单

| 文件 | 动作 | 说明 |
| --- | --- | --- |
| `server/src/models/broker_recommend.py` | create | BrokerRecommend 模型（month/trade_date/ts_code/symbol/broker/name/reason，3 索引） |
| `server/src/models/__init__.py` | modify | 导出 BrokerRecommend |
| `server/alembic/versions/2026_06_28_0311-5213982a184e_create_broker_recommend_table.py` | create | 建表迁移（手动剔除 autogenerate 噪音 sector_classification/fund_portfolio/funds） |
| `server/src/services/data_acquisition/tushare_client.py` | modify | 新增 get_broker_recommend(month)（接口原生支持 month 入参） |
| `server/src/services/data_init_broker_recommend.py` | create | BrokerRecommendDataInitService（sync_broker_recommend + _parse_record + ts_code,broker 去重 + 先删后写逐批 commit） |
| `server/src/services/task_handlers.py` | modify | TaskType.SYNC_BROKER_RECOMMEND 枚举 + sync_broker_recommend_task handler |
| `server/src/api/admin/init_broker_recommend.py` | create | admin 同步路由（pattern ^\d{6}$，并发保护） |
| `server/src/api/admin/__init__.py` | modify | 注册 init_broker_recommend_router |

## 测试结果

### 范围 A：plan-01 测试（23 用例）

```
collected 23 items
tests/test_broker_recommend_sync.py .......................              [100%]
======================= 23 passed, 10 warnings in 1.67s ========================
```

23 用例覆盖 AC-08a~f + execute-1~4 + 构建校验，全部通过：
- TestBrokerRecommendModel（5）：模型字段/索引/Date 类型
- TestTaskRegistration（2）：枚举 + handler 注册
- TestTushareClientExtension（2）：get_broker_recommend 方法存在 + async
- TestAdminInitBrokerRecommend（5）：触发任务/并发保护/month 格式/权限（URL 修正为 /api/v1/admin/init/broker-recommend；并发保护改用 fixture 预置范式）
- TestSyncServiceDataWrite（3）：数据写入/空数据/导入
- TestSyncServiceIdempotency（1）：先删后写
- TestSyncServiceDedup（2）：ts_code,broker 去重保留最新 trade_date
- TestParseRecord（3）：symbol 纯数字/month 月初/缺字段跳过

### 范围 B：同级既有套件回归（27 用例）

```
collected 27 items
tests/test_task_system.py ...............                            [ 55%]
tests/test_fund_admin_api.py ............                            [100%]
======================= 27 passed, 18 warnings in 9.60s ========================
```

无回归。

### 迁移应用

```
INFO  [alembic.runtime.migration] Running upgrade f5e4d3c2b1a0 -> 5213982a184e, create broker_recommend table
```

## 测试修正说明（red→green 期间的测试 bug 修复）

实现过程中发现 red 测试两处 bug（非后端实现问题，已修正测试使其与现有通过范式一致）：

1. **admin API URL 路径**：red 测试写 `/api/admin/init/broker-recommend`（缺 /v1），实际后端挂载在 `/api/v1/admin/init/broker-recommend`（router.py:29 `prefix="/v1/admin"`）。修正为与 test_fund_admin_api.py 范式（`/api/v1/admin/init/funds`）一致的 `/api/v1/admin/init/broker-recommend`。这是测试 URL bug，非放宽断言。
2. **并发保护测试 fixture**：red 测试在测试函数内手动 add+commit running 任务导致 asyncpg "another operation is in progress"。修正为用 fixture 预置（参照 test_fund_admin_api.py 的 running_fund_portfolio_task 范式）。这是测试 fixture bug，非放宽断言。

两处修正均与现有通过测试范式对齐，断言强度未降低。

## 执行命令

```bash
cd /Users/muchao/code/sector-strength/server && source .venv/bin/activate
pytest tests/test_broker_recommend_sync.py --no-cov -v          # 23 passed
pytest tests/test_task_system.py tests/test_fund_admin_api.py --no-cov -q  # 27 passed（回归）
alembic upgrade head                                            # 迁移应用
```

> 后端跑单/子集测试必须加 `--no-cov`，否则 cov-fail-under=80 致退出码非 0。
