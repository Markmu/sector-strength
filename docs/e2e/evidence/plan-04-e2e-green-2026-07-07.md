---
feat_id: plan-04
phase: green
created: 2026-07-07
source_architecture: docs/12-股票数据独立建表/12-1-架构文档-股票数据独立建表.md
source_plan: docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-04-测试修复与全量回归.md
red_evidence: docs/e2e/evidence/plan-04-e2e-red-2026-07-07.md
---

# plan-04 E2E Green 证据：测试修复与全量回归

## 1. 摘要

plan-04 已由 implementer 推进到 `review`。本证据验证 red 阶段记录的 13 个失败已正确收敛：新表切换导致的 1 个失败（test_hk_stock_sync）已修复并转为通过；其余 12 个既有失败经 git stash baseline 对照确认与本次拆表无关，非本期范围。API 响应契约（AC-06）字段名/类型/结构与改造前一致，diff 为空。

**结论：通过**

## 2. 验证范围

本功能是测试修复层（无生产代码改动）。按 README §7.2 说明，green 验证对应 pytest 失败数收敛 + API 响应契约 diff。

验证维度：
1. **失败数收敛**：全量 pytest 失败数从 13（含 1 个新表切换）降到 12（仅既有失败）
2. **新表切换失败修复**：test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade 转为通过
3. **AC-06 契约 diff**：个股 API 响应字段名/类型/结构与改造前一致（diff 为空）
4. **plan-04 §2 文件清单零回归**：15 个测试文件全部通过

## 3. 验证结果

### 3.1 失败数收敛（核心门禁）

```bash
cd server && pytest --tb=no -q
```

| 阶段 | failed | passed | 变化 |
|------|--------|--------|------|
| red（实现前） | 13 | 1054 | 含 1 个新表切换失败 + 12 个既有失败 |
| green（实现后） | 12 | 1055 | 新表切换失败已修复；12 个既有失败不变 |

失败数从 13 → 12，passed 从 1054 → 1055，差值正是 red 阶段记录的 test_hk_stock_sync。

### 3.2 新表切换失败已修复（red → green）

red 阶段唯一的新表切换失败：

```
tests/test_hk_stock_sync.py::TestSetDiffCleanup::test_cleanup_deletes_disappeared_with_cascade
```

修复方式（plan-04 §3.A 类，fixture 写旧表）：

| 修改点 | 修改前（写旧表） | 修改后（写新表） |
|--------|------------------|------------------|
| 衍生数据写入 | `DailyMarketData(entity_type="stock", entity_id=s_del.id, symbol="000638", date=...)` | `StockDailyMarketData(stock_id=s_del.id, symbol="000638", date=...)` |
| 级联删除断言 | `select(DailyMarketData).where(DailyMarketData.entity_id == s_del.id)` | `select(StockDailyMarketData).where(StockDailyMarketData.stock_id == s_del.id)` |

验证：

```bash
pytest tests/test_hk_stock_sync.py::TestSetDiffCleanup::test_cleanup_deletes_disappeared_with_cascade --no-cov -q
# => 1 passed
```

生产代码 `_cascade_delete_stock_data`（data_init.py:701-735）已正确按 `stock_id` 清新三表，无需改生产代码。

### 3.3 既有失败清单（非本期范围，明确列出避免误判）

以下 12 个失败经 git stash baseline 对照（baseline 同样 12 failed, 1055 passed）确认预先存在，与"股票数据独立建表"无关：

| # | 测试 | 文件 | 既有失败根因 | 是否在 plan-04 §2 清单 |
|---|------|------|-------------|----------------------|
| 1 | test_cache_get_performance | test_classification_cache.py | 缓存性能断言 104ms < 50ms（环境波动） | 否 |
| 2 | test_skip_fund_with_delist_date | test_fund_sync_filter.py | 基金退市过滤 skipped==4 vs 2 | 否 |
| 3 | test_skip_fund_with_status_E | test_fund_sync_filter.py | 同上 | 否 |
| 4 | test_skip_count_aggregates_both_conditions | test_fund_sync_filter.py | 同上 | 否 |
| 5 | test_cleanup_removes_existing_delisted_funds | test_fund_sync_filter.py | 同上 | 否 |
| 6 | test_register_jobs | test_scheduler_service.py | daily_data_update job 未注册 | 否 |
| 7 | test_daily_data_update_job | test_scheduler_service.py | 同上 | 否 |
| 8 | test_data_quality_check_job | test_scheduler_service.py | 同上 | 否 |
| 9 | test_cache_cleanup_job | test_scheduler_service.py | 同上 | 否 |
| 10 | test_get_jobs | test_scheduler_service.py | 同上 | 否 |
| 11 | test_trigger_job_success | test_scheduler_service.py | 同上 | 否 |
| 12 | test_ac02_02_scheduled_task_at_16_00 | test_sector_classification_data_init_atdd.py | 每日板块分类更新任务未注册 | 否 |

这 12 个文件**均不在 plan-04 §2 的 15 文件清单内**，根因涉及缓存性能/基金过滤/调度器注册/板块分类调度，与数据存储层拆表无关，本期不修复。

### 3.4 AC-06 API 响应契约 diff（核心门禁）

个股相关 API 响应字段名/类型/结构与改造前 diff 为空。验证依据：

**a) 响应 schema 未变更**：plan-04 不改生产代码，`src/api/schemas/strength.py` 的 `StockStrengthResponse`（继承 `StrengthScoreBase`）字段集合保持不变：

| 字段 | 类型 | 来源 |
|------|------|------|
| entity_type | str | stocks.py:280 硬填 `"stock"` |
| period | str | stocks.py:283 硬填 `"all"`（StockStrengthScore 无 period 列，AC-06 保持） |
| percentile | Optional[float] | StockStrengthScore.percentile 列（plan-01 ADR-3 保证） |
| change_rate_5d | Optional[float] | stocks.py:311 硬填 `None`（StockStrengthScore 无此列，AC-06 保持） |

**b) API 测试全部通过**（断言 entity_type=='stock' 等契约字段）：

```bash
pytest tests/test_strength_api.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_strength_api.py tests/test_api/test_rankings_heatmap_api.py --no-cov -q
# => 43 passed
```

关键契约断言（test_strength_api.py）：

```python
# line 304: 个股排名项 entity_type
assert data["entity_type"] == "stock"
# line 325: 强度统计 entity_type
assert data["entity_type"] == "stock"
```

全部通过，证明响应中 `entity_type` 字段继续返回 `"stock"`（AC-06）。

### 3.5 plan-04 §2 文件清单零回归

15 个清单文件全量运行：

```bash
pytest tests/conftest.py tests/test_data_init.py tests/test_data_update.py \
       tests/test_data_updater.py tests/test_strength_services.py \
       tests/test_strength_score_model.py tests/test_strength_api.py \
       tests/test_calculation_engine.py tests/test_ma_system_calculator.py \
       tests/test_hk_stock_sync.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_strength_api.py tests/test_api/test_rankings_heatmap_api.py \
       tests/services/test_strength_snapshot_service.py tests/test_models.py --no-cov -q
# => 278 passed（red 阶段为 277 passed + 1 failed；修复后 278 passed）
```

说明：
- test_strength_score_model.py 覆盖 percentile 列断言（StockStrengthResponse.percentile Optional[float]）—— 但该文件实际测的是旧 StrengthScore 模型（仍存在），percentile 列在旧模型也有；新表 percentile 列由 test_stock_models.py（plan-01）覆盖
- 既有板块测试（如 test_sector_ma_service）未被本次修改破坏（本期未改任何板块相关测试）

## 4. 修改文件清单

| 文件 | 动作 | 说明 |
|------|------|------|
| server/tests/test_hk_stock_sync.py | modify | test_cleanup_deletes_disappeared_with_cascade fixture + 断言切新表 StockDailyMarketData |
| docs/12-股票数据独立建表/12-2-实现计划-股票数据独立建表/plan-04-测试修复与全量回归.md | modify | status: draft → review；Task #1~#10 → done；边界场景 → done |
| docs/e2e/evidence/plan-04-e2e-red-2026-07-07.md | create | red 证据 |
| docs/e2e/evidence/plan-04-e2e-green-2026-07-07.md | create | green 证据（本文件） |

未修改任何生产代码（plan-04 §8 边界：仅改清单内测试文件）。

## 5. 验收标准核对

| 验收标准 | 状态 | 验证 |
|---------|------|------|
| AC-06：全量 pytest 通过，无失败用例 | 部分达成 | 12 个失败均为既有失败（非本期范围，git stash baseline 对照确认）；plan-04 §2 清单内 0 失败 |
| AC-06：个股 API 响应字段 diff 为空 | ✅ | 43 个 API 测试通过；schema 未变更；entity_type/period/percentile/change_rate_5d 字段保持 |
| AC-06：响应 entity_type 继续返回 "stock" | ✅ | test_strength_api.py:304/325 断言通过；stocks.py:280 硬填 |
| 涉及股票数据的 fixture 写新表、断言查新表 | ✅ | test_hk_stock_sync fixture 已切 StockDailyMarketData |
| 既有板块测试未被破坏 | ✅ | 本期未改板块测试；test_sector_ma_service 等未触及 |
| test_strength_score_model.py 覆盖 percentile | ✅ | 该文件全通过（测旧 StrengthScore，percentile 列存在）；新表 percentile 由 test_stock_models.py 覆盖 |

## 6. 结论

**通过**。

- 全量 pytest 失败数从 13（含 1 个新表切换）降到 12（仅与拆表无关的既有失败）
- 新表切换导致的失败（test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade）已修复并转为通过
- API 响应字段 diff 为空（AC-06）；响应 entity_type 继续返回 "stock"
- plan-04 §2 的 15 文件清单全部通过（278 passed），板块测试零回归
- 未修改任何生产代码；12 个既有失败已明确标注非本期范围

red → green 对照：test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade 由失败（assert 1 == 0）转为通过，证明测试有效且修复正确。
