# 架构文档检查报告：股东分组匹配明细

> 检查对象：`07-1-架构文档-股东分组匹配明细.md`
> 需求基准：`07-0-需求设计-股东分组匹配明细.md`
> 检查日期：2026-06-14

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | 架构 frontmatter `input_documents` 第一项 `docs/07-股东分组匹配明细/07-0-需求设计-股东分组匹配明细.md` 与本次实际使用的 PRD 完全一致，配对可靠 | — |
| Frontmatter contract | ✅ | 包含 `workflow_type: arch-gen`、`status: review_ready`（合法值）、`input_documents`、`open_questions: []` 四个必填字段 | — |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 §5.6「待确认问题：无」一致 | — |
| 实现影响的未决问题 | ✅ | 本期所有决策点在 PRD §1.3 已有明确口径，正文 §5.6 已声明无未决问题；不存在会改变模块边界/契约/状态机/验收口径的悬空问题 | — |

> 本节无 `⛔`，可进入下一阶段判定。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5 | 0 | 0 | 100% |
| 3. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 4. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 5. 系统上下文与模块职责 | ✅ | 3 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| **合计** | — | **45** | **0** | **0** | **100%** |

> 注：章节顺序与模板（系统摘要 → 范围/非目标/成功标准 → ADR → 用户流程 → 系统上下文 → 运行链路）一致，仅 ADR 与流程章节号在文档内顺序为「3. 用户流程与状态 → 4. 系统上下文 → 5. ADR」（架构文档把 ADR 放在 §5），符合 arch-gen 模板允许的精简变体，不算缺失。

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖 | 4 | 0 | 0 | 100% |
| 3. 用户故事满足 | 5 | 0 | 0 | 100% |
| 4. 业务规则执行 | 8 | 0 | 0 | 100% |
| 5. 异常与边界覆盖 | 3 | 0 | 0 | 100% |
| 6. 成功标准可达性 | 9 | 0 | 0 | 100% |
| 7. 风险与降级对齐 | 3 | 0 | 0 | 100% |
| 8. UX 设计支撑 | 8 | 0 | 0 | 100% |
| 9. ADR 需求溯源 | 5 | 0 | 0 | 100% |
| 10. 范围合规性 | 4 | 0 | 0 | 100% |
| 11. 一致性检查 | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 0 | 0 | 0 | 100%（无过度设计） |
| **合计** | **53** | **0** | **0** | **100%** |

### 2.3 深度质量检查

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 14. 闭环完整性 | 4 | 0 | 0 | 100% |
| 15. 安全底线 | 0 | 0 | 0 | N/A（PRD 无合规底线章节） |
| 16. 复用可行性验证 | 4 | 0 | 0 | 100% |

> 说明：PRD 未单独设立「合规底线」章节；架构 §8.3 已覆盖 SQL 注入防护、admin 权限校验、关键词长度限制等通用安全策略，对本期只读查询场景足够，不视为缺口。

## 三、结构完整性详细检查

> 所有 11 个章节均为全绿。以下对最关键的几个章节给出证据摘要，避免冗余长表。

### Frontmatter

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| F.1 | workflow_type | ✅ | `arch-gen` |
| F.2 | status | ✅ | `review_ready`（schema 允许值） |
| F.3 | input_documents | ✅ | 列出 PRD + 06 文档目录 + 05 文档目录 |
| F.4 | open_questions | ✅ | `[]` |
| F.5 | input_documents 可解析 | ✅ | 第一项唯一指向本次 PRD，路径有效 |
| F.6 | open_questions 与正文同步 | ✅ | 与正文 §5.6「无」一致 |

### 2. 范围、非目标与成功标准

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 2.1 | P0 范围 | ✅ | §2.1 列出 6 条，覆盖 F1~F4 + 草稿基准 + 降级 |
| 2.2 | P1 预留 | ⚠️→✅ | §2.2「明确不做」+ §10「演进方向」共同覆盖（架构文档采用「明确不做 + 演进」二段式而非独立 P1，符合 arch-gen 精简模式） |
| 2.3 | 明确不做 | ✅ | §2.2 列出 6 条，每条对应 PRD 不做项或本期延伸排除项 |
| 2.4 | 成功标准 | ✅ | §2.3 表格 5 项：4 项定性描述 + 1 项性能数值（≤1s）；符合「功能性定性、性能量化」规则 |
| 2.5 | 验收标准承接矩阵 | ✅ | §2.4 完整承接 AC-01 ~ AC-09，每条包含 `AC-ID + PRD 原文摘要 + 承接模块 + 关键链路/状态 + 风险/降级说明`，符合模板要求 |

### 3. 关键架构决策（ADR）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 5.1 | ADR 编号与标题 | ✅ | ADR-1 ~ ADR-5，每条有清晰标题 |
| 5.2 | ADR 选择 | ✅ | 每条明确写出「选择」段 |
| 5.3 | ADR 理由 | ✅ | 每条包含「理由」段，并对比否决方案 |
| 5.4 | ADR 风险/演进 | ✅ | 每条包含「风险与对策」段 |
| 5.5 | ADR 数量合理 | ✅ | 5 条，符合 4-8 区间；每条正文 ≤ 5 行 |
| 5.6 | 待确认问题 | ✅ | §5.6 单独成节并声明「无」，与 frontmatter 同步 |

### 4. 用户流程与状态

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 3.1 | 主流程 | ✅ | §3.1 8 步覆盖从打开弹窗到保存的完整路径 |
| 3.2 | 关键分支 | ✅ | §3.2 表格 6 个分支：空关键词 / 股数失败 / 明细失败 / 明细为空 / 翻页中变更 / 弹窗关闭 |
| 3.3 | 状态机 | ✅ | §3.3 mermaid 状态机含 9 个状态 + 13 条迁移，附 3 条关键规则 |

### 5. 系统上下文与模块职责

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 4.1 | 系统上下文 | ✅ | §4.1 mermaid flowchart，展示浏览器/后端/DB 三层 + 数据流向，并附数据流说明 |
| 4.2 | 模块职责 | ✅ | §4.2 表格 9 个模块（前端 3 + 后端 5 + 复用 1），每个含职责/上游/下游 |
| 4.3 | 避免过度设计 | ✅ | §4.3 显式列出 6 项不做（缓存层/导出接口/规则引擎/异步队列/分析面板改动/新数据表） |

### 6. 运行链路

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 6.1 | 链路完整性 | ✅ | §6.1~§6.4 共 4 条链路，每条 3~8 步编号 + 实现原则段 |
| 6.2 | 实现原则 | ✅ | 每条链路末尾均有「实现原则」段，包含 SQL 细节、PG 语法要求、降级行为 |
| 6.3 | 链路覆盖 | ✅ | 6.1 逐关键词股数（AC-01）/ 6.2 查看明细（AC-03/04/05）/ 6.3 实时刷新（AC-06）/ 6.4 降级重试（AC-07）；AC-02/08/09 通过 §6.1 实现原则 + §3.2 关键分支 + §3.3 状态机覆盖 |

### 7. 领域对象与关键契约

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 7.1 | 核心对象 | ✅ | §7.1 表格 4 个对象（关键词草稿/明细行/股票名称/合并股数），含 Source of Truth / Owner / 用途 |
| 7.2 | 最小 Schema | ✅ | §7.2 TypeScript interface 4 个（KeywordCountItem / PreviewBreakdownData / KeywordMatchItem / KeywordMatchesData），扁平结构，嵌套 ≤ 2 层 |
| 7.3 | API 边界 | ✅ | §7.3 表格 2 个端点 + 完整 API 契约约定段（响应外层、字段命名、入参来源、默认 page_size、权限） |
| 7.4 | 状态流转 | ✅ | §7.4 明确本期无后端状态机，指向前端 §3.3 |
| 7.5 | 数据边界 | ✅ | §7.5 明确不写入/不读 rules 表/读取范围/不引入缓存层 |
| 7.6 | 命名与标识规则 | ✅ | §7.6 明确后端方法名、响应模型类名、adminApi 方法名、字段命名、术语映射、报告期口径 6 类 |

### 8. 非功能需求、风险与运行策略

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 8.1 | 性能目标 | ✅ | §8.1 表格 3 项指标（单关键词股数 ≤200ms / 单关键词明细 ≤1s / breakdown 整体 ≤1s）+ 已有索引说明 |
| 8.2 | 错误处理与降级 | ✅ | §8.2 三级降级（L1/L2/L3）+ 降级链摘要 |
| 8.3 | 安全策略 | ✅ | §8.3 表格 4 项：SQL 注入防护 / 权限控制 / 关键词长度限制 / 速率限制 |
| 8.4 | 成本控制 | ✅ | §8.4 明确无外部计费服务，仅本地 PostgreSQL |
| 8.5 | 可观测性 | ✅ | §8.5 后端 logger + 前端 console.error，复用现有日志模式 |
| 8.6 | 主要风险 | ✅ | §8.6 表格 5 项风险 + 影响 + 缓解 |

### 9. 实施方案

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 9.1 | 条目化方案 | ✅ | §9 Phase A/B/C，每条明确到文件路径 + 类名/方法名/行号锚点 |
| 9.2 | 验证目标 | ✅ | 每个 Phase 末尾有独立「验证目标」段（curl / UI 操作 / E2E 场景） |

### 10. 架构结论

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 10.1 | 核心判断总结 | ✅ | §10 总结 5 条核心判断（零数据模型变更/API 增量/复用 debounce/失败降级/可单次回滚） |
| 10.2 | 演进方向 | ✅ | §10 列出 3 条演进方向（列表表格行级明细 / SQL 优化 / 关键词质量评分） |

### 质量检查项（跨章节）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| Q.1 | 无实施细节 | ✅ | 仅含 TypeScript Schema interface + 1 段参考 SQL（§6.2 用于说明 DISTINCT ON + ORDER BY 语法约束，属于契约级证据，非实施代码）；无 DDL、目录树、环境变量 |
| Q.2 | 无重复内容 | ✅ | AC 承接只在 §2.4，状态机只在 §3.3，链路只在 §6；前后引用清晰 |
| Q.3 | 一致性 | ✅ | 见维度 11 |
| Q.4 | 量化约束 | ✅ | ADR 5 条（4-8）/ 模块 6 个新增/改造（≤6）/ API 2 个（≤10）/ Schema 嵌套 ≤ 2 层 |
| Q.5 | frontmatter 与正文一致 | ✅ | input_documents / open_questions / status 与正文一致 |

## 四、需求满足度详细检查

### 维度 2：范围覆盖

| PRD 需求 | 架构覆盖 | 状态 | 证据 | 差距说明 |
|----------|---------|------|------|---------|
| F1：编辑弹窗内每个非空关键词单独匹配的去重股票数 | 后端新增 `preview_match_breakdown` + 前端 `GroupEditDialog` 逐关键词渲染 | ✅ | §2.1 第 1 条 / §4.2 / §6.1 / §7.3 `preview-breakdown` 端点 | 无 |
| F2：每个关键词支持查看明细（股票代码+股票名称+股东名称） | 后端新增 `list_keyword_matches` + 前端就地展开三列 | ✅ | §2.1 第 2 条 / §4.2 / §6.2 / §7.3 `keyword-matches` 端点 / §7.2 `KeywordMatchItem`（三字段：symbol / stockName / holderName） | 无 |
| F3：明细分页按股票代码升序 | 后端 SQL `ORDER BY symbol ASC` + 每页 20 条 LIMIT/OFFSET | ✅ | §2.1 第 3 条 / §6.2 第 5.3 步 / §7.3 默认 page_size=20 | 无 |
| F4：保留所有关键词合并匹配总股数预览 | 复用现有 `_count_matched_stocks` + 现有 `preview` 端点不动 | ✅ | §2.1 第 4 条 / §4.1 系统上下文 `ExistingPreviewAPI` 节点 / §6.1 第 8 步「同时更新合并总数预览」 | 无 |

### 维度 3：用户故事满足

| 用户故事 | 架构流程/链路 | 状态 | 证据 | 差距说明 |
|----------|-------------|------|------|---------|
| US-01：编辑时看每关键词分别匹配多少股票 | §3.1 主流程第 2-3 步 + §6.1 链路 | ✅ | §6.1 完整 8 步，每关键词独立 COUNT(DISTINCT symbol) 查询；§3.3 状态机从 Empty → Computing → CountOk 覆盖全生命周期 | 无 |
| US-02：保存前即时查看具体股票和股东 | §3.1 主流程第 5-6 步 + §6.2 链路 | ✅ | §6.2 7 步覆盖点击展开 → 加载 → 渲染三列；§7.2 `KeywordMatchItem` 三字段对齐 PRD 线框图 | 无 |
| US-03：修改关键词后股数和明细自动刷新 | §3.1 主流程第 7 步 + §6.3 链路 | ✅ | §6.3 明确「已展开明细的关键词内容变化时，明细刷新与股数刷新同步触发」；翻页越界保护（重置 page=1）已覆盖 | 无 |
| US-04：明细按股票代码升序、同股票多股东相邻 | §6.2 第 5.3 步 SQL ORDER BY + 实现原则 | ✅ | SQL 语句 `ORDER BY h.symbol ASC, h.holder_name ASC, h.ann_date DESC NULLS LAST`；实现原则明确「DISTINCT ON 必须配合 ORDER BY 前缀匹配，顺序不可调换」 | 无 |
| US-05：明细加载失败不阻塞编辑和保存 | §3.2 关键分支「明细加载失败」 + §6.4 链路 | ✅ | §6.4 明确「失败状态不抛错到 formError，保存按钮始终可用」；ADR-5 进一步决策「股数与明细查询失败时各自独立降级」 | 无 |

### 维度 4：业务规则执行

| 业务规则 | 执行机制 | 状态 | 证据 | 差距说明 |
|----------|---------|------|------|---------|
| BR-1：每关键词股数按股东名称包含关键词单独匹配、按股票去重 | 后端单关键词 `COUNT(DISTINCT symbol)` + 前端按索引映射 | ✅ | §6.1 第 6.2 步 SQL 模板 / §6.1 实现原则「按 keyword 索引（而非字符串值）匹配，避免草稿中重复关键词时映射混乱」 | 无 |
| BR-2：合并总数因多关键词去重通常 ≤ 各关键词之和 | 复用现有 `preview` 端点 OR LIKE 计数 | ✅ | §2.4 AC-02 风险说明「合并数因跨关键词去重，通常 ≤ 各关键词股数之和」 | 无 |
| BR-3：股数刷新规则（与合并预览同一节奏） | 复用现有 500ms debounce | ✅ | §2.1 第 5 条 / §3.1 第 2 步 / §6.1 第 2 步均明确「500ms debounce（复用现有 debounceRef）」 | 无 |
| BR-4：明细一行 = 股票代码+股票名称+股东名称，不含比例 | Schema 限定三字段 | ✅ | §7.2 `KeywordMatchItem` 仅含 symbol / stockName / holderName；§2.2 明确不做「持股比例、持股数量等扩展列」 | 无 |
| BR-5：同一股票多匹配股东按股东分行 | 后端 SQL `DISTINCT ON (symbol, holder_name)` | ✅ | §6.2 第 5.3 步 SQL / §6.2 实现原则「不用 COUNT(DISTINCT h.symbol) 否则同股票多股东会被压缩」 / §4.2 `_get_keyword_matches` 职责段 | 无 |
| BR-6：明细按股票代码升序、同股票多股东相邻 | SQL ORDER BY 双字段 | ✅ | §6.2 SQL `ORDER BY h.symbol ASC, h.holder_name ASC, h.ann_date DESC NULLS LAST` | 无 |
| BR-7：默认每页 20 条 | API 默认 page_size=20 | ✅ | §7.3 表格明确「默认 page_size=20，PRD §3.3 明确『默认每页 20 条』」 | 无 |
| BR-8：明细基于草稿关键词（与是否已保存无关） | 后端入参 `keywords` 来自前端草稿 | ✅ | §4.1 数据流说明「本期所有查询都基于前端传入的草稿关键词，不读取 shareholder_group_rules 表」/ §7.5「不读取 shareholder_group_rules」 | 无 |

### 维度 5：异常与边界覆盖

| PRD 异常场景 | 架构错误处理 | 状态 | 证据 | 差距说明 |
|-------------|------------|------|------|---------|
| EX-1：明细加载失败 | 明细区显示错误提示 + 重试按钮，不影响保存 | ✅ | §3.2 关键分支「明细加载失败」/ §6.4 链路 / ADR-5 / §8.2 L2 降级 | 三要素完整：触发（后端错误或网络异常）→ 处理（明细区错误提示+重试）→ 后续（保存按钮可用） |
| EX-2：关键词为空 | 该关键词不显示股数与查看明细、不发起请求 | ✅ | §3.2 关键分支「空关键词行」/ §3.3 状态机 `Empty` 状态 / §6.1 第 3 步 validKeywords 过滤 | 无 |
| EX-3：明细数据量较大（需分页） | 后端分页 + 前端分页器 | ✅ | §6.2 第 5.3 步 LIMIT/OFFSET / §6.2 第 5.4 步 COUNT(DISTINCT (symbol, holder_name)) / Phase B 实现描述「分页器 onChange 重新调接口」 | 无 |

### 维度 6：成功标准可达性（关键维度）

| PRD 验收指标 | 架构目标值 | PRD 目标值 | 状态 | 证据 | 差距说明 |
|-------------|-----------|-----------|------|------|---------|
| AC-01：每个非空关键词展示单独匹配的去重股票数 | 每关键词独立 COUNT(DISTINCT symbol) | 同 PRD | ✅ | §2.4 AC-01 承接 + §6.1 链路 + §7.3 端点 | 无 |
| AC-02：保留合并匹配总数 | 复用现有 preview 端点 | 同 PRD | ✅ | §2.4 AC-02 + §6.1 第 8 步 | 无 |
| AC-03：点击查看明细展示三列 | 单关键词入参 + 三列输出 | 同 PRD | ✅ | §2.4 AC-03 + §6.2 + §7.2 Schema | 无 |
| AC-04：同股票多股东分行 | DISTINCT ON (symbol, holder_name) | 同 PRD | ✅ | §2.4 AC-04 + §6.2 SQL + §4.2 `_get_keyword_matches` 职责 | 无 |
| AC-05：明细按股票代码升序、同股票相邻 | ORDER BY symbol ASC, holder_name ASC | 同 PRD | ✅ | §2.4 AC-05 + §6.2 SQL | 无 |
| AC-06：修改关键词后股数与明细实时刷新 | 500ms debounce 同步触发股数+明细 | 同 PRD | ✅ | §2.4 AC-06 + §6.3 链路（重置 page=1） | 无 |
| AC-07：预览或明细失败均降级且不阻塞 | 各自 try/catch + 独立重试 + 不抛到 formError | 同 PRD | ✅ | §2.4 AC-07 + §6.4 链路 + ADR-5 + §8.2 L1/L2/L3 分级 | 无 |
| AC-08：空关键词不显示股数与明细入口 | trim() 为空时不渲染标签和按钮 | 同 PRD | ✅ | §2.4 AC-08 + §3.2 关键分支 + §3.3 状态机 Empty | 无 |
| AC-09：0 匹配关键词的查看明细置灰 | count === 0 时按钮 disabled | 同 PRD | ✅ | §2.4 AC-09 + §3.3 状态机 ZeroMatch + Phase B「count === 0 时按钮置灰」 | 无 |

> 9/9 全部 ✅；AC 原始语义未被弱化、替换或漂移；性能类指标 ≤ 1s 与 PRD 一致。

### 维度 7：风险与降级对齐

> PRD 未单设「风险清单」「降级策略」章节，但 §2.1 异常旅程和 §3.3 关键状态已隐含风险/降级语义。架构 §8.2 / §8.6 已超额覆盖。

| PRD 风险/降级 | 架构缓解/降级 | 状态 | 证据 | 差距说明 |
|--------------|-------------|------|------|---------|
| PRD §2.1 异常旅程：明细加载失败 | 明细区错误提示 + 重试按钮 | ✅ | §3.2 关键分支 + §6.4 + §8.2 L2 | 处理方式与 PRD 一致 |
| PRD §2.1 异常旅程：关键词为空 | 不显示股数与查看明细 | ✅ | §3.2 关键分支 + §3.3 Empty 状态 | 一致 |
| PRD §3.3 降级规则：股数/明细失败对应区域降级但不阻塞保存 | 失败区域降级、保存按钮始终可用 | ✅ | §6.4 实现原则「失败状态不抛错到 formError，保存按钮始终可用」 + ADR-5 | 一致 |

### 维度 8：UX 设计支撑

| PRD UX 交互 | 架构状态/接口 | 状态 | 证据 | 差距说明 |
|------------|-------------|------|------|---------|
| 每关键词行渲染「X只」标签 | perKeywordCounts state + CountOk 状态 | ✅ | §3.3 状态机 CountOk / Phase B 实现描述 | 无 |
| 每关键词行渲染「查看明细」按钮 | expandedKeywordIdx state | ✅ | §3.3 状态机 DetailCollapsed → DetailLoading / Phase B | 无 |
| 点击「查看明细」就地展开明细区 | expandedKeywordIdx + listShareholderGroupKeywordMatches | ✅ | §6.2 第 1-6 步 / ADR-4「同时只能展开一个关键词」 | 无 |
| 三列明细列表（股票代码/股票名称/股东名称） | KeywordMatchItem 三字段 | ✅ | §7.2 Schema | 无 |
| 分页器（上一页/下一页/页码） | detailState.page + onChange 重新调接口 | ✅ | §6.2 第 6 步 / Phase B「分页器 onChange 重新调 listShareholderGroupKeywordMatches(keyword, { page })」 | 无 |
| 底部保留合并预览 | 复用现有 previewShareholderGroupMatch 调用 | ✅ | §6.1 第 8 步 | 无 |
| 0 匹配按钮置灰 | count === 0 时按钮 disabled | ✅ | §3.3 ZeroMatch 状态 + Phase B 描述 | 无 |
| 失败区域显示「重试」按钮 | 错误状态 + 重试 handler | ✅ | §3.3 CountFailed / DetailFailed → 重试迁移 / §6.4 | 无 |

### 维度 9：ADR 需求溯源

| ADR | 溯源需求 | 判定 | 说明 |
|-----|---------|------|------|
| ADR-1：股数细分通过新增独立端点 preview-breakdown，不扩展现有 preview | PRD F1 + US-01 + AC-01 | 需求驱动 | 直接服务于 PRD §1.4「编辑分组弹窗内，每个关键词旁展示该关键词单独匹配的去重股票数」；理由段引用了「现有 preview 已被合并预览稳定使用」，与现有代码锚点（`GroupEditDialog` line 332）对齐 |
| ADR-2：明细查询入参为单关键词 | PRD §1.3 决策「明细触发方式」+ US-02 + AC-03 | 需求驱动 | PRD §1.3 明确「用户点击关键词旁的『查看明细』主动触发」「编辑时自动加载所有关键词明细：数据量和请求量不可控」 |
| ADR-3：明细 SQL 在后端一次性完成「匹配+JOIN+排序+分页」 | PRD F2+F3 + AC-03/04/05 + §1.3 决策 | 需求驱动 | PRD §1.3 明确「按股票聚合（股东名合并成一列）：无法分行核对每个股东，丢失核验关键信息」，要求 SQL 层一次成型 |
| ADR-4：前端明细展开「同时只能展开一个关键词」 | PRD §3.1 线框图（仅展示单个明细区） | 合理扩展 | PRD 线框图本身只画了一个明细区；架构据此推断「弹窗空间有限」是合理的 UX 收敛，未引入新需求 |
| ADR-5：股数细分与明细查询失败时各自独立降级 | PRD AC-07 + US-05 | 需求驱动 | AC-07 明确「预览或明细查询失败均降级且不阻塞编辑与保存」；ADR-5 进一步细化「不抛错到顶层 formError」是直接执行 |

### 维度 10：范围合规性

| PRD 不做项 | 架构中出现情况 | 状态 | 说明 |
|-----------|--------------|------|------|
| 分组列表表格行级「查看明细」 | 架构明确排除 | ✅ | §2.2 第 1 条 + §4.3「不修改用户侧分析面板」 + §10 演进方向将其列为后续可选 |
| 关键词质量评分、误伤高亮等智能建议 | 架构明确排除 | ✅ | §2.2 第 2 条 + §4.3「不抽象『关键词匹配规则引擎』」 |
| 明细数据导出（表格类文件） | 架构明确排除 | ✅ | §2.2 第 3 条 + §4.3「不为明细增加导出/批量查询接口」 |
| 用户侧股东分析面板的明细展示改造 | 架构明确排除 | ✅ | §2.2 第 4 条 + §4.3「不修改用户侧 `/api/v1/shareholder-analysis/*` 任何端点」 + §10 演进方向将其列为后续可选 |

### 维度 11：一致性检查（关键维度）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | ADR-1（独立端点）与 ADR-2（单关键词入参）协调；ADR-3（后端一次性 SQL）与 ADR-1（不扩展现有 preview）相容；ADR-4（单展开）与 ADR-2（单关键词）协调；ADR-5（独立降级）与 ADR-1/ADR-2 端点解耦一致。无矛盾 |
| Schema 与 API 一致性 | ✅ | §7.3 端点表格字段（matchedStockCount / symbol / stockName / holderName / total / page / pageSize）与 §7.2 TypeScript interface 完全对齐；§9 Phase B adminApi 方法签名与 §7.3 一致（包括 string \| null 联合类型） |
| 状态机与流程一致性 | ✅ | §3.3 状态机 9 个状态 + 13 条迁移与 §6.1~§6.4 链路对应（Empty/Computing/CountOk/CountFailed 对应 §6.1；DetailCollapsed/Loading/Shown/Failed 对应 §6.2；ZeroMatch 对应 AC-09；DetailShown → DetailLoading 对应 §6.3 翻页刷新）；§6.4 与 CountFailed/DetailFailed → 重试迁移一致 |
| 范围与设计一致性 | ✅ | §2.1 范围 6 项全部在 §6 链路 + §7 API + §9 实施中找到对应设计；§2.2 不做项 6 条全部在 §4.3 + §10 演进方向有显式排除声明 |

### 维度 13：过度设计检测

| 架构设计 | 对应需求 | 判定 | 说明 |
|----------|---------|------|------|
| 新增 2 个 admin GET 端点（preview-breakdown + keyword-matches） | F1 + F2 | 合理扩展 | 每个 GET 端点都对应明确交互（股数细分 / 明细下钻），无冗余；ADR-1 论证了「独立端点优于扩展 preview」 |
| 新增 4 个 Pydantic 响应模型 | §7.2 Schema | 合理扩展 | 与 §7.3 API 字段一一对应，无字段超过 PRD 线框图所列三列要求 |
| 新增 2 个 adminApi 方法 | §4.2 + §9 Phase B | 合理扩展 | 与 2 个端点一一对应 |
| 新增 3 个后端 Service 方法（preview_match_breakdown / list_keyword_matches / _get_keyword_matches） | F1 + F2 + 复用拆分 | 合理扩展 | _count_matched_stocks_single 拆自现有 _count_matched_stocks（保留原 OR 版不动），符合最小改动原则 |
| 不引入缓存 / 异步队列 / 规则引擎 / 新数据表 | — | 合理扩展（反向） | §4.3 显式列出 6 项不做，符合「精简优先」 |

> 无存疑设计。

## 五、深度质量检查

### 维度 14：闭环完整性（关键维度）

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| 6.1 逐关键词股数预览 | ✅ | 无 | 8 步从「keywords state 变化」→ debounce → 过滤 validKeywords → 调 API → 后端取最新报告期 + 逐关键词 LIKE COUNT → 前端按索引映射 → 渲染标签 + 同步合并预览；输入输出完整，与 §7.1 核心对象、§7.3 API 边界对齐 |
| 6.2 查看明细 | ✅ | 无 | 7 步从「点击查看明细」→ 设置 expandedKeywordIdx → 加载指示 → 调 API → 后端最新报告期 + LIKE + DISTINCT ON + JOIN stocks + ORDER BY + LIMIT/OFFSET → 渲染三列+分页；含 total 查询独立步骤；与 §7.2 Schema 对齐 |
| 6.3 修改关键词后实时刷新 | ✅ | 无 | 3 步从「关键词变更」→ debounce 触发 6.1+6.2 同步 → 明细 page 重置 1；明确「关键词被清空时自动收起明细并清空状态」补全边界 |
| 6.4 降级与重试 | ✅ | 无 | 3 步从「查询失败」→ catch 置错误状态 → 渲染错误提示+重试按钮 → 点击重试重新执行对应链路；与 §3.3 状态机 CountFailed/DetailFailed → Computing/DetailLoading 迁移一致 |

### 维度 15：安全底线

> PRD 未单设「合规底线」章节。架构 §8.3 已覆盖：

| 安全项 | 架构安全设计 | 状态 | 证据 |
|--------|------------|------|------|
| SQL 注入防护 | 复用 `_escape_like_keyword` 转义 `%`/`_` + 参数绑定 | ✅ | §8.3 + §6.1 实现原则 + §6.2 实现原则 |
| 管理员权限校验 | `Depends(require_admin)`，与现有 admin shareholder-groups 一致 | ✅ | §8.3 + §4.2 复用声明 + §7.3 契约约定 |
| 关键词长度 sanity check | 参考现有 `ShareholderGroupRule.keyword String(200)` 限制 | ✅ | §8.3 |

### 维度 16：复用可行性验证

| 复用声明 | 数据源 | 写入目标 | 前置依赖 | 状态 | 说明 |
|---------|--------|---------|---------|------|------|
| 复用现有 admin 路由前缀与认证 | — | — | `router.include_router(admin_router, prefix="/v1/admin")` + adminApiClient baseURL `${API_BASE_URL}/api/v1` | ✅ 可行 | §4.2 已明确代码级核对（`shareholder_groups.py:22`、`router.py:29`），无双前缀风险 |
| 复用 `require_admin` 依赖 | — | — | 现有 `preview` 端点 line 88 | ✅ 可行 | admin 路由已统一加管理员权限校验 |
| 复用 `_escape_like_keyword` + `_get_latest_report_period` | `top10_float_holders` 表（同现有 _count_matched_stocks） | 只读查询（无写入） | 无 | ✅ 可行 | 数据源与目标场景（本期明细查询）完全一致：都查 `top10_float_holders` 最新期 + LIKE 匹配 |
| 复用 JOIN stocks 取股票名称（参考 `_get_industry_for_stocks`） | `stocks` 表 LEFT JOIN | 只读 | stocks 表数据存在（缺失时 stockName=null，前端显示「-」） | ✅ 可行 | §4.2 明确「本期明细场景的 JOIN 更简单（只需 stocks 表，无需 sector）」，并保留兜底逻辑；§6.2 SQL 用 LEFT JOIN 显式处理 null |

> 无 ❌ 不可行的复用声明。MEMORY 中的「复用声明可行性验证」典型错误模式（entity_type 不匹配、依赖表为空）均已规避。

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无。

### 部分满足的需求（中优先级）

无。

### 存疑设计（需评估）

无。

### 轻微观察（非阻塞，可选优化）

| 编号 | 观察 | 建议 |
|------|------|------|
| O-1 | §6.2 第 4 步提到「单独执行 `COUNT(*)` 查询总数」，§9 Phase A `list_keyword_matches` 实现描述却写「调用 `_get_keyword_matches` 取全量列表，在 Python 层做分页（与现有 `ShareholderAnalysisService.get_holdings` 一致：rows.sort 后切片）」 | 二者在「分页是 SQL 层 LIMIT/OFFSET 还是 Python 层切片」上略有出入。当前不影响验收（典型 < 200 条，两种实现性能均满足 ≤ 1s），但实施时需明确选定一种。建议在实施阶段统一为 SQL LIMIT/OFFSET（与 §6.2 SQL 模板一致），避免拉取全量后再切片的无谓开销。**这是实施细节，不构成架构缺口**。 |
| O-2 | §6.2 实现原则提到「`exclude_group_id` 参数本期后端实现时可忽略（与现有 `_count_matched_stocks` 一致的占位行为），但保留入参以保持 API 一致性」 | 入参保留但后端忽略是合理的占位策略，与现有端点行为一致。建议在实施时通过注释明确「本期占位，未来若需『排除已属于该分组的关键词』可启用」，避免后续维护者误以为是 bug。**非架构缺口**。 |

## 七、总结

- **结构完整度**：100%（45 / 45 检查项通过）
- **需求满足度**：100%（53 / 53 需求项充分满足；4 条深度质量检查全部 ✅；安全底线对本期只读查询场景充分）
- **整体评价**：✅ **架构充分满足需求，可进入开发计划阶段**
  - 无前置阻塞
  - PRD 范围 4 条 + 用户故事 5 条 + 业务规则 8 条 + 异常 3 条 + AC 9 条全部 ✅
  - 关键维度（6/11/14/15/16）无 ❌
  - 非关键维度 ⚠️ 项 = 0
- **优先修复建议**：
  1. 无需修改架构文档。两处轻微观察（O-1 分页策略、O-2 占位入参）可在 create-dev-plan / 实施阶段细化，不影响架构验收。
  2. 后续若进入 `create-dev-plan`，建议把 ADR-1 的 N 次单关键词查询（典型 N ≤ 10）作为开发任务的明确边界，避免实施者自行决定是否批量化。
  3. 后续若进入 `task-review`，重点关注 O-1 的分页实现是否与 §6.2 SQL 模板一致（SQL LIMIT/OFFSET 而非 Python 切片）。
