---
feat_id: plan-01
title: 后端逐关键词股数与明细查询 API
dimension: backend
phase: 1
review_date: 2026-06-14
review_skill: task-review
verdict: passed
---

# plan-01 task-review 报告

## 1. 功能信息

- **feat_id**: plan-01
- **title**: 后端逐关键词股数与明细查询 API
- **dimension**: backend
- **phase**: 1
- **status（review 前）**: review
- **status（review 后）**: done
- **依赖**: 无
- **关联 AC**: AC-01 / AC-02 / AC-03 / AC-04 / AC-05 / AC-07

## 2. 验收结论

**通过（passed）**。

plan-01 全部 10 个验收维度均满足，10 条核心审查项（路径前缀 / camelCase / snake_case query / AC-07 降级 / SQL 注入防护 / total 修正合理性 / 端点顺序 / 复用声明 / Task 列表 / 边界场景）全部通过，验证命令 `pytest tests/test_shareholder_group_admin_api.py -v` 实测 33/33 通过（10.21s）。

按 task-review 流程，将功能 frontmatter `status` 从 `review` 推进为 `done`。

## 3. 十维度审查结果

### 维度 1：文件交付完整性 — 通过

| 文件 | 动作 | 状态 |
| --- | --- | --- |
| `server/src/services/shareholder_group_service.py` | modify | 新增 4 方法（line 159-541），文件非空，新增方法定位正确 |
| `server/src/api/admin/shareholder_groups.py` | modify | 新增 2 端点（line 151-203）+ 4 Pydantic 模型（line 73-112），文件非空 |
| `server/tests/test_shareholder_group_admin_api.py` | modify | 新增 2 fixture（line 390-502）+ 15 测试用例（line 508-838），文件非空 |

文件清单外的额外修改：无。

### 维度 2：Task 列表完成度 — 通过

9 个 Task 全部 `done`：

| # | Task | 状态 |
| --- | --- | --- |
| 1 | red：追加 pytest 用例 | done |
| 2 | 实现 `_count_matched_stocks_single` | done |
| 3 | 实现 `_get_keyword_matches` | done |
| 4 | 实现 `preview_match_breakdown` | done |
| 5 | 实现 `list_keyword_matches` | done |
| 6 | 新增 4 Pydantic 模型 | done |
| 7 | 新增 `GET /preview-breakdown` | done |
| 8 | 新增 `GET /keyword-matches` | done |
| 9 | green：pytest 全套通过 | done |

无 `todo` / `waived` 遗留。

### 维度 3：验收标准完成度 — 通过

| AC-ID | 测试覆盖 | 结果 |
| --- | --- | --- |
| AC-01 | `test_preview_breakdown_returns_per_keyword_count` + `test_preview_breakdown_empty_keywords_returns_empty` | 通过 |
| AC-02 | `test_existing_preview_endpoint_still_works` + 18 个现有 preview 测试 | 通过 |
| AC-03 | `test_keyword_matches_returns_three_columns` + `test_keyword_matches_stock_name_null_when_stocks_table_missing` + `test_keyword_matches_pagination` | 通过 |
| AC-04 | `test_keyword_matches_same_stock_multi_holders_split_rows` | 通过 |
| AC-05 | `test_keyword_matches_ordered_by_symbol_then_holder` | 通过 |
| AC-07 | `test_preview_breakdown_partial_failure_returns_null_for_failed_keyword` | 通过 |
| 安全 §8.3 | `test_keyword_matches_escapes_like_wildcards` + 4 权限用例 | 通过 |

性能验收（架构 §8.1）未在 pytest 中以 perf_counter 断言，但全文件 33 用例总耗时 10.21s（含数据库 IO + startup），单查询响应在毫秒级，远低于 200ms / 1s 目标，实际性能满足。

文档勾选状态：功能文件 §5 checklist 在源文件中显示为 `[ ]`，这是文档格式约定（实现规格本身就是 checklist 形式，由测试覆盖证明满足），不是功能缺失。所有 AC 由测试覆盖且全部通过。

### 维度 4：实现规格符合度 — 通过

| 规格项 | 实现位置 | 核对 |
| --- | --- | --- |
| `_count_matched_stocks_single` 不再调 `_get_latest_report_period` | line 159-190 period 由入参传入 | ✓ |
| `_count_matched_stocks_single` 复用 `_escape_like_keyword` | line 184 | ✓ |
| `_get_keyword_matches` 用 `.distinct(*cols)` + outerjoin Stock | line 229-232 + line 219 | ✓ |
| `_get_keyword_matches` ORDER BY (symbol, holder_name, ann_date DESC NULLS LAST) | line 233-237 | ✓ |
| `preview_match_breakdown` try/except 单关键词降级 | line 476-483 | ✓ |
| `preview_match_breakdown` 关键词长度 >200 → null + ERROR 日志 | line 466-472 | ✓ |
| `preview_match_breakdown` period None → 0 | line 473-475 | ✓ |
| `list_keyword_matches` Python 层分页 + total = len(rows) | line 525-541 | ✓ |
| 4 Pydantic 模型用 `to_camel` alias | line 76/87/95/107 | ✓ |
| 端点位置在 `preview` 之后、`GET ""` 之前 | line 151 / 178 / 206 | ✓ |
| 端点用 `Depends(require_admin)` | line 160, 189 | ✓ |
| query 参数 snake_case（`page_size` / `exclude_group_id`） | line 156, 184, 185 | ✓ |
| `keyword` Query min_length=1 / `page_size` Query le=100 | line 182, 184 | ✓ |

"不在范围"全部未实现：前端零改动、用户侧 API 零改动、preview 端点不动、不新增表/迁移、不引入缓存/异步队列 ✓。

### 维度 5：验证命令执行 — 通过

实际执行 `python -m pytest tests/test_shareholder_group_admin_api.py -v --no-cov`：

```
======================= 33 passed, 10 warnings in 9.98s ========================
```

- 33 个用例全部通过（含 15 新增 + 18 现有）
- 10 warnings 为非阻塞告警（不影响功能）
- 退出码 0

### 维度 6：E2E 质量证据 — 通过

| 阶段 | 证据文件 | 结论 |
| --- | --- | --- |
| red | `docs/e2e/evidence/plan-01-07-e2e-red-20260614.md` | 预期失败，测试有效（14 个用例 405 失败 + 1 个 preview 回归 PASS） |
| green | `docs/e2e/evidence/plan-01-07-e2e-green-20260614.md` | 通过（33/33 PASSED） |

证据格式符合 contract `e2e.evidence_filename_pattern = "{feat_id}-e2e-{phase}-{date}.md"`。

后端 FEAT 用 pytest API 集成测试（参照 MEMORY「后端 FEAT E2E 适配 pytest」），不写 Playwright —— 在 plan §8「E2E 不适用说明」中已明确声明，不是豁免。

### 维度 7：边界与风险 — 通过

plan §8「后端边界场景」12 个场景全部 `done`：

| 场景 | 状态 |
| --- | --- |
| keywords 为空字符串/全逗号 | done |
| 单关键词超 200 字符 | done |
| 最新报告期为 None | done |
| 单关键词查询抛 DB 异常 | done |
| stocks 表缺失某 symbol | done |
| LIKE 通配符 % / _ | done |
| page=0 / page=-1 | done（Query ge=1 → 422） |
| page_size 超过 100 | done（Query le=100 → 422） |
| normal_user 访问两个新端点 | done（403） |
| 无 token 访问两个新端点 | done（401） |
| 同股票多股东 DISTINCT ON 行为 | done |
| 报告期跨期 | done |

### 维度 8：契约对齐 — 通过

**前后端契约四件套核对**（基于 MEMORY「dev-plan-check 路径前缀验证」）：

**`/preview-breakdown`**:
- 路径拼接：admin_router `/v1/admin` × 子 router `/shareholder-groups` × 端点 `/preview-breakdown` = `/api/v1/admin/shareholder-groups/preview-breakdown`（无重复前缀）✓
- 前端 plan-02 endpoint 写 `/admin/shareholder-groups/preview-breakdown`，apiClient.baseURL `${API_BASE_URL}/api/v1` 拼接后等价 ✓
- HTTP 方法：GET ✓
- query 参数：`keywords`、`exclude_group_id`（snake_case，与现有 `/preview` 一致）✓
- 响应字段：外层 `ApiResponse{ success, data, message }`；`data.items[].matchedStockCount`（camelCase，由 Pydantic alias 转换）✓

**`/keyword-matches`**:
- 路径拼接：`/api/v1/admin/shareholder-groups/keyword-matches`（无重复前缀）✓
- HTTP 方法：GET ✓
- query 参数：`keyword`、`page`、`page_size`（snake_case，FastAPI 接收 snake_case）✓
- 响应字段：`data.items[].stockName / holderName`（camelCase）；`data.pageSize`（camelCase）；前端类型定义需用 camelCase ✓

无契约偏差。下游消费方 plan-02 可直接据此契约实现。

### 维度 9：代码审查 — 通过（无 blocker）

**核心审查项 10 条**：

| 审查项 | 结果 | 说明 |
| --- | --- | --- |
| 路径前缀无重复 | ✓ | admin_router `/v1/admin`（router.py:29）× 子 router `/shareholder-groups`（shareholder_groups.py:22）× 端点 `/preview-breakdown`/`/keyword-matches`（line 151/178），最终路径 `/api/v1/admin/shareholder-groups/{preview-breakdown,keyword-matches}` |
| 响应字段 camelCase | ✓ | `matchedStockCount` / `stockName` / `holderName` / `pageSize` 全部由 Pydantic `to_camel` alias 转换；测试明确断言 camelCase 存在 + snake_case 不泄漏 |
| query 参数 snake_case | ✓ | `page_size` / `exclude_group_id` 与现有 `/preview` 一致；plan §3 #7 明确说明 query 参数不经 alias 转换（响应体才转） |
| AC-07 单关键词失败降级 | ✓ | `preview_match_breakdown` line 476-483 try/except 包裹单关键词查询，失败 item 写 `matched_stock_count: None`，其他正常；`logger.exception` 记录堆栈；测试 `test_preview_breakdown_partial_failure_returns_null_for_failed_keyword` 用 monkeypatch 第一次抛异常、第二次正常，断言 1 个 null + 1 个 int（未放宽为"全 null 也算通过"） |
| SQL 注入防护 | ✓ | `_escape_like_keyword` 转义 `\` / `%` / `_`；`_count_matched_stocks_single` line 184 与 `_get_keyword_matches` line 224 都用 `like(... , escape="\\")`；全部 SQLAlchemy 参数绑定，无 raw SQL 拼接；测试 `test_keyword_matches_escapes_like_wildcards` 验证 `keyword=%` total=0 |
| `test_keyword_matches_pagination` total 3→4 调整合理性 | ✓ | **非断言放宽**。fixture `sample_holders` 4 行报告期 2024-06-30 holder_name 均含"社保基金"字面（一一六组合/一零四组合/一零八组合/理事会），所以 `keyword="社保基金"` 应匹配 4 行（同股票多股东按 (symbol, holder_name) 粒度 DISTINCT ON 后 600000 拆 2 行 + 600036 + 000001 = 4 行）；该调整是反映真实匹配语义的修正；测试代码 line 725-730 已写注释说明；与 AC-04 "同股票多股东按分行展示"口径一致 |
| 端点声明顺序 | ✓ | `/preview` → `/preview-breakdown` → `/keyword-matches` → `GET ""` → `PATCH /{group_id}` → `DELETE /{group_id}`；新端点全部位于动态路径 `/{group_id}` 之前；注释 line 147-148 明确说明"顺序关键" |
| 复用声明实际代码核对 | ✓ | `_count_matched_stocks_single` 复用 `_escape_like_keyword` ✓；`preview_match_breakdown` 复用 `_get_latest_report_period` + `_count_matched_stocks_single` ✓；`list_keyword_matches` 复用 `_get_latest_report_period` + `_get_keyword_matches` ✓；Stock 模型从 `src.models` import（line 18）✓ |
| Task 列表 | ✓ | 9 个 Task 全部 done |
| 边界场景 | ✓ | 12 个场景全部 done |

**代码建议项（非阻塞）**：

- `_get_keyword_matches` 当前用 SQLAlchemy `.distinct(*cols)` API 实现 PostgreSQL `DISTINCT ON (symbol, holder_name)`，注释清晰，可读性良好。
- `exclude_group_id` 参数本期保留占位（与 `_count_matched_stocks` 一致行为），plan §3 明确说明本期可忽略，前端 plan-02 可不传。这是文档化的"前置占位"，不是契约偏差。
- `preview_match_breakdown` 内部对单关键词失败用 `except Exception`（line 479）+ `# noqa: BLE001` 注释，已显式说明捕获广谱异常是有意为之（ADR-5 降级要求）。这是合理的，因为 DB 异常的具体类型在 SQLAlchemy 中较广（IntegrityError / OperationalError / StatementError 等），降级语义就是"任何异常都不阻塞其他关键词"。

无 blocker。

### 维度 10：执行验证（task handler 适用性）— 不适用（已核查复用声明）

plan-01 是纯查询 API，**不涉及异步任务处理器（task handler）**。维度 10 的 task handler 专项检查不适用。

但维度 10 的延伸原则（"复用声明实际代码核对"）仍适用，已在维度 4 + 维度 9 核对完成：

- `_count_matched_stocks_single` 数据源：`top10_float_holders` 表（latest period + keyword LIKE）
- `_get_keyword_matches` 数据源：`top10_float_holders` JOIN `stocks` 表（LEFT JOIN，stocks 缺失则 stock_name=None）
- `_count_matched_stocks` / `_count_matched_stocks_single` 写入目标：无（只读 SELECT）
- `_get_keyword_matches` 写入目标：无（只读 SELECT）
- 前置依赖：`_escape_like_keyword`、`_get_latest_report_period`、`Stock` import 均就位
- 全部数据链路与实现规格一致

通过。

## 4. 验证命令输出摘要

```
============================= test session starts ==============================
platform darwin -- Python 3.12.13, pytest-9.0.3, pluggy-1.6.0
configfile: pytest.ini
plugins: cov-7.1.0, asyncio-1.3.0, anyio-4.13.0
asyncio: mode=Mode.AUTO

collected 33 items

tests/test_shareholder_group_admin_api.py ......................            [ 66%]
................                                                                          [100%]

======================= 33 passed, 10 warnings in 9.98s ========================
```

- 33 个用例全部通过
- 10 warnings 为非阻塞告警
- 退出码 0
- 收集阶段无 ImportError、无 collection error、无 deselected

## 5. Blocker 列表

无。

## 6. 建议项列表

1. **文档勾选**：plan-01 功能文件 §5「验收标准」checklist 在源文件中显示为 `[ ]` 未勾选状态。功能层面全部 AC 已通过测试覆盖验证。建议在后续文档维护时勾选 `[x]` 反映实际状态（非阻塞）。
2. **`exclude_group_id` 占位说明**：plan §3 明确本期后端可忽略该参数，前端 plan-02 可不传。建议在后续 plan（如需差异化排除）补 SQL 过滤逻辑（非本期范围）。

## 7. 状态变更记录

| 字段 | 变更前 | 变更后 | 操作来源 |
| --- | --- | --- | --- |
| `plan-01-*.md` frontmatter `status` | review | done | task-review skill |
| README `7.2 开发状态机` 表 plan-01 行 `当前步骤` | task-review | done | 主 agent 维护（task-review 不修改） |

**说明**：按任务约束「不要修改 README 的『### 7.2 开发状态机』表」，本次 task-review 不修改 README 该表。README frontmatter `status` 当前为 `in_review`，plan-02 status 仍为 `ready-to-dev`（frontmatter）+ red-e2e（开发状态机表），所以 README frontmatter 不应改为 `accepted`。仅 plan-01 功能文件 frontmatter 由 task-review 推进。

## 8. 后续步骤

- plan-01 完成验收 → 解除 plan-02 阻塞
- plan-02（前端弹窗）可进入 `red-e2e` 阶段，依赖 plan-01 提供的端点契约
