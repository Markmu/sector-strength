# 架构文档检查报告：数据状态总览与一键补齐

> 检查对象：`02-1-架构文档-数据状态总览与一键补齐.md`
> 需求基准：`02-0-需求设计-数据状态总览与一键补齐.md`
> 检查日期：2026-05-27

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | 架构 frontmatter `input_documents` 唯一指向 `02-0-需求设计-数据状态总览与一键补齐.md`，配对可靠 | — |
| Frontmatter contract | ✅ | 4 个必填字段齐全：`workflow_type=arch-gen`、`status=review_ready`、`input_documents`、`open_questions=[]`，status 取值合法 | — |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 与正文 `5.x 待确认问题: 无` 一致 | — |
| 实现影响的未决问题 | ✅ | 无未决问题 | — |

> 无前置阻塞。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 4 | 1 | 0 | 94% |
| 3. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 4. 系统上下文与模块职责 | ⚠️ | 2 | 1 | 0 | 89% |
| 5. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施方案 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| 质量检查项 | ✅ | 5 | 0 | 0 | 100% |
| **合计** | — | **48** | **2** | **0** | **99%** |

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖 | 5 | 0 | 0 | 100% |
| 3. 用户故事满足 | 5 | 0 | 0 | 100% |
| 4. 业务规则执行 | 7 | 0 | 0 | 100% |
| 5. 异常与边界覆盖 | 8 | 0 | 0 | 100% |
| 6. 成功标准可达性 | 6 | 0 | 0 | 100% |
| 7. 风险与降级对齐 | 3 | 0 | 0 | 100% |
| 8. UX 设计支撑 | 7 | 0 | 0 | 100% |
| 9. ADR 需求溯源 | 5 | 0 | 0 | 100% |
| 10. 范围合规性 | 5 | 0 | 0 | 100% |
| 11. 一致性检查 | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 4 | 0 | 0 | 100% |
| **合计** | **59** | **0** | **0** | **100%** |

## 三、结构完整性详细检查

### Frontmatter（6/6 通过）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| F.1 | workflow_type | ✅ | `arch-gen` |
| F.2 | status | ✅ | `review_ready`，合法取值 |
| F.3 | input_documents | ✅ | 指向 PRD `02-0-需求设计-数据状态总览与一键补齐.md` |
| F.4 | open_questions | ✅ | `[]` |
| F.5 | input_documents 可解析 | ✅ | 唯一指向现存 PRD，路径有效 |
| F.6 | open_questions 与正文同步 | ✅ | frontmatter `[]` 与正文 `5.x 待确认问题: 无` 一致 |

### 1. 系统摘要（3/3 通过）

全部 ✅：核心能力描述清晰（一句话概括功能 + 闭环锚点）、核心闭环 `Status → Detect → Backfill → Refresh`、首版定位明确（面向管理员，复用现有任务系统）。

### 2. 范围、非目标与成功标准（4 通过 / 1 部分完成）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 2.1 | P0 范围 | ✅ | §2.1 列出 5 项首版范围，清晰可辨 |
| 2.2 | P1 预留 | ⚠️ | 无独立 P1 节。但 PRD 本身也无 P1 功能项，架构与 PRD 对齐，不影响实现 |
| 2.3 | 明确不做 | ✅ | §2.2 列出 5 项不做，与 PRD 一致 |
| 2.4 | 成功标准 | ✅ | §2.3 含 6 项指标表格，功能性使用定性描述，仅性能类保留数值（< 2s） |
| 2.5 | 验收标准承接矩阵 | ✅ | §2.4 逐条承接 AC-01~AC-06，含承接模块、关键链路和风险说明 |

### 3. 用户流程与状态（3/3 通过）

全部 ✅：§3.1 主流程覆盖从打开页面到补齐完成的 9 步端到端流程；§3.2 关键分支以表格列出 5 种分支场景及处理方式；§3.3 mermaid 状态机覆盖 Loading/Normal/Missing/Backfilling/Failed/Error 及其转换规则。

### 4. 系统上下文与模块职责（2 通过 / 1 部分完成）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 4.1 | 系统上下文 | ⚠️ | 使用文字描述（"变更点说明"格式）而非 ASCII 框图。数据流向有描述：`前端 → 状态 API → DataStatusService → Repository 层`，信息传达充分但形式不符模板 |
| 4.2 | 模块职责 | ✅ | §4.2 表格列出 7 个模块的职责、上游输入、下游输出 |
| 4.3 | 避免过度设计 | ✅ | §4.3 显式列出 5 项不引入的设计及原因 |

### 5. 关键架构决策（ADR）（6/6 通过）

全部 ✅：ADR-1~ADR-5 各含编号/标题、选择、理由、风险与对策。5 条 ADR 在 4-8 条范围内。§5.x 待确认问题为"无"，与 frontmatter 同步。

### 6. 运行链路（3/3 通过）

全部 ✅：§6.1 状态查询链路（4 步 + 实现原则）、§6.2 一键补齐链路（6 步 + 实现原则）、§6.3 进度轮询链路（3 步 + 实现原则），覆盖所有 P0 功能。

### 7. 领域对象与关键契约（6/6 通过）

全部 ✅：§7.1 核心对象表（3 个对象含 Source of Truth/Owner/用途）、§7.2 最小 Schema（DataStatusResponse/DataTypeStatus/BackfillResponse，嵌套 ≤2 层）、§7.3 API 边界表（2 个端点含路径/方法/用途）、§7.4 状态流转（mermaid 图）、§7.5 数据边界（4 个存储层）、§7.6 命名规则（API 风格/数据类型标识/日期格式/JSON 命名）。

### 8. 非功能需求、风险与运行策略（6/6 通过）

全部 ✅：§8.1 性能目标表、§8.2 三级降级策略（L1~L3）、§8.3 安全策略（RBAC/输入验证/并发控制）、§8.4 成本控制、§8.5 可观测性、§8.6 三项主要风险及缓解。

### 9. 实施方案（2/2 通过）

全部 ✅：§9 Phase A（后端 4 步）+ Phase B（前端 4 步），每步含文件路径和具体实现内容，每个 Phase 末有独立验证目标。

### 10. 架构结论（2/2 通过）

全部 ✅：§10 总结 4 条核心判断（最小改动/职责清晰/渐进演进/风险可控）并指出演进方向（扩展到个股数据只需 Service 层增加查询维度）。

### 质量检查项（5/5 通过）

全部 ✅：无 DDL/目录树/环境变量（仅 TS interface）；无重复内容；ADR/Schema/API/状态机一致；量化约束合理（5 ADR / 2 API / 嵌套 ≤2 层）；frontmatter 与正文一致。

## 四、需求满足度详细检查

### 维度 2：范围覆盖（5/5 ✅）

| PRD 需求 | 架构覆盖 | 状态 | 证据 | 差距说明 |
|----------|---------|------|------|---------|
| P0-01: 新增"数据状态"标签页，作为默认展示 | ADR-5 标签页集成 + §9 Phase B step 7 | ✅ | 架构文档 §5 ADR-5 选择在现有标签页体系中新增标签；§9 step 7 明确修改 `page.tsx` 新增标签 | — |
| P0-02: 展示三类数据最新日期、状态标记、缺失日期范围 | §6.1 状态查询链路 + §7.2 Schema | ✅ | 架构文档 §6.1 定义 max(date) 查询 + TradingCalendar 缺失检测；§7.2 DataTypeStatus 含 latest_date/status/missing_range | — |
| P0-03: 独立补齐按钮，自动检测缺失范围并触发补齐 | §6.2 一键补齐链路 + §4.2 DataTypeCard | ✅ | 架构文档 §6.2 定义 `POST /admin/data/backfill/{type}` 自动检测缺口；§4.2 DataTypeCard "点击补齐按钮调用 API" | — |
| P0-04: 补齐任务创建后显示进度 | §6.3 进度轮询链路 + §4.2 DataTypeCard | ✅ | 架构文档 §6.3 定义 SWR refreshInterval:2000 轮询获取 progress/total；§4.2 "进度条使用 div 宽度百分比渲染" | — |
| P0-05: 页面加载时自动获取最新状态 | §6.1 + §3.1 主流程 | ✅ | 架构文档 §3.1 step 2-3 "页面加载，请求 GET /admin/data/status" | — |

### 维度 3：用户故事满足（5/5 ✅）

| 用户故事 | 架构流程/链路 | 状态 | 证据 | 差距说明 |
|----------|-------------|------|------|---------|
| US-01: 看到各类板块数据的最新日期和健康状态 | §6.1 状态查询链路 → DataStatusPanel → DataTypeCard | ✅ | 架构文档 §6.1 step 1-4 完整覆盖请求→查询→返回→展示 | — |
| US-02: 看到有缺口时具体的缺失日期范围 | §6.1 step 3b-c + §7.2 missing_range | ✅ | 架构文档 §6.1 通过 TradingCalendar 获取交易日列表，过滤得到 missing_dates；§7.2 Schema 定义 missing_range.start/end | — |
| US-03: 点击一个按钮自动补齐缺失数据 | §6.2 一键补齐链路 | ✅ | 架构文档 §6.2 step 1-6：点击→API→自动检测范围→创建任务→返回→mutate刷新 | — |
| US-04: 补齐任务创建后能看到进度 | §6.3 进度轮询链路 | ✅ | 架构文档 §6.3 step 1-3：SWR 轮询→active_task.progress/total→UI 更新 | — |
| US-05: 任务完成后状态自动刷新 | §6.3 step 3 + §4.2 | ✅ | 架构文档 §6.3 "completed → 切换为正常态（此轮询周期内会刷新）"；§4.2 "补齐按钮点击后立即 mutate() 刷新" | — |

### 维度 4：业务规则执行（7/7 ✅）

| 业务规则 | 执行机制 | 状态 | 证据 | 差距说明 |
|----------|---------|------|------|---------|
| BR-01: 最新日期 = max(date) | §6.1 查询 max(date) + §7.5 数据边界 | ✅ | 架构文档 §6.1 "查询 max(date) 获取 latest_date"；§7.5 明确 daily_market_data/moving_average_data/strength_scores 各表的查询 | — |
| BR-02: 缺失判定 via 交易日 | §6.1 step 3b-c TradingCalendar | ✅ | 架构文档 §6.1 "调用 TradingCalendar 获取 latest_date+1 到今日之间的交易日列表"；§6.1 实现原则 "交易日查询使用 TradingCalendar 缓存" | — |
| BR-03: 缺失范围格式 YYYY-MM-DD ~ YYYY-MM-DD | §4.2 交互细节 + §7.6 命名规则 | ✅ | 架构文档 §4.2 "缺失范围格式：YYYY-MM-DD ~ YYYY-MM-DD"；§7.6 "日期格式 YYYY-MM-DD" | — |
| BR-04: 补齐范围 = 最新日期+1 到当日 | §6.2 step 2 + 实现原则 | ✅ | 架构文档 §6.2 "调用 DataStatusService.get_backfill_range(type) 获取缺口范围"；实现原则 "补齐范围计算逻辑与状态查询一致（latest_date+1 到今日）" | — |
| BR-05: 进度每 2 秒刷新 | ADR-4 + §6.3 | ✅ | 架构文档 §5 ADR-4 "SWR refreshInterval: 2000"；§6.3 "每 2 秒通过 SWR refreshInterval 自动更新" | — |
| BR-06: 刷新时机（首次加载 + 完成后） | §3.1 + §6.3 | ✅ | 架构文档 §3.1 step 2 "页面加载，请求 API"；§6.3 step 3 "completed → 切换为正常态" | — |
| BR-07: 补齐中按钮禁用 | §3.3 状态机规则 + §6.2 实现原则 | ✅ | 架构文档 §3.3 "Backfilling 状态下按钮禁用，防止重复创建任务"；§6.2 实现原则 "创建任务前检查是否已有同类型活跃任务，有则返回 409" | — |

### 维度 5：异常与边界覆盖（8/8 ✅）

| PRD 异常场景 | 架构错误处理 | 状态 | 证据 | 差距说明 |
|-------------|------------|------|------|---------|
| 补齐任务失败 | §3.2 关键分支 + §8.2 降级 + §2.4 AC-05 | ✅ | 架构文档 §3.2 "补齐失败：显示错误信息 + '重新补齐'按钮；重新创建任务"；AC-05 承接 "task error_message" | — |
| 补齐任务进行中 | §3.2 关键分支 + §3.3 状态机 | ✅ | 架构文档 §3.2 "补齐进行中：按钮禁用，显示进度条；通过 active_task 字段关联" | — |
| 状态获取失败 | §3.2 关键分支 + §2.4 AC-06 | ✅ | 架构文档 §3.2 "状态加载失败：卡片显示错误态 + '重试'链接；调用 mutate() 刷新"；AC-06 承接 "DataStatusPanel error state + mutate()" | — |
| 加载中（骨架屏） | §3.1 step 2 + §9 Phase B step 8 | ✅ | 架构文档 §3.1 "三张卡片展示骨架屏"；§9 step 8 "加载中展示骨架屏" | — |
| 三类数据都正常 | §3.1 情况 A + §3.2 "全部正常"分支 | ✅ | 架构文档 §3.2 "全部正常：卡片只展示最新日期，不显示按钮" | — |
| 数据表为空 | §3.2 "数据表为空"分支 + §6.1 实现原则 | ✅ | 架构文档 §3.2 "latest_date 为 null，显示'暂无数据'，不显示补齐按钮"；§6.1 "数据表为空时 status 返回 no_data" | — |
| 非交易日无缺失 | §6.1 step 3b-c | ✅ | 架构文档 §6.1 "过滤掉非交易日后得到 missing_dates；若列表为空则 status=normal" | — |
| 补齐期间产生新缺口 | §2.4 AC-03 风险说明 | ✅ | 架构文档 §2.4 AC-03 "若补齐期间产生新缺口，需再次点击补齐；不自动链式补齐" | — |

### 维度 6：成功标准可达性（6/6 ✅，关键维度）

| PRD 验收指标 | 架构目标值 | PRD 目标值 | 状态 | 证据 | 差距说明 |
|-------------|-----------|-----------|------|------|---------|
| AC-01: 状态卡片展示 | DataStatusPanel + GET /admin/data/status | 三类数据状态正常展示 | ✅ | 架构文档 §2.4 AC-01 承接矩阵；§6.1 状态查询链路支撑 | — |
| AC-02: 缺失检测与范围展示 | DataStatusService._detect_gap + 前端卡片 | 缺失时正确显示缺口日期范围 | ✅ | 架构文档 §2.4 AC-02 承接矩阵；§6.1 step 3b-c 交易日检测逻辑 | — |
| AC-03: 一键补齐触发 | POST /admin/data/backfill/{type} + TaskRegistry | 自动检测范围、创建任务、显示进度 | ✅ | 架构文档 §2.4 AC-03 承接矩阵；§6.2 一键补齐链路完整覆盖 | — |
| AC-04: 补齐完成后状态刷新 | 前端轮询 + 状态 API | 完成后状态自动刷新 | ✅ | 架构文档 §2.4 AC-04 承接矩阵；§6.3 进度轮询链路 "completed → 切换为正常态" | — |
| AC-05: 补齐失败处理 | DataStatusPanel + task error_message | 红色标记 + 错误信息 + 重新补齐按钮 | ✅ | 架构文档 §2.4 AC-05 承接矩阵；§3.2 关键分支 "failed" | — |
| AC-06: 状态获取失败处理 | DataStatusPanel error state + mutate() | 失败提示 + 重试链接 | ✅ | 架构文档 §2.4 AC-06 承接矩阵；§3.2 "状态加载失败" 分支 | — |

成功标准对照：

| 架构 §2.3 指标 | 目标值 | 定性/定量分类 | 支撑方案 | 状态 |
|---------------|--------|-------------|---------|------|
| 状态卡片展示 | 功能正常展示 | 定性 ✅ | §6.1 查询链路 | ✅ |
| 缺失检测 | 正确显示缺口日期范围 | 定性 ✅ | §6.1 TradingCalendar | ✅ |
| 一键补齐 | 自动创建任务无需手动填写 | 定性 ✅ | §6.2 自动检测范围 | ✅ |
| 进度展示 | 显示百分比完成后自动刷新 | 定性 ✅ | §6.3 SWR 轮询 | ✅ |
| 失败处理 | 显示错误信息和重试按钮 | 定性 ✅ | §3.2 + §8.2 | ✅ |
| 状态 API 响应时间 | < 2s | 性能定量 ✅ | §8.1 利用索引查询 | ✅ |

### 维度 7：风险与降级对齐（3/3 ✅）

PRD 无独立风险/降级章节（§5 仅有决策记录）。架构文档 §8.6 补充了 3 项合理风险及缓解措施，§8.2 定义了 3 级降级策略，属于合理扩展。

### 维度 8：UX 设计支撑（7/7 ✅）

全部 ✅：三张状态卡片（§4.2 DataStatusPanel + DataTypeCard）、正常/缺失/失败三种卡片状态（§4.2 交互细节）、进度条渲染（§4.2 "div 宽度百分比"）、骨架屏（§9 step 8）、标签页集成（ADR-5）、补齐按钮交互（§4.2 "点击后立即禁用 + mutate()"）、重试链接（§3.2 "错误态 + 重试链接"）。

### 维度 9：ADR 需求溯源（5/5 ✅）

| ADR | 溯源需求 | 判定 | 说明 |
|-----|---------|------|------|
| ADR-1: 复用现有任务系统 | PRD §1.3 "复用现有任务系统" | 需求驱动 | PRD 明确要求复用，避免重复建设 |
| ADR-2: 新增 3 个专用 task type | PRD §1.3 "每类数据独立的补齐缺失按钮" | 需求驱动 | 按类型独立追踪活跃任务，支撑独立补齐按钮 |
| ADR-3: 聚合状态 API 一次返回 | PRD P0-02 "展示三类板块数据最新日期和状态" | 合理扩展 | 单次请求获取全部状态，减少前端请求次数和加载态复杂度 |
| ADR-4: SWR 轮询获取进度 | PRD §3.3 BR-05 "每 2 秒自动刷新任务状态" | 需求驱动 | PRD 明确要求 2 秒刷新频率 |
| ADR-5: 标签页方式集成 | PRD §1.3 "在数据管理页面新增数据状态标签页" | 需求驱动 | PRD 明确选择标签页方式作为功能入口 |

### 维度 10：范围合规性（5/5 ✅）

| PRD 不做项 | 架构中出现情况 | 状态 | 说明 |
|-----------|--------------|------|------|
| ND-01: 个股数据的状态展示和补齐 | §2.2 明确列出不做，§4.3 未引入 | ✅ | 架构仅覆盖板块维度三类数据 |
| ND-02: 自动定时补齐 | §2.2 明确列出不做 | ✅ | 补齐为手动触发 |
| ND-03: 数据质量评分或健康度评分 | §2.2 + §4.3 "不引入统一健康度评分" | ✅ | 三类数据独立展示，无统一评分 |
| ND-04: 历史补齐记录查看 | §2.2 明确列出不做 | ✅ | 未引入记录查看功能 |
| ND-05: 数据记录数等统计信息展示 | §2.2 明确列出不做 | ✅ | 状态 API 仅返回日期和缺失信息 |

### 维度 11：一致性检查（4/4 ✅，关键维度）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | ADR-1（异步任务）与 ADR-2（3 个 task type）互补；ADR-3（聚合 API）与 ADR-4（轮询该 API）一致；ADR-5（标签页）与其他 ADR 无冲突 |
| Schema 与 API 一致性 | ✅ | §7.2 DataStatusResponse.items 与 §7.3 GET /admin/data/status 返回一致；BackfillResponse.task_id 与 §6.2 返回一致 |
| 状态机与流程一致 | ✅ | §3.3 状态机（Loading/Normal/Missing/Backfilling/Failed/Error）与 §6.1-6.3 链路步骤一一对应 |
| 范围与设计一致 | ✅ | §2.1 的 5 项 P0 范围均有 ADR 或运行链路支撑 |

### 维度 13：过度设计检测（4/4 ✅）

| 架构设计 | 对应需求 | 判定 | 说明 |
|----------|---------|------|------|
| 2 个 API 端点 | PRD P0 范围内的查询 + 触发 | 合理扩展 | 最小 API 设计，聚合端点减少请求 |
| 5 条 ADR | 每条均可溯源至 PRD 需求 | 合理 | 无孤儿决策 |
| 无新基础设施 | 与 PRD "最小改动"一致 | 合理 | §4.3 显式列出 5 项不引入的设计 |
| 无新数据表 | 状态实时计算 | 合理 | §4.3 "不为状态数据建新表" |

## 五、深度质量检查

### 维度 14：闭环完整性（关键维度）

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| 6.1 状态查询链路 | ✅ | 无 | 前端请求 → API handler → Service（max date + TradingCalendar + 活跃任务）→ 组装响应 → 返回。每步输入输出明确 |
| 6.2 一键补齐链路 | ✅ | 无 | 点击按钮 → POST API → get_backfill_range → 检查缺口 → create_task → 返回 task_id → mutate()。完整闭环 |
| 6.3 进度轮询链路 | ✅ | 无 | SWR 轮询 → 状态 API → active_task 状态 → UI 更新（running/completed/failed）。终态明确 |

### 维度 15：安全底线（关键维度）

PRD 无独立合规底线章节。架构 §8.3 安全策略覆盖：访问控制（复用 admin RBAC）、输入验证（type 枚举限定）、并发控制（409 冲突检查），属于合理安全加固。

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无。

### 部分满足的需求（中优先级）

| 编号 | 维度 | 需求 | 当前状态 | 完善建议 |
|------|------|------|---------|---------|
| S-01 | 结构完整性 §4.1 | 系统上下文建议用 ASCII 框图 | 使用文字描述，信息充分但形式不符模板 | 可选：将 §4.1 补充为 ASCII 框图格式，或保持当前形式（不影响实现） |
| S-02 | 结构完整性 §2.2 | P1 预留章节 | 未独立列出 P1 功能项 | PRD 本身无 P1 项，架构与之对齐；若后续有 P1 功能需在此补充 |

### 存疑设计（需评估）

无。

## 七、总结

- **结构完整度**：99%（48 通过 / 2 部分完成 / 0 缺失）
- **需求满足度**：100%（59 满足 / 0 部分满足 / 0 未满足）
- **整体评价**：✅ **架构充分满足需求，可进入开发计划阶段**
  - 无前置阻塞
  - 范围内所有 P0 需求均有充分架构覆盖
  - 关键维度（成功标准可达性、一致性、闭环完整性、安全底线）全部 ✅
  - 2 个 ⚠️ 项均为形式问题，不影响实现正确性
- **优先修复建议**：
  1. 无阻塞项需修复
  2. （可选）§4.1 可补充 ASCII 系统上下文框图以符合模板规范
  3. （可选）§2.2 可补充 P1 预留节，为后续扩展（如个股数据状态）留位
