# 架构文档检查报告：券商月度金股

> 检查对象：`09-1-架构文档-券商月度金股.md`
> 需求基准：`09-0-需求设计-券商月度金股.md`（PRD）
> 检查日期：2026-06-28
> 方法论：arch-check skill（16 维度）

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | frontmatter `input_documents` 唯一指向 `docs/09-券商月度金股/09-0-需求设计-券商月度金股.md`，文件存在，与文件名推断一致 | 无 |
| Frontmatter contract | ✅ | 含全部 required 字段（`workflow_type: arch-gen` / `status: review_ready` / `input_documents` / `open_questions`），`status` 取值合法 | 无 |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []`，正文 `5.x 待确认问题` 为"无"，二者一致；原 Q1（接口字段名）已收敛为"开发阶段验证 + `_parse_record` 单点兜底"，并记录为非阻塞假设 | 无 |
| 实现影响的未决问题 | ✅ | 无遗留的实现阻塞未决问题；Q1 已有架构级兜底（字段映射集中点），不改表结构/聚合逻辑/状态机/验收口径 | 无 |

> 本节无阻塞项，整体评价可进入「非阻塞」判定轨道。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5 | 0 | 0 | 100% |
| 3. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 4. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 5. 系统上下文与模块职责 | ✅ | 3 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| 质量检查项（跨章节） | ✅ | 5 | 0 | 0 | 100% |
| **合计** | — | **50** | **0** | **0** | **100%** |

结构完整性全绿。关键证据：§1 含 ≤5 词核心闭环锚点（Sync→Snapshot→Aggregate→Browse）；§2.4 验收承接矩阵逐条映射 AC-01~AC-14；§5 ADR 6 条（在 4-8 区间）、正文 ≤5 行；§7.2 Schema 嵌套 ≤2 层；模块 7 个、API 5 个，均未超量化约束上限。

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖 | 6 | 0 | 0 | 100% |
| 3. 用户故事满足 | 4 | 0 | 0 | 100% |
| 4. 业务规则执行 | 9 | 1 | 0 | 90% |
| 5. 异常与边界覆盖 | 5 | 0 | 0 | 100% |
| 6. 成功标准可达性（关键） | 7 | 0 | 0 | 100% |
| 7. 风险与降级对齐 | 6 | 0 | 0 | 100% |
| 8. UX 设计支撑 | 全部 | 0 | 0 | 100% |
| 9. ADR 需求溯源 | 6 | 0 | 0 | 100% |
| 10. 范围合规性 | 8 | 0 | 0 | 100% |
| 11. 一致性检查（关键） | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 0 | 0 | 0 | 100%（无存疑设计） |
| **合计** | — | **1** | **0** | **高** |

关键维度（6/11/14/15/16）无 ❌ 项。仅维度 4 有 1 处 ⚠️（同券商多理由归并的展示方案未明确）。详见第四节。

## 三、结构完整性详细检查

全绿，按压缩规则以摘要替代长表。

- **Frontmatter（6/6 ✅）**：`workflow_type: arch-gen`、`status: review_ready`（合法取值）、`input_documents` 唯一可解析、`open_questions: []` 与正文 §5.x 同步。
- **§1 系统摘要（3/3 ✅）**：核心能力 1 段 <200 字；核心闭环锚点 `Sync(month) → Snapshot → Aggregate(dual-view) → Browse` 为 4 词；首版定位明确（与 08 买方共识对称互补）。
- **§2 范围/非目标/成功标准（5/5 ✅）**：2.1 P0 范围 6 项清晰；2.2 明确不做 8 项；2.3 成功标准为定性描述（"功能正常完成/全路径可走通"），仅性能项 `<2s` 量化，符合 checklist Q.4 规则（未误用百分比）；2.4 AC 承接矩阵逐条覆盖 AC-01~AC-14，含 PRD 原文摘要+承接模块+链路+降级说明。
- **§3 ADR（6/6 ✅）**：ADR-1~ADR-6 均有选择/理由（含否决）/风险对策；6 条在 4-8 区间；§5.x 待确认问题为"无"，未直接删除章节而是给出决策记录。
- **§4 用户流程与状态（3/3 ✅）**：3.1 主流程 4 条（A 股票维度/B 券商维度/C 历史/D 同步）；3.2 关键分支 7 行表格（入口+处理方式）；3.3 状态机 mermaid 含关键规则。
- **§5 系统上下文与模块职责（3/3 ✅）**：4.1 ASCII 数据流向；4.2 模块职责 7 模块表格 + 复用声明代码级验证（见维度 16）；4.3 显式列出 6 项避免的过度设计。
- **§6 运行链路（3/3 ✅）**：6.1 双视图加载（含聚合 SQL + 实现原则）；6.2 券商展开懒加载；6.3 同步任务 7 步；6.4 页面/菜单加载。链路覆盖全部 P0。
- **§7 领域对象与契约（6/6 ✅）**：核心对象表 + TypeScript Schema（嵌套 ≤2）+ API 边界表（5 端点）+ 状态机 + 数据边界 + 命名/术语映射。
- **§8 NFR/风险（6/6 ✅）**：性能/错误降级/安全/成本/可观测/风险表完整。
- **§9 实施（2/2 ✅）**：Phase A/B/C 分阶段，每条到文件路径；每 Phase 有验证目标。
- **§10 结论（2/2 ✅）**：核心判断 + 演进方向。
- **质量项（5/5 ✅）**：无 DDL/目录树/代码片段（仅 Schema interface）；无重复；自洽；量化合规；frontmatter 与正文一致。

## 四、需求满足度详细检查

### 维度 2：范围覆盖（6/6 ✅）

| PRD 需求 | 架构覆盖 | 状态 | 证据 |
|---|---|---|---|
| 数据同步任务 | ADR-2 + 链路 6.3 + admin API | ✅ | §6.3 七步链路，§7.3 `/api/admin/init/broker-recommend` |
| 股票维度排行 | ADR-1 + 链路 6.1 聚合算法 | ✅ | §6.1 GROUP BY symbol COUNT DISTINCT broker |
| 券商维度反查 | ADR-1 + 链路 6.1 券商聚合 | ✅ | §6.1 GROUP BY broker |
| 月份选择 | ADR 链路 6.1 step2 + months API | ✅ | §7.3 `/months`，默认 MAX(month) |
| 搜索+分页 | ADR-4 + 链路 6.1 实现原则 | ✅ | §5 ADR-4 服务端全量重查+回第1页 |
| 侧边栏菜单 | 链路 6.4 + §9 Phase C 第11项 | ✅ | §6.4 baseSidebarItems 追加 |

### 维度 3：用户故事满足（4/4 ✅）

US-01（共识排行）→ 流程 A + 链路 6.1；US-02（券商反查）→ 流程 B + 链路 6.1/6.2；US-03（历史月）→ 流程 C + 链路 6.1 month 参数；US-04（管理员同步）→ 流程 D + 链路 6.3。操作路径完整可走通。

### 维度 4：业务规则执行（9 ✅ / 1 ⚠️）

| 业务规则（PRD §3.3） | 执行机制 | 状态 | 证据 |
|---|---|---|---|
| 排序规则（家数降序+代码升序/推荐数降序+名称） | SQL 双字段 ORDER BY | ✅ | §6.1 聚合算法 `ORDER BY broker_count DESC, symbol ASC`；AC 承接 AC-07 |
| 推荐家数口径（按券商名称去重 COUNT DISTINCT） | SQL `COUNT(DISTINCT broker)` | ✅ | §6.1 + ADR-1 |
| **同券商多理由归并**：同券商多条理由合并为 1 家计数，展开时展示全部理由 | **计数部分 ✅（COUNT DISTINCT 自动归并）；展示部分 ⚠️** | ⚠️ | §6.1 计数用 DISTINCT broker；但展开展示"多条理由分行/分隔符拼接"在 ADR-3/§7.2 的 `brokers: {broker, reason}[]` 中只体现单条 reason 字符串，未说明同券商多理由如何聚合成一条展示项。PRD §3.3 明确"不丢弃任何理由文本"。 |
| 搜索规则（代码前缀+名称包含+服务端全量重查+回第1页） | ADR-4 SQL WHERE LIKE + `page=1` | ✅ | §6.1 `symbol LIKE 'x%' OR name ILIKE '%x%'` |
| 视图切换规则（清搜索词+保月份+回第1页+会话保持） | ADR-5 前端 React state | ✅ | §5 ADR-5 |
| 月份选择规则（仅已同步月+YYYY-MM显示+默认MAX值） | months API + MAX(month) | ✅ | §6.1 step2 + §7.3 |
| 分页规则（每页20+共N为搜索条件总数+≤20隐藏分页器） | 链路 6.1 + AC-06 承接 | ✅ | §7.3 page_size 默认20；§2.3 成功标准 |
| 展开加载机制（股票预加载/券商懒加载+骨架/失败重试） | ADR-3 双机制差异化 | ✅ | §5 ADR-3 + §6.2 |
| 行业字段规则（取站内行业+无行业显示"—"） | service 批量 JOIN `_get_industry_for_stocks` | ✅ | §6.1 行业获取段 |
| 单位与精度（家数为整数） | COUNT 返回整数 | ✅ | §7.2 brokerCount: number |

### 维度 5：异常与边界覆盖（5/5 ✅）

PRD §2.1 异常旅程 5 个场景三要素完整覆盖：搜索无结果（§3.2 分支+§8.2 L1）、从未同步（§6.1 step2 has_data:false+§8.2 L5）、所选月无数据（§6.1 step4 items:[]+§8.2 L4）、同步并发（§6.3 step2 并发保护）、同步失败（§6.3+§8.2 failed+§8.6 风险）。另有股票维度展开为空、券商展开失败两个分支补充。

### 维度 6：成功标准可达性（7/7 ✅，关键维度）

AC-01~AC-14 全部被 §2.4 承接矩阵逐条承接，PRD 原意未漂移。功能性指标用定性描述（"功能正常完成""全路径可走通"），性能项 `<2s` 数值量化符合规则。逐条核对：

| AC | 架构承接 | 链路支撑 | 评估 |
|---|---|---|---|
| AC-01~AC-14 | §2.4 矩阵逐行映射 | 6.1/6.2/6.3/6.4 + AC-07 次级排序双字段、AC-10 MAX(month)、AC-13 懒加载+失败重试、AC-14 前端状态清 search+page=1 | 均无漂移、无静默改写、均有技术支撑 |

### 维度 7：风险与降级对齐（6/6 ✅）

PRD 未设独立风险/降级章节，相关内容散布于异常旅程与决策记录。架构 §8.6 主要风险表 6 项（含接口字段名、积分不足、无交易日、百家推荐膨胀、状态错乱、券商名称不稳定）均有缓解；§8.2 降级策略 L1~L5 五级，与 PRD 异常场景一一对应。无遗漏风险。

### 维度 8：UX 设计支撑 ✅

PRD §3.1 线框图交互（视图切换/月份选择/搜索/行展开/分组懒加载/分页）在 §3.3 状态机 + §5 ADR-3/5 + §6 链路均有前端状态与接口支撑。

### 维度 9：ADR 需求溯源（6/6 ✅，全部需求驱动）

| ADR | 溯源需求 | 判定 |
|---|---|---|
| ADR-1 单表 broker_recommend | AC-02/04/08 数据模型 | 需求驱动 |
| ADR-2 YYYYMM→月末交易日 | AC-08 月末快照 | 需求驱动 |
| ADR-3 预加载 vs 懒加载 | AC-03/13 + §3.3 展开加载机制 | 需求驱动 |
| ADR-4 服务端全量重查+回第1页 | AC-11/12/06 + §3.3 搜索规则 | 需求驱动 |
| ADR-5 前端状态会话保持 | §3.3 视图切换规则 | 需求驱动 |
| ADR-6 不缓存实时聚合 | §1 性能（合理技术选型） | 合理扩展 |

无孤儿决策。

### 维度 10：范围合规性（8/8 ✅）

PRD §1.4 明确不做 8 项（收益跟踪/交叉推荐/多期走势/行业聚合/自定义池/ NLP/手动选期/预计算表），架构 §2.2 完整复述并在 §4.3 显式声明"不引入"对应组件，全文未引入这些功能。演进方向（§10）提及的扩展项均标注为"后续"，非首版实现。

### 维度 11：一致性检查（4/4 ✅，关键维度）

- ADR 间一致性 ✅：ADR-1 单表 与 ADR-3 预加载/懒加载、ADR-6 不缓存 互不矛盾。
- Schema 与 API ✅：§7.2 Schema 字段与 §7.3 API 参数/返回对齐（stock-ranking 返回 BrokerStockRankingItem[]，broker-detail 返回 BrokerDetailItem[]）。
- 状态机与流程 ✅：§3.3 前端状态机（Loading/Ready/RowExpanded/BrokerExpanding）与 §6 链路步骤对应；§7.4 后端 AsyncTask 状态机与 §6.3 同步步骤对应。
- 范围与设计 ✅：§2.1 范围 6 项均有 ADR/链路支撑。

### 维度 13：过度设计检测 ✅

无存疑设计。架构主动收敛：§4.3 列出 6 项刻意避免的过度设计（不缓存/不预计算/不独立券商表/不 AI/不手动选期/不定时同步）；§4.2 模块仅 7 个、API 仅 5 个，与 PRD 双视图+同步的范围匹配。

## 五、深度质量检查

### 维度 14：闭环完整性（关键维度）

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|---|---|---|---|
| 6.1 双视图加载 | ✅ | 无 | 触发→month 兜底(MAX)→聚合→返回→渲染，无悬空 |
| 6.2 券商展开懒加载 | ✅ | 无 | 点击→broker-detail API→骨架→返回明细→渲染/失败重试 |
| 6.3 数据同步 | ✅ | 无 | POST→并发保护→create_task→handler→service(月末交易日→拉取→DELETE→写)→进度/取消→结果。被复用方法均有实现 |
| 6.4 页面/菜单加载 | ✅ | 无 | DashboardLayout→路由→BrokerRecommendPage→链路6.1 |

### 维度 15：安全底线（关键维度）

PRD 为 MVP，无显式合规底线章节。架构 §8.3 已做基础安全设计（JWT 认证复用、require_admin、SQL 注入防护参数化+`_escape_like_keyword`、仅同步服务写入），属合理扩展。无合规底线缺口（因 PRD 未设）。

### 维度 16：复用可行性验证（关键维度）

对架构 §4.2 的 6 条复用声明逐一验证三要素（数据源/写入目标/前置依赖），已代码级核实：

| 复用声明 | 被复用方法（代码核实） | 数据源 | 写入目标 | 前置依赖 | 判定 |
|---|---|---|---|---|---|
| 异步任务体系 | `task_handlers.py:1172` `sync_top10_holders_task`（已核实：构造 `(manager.db)`、set_progress_callback/set_cancel_check、`_check_cancelled` 直查 `AsyncTask.status` 标量、结果 dict 含 added/skipped/failed） | — | AsyncTask 表 | AsyncTask 模型已有 | ✅ 可行，范式对称 |
| admin API 触发 | `init_top10_holders.py`（已核实：pattern `^\d{8}$`、并发保护查 `task_type==SYNC_TOP10_HOLDERS AND status IN('pending','running')`、create_task） | — | AsyncTask | — | ✅ 可行，仅 TaskType/参数键/pattern 改 |
| 查询服务范式 | `shareholder_analysis_service.py` 注入 AsyncSession + `_serialize_value` | — | API 响应 | — | ✅ 可行 |
| stocks + sector_stocks 行业 JOIN | `shareholder_analysis_service.py:304 _get_industry_for_stocks`（已核实：`SectorStock.stock_code==Stock.symbol AND Sector.code==SectorStock.sector_code AND Sector.type=='industry'`） | 个股行业 | 行业数组 | sector_stocks 由板块体系填充 | ✅ 可行，JOIN 方式匹配 |
| 前端 API 客户端 | `api.ts:1033 fundCrowdAnalysisApi`（已核实：`apiClient.get<{success,data}>`、query snake_case `pageSize`→`page_size`、endpoint 不带 `/v1`） | — | — | — | ✅ 可行 |
| Tushare 客户端 trade_cal | 现有 `get_trading_calendar`（全量 `pro.trade_cal(exchange='SSE', is_open='1')`） | 交易日历 | — | — | ✅ 可行（ADR-2 的按月范围 trade_cal 为新增方法 `get_last_trade_date_of_month`，§9 Phase A 已列为新增，非复用现有） |

> 关键注意点（已澄清非问题）：`get_top10_float_holders` 是**逐只 ts_code** 拉取，而 broker_recommend 是按 trade_date 整批。架构 §4.2 复用声明指的是"同步服务/任务 handler 的范式"（先删后写、逐批 commit、进度回调、取消检查），**非**取数方式。取数由新增的 `get_broker_recommend(trade_date)` 承担（§9 Phase A 第 2 点），描述准确无误导。

所有复用声明的三要素与 09 场景匹配，无方法签名匹配但 entity_type/写入目标错配的情况。

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无。

### 部分满足的需求（中优先级）

| 编号 | 维度 | 需求 | 当前状态 | 完善建议 |
|------|------|------|---------|---------|
| P-01 | 4 业务规则 | PRD §3.3「同券商多理由归并」——同一券商对同一股票多条理由合并 1 家计数、展开展示**全部**理由（不丢弃） | 计数层 ✅（COUNT DISTINCT broker 自动归并）；展示层架构 Schema `brokers: {broker, reason}[]` 仅含单条 `reason` 字段，未说明同券商多理由如何在一条展示项内聚合（分行/分隔符拼接） | 在 ADR-3 或 §7.2 BrokerStockRankingItem.brokers 元素补充：`reason` 为多理由拼接（如 `\n` 分隔）或改为 `reasons: string[]`，并在 §6.1 预加载聚合 SQL 说明用 `GROUP_CONCAT/ARRAY_AGG(broker)` 时如何对同 broker 的多条 reason 合并，确保不丢弃理由文本（与 PRD AC-03"全部展示不丢弃"对齐） |

### 存疑设计（需评估）

无。

## 七、总结

- **结构完整度**：100%（50/50 检查项通过）
- **需求满足度**：高（14 条 AC 逐条无漂移承接；6 条复用声明代码级核实可行；范围/异常/降级/风险/一致性/闭环全覆盖；仅 1 处展示细节 ⚠️）
- **整体评价**：✅ **可进入下一阶段（create-dev-plan）**
  - 无前置阻塞；范围内所有 P0 需求均有充分架构覆盖；关键维度（6 成功标准 / 11 一致性 / 14 闭环 / 15 安全 / 16 复用）无 ❌ 项；非关键维度仅 1 个 ⚠️（≤3），符合"可进入下一阶段"判定。
- **优先修复建议**：
  1. （⚠️ 唯一项）补全「同券商多理由」展示层方案：明确 BrokerStockRankingItem.brokers 元素的 reason 聚合形态（拼接/数组），并在 §6.1 预加载聚合说明同 broker 多 reason 合并逻辑，确保与 AC-03"全部展示不丢弃"一致。此项不阻塞进入开发计划，可在实现阶段细化，但建议在架构层先定口径。
