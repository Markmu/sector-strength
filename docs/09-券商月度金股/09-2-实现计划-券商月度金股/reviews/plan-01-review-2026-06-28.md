---
feat_id: plan-01
title: 数据层与同步服务（模型 + Tushare 扩展 + 同步 service + task handler + admin API）
dimension: backend
phase: 1
review_date: 2026-06-28
reviewer: task-review skill
status_before: review
status_after: done
verdict: 通过（pass）
---

# plan-01 验收报告

## 功能信息

- **feat_id**: plan-01
- **标题**: 数据层与同步服务（模型 + Tushare 扩展 + 同步 service + task handler + admin API）
- **维度**: backend / Phase 1
- **状态变更**: `review` → `done`

## 验收结论

**通过（pass）。** 10 个验收维度全部满足，无 blocker。代码与 plan-01 §3 实现规格逐条一致，完整复用 Top10 同步范式（构造签名 / import 路径 / 导出形式 / JOIN 方式 / 方法存在性），red/green 证据有效并覆盖 AC-08a~f + execute，task handler 数据链路正确（数据源/写入目标/前置依赖均匹配）。

下述验证命令在本机实际复跑通过：
- `pytest tests/test_broker_recommend_sync.py --no-cov -q` → **23 passed**
- `pytest tests/test_task_system.py tests/test_fund_admin_api.py --no-cov -q` → **27 passed**（无回归）
- `alembic current` → `5213982a184e (head)`（broker_recommend 迁移已应用）
- import 校验命令 → 全部导入成功，`TaskType.SYNC_BROKER_RECOMMEND == TaskType.SYNC_BROKER_RECOMMEND`
- TaskRegistry 注册校验（import task_handlers 后）→ `sync_broker_recommend` 已注册，handler callable

## 验收维度逐项结果

### 维度 1：文件交付完整性 — 通过

| 文件 | 动作 | 交付 | 说明 |
| --- | --- | --- | --- |
| `server/src/models/broker_recommend.py` | create | 是 | 36 行，字段/索引齐全 |
| `server/src/models/__init__.py` | modify | 是 | line 19 import + line 43 `__all__`（范式参照 Top10 line 18/42） |
| `server/alembic/versions/2026_06_28_0311-5213982a184e_create_broker_recommend_table.py` | create | 是 | upgrade/downgrade 完整，已剔除 autogenerate 噪音 |
| `server/src/services/data_acquisition/tushare_client.py` | modify | 是 | 新增 `get_broker_recommend(month)`（line 747-789） |
| `server/src/services/data_init_broker_recommend.py` | create | 是 | 225 行，sync_broker_recommend + _parse_record |
| `server/src/services/task_handlers.py` | modify | 是 | 枚举 line 68 + handler line 1244-1304 |
| `server/src/api/admin/init_broker_recommend.py` | create | 是 | 79 行，pattern `^\d{6}$` + 并发保护 |
| `server/src/api/admin/__init__.py` | modify | 是 | line 17 import + line 33 注册 |

所有 8 个文件清单中的必需文件均已交付且内容非空。

**备注（非阻塞）**：工作区存在两个与 plan-01 无关的未提交改动 `server/src/services/shareholder_analysis_service.py`、`web/src/components/shareholder-analysis/IndustryDistribution.tsx`（shareholder 排序功能的工作区残留），不在 plan-01 文件清单内，亦非 plan-01 引入，不影响验收。`server/tests/test_broker_recommend_sync.py` 为 red 阶段新增测试文件（green 证据已记录），属正常交付。

### 维度 2：Task 列表完成度 — 通过

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 新建 BrokerRecommend 模型 + 导出 | done | 与实现规格 #1 一致 |
| 2 | alembic 迁移 | done | down_revision=f5e4d3c2b1a0，已应用至 head |
| 3 | Tushare get_broker_recommend(month) | done | 接口原生 month 入参（ADR-2） |
| 4 | BrokerRecommendDataInitService | done | 含 ts_code,broker 去重 + 先删后写 |
| 5 | TaskType.SYNC_BROKER_RECOMMEND + handler | done | 含 cancel_check 直查 status 标量 |
| 6 | admin init_broker_recommend 路由 + 注册 | done | 并发保护 + create_task |

6 个 Task 全部 `done`，无遗留 `todo`/`waived`。

### 维度 3：验收标准完成度 — 通过

数据同步验收（AC-08a~f）与执行验证（AC-08-execute-1~4）由 23 个 pytest 用例覆盖并通过：
- AC-08a：模型字段/索引/Date 类型（TestBrokerRecommendModel 5 个）
- AC-08b：admin API 并发保护 + task_id 返回（TestAdminInitBrokerRecommend 5 个）
- AC-08c：枚举 + handler 注册（TestTaskRegistration 2 个）
- AC-08d：get_broker_recommend 方法存在 + async（TestTushareClientExtension 2 个）
- AC-08e：先删后写幂等（TestSyncServiceIdempotency 1 个）
- AC-08f：ts_code,broker 去重保留最新 trade_date（TestSyncServiceDedup 2 个）
- execute-1~3：触发任务/写入数据/解析字段（TestSyncServiceDataWrite 3 + TestParseRecord 3 + admin happy path）
- execute-4：覆盖式刷新由"先删后写"语义锁定（TestSyncServiceIdempotency + Dedup 合并覆盖）

可观测性（架构 §8.5）通过 `_make_progress_callback` + `manager.log_message` INFO/ERROR 实现，符合实现规格 #5。

### 维度 4：实现规格符合度 — 通过

逐条核对 plan-01 §3 实现规格：
- **模型**：字段名/类型/nullability/comment 与规格 #1 精确一致；`from .base import Base` 一致；3 索引名精确（`ix_broker_symbol_month`/`ix_broker_broker_month`/`ix_broker_month`）。
- **Alembic**：`op.create_table('broker_recommend')` + 3 个 `op.create_index`，downgrade 对应 drop，down_revision 链路正确。
- **Tushare**：`pro = self._get_pro_api()` + 内部 `_fetch` 闭包 + `self._execute_with_retry(_fetch)`；空数据 `logger.warning` + 返回 `[]`；`import pandas as pd` 局部导入，范式与 `get_top10_float_holders`（line 698）一致。
- **同步服务**：构造签名 `BrokerRecommendDataInitService(session)`、`self.tushare = DataSourceFactory.create()`、import 路径（`data_acquisition` + `tushare_client.TushareDataSource`）与 Top10 line 17-19 一致；`set_progress_callback`/`set_cancel_check`/`_check_cancelled`/`_update_progress` 逐字复制 Top10（含 `asyncio.CancelledError` 语义）；月初 date 解析 `datetime.strptime(f"{month}01", "%Y%m%d").date()`；去重按 (ts_code, broker) key 保留 trade_date 最大；先删后写 `delete(BrokerRecommend).where(month == month_date)`；batch_size=500 分批 `add_all`+`flush`+`commit`。
- **Task 注册**：枚举 `SYNC_BROKER_RECOMMEND = "sync_broker_recommend"`（line 68，紧接 SYNC_TOP10_HOLDERS line 65）；handler 完全照搬 line 1172-1239 的 `sync_top10_holders_task`（含 cancel_check 直查 status 标量 + identity map 注释 + original_error 链路）。
- **Admin API**：pattern `^\d{6}$`、并发保护查 `task_type == SYNC_BROKER_RECOMMEND.value AND status IN ('pending','running')`、延迟导入 TaskManager、create_task params `{"month": request.month}`、返回 ApiResponse，范式照搬 `init_top10_holders.py`。

"不在范围"项（plan-02 查询 API / plan-03 前端 / 缓存层）均未实现，无越界。

### 维度 5：验证命令执行 — 通过

| 命令 | 退出码 | 关键输出 |
| --- | --- | --- |
| import 校验（models/TaskType/service/router） | 0 | 全部导入成功，TaskType.SYNC_BROKER_RECOMMEND 可打印 |
| `pytest tests/test_broker_recommend_sync.py --no-cov -q` | 0 | 23 passed in 1.66s |
| `pytest tests/test_task_system.py tests/test_fund_admin_api.py --no-cov -q` | 0 | 27 passed（无回归） |
| `alembic current` | 0 | `5213982a184e (head)`（broker_recommend 迁移已应用） |

### 维度 6：E2E 质量证据 — 通过

- **red 证据**：`docs/e2e/evidence/plan-01-09-e2e-red-2026-06-28.md`，结论"预期失败，测试有效"。23 用例全部失败，根因均为功能未实现（ImportError / ModuleNotFoundError / AttributeError / 404），无测试自身错误；同级 27 用例通过无回归；收集阶段无错误。
- **green 证据**：`docs/e2e/evidence/plan-01-09-e2e-green-2026-06-28.md`，结论"通过"。23 用例全部通过，27 用例无回归，alembic 迁移应用成功。red→green 期间修正的两处（admin URL 加 /v1、并发保护 fixture 化）属测试 bug 修复而非放宽断言，已说明。
- **E2E 适配**：本功能为纯后端 task handler，无前端 UI，E2E 形态适配为 pytest（参照 MEMORY「后端 FEAT E2E 适配 pytest」+ plan-01 §8 风险与边界说明），red/green 双证据齐备，未豁免。

### 维度 7：边界与风险 — 通过

风险与边界表 7 个后端边界场景（接口返回空 / 同券商同股多次推荐 / 重复触发覆盖刷新 / 并发保护 / 任务取消 / 接口异常 / 单批 commit 失败）均有对应实现与测试覆盖。边界场景状态列仍为 `todo`（plan 文档字段未更新），但实际代码均已落地且有测试验证——此处为文档维护问题，非功能缺陷，记为非阻塞 warning（见下）。

### 维度 8：契约对齐 — 通过

- `BrokerRecommend` 模型字段与下游 plan-02 只读聚合所需一致（month/trade_date/ts_code/symbol/broker/name/reason）。
- `TaskType.SYNC_BROKER_RECOMMEND = "sync_broker_recommend"` 与 plan-03 前端固定 SWR key `task_types=sync_broker_recommend` 一致。
- admin 请求 `{month: "YYYYMM"}` → `{task_id}` 与下游 plan-03 BrokerRecommendSyncPanel 契约一致。
- 响应包裹 `ApiResponse(success, data, message)` 与项目统一 `{success, data}` 结构一致。

### 维度 9：代码审查 — 通过（无 blocker）

阻塞项检查：
- 正确性：去重保留最新 trade_date 的字符串字典序比较依赖 YYYYMMDD 定长格式，正确（与 Top10 范式一致）；先删后写在 commit 前 DELETE，幂等正确。
- 安全：admin 路由有 `require_admin` 依赖；无 SQL 注入（用 ORM delete/where）；无敏感信息泄漏。
- 数据损坏风险：逐批 commit + 单批 rollback 隔离；DELETE WHERE month 精确限定，无误删。

建议项（非阻塞，不阻止 done）：
- `data_init_broker_recommend.py` 的 `_check_cancelled`/`_update_progress` 内部 `import asyncio` 为局部导入（逐字复制 Top10 范式），可考虑提到模块顶部，但与既有范式一致，不强求。

### 维度 10：执行验证（task handler） — 通过

plan-01 §实现规格 #5 声明 handler "范式照搬 sync_top10_holders_task"，即复用既有同步服务范式。数据链路核对：
- **数据源**：`self.tushare.get_broker_recommend(month)` → `pro.broker_recommend(month=month)`（Tushare 原生 month 入参，ADR-2/doc 267 核实）。
- **写入目标**：`broker_recommend` 表（`BrokerRecommend` 实例 add_all），`month` = 月初 Date 比较键。
- **前置依赖**：`delete(BrokerRecommend).where(month == month_date)` 仅依赖 broker_recommend 表本身（无跨表 JOIN），与 Top10 范式一致；无外部关联表缺失风险。

handler 内部逻辑（month 缺失校验 → 构造 service → 进度回调 → cancel_check → try/except 记录 INFO/ERROR）与实现规格 #5 描述一致。

真实任务触发（AC-08-execute-1~4 的 curl→completed→SELECT）依赖运行中的 Tushare token 与 PostgreSQL，red/green 已用 pytest 在 service 层锁定写入语义（added 计数 / add_all / DELETE 执行 / 去重结果），数据链路正确性已验证。维度 10 通过。

## 验证命令输出摘要

```
# 1. plan-01 测试
tests/test_broker_recommend_sync.py ....................... [100%]
======================= 23 passed, 10 warnings in 1.66s ========================

# 2. 同级回归
collected 27 items
tests/test_task_system.py ...............                  [ 55%]
tests/test_fund_admin_api.py ............                  [100%]
======================= 27 passed, 18 warnings in 9.57s ========================

# 3. alembic
5213982a184e (head)

# 4. import 校验
TaskType: TaskType.SYNC_BROKER_RECOMMEND
tablename: broker_recommend
OK imports

# 5. TaskRegistry 注册（import task_handlers 后）
sync_broker_recommend registered: True
handler callable: True
```

## blocker 列表

无。

## 建议项列表（非阻塞）

1. **plan-01 文档边界场景状态字段**：§风险与边界「后端边界场景」表 7 行状态列仍为 `todo`，但功能与测试均已落地。建议后续将状态列更新为 `done`，保持文档与实现一致。此为文档维护项，不影响功能验收。
2. **工作区无关残留**：`server/src/services/shareholder_analysis_service.py` 与 `web/src/components/shareholder-analysis/IndustryDistribution.tsx` 存在未提交改动（shareholder 排序功能），与 plan-01 无关。建议提交或隔离，避免混入 plan-01 交付。本 review 不处理。
3. **`import asyncio` 局部化**：`data_init_broker_recommend.py` 的 `_check_cancelled`/`_update_progress` 内部局部 `import asyncio`（逐字复制 Top10 范式），与既有范式一致，可保持现状。

## 状态变更记录

- plan-01 frontmatter `status`: `review` → `done`
- README §7.2 开发状态机 plan-01 行 `review` 列：`todo` → `done`（按约束不动该表其它列/行）
- 本验收报告写入 `reviews/plan-01-review-2026-06-28.md`

