---
feat_id: plan-03
title: 前端页面与组件（API 客户端 + 双视图页面 + 月份/视图控件 + 菜单注册 + 管理员同步面板）
review_date: 2026-06-28
reviewer: task-review (独立执行 agent)
verdict: 通过
status_change: review → done
---

# plan-03 验收报告（09 需求 — 前端页面与组件）

## 验收结论

**通过。** plan-03 全部 11 个文件交付、Task 1-11 全部 done、实现规格符合、前后端契约无偏差、验证命令全部通过（tsc / eslint exit 0、16 用例 E2E 独立重跑 16 passed in 8.9s、fund-crowd 回归 11 passed 无新回归）、red/green 证据齐备有效、边界场景代码全部处理、data-testid 清单完整落地。无 blocker。

## 维度 1：文件交付完整性 — 通过

文件清单全部交付且非空（实读确认）：

| 动作 | 路径 | 实读 |
| --- | --- | --- |
| modify | `web/src/lib/api.ts` | ✓ brokerRecommendApi（4 端点，line 1125-1172）+ types（BrokerView/Item/Response，line 1075-1123）+ adminApi.initBrokerRecommend（line 607-608） |
| create | `web/src/hooks/useBrokerRecommend.ts` | ✓ 4 SWR hooks（months/stockRanking/list/detail）+ SWR_OPTIONS + enabled 条件启用 |
| create | `web/src/app/dashboard/broker-recommend-analysis/page.tsx` | ✓ 'use client' + DashboardLayout 包裹 |
| create | `web/src/components/broker-recommend-analysis/BrokerRecommendPage.tsx` | ✓ view/month/search/page 状态 + AC-14 重置 + AC-09 空状态 + 条件启用 SWR |
| create | `web/src/components/broker-recommend-analysis/BrokerStockRanking.tsx` | ✓ 预加载展开 + 前 3 省略 + ≤20 隐藏分页器 |
| create | `web/src/components/broker-recommend-analysis/BrokerGroupList.tsx` | ✓ 懒加载展开骨架/失败重试 |
| create | `web/src/components/broker-recommend-analysis/MonthSelector.tsx` | ✓ 原生 select，YYYY-MM 显示 |
| create | `web/src/components/broker-recommend-analysis/ViewSwitcher.tsx` | ✓ aria-pressed 单选 |
| modify | `web/src/components/dashboard/DashboardLayout.tsx` | ✓ baseSidebarItems 追加"券商每月荐股"（Star 图标，line 47-51，在"基金扎堆分析"项后） |
| create | `web/src/components/admin/BrokerRecommendSyncPanel.tsx` | ✓ 范式照搬 StockTop10SyncPanel（月份选择 + 触发 + useTaskStatus 轮询 + 进度 + 记录表） |
| create | `web/src/app/dashboard/admin/broker-recommend-init/page.tsx` | ✓ AdminLayoutWithSidebar + BrokerRecommendSyncPanel |
| modify | `web/src/components/admin/AdminSidebar.tsx` | ✓ navItems 追加"券商金股同步"（Star 图标，line 69-75） |
| create | `web/tests/e2e/broker-recommend-analysis.spec.ts` | ✓ 16 用例（TC-3.1~3.16） |
| create | `web/tests/e2e/helpers/mock-broker-recommend-api.ts` | ✓ 6 端点 mock + 任务轮询 + 全量安装 |
| create | `docs/e2e/09-e2e-用例-券商月度金股.md` | ✓ 用例文档 |

文件清单外无新增/修改文件。`允许修改的额外文件: 无`，遵守。

## 维度 2：Task 列表完成度 — 通过

Task 1-11 全部 `done`（实读 plan-03 §Task 列表 line 218-228）。无遗留 todo/waived/非法状态。

## 维度 3：验收标准完成度 — 通过

AC-01~AC-14 + AC-08-ui-1~5 + 降级回归 + 构建/类型，全部由 16 用例 E2E 独立重跑覆盖并验证（见维度 5/6）。其中：
- AC-01 菜单入口：TC-3.1 ✓
- AC-02/03/06/07/11 股票维度：TC-3.2/3.3/3.4/3.8 ✓
- AC-04/12/13 券商维度：TC-3.5/3.9/3.10/3.11 ✓
- AC-05/09/14 月份/空状态/重置：TC-3.6/3.7/3.12 ✓
- AC-08-ui-1~5 admin 同步面板：TC-3.13~3.16 ✓
- 降级回归（架构 §8.2 L1-L5）：name/industries null → "—"、明细空 → "暂无推荐理由"、无数据 → 整页空状态，代码已处理并经 TC-3.2/3.4/3.7 验证 ✓

> 说明：plan-03 验收标准 checklist 与边界场景表的"前端边界场景"状态列仍标 `todo`（未回写 done），但代码层 11 个边界场景全部落地（实读源码确认，见维度 7）。该文档状态列滞后不影响功能实质验收，作为 warning 记录，建议后续回写。

## 维度 4：实现规格符合度 — 通过

实现规格 12 项全部满足，复用范式正确：

1. **brokerRecommendApi**（api.ts §1）：endpoint 写 `/broker-recommend-analysis/*`（不带 /v1，baseURL 已含 /api/v1）；4 端点 GET；query 用 `page_size`（snake_case，line 1145/1162）；TS interface 全 camelCase 对齐 §7.2。✓
2. **SWR hooks**（useBrokerRecommend §2）：`.then(res => res.data)` 解一层（AxiosResponse → body），组件读 `data?.data`，与 useFundCrowdAnalysis 注释范式一致。✓
3. **page.tsx 路由**（§3）：'use client' 必加 + DashboardLayout，与 fund-crowd-analysis/page.tsx 一致。✓
4. **BrokerRecommendPage**（§4）：DEFAULT_PAGE_SIZE=20、SEARCH_DEBOUNCE_MS=300、view/month/search/page state、AC-14 三处重置逻辑（handleViewChange/handleMonthChange/search debounce）均正确。✓
5. **BrokerStockRanking**（§5）：排名/代码/名称(null→"—")/行业(空→"—")/家数/前3省略/预加载展开（无二次请求）理由不丢弃/分页≤20隐藏，全部落地。✓
6. **BrokerGroupList**（§6）：折叠分组 + 懒加载（展开才挂 SWR key）+ 骨架 + 失败重试 + 明细空提示，全部落地。✓
7. **MonthSelector/ViewSwitcher**（§7）：YYYY-MM 显示、aria-pressed 单选（参照 CrowdScopeSelector）。✓
8. **DashboardLayout 菜单**（§8）：在"基金扎堆分析"项后追加，Star 图标已 import（line 5）。✓
9. **adminApi.initBrokerRecommend**（§9）：POST `/admin/init/broker-recommend` body `{ month }`，与 initStockTop10Holders 范式一致。✓
10. **BrokerRecommendSyncPanel**（§10）：SWR key `task_types=sync_broker_recommend`、getRecentMonths(12)、formatMonth YYYY-MM、useTaskStatus 轮询 pollInterval 2000、Toast + 进度条 + 记录表 + StatusBadge，范式照搬 StockTop10SyncPanel。✓
11. **admin 子页路由**（§11）：AdminLayoutWithSidebar + DashboardHeader + BrokerRecommendSyncPanel。✓
12. **AdminSidebar 菜单**（§12）：navItems 追加"券商金股同步"（Star，line 69-75）。✓

## 维度 5：验证命令执行 — 通过

实跑验证命令（独立于文档证据，本次执行）：

| 命令 | 结果 |
| --- | --- |
| `npx tsc --noEmit`（broker 相关） | 0 错误（grep broker 命中 0 行错误）；tests/ 既有错误与本期无关 |
| `npx eslint <broker 源文件>` | exit 0（2 warning，非阻塞，见建议项） |
| `npx playwright test broker-recommend-analysis --project=admin` | **16 passed (8.9s)** — TC-3.1~3.16 全过 |
| `npx playwright test fund-crowd-analysis --project=admin`（回归） | **11 passed (5.8s)** — 无新回归 |

所有验证命令通过。lint 的 2 个 warning（未使用 `tasksApi` 导入、未使用 `user` 解构）记录为建议项，非 blocker。

## 维度 6：E2E 质量证据 — 通过

- **red 证据**：`docs/e2e/evidence/plan-03-09-e2e-red-2026-06-28.md`，结论"预期失败，测试有效"，16 用例失败根因全部为"页面/菜单/组件不存在"，mock helpers 可正常收集（16 tests collected），非测试/环境/选择器错误。✓
- **green 证据**：`docs/e2e/evidence/plan-03-09-e2e-green-2026-06-28.md`，结论"通过"，16 passed in 8.8s；记录了实现中修复的 3 处缺陷（SWR 条件启用 / task_id 解包 response.data / 测试 selector 适配），明确标注"非放宽断言"。本次 review 独立重跑 16 passed in 8.9s 复现通过结论。✓
- 既有回归：fund-crowd-analysis 全过；admin-fund-sync 12 failed 经 git stash 验证为既有失败（该 spec 自身认证 mock 问题），未引入新回归。✓

E2E red+green 两类证据齐全有效，覆盖 plan-03 feat_id。

## 维度 7：边界与风险 — 通过（含文档 warning）

11 个前端边界场景代码全部处理（实读源码确认）：

| 场景 | 处理位置 | 验证 |
| --- | --- | --- |
| 从未同步 hasData=false | BrokerRecommendPage L95-107 整页空状态 | TC-3.7 ✓ |
| 所选月无数据 | BrokerStockRanking L147 / BrokerGroupList L62 | TC-3.6 ✓ |
| 搜索无结果 | BrokerRecommendPage L164/173 | TC-3.8/3.9 ✓ |
| 股票无 name/行业 | L78/L80 "—" | TC-3.2 ✓ |
| 同券商多 reason | reasons.join('；') 不丢弃 | TC-3.3 ✓ |
| 券商明细懒加载失败 | BrokerGroupList L150 重试 | TC-3.11 ✓ |
| 券商明细为空 | L175 "暂无推荐理由" | TC-3.11 ✓ |
| 状态错乱 | AC-14 三处重置 | TC-3.6/3.12 ✓ |
| 分页 total≤20 | showPagination 隐藏 | TC-3.4 ✓ |

> warning：plan-03「前端边界场景」表的状态列仍标 `todo`（line 345-358），与实际代码 done 状态不符，属文档状态列滞后。建议后续回写为 done。不影响功能实质（代码 + E2E 已验证）。

风险与边界中"验证失败排查方向""暂停条件"与实际执行一致；"E2E 不适用说明"已声明完整 E2E。

## 维度 8：契约对齐 — 通过

实读 api.ts + AdminApiClient 验证前后端契约四件套：

1. **路径拼接**：`apiClient.baseURL = API_BASE_WITH_PREFIX = ${API_BASE_URL}/api/v1`（已含 /v1）。endpoint 写 `/broker-recommend-analysis/stock-ranking`，最终 `/api/v1/broker-recommend-analysis/stock-ranking`，与后端 v1 主路由 `/v1` + router prefix `/broker-recommend-analysis` 一致。admin 同步 `/admin/init/broker-recommend` → `/api/v1/admin/init/broker-recommend`，与后端 router.py prefix `/v1/admin` + init 子路由 prefix `/init` 一致。**无双前缀。** ✓
2. **HTTP 方法存在性**：4 端点 GET（brokerRecommendApi）+ 1 端点 POST（initBrokerRecommend）。✓
3. **query 参数命名**：后端 FastAPI Query snake_case（`page_size`），前端 getStockRanking/getBrokerList 写 `page_size`（**不写 pageSize**）。✓
4. **响应字段命名**：后端 Pydantic to_camel 输出 camelCase（brokerCount/stockCount/hasData/pageSize），前端 TS interface 全 camelCase 对齐。✓
5. **响应包裹与解包层级**：后端 `{success:true, data:{...}}`；SWR fetcher `.then(res => res.data)` 解一层（body），组件读 `.data?.data` 取业务对象（与 useFundCrowdAnalysis 一致）；adminApiClient.request 返回 `{ data: json.data }`（line 507 已提取嵌套 data），故 `adminApiClient.post<{task_id}>` 返回 `{ data: {task_id} }`，面板 `response.data?.task_id` 正确解包（与 StockTop10SyncPanel L170 一致）。✓

无契约偏差。

## 维度 9：代码审查 — 通过（无 blocker，2 建议项）

阻塞项：无。

建议项（非阻塞）：
1. **BrokerRecommendSyncPanel 未使用 import**（warning）：`tasksApi`（line 16）已 import 但未使用，`user`（line 80 解构自 useAuth）已赋值但未使用。建议清理以保持代码整洁（参照 StockTop10SyncPanel 是否需要 user 做权限校验）。
2. **文档状态列滞后**（warning）：plan-03「前端边界场景」表状态列仍标 `todo`，与代码实际 done 不符；验收标准 checklist 未勾选。建议后续回写以保持文档与实现一致。

其余：正确性、安全性、数据损坏风险、契约正确性均无问题；命名/结构/风格与 fund-crowd-analysis + StockTop10SyncPanel 范式高度一致（复用范式正确）。

## 维度 10：执行验证（task handler） — N/A

plan-03 为纯前端功能（API 客户端 + 页面 + 组件 + 菜单注册），**不包含 task handler**。task handler 在 plan-01（后端 sync_broker_recommend），其执行验证由 plan-01 review 覆盖（已 done）。本维度不适用。

## 状态变更记录

- plan-03 frontmatter `status`: **review → done**（本次 review 执行）
- README §7.2 开发状态机表：**不修改**（由主 agent 处理，本 review 遵守约束）

## 阻塞项

无。

## 建议项（不阻塞 done）

1. 清理 BrokerRecommendSyncPanel 未使用的 `tasksApi` import 与 `user` 变量。
2. 回写 plan-03「前端边界场景」表状态列 todo→done、勾选验收标准 checklist。

## 验证命令输出摘要

```
$ npx tsc --noEmit | grep broker          # 0 行错误
$ npx eslint <broker 源文件>              # exit 0，2 warning（未阻塞）
$ npx playwright test broker-recommend-analysis --project=admin
  16 passed (8.9s)                        # TC-3.1~3.16
$ npx playwright test fund-crowd-analysis --project=admin
  11 passed (5.8s)                        # 无新回归
```
