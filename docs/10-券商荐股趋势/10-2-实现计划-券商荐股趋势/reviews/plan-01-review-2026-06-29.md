---
feat_id: plan-01
title: "后端趋势聚合与趋势 API（repository 3 方法 + service 趋势聚合 + 1 个 v1 端点）"
review_date: 2026-06-29
reviewer: task-review (独立执行 agent)
verdict: 通过
status_change: "review → done"
---

# plan-01 验收报告

## 功能信息

- **feat_id**: plan-01
- **标题**: 后端趋势聚合与趋势 API（repository 3 方法 + service 趋势聚合 + 1 个 v1 端点）
- **维度**: backend（纯后端 API，无独立 UI）
- **关联 AC**: AC-02 / AC-03 / AC-04 / AC-06 / AC-07 / AC-08 / AC-09 / AC-11 / AC-12
- **前置**: 09 plan-01/02/03 已完成（broker_recommend 表可读写）

## 验收结论

**通过。** 10 个验收维度全部满足，无 blocker。

- 实现代码正确对照 §实现规格（核心算法：连续性计数沿 months 序列、多级排序、分页、口径一致、search 转义、行业 JOIN、展开券商前 3 预加载）。
- 30 个 pytest 用例全部通过（断言强度未放宽），09 期 broker 回归 109 用例无破坏。
- red/green evidence 有效；AC 覆盖完整；Task 列表与边界场景全 done。

## 验收维度结果

| # | 维度 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 文件交付完整性 | 通过 | 3 个 modify 文件均存在且含功能相关修改；测试文件在允许列表（`server/tests/`）内 |
| 2 | Task 列表完成度 | 通过 | Task #1–#6 全部 done |
| 3 | 验收标准完成度 | 通过 | AC-02/03/04/06/07/08/09/11/12 checklist 均被 pytest 覆盖（见下方 AC 映射） |
| 4 | 实现规格符合度 | 通过 | 核心算法逐条对照正确（见下方算法审查） |
| 5 | 验证命令执行 | 通过 | trend 测试 30 passed；broker 回归 109 passed |
| 6 | E2E 质量证据 | 通过 | red（预期失败 30/30）+ green（通过 30/30）pytest 证据齐全；纯后端 API，E2E 豁免已写在「风险与边界」 |
| 7 | 边界与风险 | 通过 | 8 个后端边界场景全部 done |
| 8 | 契约对齐 | 通过 | 路径/方法/query snake_case/响应 camelCase/包裹/序列化全对齐架构 §7.3 |
| 9 | 代码审查 | 通过（无 blocker） | 见下方代码审查；仅 1 项建议（非阻塞） |
| 10 | 执行验证（task handler） | N/A | 纯只读 API，无 task handler 调用趋势方法（grep 确认） |

## 核心算法审查（对照 §实现规格）

### Repository（`broker_recommend_repository.py`）

| 算法点 | 规格要求 | 实现 | 结论 |
| --- | --- | --- | --- |
| `get_trend_aggregations` | GROUP BY symbol,month + COUNT(DISTINCT broker) + search | L307-330，结构与规格 SQL 完全一致 | ✅ |
| `get_trend_cumulative_counts` | GROUP BY symbol + COUNT(DISTINCT broker) + 同口径 search | L332-353，返回 `{symbol: count}` | ✅ |
| `get_trend_brokers` | 当页 symbols 窗口 (symbol,month,broker)；空 symbols 返回 {} | L355-383，空守卫 + 去重 broker 列表 | ✅ |
| search 同口径（架构链路 6.1） | 抽公共 WHERE 复用 | `_trend_search_condition` static method 被 aggregations + cumulative_counts 共用，避免两查询命中不同 symbol 集合 | ✅ 关键约束满足 |
| search 转义 | `_escape_like_keyword` 转义 %/_ | L301 `_escape_like_keyword(search)` 转义 `\ % _`；测试 `%` → total=0 | ✅ |

### Service `get_trend_ranking`（`broker_recommend_analysis_service.py`）

| 算法点 | 规格要求 | 实现（行号） | 结论 |
| --- | --- | --- | --- |
| 窗口月份序列 | `months = repo.get_months()`（DESC），空 → 空状态 | L360-368 | ✅ |
| 连续性计数（ADR-3, AC-07） | 从 `months[0]` 沿 months 序列向前，有记录 +1，遇断档 break | L391-396：`for m in months: if m in month_counts: consecutive+=1 else: break`（months 已 DESC） | ✅ 严格沿月份序列非自然月 |
| cumulative | `cumulative_map.get(symbol, 0)` | L398 | ✅ |
| latest（AC-04） | `month_counts.get(months[0], 0)`（全局最新月） | L383,399 `latest_month = months[0]; latest = month_counts.get(latest_month, 0)` | ✅ 与 09 同月同口径 |
| monthly_series（旧→新升序，无推荐月=0） | reverse months 后遍历 | L385 `months_asc = list(reversed(months))`；L402-405 用 `.get(m, 0)` | ✅ |
| 多级排序（AC-03） | consec↓→cum↓→latest↓→symbol↑ | L419-421 `key=lambda x: (-x["consecutive"], -x["cumulative"], -x["latest"], x["symbol"])` | ✅ 四级精确 |
| 分页（AC-08） | total = len(sorted)；offset/limit | L424-426 | ✅ |
| 行业 JOIN | `_get_industry_for_stocks(symbols)` | L430 | ✅ |
| monthly_brokers（新→旧降序，前 3） | months 降序遍历 + `[:3]` | L443-451 遍历 `months`(DESC)，`top_brokers = brokers_map.get((sym,m),[])[:3]` | ✅ |
| name 取值 | `industries_map[symbol]["stock_name"]` | L438 `stock_info.get("stock_name")` | ✅ |

### API 端点（`broker_recommend_analysis.py`）

| 契约点 | 规格要求 | 实现 | 结论 |
| --- | --- | --- | --- |
| 4 个 Pydantic model | TrendMonthPoint/TrendMonthBroker/TrendRankingItem/TrendRankingData，snake_case + `to_camel` | L141-188，字段与 ConfigDict 完全对齐 | ✅ |
| 端点签名 | search/page/page_size，**无 month 参数** | L306-313 仅 3 个 query 参数 + 认证 | ✅ |
| 响应包裹 | `{success: True, data: _dict_to_camel(result)}` | L322 | ✅ 与 09 既有端点范式一致 |
| page_size 边界 | `ge=1, le=100` | L310 | ✅ |
| 认证 | `Depends(get_current_user)` | L311 | ✅（测试验证 401） |

## AC 覆盖映射

| AC | 覆盖用例（pytest） | 状态 |
| --- | --- | --- |
| AC-02 跨月聚合 + 四指标 | test_endpoint_exists_and_wrapper / test_no_month_param_full_window / test_fields_complete_camel_case / test_continuous_months_desc_top / test_monthly_series_values | ✅ |
| AC-03 多级排序（4 级 tiebreak） | test_full_multi_level_order / test_cumulative_tiebreak / test_latest_month_count_tiebreak / test_symbol_tiebreak_asc | ✅ |
| AC-04 口径一致 | test_latest_month_count_matches_stock_ranking（每股逐只对照 09 stock-ranking） | ✅ |
| AC-06 展开券商前 3 | test_monthly_brokers_desc_with_top3 | ✅ |
| AC-07 断档降级 | test_gap_stock_consecutive_stops_at_gap / test_gap_stock_cumulative_includes_pre_gap_months / test_gap_stock_monthly_series_includes_gap_month_zero | ✅ |
| AC-08 分页 total 全窗口 | test_pagination_total_is_full_window / test_pagination_second_page / test_pagination_last_page_partial | ✅ |
| AC-09 服务端搜索 | test_search_by_symbol_prefix / test_search_by_name_contains / test_search_no_match_returns_empty / test_search_escapes_like_wildcards / test_search_keeps_multi_level_order | ✅ |
| AC-11 单月降级 | test_single_month_consecutive_all_one / test_single_month_series_single_point / test_single_month_latest_equals_cumulative | ✅ |
| AC-12 空表降级 | test_empty_table_has_data_false | ✅ |

## 验证命令输出摘要

### 范围 1：plan-01 新增测试

```
cd /Users/muchao/code/sector-strength/server && python -m pytest tests/test_broker_recommend_trend.py --no-cov -q
collected 30 items
tests/test_broker_recommend_trend.py ..............................      [100%]
======================= 30 passed, 10 warnings in 10.86s =======================
```

退出码 0，30/30 通过。

### 范围 2：09 broker 回归

```
python -m pytest tests/ -k "broker" --no-cov -q
collected 1050 items / 941 deselected / 109 selected
tests/test_broker_recommend_analysis_api.py ............................ [ 25%] ... [ 51%]
tests/test_broker_recommend_sync.py .......................              [ 72%]
tests/test_broker_recommend_trend.py ..............................      [100%]
============== 109 passed, 941 deselected, 13 warnings in 30.94s ==============
```

退出码 0，109/109 通过（71 + 23 + 30 + 其余 broker 命中），无回归破坏。

## Red / Green 证据有效性

### Red（`plan-01-10-pytest-red-2026-06-29.md`）

- 结论：**预期失败，测试有效** ✅
- 30 用例全部失败：28 个 API 用例 → 404（路由未注册），1 个认证 → 404（==401 断言），2 个 import → AssertionError（方法缺失）
- 测试文件正常收集（30 collected in 0.03s，无 ImportError），失败完全来自功能缺失，符合 red 关键原则
- 09 既有 79 用例全部通过（无回归）

### Green（`plan-01-10-pytest-green-2026-06-29.md`）

- 结论：**通过** ✅
- 30 用例全部通过（30 passed in 10.80s），断言强度未放宽（4 级 tiebreak 独立用例、逐只对照 AC-04、断档精确值、LIKE 转义验证）
- 09 broker 回归 109 passed，无破坏

### E2E 豁免说明（维度 6）

本功能为纯后端 API（无独立 UI），E2E 形态适配为 pytest API 集成测试（参照 08/09 期先例 + MEMORY「后端 FEAT E2E 适配 pytest」）。功能文件「风险与边界」已显式声明：

> E2E 不适用说明：本功能为纯后端 API，无独立 UI；用户可观察验证由 plan-02 E2E 覆盖（趋势视图 UI 联调）。本功能质量门为 pytest + curl。

符合 skill 维度 6 的豁免条款（纯内部/无用户可观察路径 + 豁免原因写在「风险与边界」）。pytest red/green 证据作为该功能的 E2E 质量门。

## Task 列表与边界场景状态

- Task #1–#6：全部 done ✅
- 8 个后端边界场景：全部 done ✅
  - 无数据 / 单月 / 中间断档 / 最新月无推荐 / 搜索无匹配 / search 通配符 / 主聚合与累计家数同口径 / monthlyBrokers 某 month 无券商

## 契约对齐

- 路径 `/api/v1/broker-recommend-analysis/trend-ranking`，无双前缀 ✅
- query snake_case（page_size），FastAPI Query 不经 alias ✅
- 响应 camelCase（`_dict_to_camel` 递归 + Pydantic `to_camel`），无 snake_case 泄漏（测试断言）✅
- 响应包裹 `{success, data}` ✅
- 无 month 参数（趋势固定全窗口）✅
- 下游 plan-02 消费契约（TrendRankingItem 字段）已暴露 ✅

## 代码审查

### Blocker：无。

### 建议项（非阻塞）

1. **Pydantic model 未作为 response_model 强制约束**：端点返回 `{"success": True, "data": _dict_to_camel(result)}` 而非 `response_model=TrendRankingData`。这与 09 既有端点范式完全一致（统一原始 dict + helper 转换），保持一致性优先；4 个 model 作为声明式 schema/文档存在。若未来要统一收紧契约，可全量改造（超出本功能范围）。

## 状态变更记录

| 项 | 变更前 | 变更后 |
| --- | --- | --- |
| plan-01 frontmatter `status` | review | **done** |
| README 状态机表 | （不修改，由主 agent 同步） | — |

## 结论

plan-01 验收通过。实现代码正确、测试覆盖完整（AC-02/03/04/06/07/08/09/11/12）、red/green 证据有效、回归无破坏、契约对齐、无 blocker。plan-01 frontmatter `status` 改为 `done`，可解锁下游 plan-02（前端趋势视图）开发。
