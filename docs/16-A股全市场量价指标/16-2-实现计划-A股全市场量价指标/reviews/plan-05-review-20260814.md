# plan-05 验收报告：市场量价范围同步与自动日更

- **功能 ID**: plan-05
- **标题**: 市场量价范围同步与自动日更
- **维度**: backend / Phase 2
- **验收日期**: 2026-08-14
- **验收前 status**: review
- **验收结论**: **通过（0 blocker）**
- **验收后 status**: done

---

## 1. 验收结论汇总

| 维度 | 结果 | 说明 |
| --- | --- | --- |
| 1. 文件交付完整性 | 通过 | create/modify 文件全部交付且非空；1 个额外测试基建文件已记录（见 §6） |
| 2. Task 列表完成度 | 通过 | 8/8 Task 全部 done |
| 3. 验收标准完成度 | 通过 | checklist 全部经测试/真实执行验证满足（E2E 计划内豁免；勾选框状态说明见 §4 备注） |
| 4. 实现规格符合度 | 通过 | 路由校验链/零交易日不建任务、handler camelCase/skippedCount 口径/一次 preflight/partial finalize、collector 日历守卫/失败不阻断/当日在市过滤、scheduler 注释模板均符合 §3 |
| 5. 验证命令执行 | 通过 | §6 命令 1：24 passed；命令 3 全量回归：851 passed，失败/错误全部落在既有环境白名单（§3） |
| 6. E2E 质量证据 | 豁免（通过） | 后端任务功能无浏览器界面（plan-05 §5 声明 + README 7.2 waived）；质量门为执行验证（维度 10） |
| 7. 边界与风险 | 通过 | 6 个边界场景全部 done |
| 8. 契约对齐 | 通过 | result camelCase 三方契约、RESERVED 封堵、专用入口路径、下游 plan-06/08 契约一致 |
| 9. 代码审查 | 通过 | 0 blocker，4 条建议项（非阻塞） |
| 10. 执行验证 | **通过（实证）** | 证据核验 + review 独立查库复核 + 一次**真实执行器全链路**运行（含失败日语义） |

---

## 2. 维度 10：执行验证（task handler 强制项）

### 2.1 证据文件核验（docs/e2e/evidence/plan-05-exec-verify-2026-08-14.md）

| 验收项 | 证据 | 核验结果 |
| --- | --- | --- |
| 任务创建成功 | 第一次 `task_8ca017a22200`（harness patch 绕过 plan-03 savepoint 缺陷）、第二次 `task_ef59d8ccb2f4`（全真实路径） | ✅ 两条任务行均在库且 `status=completed` |
| 执行成功 | progress 2/2，successCount=2 / failedCount=0 / unprocessedDates=[] | ✅ 与库中 result 一致 |
| 目标表数据正确写入 | `market_daily_metrics` 2026-08-11/12 各恰一行，volume ~1.135–1.179e11 股、amount ~2.17–2.34e12 元、avg ~28.0–28.4 元，final==expected | ✅ review 独立查库逐值一致（见 §2.2） |
| 幂等覆盖 | 同范围重跑后仍 2 行、逐值完全相同 | ✅ 查库确认无重复行 |
| result 契约 | 键 successCount/skippedCount/failedCount/dateResults/unprocessedDates，dateResults 逐项 `{tradeDate,status,expected,daily,suspended,final,reason?}` camelCase | ✅ 库中 JSONB 键名逐项核对 |

第一次执行发现的 plan-03 `init_stocks_lifecycle` 逐股 savepoint 无限 RecursionError 属 plan-03 范围，已由后补丁 #2 修复（§7），第二次执行**无任何 patch** 走完整真实路径（9.9s / 5882 records preflight + 2 交易日）通过——证据链闭环。

### 2.2 review 独立查库复核（2026-08-14，验收时）

```
market_daily_metrics（共 2 行）:
2026-08-11 | vol=117,890,595,949 | amt=2,335,593,609,501.76 | avg=27.9995 | 5542/5539/3/5542
2026-08-12 | vol=113,523,313,015 | amt=2,167,164,143,116.15 | avg=28.4478 | 5543/5539/4/5543

sync_market_metrics 任务行（共 6 条）:
task_8ca017a22200 completed 2/2  result 完整 camelCase（dateResults 2 项 success）
task_ef59d8ccb2f4 completed 2/2  result 与第一次逐值相同（幂等覆盖）
其余 4 条为验证 harness 调试产物（error 标注 exec_verify/orphaned，均已终态，不占互斥）
```

### 2.3 额外真实执行器全链路验证（review 期间意外获得，强于证据文件）

review 直调专用入口 `POST /v1/admin/init/market-metrics`（start=end=2026-08-14，真实 admin）：

- 返回 `success=True, task_id=task_0e662676b80a`（当日为交易日，真实日历刷新通过、拆分 1 个交易日）——**专用入口 HTTP 全链路可用**；
- 本机真实 TaskExecutor 自动拾取该 pending 任务（非 harness 直跑 handler）：执行器派发 → fence 注入 → preflight → `sync_date(2026-08-14)` → 因当日行情尚未发布失败（`全市场行情为空 trade_date=2026-08-14`，验收时为北京时间清晨）；
- 终态语义完全符合规格：任务落 `failed`、`progress 1/1`、result=`{successCount:0, failedCount:1, dateResults:[{tradeDate:"2026-08-14",status:"failed",expected:5543,daily:0,suspended:0,final:0,reason:"..."}], unprocessedDates:[]}`、error=`market_metrics 范围同步存在失败日: success=0 failed=1`；
- **失败日不留半成品**：market_daily_metrics 仍 2 行（无 2026-08-14 行）；
- **互斥释放**：failed 后 pending/running sync_market_metrics 计数=0。

该运行以真实执行器路径（证据文件两次为手构 fence context 直跑 handler）额外覆盖了 AC-07 失败日语义与执行器接线，验证后任务保持 failed 终态（互斥已释放，无需清理）。

---

## 3. 验证命令实际重跑结果

```bash
cd server && source .venv/bin/activate
```

### 3.1 §6 命令 1：路由与 collector 单测

```
pytest tests/api/admin/test_init_market_metrics.py tests/test_data_updater.py -v --no-cov
→ 24 passed（路由 8：合法创建/四类拒绝不建任务/日历刷新失败/互斥/403；collector 16，其中 plan-05 相关 6）
```

### 3.2 §6 命令 2：执行验证

已由 implement 阶段产出证据（§2.1），review 复核查库通过（§2.2），并补充真实执行器运行（§2.3）。

### 3.3 §6 命令 3：全量回归

```
pytest tests/ -q --no-cov
→ 10 failed, 851 passed, 296 errors
```

失败/错误**全部**落在既有环境白名单同类问题，与 plan-05 无关：

| 类别 | 文件 | 根因 |
| --- | --- | --- |
| FAILED 6 | `tests/test_scheduler_service.py` | APScheduler 环境问题（白名单） |
| FAILED 4 | `tests/test_fund_sync_filter.py` | 既有 fund 同步断言（白名单） |
| ERROR 29+17 | `tests/test_shareholder_group_admin_api.py` / `tests/test_strength_api.py` | auth fixture `AttributeError: 'ProcessTimeMiddleware' object has no attribute 'dependency_overrides'`（白名单） |
| ERROR 267 | test_fund_api / test_shareholder_analysis_api / test_broker_recommend_* / test_fund_crowd / test_fund_admin / test_api/* / tests/api/test_analysis 等 | **同一根因**：`main.app` 双层中间件（ResponseLoggingMiddleware → ProcessTimeMiddleware → FastAPI）下旧测试单层解包失效（实测 `tests/test_fund_api.py:64` 报同款 AttributeError） |

`git status` 确认以上文件均未被 16 期修改；plan-03/04 review 全量回归呈同一失败集（296 errors 同量级），非本期引入、非本期扩大。

### 3.4 相关回归补充

```
pytest tests/test_data_init.py tests/services/ -q --no-cov → 240 passed
```

（含 plan-03 后补丁 #2 的 data_init 22 用例与 plan-04 任务系统 55 用例，无回归。）

---

## 4. 维度 3：验收标准逐项核验

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| AC-10 四类请求 success=False 且 AsyncTask 无新行；合法请求返回 task_id | 通过 | 8 条路由单测（mock `create_exclusive_task` 断言 `assert_not_awaited`）+ §2.3 真实 HTTP 合法创建 |
| AC-11 非管理员 403；互斥拒绝 | 通过 | `test_non_admin_returns_403` / `test_mutex_rejected_when_existing_running`；§2.2/2.3 真实任务互斥释放复核 |
| AC-02 执行验证（触发→等待→查库） | 通过 | §2.1–2.3（两次真实执行 + review 查库 + 真实执行器运行） |
| AC-02 范围含失败日：failed、failedCount≥1、成功日保留、重跑覆盖 | 通过（部分实证） | 失败日：§2.3 真实运行；重跑覆盖：第二次执行逐值相同；"成功日保留在库"由日级 commit 代码路径 + 全失败/全成功两组真实运行佐证（混合范围建议后续复验，见 §8 建议 3） |
| skippedCount = 自然日−交易日；progress/total 只计交易日；逐日四类计数 | 通过 | handler L1926-1928 + 证据（skipped=0，progress 2/2）+ 库中 dateResults 四类计数齐全 |
| preflight 每任务仅一次 | 通过（间接） | collector 侧 `test_update_stocks` 断言 `build_lifecycle_snapshot` awaited_once；handler 侧 preflight 位于循环外（L1942）+ 两次真实执行日志；handler 级 mock 断言缺直接单测（建议 2） |
| AC-08 collector 三项（休市不调 Provider / 日历失败日更失败 / 指标失败不阻断） | 通过 | `test_run_daily_update_non_trading_day` / `..._calendar_refresh_failure_fails` / `..._metrics_failure_does_not_block`（断言 index 步骤仍执行） |
| AC-09（collector 侧）休市记录存在时 `_update_market_metrics` 不执行 | 通过 | 同上 `mock_metrics.assert_not_awaited()` |
| AC-07 恢复路径：失败日重跑后变 success 且值更新 | 通过（推断+部分实证） | 幂等 upsert 由 plan-03 同日覆盖验收承接；本次真实失败日为当日（行情未发布），需收盘后重跑方可实证 status 翻转（建议 3） |
| pytest 全量回归通过 | 通过 | 白名单外全部通过（§3.3） |
| E2E 不适用 | 豁免 | plan-05 §5 明示 + README 7.2 waived；质量门为执行验证 |
| 性能验收：逐日串行、日级提交、progress 逐日更新 | 通过 | handler 逐日 `update_progress`；真实执行 progress 0→2（2 日范围）与 1/1（单日范围）落库 |

> 备注：plan-05 §5 checklist 勾选框保持 `- [ ]` 未打勾，与本计划 plan-01~04 验收时的文件状态一致（该目录既定惯例：以 review 实质核验为准，勾选框不作为状态裁决来源）。非阻塞。

---

## 5. 维度 4/8：实现规格与契约对齐（代码审查摘要）

| 规格要求 | 状态 | 核对位置 |
| --- | --- | --- |
| 路由范式照抄 init_index_basic（prefix=/init、require_admin、ApiResponse） | 通过 | `src/api/admin/init_market_metrics.py:42,61-65` |
| 校验链顺序：倒置→未来→跨度(3650)→日历刷新(失败不降级)→零交易日不建任务 | 通过 | L79-123；`refresh_range` 异常 rollback + 明确提示 |
| 延迟导入 TaskManager → `create_exclusive_task`；互斥 None → HTTP 200 success=False | 通过 | L126-144 |
| 不透传 max_retries | 通过 | `create_exclusive_task` 固定 max_retries=0（plan-04） |
| TaskType 枚举 + 注册 + 三参签名 | 通过 | `task_handlers.py:92,1876-1881` |
| fence 取不到即抛错（自动路径不走 handler） | 通过 | L1913-1918 |
| 本地日历交易日升序；skippedCount=自然日−交易日 | 通过 | L1921-1927（`get_trading_days` 升序，plan-01 契约） |
| 一次生命周期 preflight → LifecycleSnapshot 复用 | 通过 | L1942（`build_lifecycle_snapshot` 内部调 `init_stocks_lifecycle`，market_metrics_service.py:271） |
| 逐日串行 + close_cache 跨日复用 + 每日 update_progress + 当前日期日志 | 通过 | L1950-2059 |
| result camelCase 构造、`to_dict()` 原样透传 | 通过 | `_build_market_metrics_result` L1758-1776；库中 JSONB 键名实证 |
| failedCount>0 抛一次摘要（成功日不回滚） | 通过 | L2073-2080 + §2.3 真实运行实证 |
| cancel/timeout → partial result + unprocessedDates | 通过 | L1962-1995 + `_finalize_market_metrics_stop`（首因选择与 recovery 一致；recovery 兜底由 plan-04 测试覆盖）；调试期任务 `task_ed50cf073f2f` 库中 result 实证 unprocessedDates 路径 |
| collector 步骤 1 替换为 refresh_range(today,today) → open_count=0 skipped | 通过 | `collector.py:113-130` |
| `_update_stocks` 升级 init_stocks_lifecycle + run 级 snapshot | 通过 | L237-251 |
| `_update_market_data` 仅遍历当日在市集合 | 通过 | L309-320（`expected_codes(today)` → symbol 前缀映射过滤） |
| `_update_market_metrics`：task_context=None、不读写 AsyncTask、失败不阻断 | 通过 | L380-418 + run_daily_update L144-149（try/except → errors + =0） |
| results 增加 market_metrics_updated 键 | 通过 | L100 |
| scheduler 注释模板：CronTrigger(hour=18, minute=0, tz=Asia/Shanghai) + max_instances=1 + replace_existing=True + 缓冲时段注释 + 不依赖 cron 工作日表达式 | 通过 | `job_manager.py:115-132,201-222`（与 L102-113 既有停用惯例同构） |
| 通用 POST /admin/tasks 封堵 sync_market_metrics | 通过 | `admin/tasks.py` RESERVED 检查置于注册校验之前（plan-04 清单文件，本次直调实证：返回"保留任务类型，请使用 POST /api/v1/admin/init/market-metrics"）+ `test_admin_create_rejects_reserved_task_type` |
| 下游契约：plan-06 读 market_daily_metrics/trading_calendar_days、plan-08 直消费 result | 通过 | result 键 camelCase 无二次转换；未提前实现查询 API/前端（不在范围未越界） |

## 6. 额外文件核查（清单外）

| 文件 | 判定 | 说明 |
| --- | --- | --- |
| `server/tests/api/admin/conftest.py`（新增） | **可接受（测试基建），已记录** | plan-05 风险与边界未列，但为必需：`tests/api/conftest.py` 的单层解包（`app.app`）在 `main.app` 双层中间件下取到 ProcessTimeMiddleware，无 `dependency_overrides` → 新目录全部路由测试 setup 报错（同根因的全量回归既有失败见 §3.3，白名单勿修）。本 conftest 以最近 conftest 同名 fixture 覆盖父级、沿 `.app` 链解包，仅作用于新目录，不触碰既有白名单失败。根因修复（统一解包 helper / 收敛中间件包装）应作为独立测试基建任务处理，不属 plan-05 |
| `server/src/api/admin/tasks.py`（RESERVED 封堵） | 无需记录 | 属 plan-04 文件清单（已由 plan-04 review 验收），本次仅直调复核 |
| `server/src/services/data_init.py` / `server/tests/test_data_init.py` | 无需记录 | plan-03 后补丁 #2，记录完整（§7） |

## 7. plan-03 跨功能修复记录核查（后补丁 #2）

`reviews/plan-03-review-20260814.md` §13 记录完整：根因（SQLAlchemy 2.0.23 savepoint 编译无限递归，5882 股规模触发，setrecursionlimit(8000) 仍崩溃）、修复方式（批量 upsert 500/批、预加载分区、冲突键 `symbol` 即 Stock 唯一键、整批失败回退逐行）、真实规模验证（4.53s/5882 records、handler 端到端 9.9s、22 passed）、修改文件清单与证据交叉引用。**记录完整性通过**；plan-03 保持 done、状态机不变，处理符合流程。

## 8. 代码审查

### 阻塞项

无。

### 改进建议（非阻塞）

| # | 文件 | 位置 | 类别 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | `src/api/admin/init_market_metrics.py` | L77 | 边界一致性 | `today = date.today()` 用服务器本地时区，collector/handler 侧用北京时间（BJ_TZ）。跨时区部署时凌晨窗口（北京 0-8 点）可能拒绝北京"今天"为 end_date——保守方向（该时段当日行情必然未发布），可统一为 BJ_TZ 提升口径一致性 |
| 2 | `tests/`（handler 级） | — | 测试补充 | handler 循环语义（skippedCount>0 的混合范围、preflight 调用次数=1 的 mock 断言、停止分支 partial result）无直接单测，当前由 collector 侧断言 + 真实执行 + recovery 测试组合覆盖；建议后续补 handler 级单测降低回归成本 |
| 3 | 执行验证 | — | 建议复验 | 混合范围（含成功日+失败日）"成功日保留在库"与"失败日收盘后重跑 status 翻转"两组场景未真实执行（本次真实失败日为行情未发布的当日）；待 2026-08-14 收盘后对 task_0e662676b80a 同范围重跑可一并实证 AC-07 恢复路径 |
| 4 | `src/services/data_updater/collector.py` | L395-404 | 可维护性 | `_update_market_metrics` 直接用 `db_module.AsyncSessionLocal` 而非文件内兼容 `get_session()`（后者为 tests patch 点）；两条 session 入口并存略隐晦，可加注释或统一 |

## 9. 风险与边界核查

| 场景 | 状态 | 说明 |
| --- | --- | --- |
| 范围全为非交易日 | done | 路由零交易日拒绝（单测 + 路由代码 L118-123） |
| 范围含非交易日 | done | skippedCount 计数不进计算（handler L1926-1927） |
| 单日失败 | done | 回滚该日、记录 dateResults、继续（§2.3 真实实证） |
| 任务运行中被取消 | done | partial result + unprocessedDates 后 finalize cancelled（代码 + plan-04 recovery 测试 + 调试期任务库中实证） |
| collector 指标失败 | done | errors 记录、=0、不阻断（单测断言 index 步骤仍执行） |
| 日历刷新失败（日更） | done | 日更失败不用旧批冒充（单测） |

暂停条件（Tushare 积分不足）未触发；18:00 job 注释预留符合仓库"开发期停用"惯例并在风险备注声明。

## 10. 总结

**验收结论: 通过（0 blocker，4 条建议项）**

- 功能状态已从 `review` 更新为 `done`（plan-05 frontmatter）
- 验收报告写入 `reviews/plan-05-review-20260814.md`
- README §7.2 开发状态机表按验收指令**未修改**（状态唯一可信源为功能文件 frontmatter，表由主流程维护）
- README frontmatter 保持 `in_review`（plan-06/07/08 未完成，不满足 accepted 条件）
- 额外文件 `tests/api/admin/conftest.py` 判定为可接受测试基建并已在本报告 §6 记录
- plan-03 后补丁 #2 记录核验完整（§7）
