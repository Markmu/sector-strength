# 功能验收报告

## 基本信息

- **功能**: plan-01: 数据模型与本地交易日历
- **维度**: backend
- **阶段**: Phase 1
- **依赖**: 无
- **验收时间**: 2026-08-14
- **验收依据**: plan-01 §5 验收标准 checklist + §8 边界场景 + §6 验证命令实际重跑 + 代码审查（对照实现规格）
- **验收结论**: 通过

## 一、文件交付完整性

| 动作 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| create | `server/src/models/market_daily_metric.py` | 通过 | 文件存在，列/约束/索引/comment/__repr__ 与实现规格 #1 一致 |
| create | `server/src/models/trading_calendar_day.py` | 通过 | 文件存在，与实现规格 #2 一致（额外含 `created_at`，见改进建议 #2） |
| modify | `server/src/models/__init__.py` | 通过 | 已 import 并导出 `MarketDailyMetric` / `TradingCalendarDay` |
| create | `server/alembic/versions/2026_08_14_0001-c4b9e2a7f813_add_market_metrics_and_calendar.py` | 通过 | revision=c4b9e2a7f813、down_revision=7e3309ce89da，双表+索引，upgrade/downgrade 对称 |
| modify | `server/src/services/data_acquisition/models.py` | 通过 | 新增 `TradingCalendarEntry { cal_date, is_open }` |
| modify | `server/src/services/data_acquisition/base.py` | 通过 | 新增抽象方法 `get_trading_calendar_range`，旧 `get_trading_calendar()` 原样保留 |
| modify | `server/src/services/data_acquisition/tushare_client.py` | 通过 | 实现 `get_trading_calendar_range`（无 is_open 过滤、`_execute_with_retry` 包裹），旧方法 L155-175 未改 |
| create | `server/src/services/trading_calendar_repository.py` | 通过 | refresh_range 校验+单批 upsert+只读查询齐全 |
| create | `server/tests/services/test_trading_calendar_repository.py` | 通过 | 14 个用例覆盖合法刷新/校验拒绝/upsert 覆盖/只读查询 |

**额外文件（计划允许列表内）**:

| 路径 | 是否声明 | 说明 |
| --- | --- | --- |
| `server/tests/test_data_source_factory.py`（修改 +1 行） | 是 | plan-01 §风险与边界「允许修改的额外文件」；为 `CompleteSource` 测试替身补 `get_trading_calendar_range` 最小实现（返回空列表），不削弱真实现 |

**结论**: 文件清单全部交付，额外修改在允许列表内。通过。

## 二、Task 列表完成度

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 创建两模型并注册 `models/__init__.py` | done | comment/唯一键/索引按规格 1-2 |
| 2 | 编写 Alembic 迁移（down_revision=7e3309ce89da） | done | 双表+索引，upgrade/downgrade 对称 |
| 3 | 新增 TradingCalendarEntry 与 BaseDataSource 抽象方法 | done | 已修复测试替身破坏 |
| 4 | 实现 get_trading_calendar_range | done | trade_cal 无 is_open 过滤，retry 包裹 |
| 5 | 实现 refresh_range 校验与单批 upsert | done | 闭区间一一对应/无重复/无越界 |
| 6 | 实现 Repository 只读查询方法 | done | 4 个方法齐全 |
| 7 | 编写 test_trading_calendar_repository.py | done | 覆盖合法/拒绝/覆盖/查询/性能 |

**结论**: 7/7 done，无 todo/waived 遗留。通过。

## 三、验收标准完成度

### 后端验收

| 验收项 | 状态 | 说明 |
| --- | --- | --- |
| AC-09：refresh_range 后本地表对闭区间每个自然日恰有一行，get_record(休市日).is_open=False | 通过 | `test_refresh_range_valid_writes_all_natural_days` 验证行数==自然日数、休市日记 False |
| 部分/重复/越界三场景均抛错且不提交任何行，旧批次保留（架构 §8.2-5） | 通过 | 3 个拒绝用例 + `test_refresh_range_failure_preserves_old_batch` 验证旧 batch_id 不变 |
| 同日重复 refresh_range 为 upsert 覆盖，不产生重复行，batch_id/refreshed_at 更新 | 通过 | `test_refresh_range_upsert_overwrites_same_date` 验证行数仍为 3、batch_id 变更 |
| Provider 抛错时 refresh_range 透传失败，不吞异常 | 通过 | `test_refresh_range_propagates_provider_error` 验证 DataFetchError 透传、不提交 |
| 旧 get_trading_calendar() 行为不变（现有测试回归通过） | 通过 | 回归套件 `test_trading_calendar.py` 9 用例 + factory 旧方法 2 用例全通过 |
| alembic upgrade head 成功且 downgrade -1 可回退 | 通过 | 实测 upgrade/downgrade/再 upgrade 三段均退出码 0，表正确 drop/recreate |
| E2E 不适用：纯数据层功能（plan-01 §5 声明） | 通过（豁免） | 详见维度六 |

### 性能验收（架构 §8.1）

| 验收项 | 状态 | 说明 |
| --- | --- | --- |
| get_recent_open_days(250) 走 cal_date/is_open 索引，EXPLAIN 无 seq scan，行数 ≤250 | 通过（附注） | 行数断言满足（≤250）。EXPLAIN 在生产级数据量（10 年/3653 行 ANALYZE 后）走 `Index Scan Backward`，无 seq scan；仅在小测试表（365 行）时规划器正确选择 seq scan+sort，属合理优化器行为。见改进建议 #1 |

**结论**: checklist 全部完成或合理豁免。通过。

## 四、实现规格符合度

| 规格要求 | 状态 | 说明 |
| --- | --- | --- |
| #1 MarketDailyMetric 列/约束/索引/comment | 通过 | Numeric(24,2)/Numeric(16,4)/Integer/DateTime(timezone) 全部匹配，uq+idx 齐全 |
| #2 TradingCalendarDay 列/约束/索引 | 通过 | cal_date 唯一、is_open nullable=False、refresh_batch_id String(36)、复合索引匹配（额外 created_at 见建议 #2） |
| #3 Alembic 迁移范式（仿 f92bfffc49c3） | 通过 | 逐表 create_table+create_index，列带 comment，downgrade 逆序 drop，无 drift |
| #4 TradingCalendarEntry 契约 | 通过 | `{ cal_date: date; is_open: bool }` |
| #5 BaseDataSource 抽象扩展（旧方法不动） | 通过 | 抽象方法签名匹配；旧 get_trading_calendar() 未改；仅 1 个真实现 TushareDataSource，替身补最小实现符合 §5 注释 |
| #6 tushare get_trading_calendar_range（无 is_open 过滤 + retry） | 通过 | `trade_cal(exchange='SSE', start_date, end_date, fields='cal_date,is_open')` 明确不传 is_open，包在 `_execute_with_retry` 内 |
| #7 TradingCalendarRepository.refresh_range（校验+单事务 upsert+返回值+日志） | 通过 | 内存集合严格校验、pg_insert.on_conflict_do_update、返回 (open, closed)、结构化日志齐全 |
| #7 只读查询 4 方法 | 通过 | get_record/get_trading_days/get_recent_open_days/has_any_open_day 签名与排序语义匹配 |
| #7 写侧禁止旧批次降级 | 通过 | 校验失败抛 ValueError 不提交，Provider 失败透传，旧行仅供读侧 |

**结论**: 实现规格全部满足，无超出范围实现。通过。

## 五、验证命令执行

| 命令 | 退出码 | 状态 | 输出摘要 |
| --- | --- | --- | --- |
| `alembic upgrade head` | 0 | 通过 | DB 已在 head c4b9e2a7f813，no-op 升级 |
| `alembic downgrade -1`（追加可逆性验证） | 0 | 通过 | c4b9e2a7f813→7e3309ce89da，两张表 drop |
| `alembic upgrade head`（再升级） | 0 | 通过 | 表 recreate，索引/约束/ comment 复核一致 |
| `python -c "from src.models import MarketDailyMetric, TradingCalendarDay"` | 0 | 通过 | OK |
| `pytest tests/services/test_trading_calendar_repository.py -v --no-cov` | 0 | 通过 | 14 passed |
| `pytest tests/ -k "trading or calendar" -v --no-cov` | 0 | 通过 | 34 passed（含旧方法回归 + factory 替身） |
| `pytest tests/test_data_source_factory.py -v --no-cov`（替身修复复核） | 0 | 通过 | 32 passed |
| 真实采集冒烟（`DataSourceFactory.create().get_trading_calendar_range(...)`） | — | 跳过 | 当前环境无 `TUSHARE_TOKEN`；该命令 plan §6 标注「需 TUSHARE_TOKEN」。代码审查确认实现与规格一致（见维度四 #6），implementer 自报冒烟通过 |

<details>
<summary>性能 EXPLAIN 详情（架构 §8.1）</summary>

```
-- 小测试表（365 行，ANALYZE 后）→ 规划器选择 seq scan（合理）
Limit -> Sort -> Seq Scan on trading_calendar_days  (Filter: is_open)

-- 生产级数据量（10 年 / 3653 行，ANALYZE 后）→ Index Scan Backward，无 seq scan
Limit -> Index Scan Backward using uq_trading_calendar_days_cal_date  (Filter: is_open)

-- SET enable_seqscan=off 强制走索引 → 同样 Index Scan Backward
```

结论：索引 `idx_trading_calendar_days_cal_date_is_open` 已正确建立；生产数据量下查询走索引、行数 ≤250，满足架构 §8.1 意图。

</details>

## 六、E2E 质量证据

| 项 | 值 |
| --- | --- |
| 用例文档 / Spec | 不适用 |
| Red / Green 证据 | 不适用 |
| 豁免判定 | **豁免成立** |

**豁免理由核对（Done Gate）**：

- plan-01 §5 显式声明「E2E 不适用：纯数据层功能，无用户可见界面；其用户可见效果由 plan-07 首页缺口轴与 plan-05 非交易日跳过间接验证」。
- README §7.2 开发状态机注记 plan-01~06 为纯后端功能，E2E 步骤按 `waived` 处理，质量门为各 §6 验证命令 + task-review。
- 符合 task-review 维度六规则「只有纯内部、无用户可观察路径的功能可以豁免 E2E」——本功能为模型/迁移/Repository/采集方法，无任何用户可观察路径。
- 间接验证链已规划：plan-05（collector 非交易日守卫执行验证）+ plan-07（首页缺口轴 E2E red/green）。

**结论**: 豁免理由充分，不因缺 Playwright 证据判 blocker。

## 七、风险与边界

| 场景 | 状态 | 说明 |
| --- | --- | --- |
| Provider 响应行数 < 自然日数 | done | `test_refresh_range_rejects_missing_days_no_commit` 验证抛 ValueError、不提交 |
| Provider 响应含重复 cal_date | done | `test_refresh_range_rejects_duplicates_no_commit` 验证抛 ValueError、不提交 |
| Provider 响应含区间外日期 | done | `test_refresh_range_rejects_out_of_range_no_commit` 验证抛 ValueError、不提交 |
| 闭区间仅 1 天（start==end） | done | `test_refresh_range_single_day_start_equals_end` 验证正常处理 |
| 二次刷新覆盖旧批次 | done | `test_refresh_range_upsert_overwrites_same_date` 验证 upsert 覆盖、batch_id 全量更新 |

**结论**: 5/5 边界场景全 done。通过。

## 八、契约对齐

| 契约项 | 方向 | 状态 | 说明 |
| --- | --- | --- | --- |
| `TradingCalendarEntry { cal_date, is_open }` | 上游消费（Repository） | 通过 | Pydantic 模型字段与规格一致 |
| `refresh_range(start, end) -> (open_count, closed_count)` | 下游提供（plan-03/05） | 通过 | 签名、返回值、失败语义（抛错不提交）匹配 |
| `get_record / get_trading_days / get_recent_open_days / has_any_open_day` | 下游提供（plan-03/05/06） | 通过 | 4 个只读方法签名与排序/过滤语义匹配 |
| `get_trading_calendar_range(start, end)` | 上游消费（Repository） | 通过 | BaseDataSource 抽象 + Tushare 实现签名一致 |
| 旧 `get_trading_calendar() -> List[date]` | 既有契约 | 通过 | 未改动，回归测试通过 |
| 迁移 revision 链 `7e3309ce89da -> c4b9e2a7f813` | 下游（plan-02+ 迁移链） | 通过 | alembic head = c4b9e2a7f813，down_revision 正确 |

**结论**: 契约无偏差。通过。

## 九、代码审查

### 阻塞项

无。

### 维度十：执行验证（task handler）

**不适用**。plan-01 为纯数据层（模型/迁移/Repository/采集方法），不含异步任务处理器；同步任务 handler 属 plan-05。无需触发任务验证数据写入。

### 改进建议（非阻塞）

| # | 文件 | 行号 | 类别 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | `tests/services/test_trading_calendar_repository.py` | test_get_recent_open_days_capped_at_n | 测试充分性 | 当前仅断言行数 ≤250；架构 §8.1 字面要求「EXPLAIN 无 seq scan」。生产数据量下已验证走 Index Scan，但小测试表下规划器选 seq scan 属正常。建议后续在更大数据量 fixture 或 `SET enable_seqscan=off` 下补一条 EXPLAIN 断言以闭环字面要求（非阻塞，可放 plan-03/05 引入真实数据后处理） |
| 2 | `src/models/trading_calendar_day.py` | 58-60 | 规格一致性 | 实现规格 #2 未列出 `created_at`，实现额外加了一列（模型与迁移一致，符合全库惯例）。属良性约定补全，不影响下游契约；如需严格对齐规格可在后续计划文档同步补列说明 |
| 3 | `src/services/trading_calendar_repository.py` | 101 | 可维护性 | `refresh_range` 内部 `await self._session.commit()` 自管事务。当前 scope 下 refresh_range 是唯一写、自洽合理；后续 plan-05 若需把 refresh_range 纳入更大事务，需注意此处提交点（届时可评估改为由调用方控制提交） |

## 十、总结

**验收结论**: 通过

- 功能 frontmatter `status` 已从 `review` 更新为 `done`
- 验收报告已写入 `reviews/plan-01-review-20260814.md`
- 改进建议（3 项，均非阻塞）可在后续迭代处理
- README §7.2 开发状态机表按用户要求**未修改**（状态以 plan-01 frontmatter 为准）

### 状态变更记录

| 项目 | 变更前 | 变更后 |
| --- | --- | --- |
| plan-01 frontmatter status | review | done |
| plan-01 §5 验收 checklist | 全部未勾选 | 全部通过（性能项附注） |
