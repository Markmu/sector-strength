# 架构文档检查报告：A股全市场量价指标（第十七轮）

> 检查对象：`16-1-架构文档-A股全市场量价指标.md`  
> 需求基准：`16-0-需求设计-A股全市场量价指标.md`  
> 检查日期：2026-08-13  
> 检查方式：按 brownfield 全量重读，复核结构、AC-01～13、真实代码复用、数据/NFR、停止终态及 acquisition-token recovery/fencing

## 一、阻塞检查

| 项目 | 状态 | 结论 |
|------|------|------|
| PRD / 架构配对 | ✅ | frontmatter `input_documents` 唯一指向本次完整读取的 PRD，路径有效 |
| Frontmatter contract | ✅ | `workflow_type/status/input_documents/open_questions` 齐全，`status: review_ready` 合法 |
| `open_questions` 同步 | ✅ | frontmatter 为 `[]`，正文 §5.x 为“无”，两者一致 |
| 实现影响的未决问题 | ✅ | 无会改变模块、API、Schema、状态机、数据源或验收口径的未决项 |

## 二、总览

- **结构完整度：100%**：10 章、必需子节、6 模块、5 API、6 ADR、P1、ASCII 上下文、状态机及 Phase A～C 全部合格。
- **需求满足度：100%**：PRD 范围、US-01～08、业务规则、异常/降级、AC-01～13 和权限/UX 均有充分且无语义漂移的架构承接。
- **本轮问题：0**：无 ⚠️、❌、⛔，无存疑设计。
- **整体评价：✅ 可进入下一阶段**。

## 三、历史问题关闭记录

| 范围 | 状态与证据 |
|------|------------|
| 第一～第十五轮全部问题 | ✅ 全部保持关闭；L/D/P/G、daily 分页、停牌补价、Decimal/单位、日期原子性、日历、reserved type、任务停止、专属 ownership/recovery/fencing 及同实例重连 acquisition token 均未回归 |
| R16-H-01 | ✅ 架构 §6.2 第 3/6 步在当前 acquisition 持有专属 lock 的 Task 行锁事务内，从已提交 `dateResults` 重建 counts/`unprocessedDates`，并按 cancel、timeout、无停止字段分别原子写 partial result、recovery 日志和 `cancelled`、`failed(task_timeout)`、`failed(executor_restarted)`；双字段异常按较早数据库时间、同刻 cancel 优先确定性收敛并 critical 告警 |
| R16 术语项 | ✅ 架构 §6.1 第 9 步已统一为 `executor_acquisition_token=context.token`，不存在遗留 owner 字段实现歧义 |

## 四、结构完整性

- **Frontmatter / §1～§5**：字段、系统摘要、4 词闭环、P0/P1/非目标、定性成功标准、AC 矩阵、主流程/分支/状态机、ASCII 上下文、6 模块和 6 ADR 全部满足 contract。
- **§6～§10**：四条运行链路均有编号步骤和实现原则；核心对象、最小 Schema、5 个 API、状态/数据/命名契约齐全；性能、降级、安全、成本、可观测性、风险、Phase 文件与验证目标完整。
- **质量约束**：无 DDL/目录树/环境变量实现泄漏；模块/API/ADR/Schema 嵌套均在上限内；P1 明确为“暂无”；正文与 frontmatter 一致。

## 五、需求与 AC 全量追踪

| 需求组 | 状态 | 架构证据 |
|--------|------|----------|
| AC-01/03/07/13：单日、覆盖、失败恢复、停牌 | ✅ | §6.1 以 L/D/P/G 历史在市集合为基准，参数化 daily 分页、`suspend_d` 整日证据、有界未复权 close、四计数平衡、Decimal 汇总及单事务 upsert；重复/缺失整日不写 |
| AC-02/10/11：范围、校验、权限互斥 | ✅ | §6.2 的本批日历验证、reserved 专用 POST、管理员鉴权、事务 advisory create lock、单类型 running 互斥、进度/result/历史记录完整 |
| AC-08/09：自动日更与休市 | ✅ | §6.3 先刷新并严格校验当日本地日历，股票行情成功后调用同一 `sync_date`；休市明确 skip，Provider 故障不以旧批伪装写侧验证 |
| AC-04～06/12：首页 | ✅ | §6.4/§7.3/Phase C 覆盖两首页指定位置、三项最新值、成交额/量柱图、平均价折线、30/90/250、tooltip、缺口 null/断线、空态和 SWR 局部重试 |
| US-01～08 与业务规则 | ✅ | §3 主旅程、§6 四条链路和 §8 降级共同覆盖查看、切换、同步、进度、最新日期、幂等、缺口及恢复 |
| 明确不做 | ✅ | §2.3/§4.3 未引入盘中、分市场、加权价格、行业聚合、告警、原始快照、额外队列/缓存或多端点拆分 |

AC-01～13 的原始结果语义、参与数、单位、首页位置、日期数量、失败保留和权限要求均未被弱化或替换。

## 六、深度质量与 Brownfield 核验

| 核验点 | 状态 | 证据结论 |
|--------|------|----------|
| A 股生命周期范围 | ✅ | `stock_basic` 官方契约覆盖全市场 A 股；架构按 L/D/P/G 分页、L/D/P 日期校验、G 固定排除、联合 set-diff，daily 只接纳预期集合，B 股/基金/债券/指数不会混入 |
| daily 分页 | ✅ | 单日/历史模式分别约束日期、代码、候选上限、页签名、新 key、短页/合法空尾页和硬页数；不会静默截断、重复修复或死循环 |
| 全天停牌 | ✅ | daily 优先，只有明确整日 `suspend_d` 才补值；按 100 代码×60 日窗口有界回溯未复权 close，最远 list_date，禁止 qfq 与逐股 N+1 |
| Decimal/单位 | ✅ | 原始字符串或 `Decimal(str(value))`、finite/正负校验，`vol×100` 手→股、`amount×1000` 千元→元，禁止 binary float 累加 |
| 日历与读侧 | ✅ | `get_trading_calendar_range` 无 `is_open` 过滤，闭区间自然日一一覆盖、无重复越界、同批提交；首页 GET 只查本地 PostgreSQL |
| 任务与终态 | ✅ | `max_retries=0`、逐日成功保留、统一 `dateResults`、真实日志 count、停止首因、原子 partial result+终态、取消/超时/重启 recovery 全部自洽 |
| acquisition/fencing | ✅ | 每次 session lock acquisition 新 UUID token；claim/普通写/finalize/recovery 均校验 token；guard 双检、Task 行锁与旧/新事务二选一禁止 recovery 后旧提交；同实例重连可回收旧 token |
| 其他任务与自动日更 | ✅ | 所有专属扩展显式限制 `sync_market_metrics`；既有约 28 类任务沿旧路径，自动日更传 `TaskFenceContext=None` 且不读写 AsyncTask |
| NFR | ✅ | 250 点 P95≤500ms、读侧 0 Provider、范围串行、有界外部调用、鉴权/参数化查询/凭据保护、日志告警和失败降级均可验证 |

## 七、ADR 与过度设计

- ADR-1～5 直接服务完整行情、历史集合、停牌、原子保存和范围回填；ADR-6 服务首页性能与缺口展示，均可追溯到 PRD P0/AC。
- acquisition token、OwnerGenerationGuard、本地交易日表属于保证任务互斥/恢复与首页稳定性的合理可靠性扩展；没有引入新中间件或不必要平台层。
- 6 模块、5 API、2 张业务支持表和 AsyncTask 轻量扩展与首版范围相称，无孤儿 ADR、悬空依赖或存疑设计。

## 八、结论

- **结构完整度：100%**
- **需求满足度：100%**
- **未关闭问题：0**
- **整体评价：✅ 架构充分满足 PRD，可进入开发计划阶段。**
