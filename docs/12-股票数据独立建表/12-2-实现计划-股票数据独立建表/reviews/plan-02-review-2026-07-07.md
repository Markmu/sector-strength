---
feat_id: plan-02
title: 股票写入路径切换
phase: 2
review_date: 2026-07-07
reviewer: task-review (independent)
conclusion: 通过
---

# plan-02 验收报告：股票写入路径切换

## 0. 验收结论

**通过** ✅

- 板块测试零回归硬门禁通过（57/57）
- 生产代码 6 个目标文件切换正确（AC-01 / AC-04 实测通过）
- 共享方法 ADR-4 内部分发不误伤板块分支（外部签名不变，sector 分支零改动）
- 唯一新增失败 `test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade` 属测试 fixture 需适配新表（plan-04 范围，非本功能 blocker）
- 状态变更：plan-02 frontmatter `status: review → done`

状态前置校验：plan-02 frontmatter 当前为 `review`，满足验收前置条件。

## 1. 文件交付完整性（维度 1）

`git diff --stat` 实际改动 7 个文件，全部在 plan-02 / plan-01 清单内，无意外文件：

| 文件 | 动作 | 范围归属 | 核对 |
|---|---|---|---|
| server/src/services/data_updater/collector.py | modify | plan-02 #1 | ✅ 股票分支切 StockDailyMarketData + 新约束名 + 可观测性日志 |
| server/src/services/data_init.py | modify | plan-02 #2/#3 | ✅ 两处历史初始化 + _cascade_delete_stock_data 三表遍历 |
| server/src/services/data_update.py | modify | plan-02 #4 | ✅ backfill_by_date + backfill_by_range + fetch_missing_dates 股票子段 |
| server/src/services/monitoring/data_quality.py | modify | plan-02 #5 | ✅ _backfill_single_date 股票补齐子段 + 新约束名 |
| server/src/services/stock_ma_service.py | modify | plan-02 #6 | ✅ read-modify-write + 断点续传 + get_latest_ma_values + get_stock_date_range |
| server/src/services/strength_service_v2.py | modify | plan-02 #7/#8 | ✅ _save_strength_score + calculate_and_update_change_rate 按 entity_type 分发 |
| server/src/models/__init__.py | modify | **plan-01** | ✅ 三新模型 import + __all__ 注册（plan-01 范围，非 plan-02 改动） |

- 6 个 plan-02 清单文件全部交付且包含与功能相关的修改
- `models/__init__.py` 的改动属于 plan-01 模型注册（前端早已 done），plan-02 未在清单中新增/移除它，符合边界声明「仅清单内 6 个文件」
- 无清单外意外改动板块文件

## 2. Task 列表完成度（维度 2）

plan-02 §4 共 10 个 Task，全部状态 `done`，无遗留 `todo` 或非法状态：

| # | Task | 状态 |
|---|---|---|
| 1 | collector 股票分支切 StockDailyMarketData | done ✅ |
| 2 | data_init 股票历史初始化切新表 | done ✅ |
| 3 | data_init._cascade_delete_stock_data 三表遍历换新模型 | done ✅ |
| 4 | data_update 股票增量更新切新表 | done ✅ |
| 5 | data_quality 股票行情补齐切新表 | done ✅ |
| 6 | StockMAService 均线 read-modify-write 切新表 | done ✅ |
| 7 | StrengthServiceV2._save_strength_score 按 entity_type 分发 | done ✅ |
| 8 | StrengthServiceV2.calculate_and_update_change_rate 按 entity_type 分发 | done ✅ |
| 9 | 触发股票采集，验证新表入库 + 旧表无新增 | done ✅ |
| 10 | 触发级联删除，验证新三表清理 + 板块不动 | done ✅ |

## 3. 验收标准完成度（维度 3）

plan-02 §5 验收标准逐条核对（独立验证）：

### 功能验收

- [x] **AC-01 行情写新表**：实测触发 collector 同款 `pg_insert(StockDailyMarketData).on_conflict_do_nothing(constraint='uq_stock_daily_market_data_stock_date')` → `stock_daily_market_data` 新增 1 行（stock_id=99999991, symbol=TESTAC01, close=10.50）；二次插入被忽略（幂等性保持）；旧表 `daily_market_data` entity_type='stock' 记录数不变（1546 行历史数据按 ADR-2 保留）
- [x] **AC-01 均线/强度写新表**（代码核查）：`stock_ma_service.py` 全部 select/add 指向 `StockMovingAverageData`；`strength_service_v2._save_strength_score` stock 分支用 `StockStrengthScore`
- [x] **AC-04 级联删除清新三表**：实测向三张新表各插 1 条 → 执行 `_cascade_delete_stock_data` 三模型循环 `delete(model).where(model.stock_id.in_([stock_id]))` → 三表全清（dmd 1→0, mad 1→0, ss 1→0）；旧表板块数据不受影响（entity_type='sector' 376759 行前后不变）
- [x] **collector 约束名**：`uq_stock_daily_market_data_stock_date`（3 处：collector / data_quality / 迁移文件），且迁移 alembic current = `dd92f496dfaf` (head)
- [x] **_save_strength_score 外部签名不变**：方法签名仍为 `(self, entity_type, entity_id, calc_date, result)`，内部 `Model = StockStrengthScore if entity_type == "stock" else StrengthScore`
- [x] **板块路径仍写旧表**（AC-03 前置）：代码核查 `_save_strength_score` / `calculate_and_update_change_rate` 的 sector 分支仍用 `StrengthScore` + `entity_type/entity_id/period=='all'`；collector 板块分支（235-264 行）仍用 `DailyMarketData` + `entity_type='sector'` + 旧约束名；57/57 板块测试通过

### 涉及 task handler 的执行验证（不可豁免）

- [x] **init_historical_data_task 数据链路**：handler (task_handlers.py:177-239) → `DataInitService(manager.db)` → `init_historical_data[_by_date_range]` → 股票写入子段用 `select(StockDailyMarketData)` 查重 + `session.add(StockDailyMarketData(stock_id=...))`。实测：mock 数据源返回 1 条 quote → DataInitService 股票写入路径写入 `stock_daily_market_data`（count=1, 字段 close=10.8 正确）→ 数据链路「数据源=Stock.symbol → 写入目标=stock_daily_market_data(stock_id)」匹配
- [x] **backfill_by_date_task 数据链路**：handler (task_handlers.py:314-353) → `DataUpdateService.backfill_by_date` → 股票写入子段（data_update.py:264-284）用 `StockDailyMarketData(stock_id=...)`。代码核查通过

## 4. 实现规格符合度（维度 4）

对照 plan-02 §3 实现规格逐条核对：

| 规格点 | 实现 | 核对 |
|---|---|---|
| §3.1 collector 股票分支 | `pg_insert(StockDailyMarketData).values(stock_id=entity_id, ...)` + 约束名换新表 + 去掉 entity_type | ✅ |
| §3.1 可观测性日志 | `logger.info("stock_market_data_inserted", extra={...})` | ✅ |
| §3.2 data_init 历史初始化（两处子段） | init_historical_data + by_date_range select/add 均换 StockDailyMarketData | ✅ |
| §3.2 _cascade_delete_stock_data 三元组 | `(StockDailyMarketData, StockMovingAverageData, StockStrengthScore)` 按 stock_id 删 | ✅ |
| §3.3 data_update 股票子段（3 处） | backfill_by_date + backfill_by_range + fetch_missing_dates 全切，sector 分支不动 | ✅ |
| §3.4 data_quality 股票补齐子段 | pg_insert(StockDailyMarketData) + 新约束名；sector 补齐子段（168-182）不动 | ✅ |
| §3.5 StockMAService read-modify-write + 断点续传 | select/add 均换 StockMovingAverageData；batch_size=500 不变；含 get_latest_ma_values + get_stock_date_range | ✅ |
| §3.6 _save_strength_score ADR-4 内部分发 | `Model = StockStrengthScore if is_stock else StrengthScore`；stock 表无 period 条件，sector 保留 period=='all'；新表不设 entity_type/period，stock_id 替代 entity_id | ✅ |
| §3.6 calculate_and_update_change_rate 分发 | 同上；StockStrengthScore 无 change_rate_5d 列，仅 sector 分支更新该字段 | ✅ |
| §3.7 result_saver 不在范围 | result_saver.py 未改动（核实只更新实体表冗余字段） | ✅ |

实现规格全部满足，无超出范围实现。

## 5. 验证命令执行（维度 5）

### 5.1 板块回归硬门禁（必须全通过）

```bash
pytest tests/services/test_sector_ma_service.py tests/services/test_sector_classification_service.py \
       tests/services/test_strength_scatter_service.py tests/services/test_strength_snapshot_service.py --no-cov -q
```

结果：**57 passed, 18 warnings in 5.26s** ✅

> 说明：任务清单中列出的 `test_sector_strength_service.py` 在仓库中不存在（命名笔误）；板块强度覆盖实际由 `test_strength_snapshot_service.py`（13）+ `test_strength_scatter_service.py`（10）承接。现有 4 个板块测试文件全部通过，硬门禁满足。

### 5.2 全量 pytest

```bash
pytest --no-cov -q
```

结果：**1054 passed, 13 failed**（235.56s）

### 5.3 受影响服务的 stock 写入测试

```bash
pytest tests/test_data_updater.py tests/test_data_init.py tests/test_data_update.py -k "stock" --no-cov -q
```

结果：**5 passed**（这些测试用 mock session，不直接断言 DB 表内容，切换后仍通过）

### 5.4 alembic head 确认

```bash
alembic current
```

结果：`dd92f496dfaf (head)` ✅（迁移已应用，新表 + 约束名实际存在）

## 6. 失败分类（13 failures）

通过 `git stash` 回退到 plan-02 改动前 baseline 独立复跑 13 个失败用例，确认归属：

| # | 失败用例 | baseline 表现 | 与 plan-02 关系 | 归属 |
|---|---|---|---|---|
| 1 | test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade | **baseline PASS** | **直接相关**（唯一新增失败） | **plan-04** |
| 2 | test_classification_cache::test_cache_get_performance | baseline FAIL | 无关（缓存性能测试） | 既有失败 |
| 3-6 | test_fund_sync_filter（4 个） | baseline FAIL | 无关（基金同步过滤） | 既有失败 |
| 7-11 | test_scheduler_service::TestJobManager（5 个） | baseline FAIL | 无关（调度器任务注册） | 既有失败 |
| 12 | test_sector_classification_data_init_atdd::test_ac02_02_scheduled_task_at_16_00 | baseline FAIL | 无关（板块分类调度） | 既有失败 |

**关键判定**：
- **唯一新增失败** `test_cleanup_deletes_disappeared_with_cascade`：测试 fixture 向**旧表** `DailyMarketData(entity_type='stock')` 插入数据并断言 `_cascade_delete_stock_data` 清理它；切换后生产代码正确地改为清新三表（按 stock_id），旧表行残留 → 测试断言失效。生产代码（按 stock_id 删新三表）符合 AC-04 + ADR-2 设计预期，**测试本身需适配新表 → 归 plan-04 范围，不算本功能 blocker**。
- 其余 12 个失败在 plan-02 改动前已存在，与本功能无关。

> 实测失败输出：`assert 1 == 0 where 1 = len([<DailyMarketData(entity_type=stock, symbol=000638, date=2026-01-01)>])` — 明确证明残留的是旧表 stock 行，而非新表数据未被清理。

## 7. E2E 质量证据（维度 6）

| 证据 | 路径 | 结论 |
|---|---|---|
| Red | docs/e2e/evidence/plan-02-e2e-red-2026-07-07.md | 「预期失败，测试有效」✅ |
| Green | docs/e2e/evidence/plan-02-e2e-green-2026-07-07.md | 「通过」✅ |

两份证据均覆盖 plan-02，结论符合 contract。本功能为纯后端数据存储层改造（无用户可观察界面），按 README §7.2 说明 red/green 对应 pytest 失败/通过证据（非 Playwright）；task handler 执行验证已在本 review 独立补做（见 §3）。

## 8. 边界与风险（维度 7）

plan-02 §8 后端边界场景 5 项全部 `done`：

| 场景 | 状态 |
|---|---|
| 共享方法 _save_strength_score 误伤板块分支 | done ✅（代码核查 + 57/57 板块测试零回归） |
| 采集重复触发（幂等性） | done ✅（实测二次插入不覆盖，约束名正确） |
| 级联删除误删板块数据 | done ✅（实测旧表 sector 376759 行不变） |
| 约束名遗漏同步 | done ✅（3 处全部用新约束名，迁移文件已确认） |
| 批量写入超时 | done ✅（batch_size=50/500 与改造前一致） |

无未关闭的边界场景。

## 9. 契约对齐（维度 8）

| 契约点 | 核对 |
|---|---|
| 三新模型字段（plan-01 产出） | StockDailyMarketData / StockMovingAverageData / StockStrengthScore 含 stock_id、无 entity_type、强度表无 period、含 percentile；与写入代码字段名一致 |
| `_save_strength_score` 外部签名 | `(self, entity_type, entity_id, calc_date, result)` 不变（调用方零改动） |
| on_conflict 约束名 | `uq_stock_daily_market_data_stock_date`（collector / data_quality / 迁移文件 三处一致） |
| 板块旧表契约 | sector 路径仍写 `daily_market_data` + `moving_average_data` + `strength_scores`（含 entity_type/entity_id/period） |

无契约偏差。下游消费方 plan-03（读取路径切换）依赖本功能写入的新表数据，链路就绪。

## 10. 代码审查（维度 9）

### 阻塞项

无。所有 stock 写入路径正确指向新表，板块路径零改动，无安全隐患/数据损坏风险/契约错误。

### 建议项（非阻塞）

- `data_update.py` backfill_by_date 中 `entity_id` 局部变量名仍保留旧命名（语义上现在是 stock_id），但赋值正确（`entity_id=stock.id`），可读性可在后续清理
- collector 股票分支的 `stock_inserted_count` 统计的是 `len(quotes)`（拉取到的条数）而非实际插入条数，可观测性日志的 `count` 含义略偏向「拉取数」而非「落库数」；不影响功能，可在后续优化为统计受影响行数
- `_save_strength_score` 与 `calculate_and_update_change_rate` 内部对 `is_stock` 的判断逻辑重复（两处独立写 `Model = ...`），可后续抽一个小工具函数 `_select_strength_model(entity_type)`；当前实现清晰、无 bug

## 11. 执行验证 / task handler（维度 10）

**通过** ✅

- `init_historical_data_task` (TaskType.INIT_HISTORICAL_DATA) → `DataInitService.init_historical_data[_by_date_range]` → 股票写入子段 `session.add(StockDailyMarketData(stock_id=...))`：数据源（Stock 列表 + DailyQuote）✓、写入目标（stock_daily_market_data.stock_id）✓、前置依赖（Stock 表存在）✓
- `backfill_by_date_task` (TaskType.BACKFILL_BY_DATE, target_type='stock') → `DataUpdateService.backfill_by_date` → 股票写入子段 `StockDailyMarketData(stock_id=...)` ✓
- 实测触发 DataInitService 股票写入路径（mock 数据源返回 1 条 quote）→ `stock_daily_market_data` 新增 1 行，字段值（close=10.8）正确 → 数据链路实测通过

## 12. 状态变更记录

| 项 | 变更前 | 变更后 |
|---|---|---|
| plan-02 frontmatter status | review | **done** |
| README §7.2 开发状态机（plan-02 行） | （未按要求修改，保持不动） | — |

> 按任务要求，不修改 README §7.2 开发状态机表（该表由后续流程统一同步）。

## 13. 移交事项（移交 plan-04，非本功能 blocker）

- **test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade** 测试 fixture 需适配新表：fixture 应向新表 `StockDailyMarketData(stock_id=...)` 插入数据（而非旧表 `DailyMarketData(entity_type='stock')`），并断言 `_cascade_delete_stock_data` 清理新三表。生产代码无需改动。
