---
feat_id: plan-03
title: 股票读取路径切换
phase: 3
review_date: 2026-07-07
reviewer: task-review (independent)
conclusion: 通过
---

# plan-03 验收报告：股票读取路径切换

## 0. 验收结论

**通过** ✅

- 板块测试零回归硬门禁通过（57/57，含 sector_ma / strength_snapshot / strength_scatter / sector_classification 四文件）
- 生产代码 10 个目标文件 stock 读路径切换正确（AC-02 / AC-07 / percentile 实测通过）
- StockStrengthScore 所有查询均去掉 `period=='all'` 条件（关键风险点 0 命中，grep + 代码核查双确认）
- ADR-4 共享方法内部分发不误伤板块分支（外部签名不变，sector 分支零改动）
- 全量 pytest 1054 passed / 13 failed，与 plan-02 green baseline 完全一致，**未新增任何失败**
- 状态变更：plan-03 frontmatter `status: review → done`

状态前置校验：plan-03 frontmatter 当前为 `review`，满足验收前置条件。

## 1. 文件交付完整性（维度 1）

`git diff --stat` 工作区改动 14 个文件，全部归属 plan-01/02/03 三期清单，无意外文件，无板块文件被误改：

| 文件 | 动作 | 范围归属 | 核对 |
|---|---|---|---|
| server/src/services/calculation/ma_system/ma_data_loader.py | modify | **plan-03 #1** | ✅ load_ma_values/load_current_price/_get_available_days 按 entity_type 内部分发 |
| server/src/services/stock_ma_service.py | modify | plan-03 #2（清理 import）/ plan-02 | ✅ select 已全切新表（plan-02），本期清理 |
| server/src/services/strength_service_v2.py | modify | plan-03 #3（核对）/ plan-02 | ✅ 读分支 plan-02 已切，本期核对无其他直查 StrengthScore |
| server/src/services/ranking_service.py | modify | **plan-03 #4** | ✅ calculate_rankings 循环内按 entity_type 选模型，stock 去 period |
| server/src/services/strength_history_service.py | modify | **plan-03 #5** | ✅ stock 分支切 StockStrengthScore；sector 分支不动 |
| server/src/services/strength_snapshot_service.py | modify | **plan-03 #6** | ✅ _update_ranks_and_percentiles + get_snapshot_status 分发 |
| server/src/services/monitoring/data_quality.py | modify | **plan-03 #7** / plan-02 | ✅ _check_invalid_strength_scores 股票分支切新表 |
| server/src/api/v1/rankings.py | modify | **plan-03 #8** | ✅ get_stock_rankings_v2 + get_strength_stats(stock) 切新表 |
| server/src/api/v1/stocks.py | modify | **plan-03 #9** | ✅ strength_stmt 切 StockStrengthScoreModel；响应硬填 AC-06 字段 |
| server/src/api/v1/strength.py | modify | **plan-03 #10** | ✅ _build_strength_data stock 分支查 StockMovingAverageDataModel |
| server/src/models/__init__.py | modify | plan-01 | ✅ 三新模型注册（非本期改动） |
| server/src/services/data_init.py | modify | plan-02 | ✅ 写入路径（非本期改动） |
| server/src/services/data_update.py | modify | plan-02 | ✅ 写入路径（非本期改动） |
| server/src/services/data_updater/collector.py | modify | plan-02 | ✅ 写入路径（非本期改动） |

- plan-03 §2 清单 10 个 modify 文件全部交付且包含与功能相关的修改
- 无清单外意外改动板块文件（`sector_*.py` / `api/v1/sectors.py` 全部未出现在 diff 中）
- 多期改动叠加在同一工作区，归属按 plan-02 review 与代码现状合理划分

## 2. Task 列表完成度（维度 2）

plan-03 §4 共 12 个 Task，全部状态 `done`，无遗留 `todo` 或非法状态：

| # | Task | 状态 |
|---|---|---|
| 1 | MADataLoader 四方法按 entity_type 内部分发 | done ✅ |
| 2 | StockMAService 全部 select 切新表 | done ✅ |
| 3 | StrengthServiceV2 读分支切新表 | done ✅ |
| 4 | RankingService 循环内按 entity_type 选模型 | done ✅ |
| 5 | StrengthHistoryService stock 分支切新表 | done ✅ |
| 6 | StrengthSnapshotService stock 循环 + _update_ranks 分发 | done ✅ |
| 7 | data_quality._check_invalid_strength_scores 股票分支切新表 | done ✅ |
| 8 | rankings.py 个股排名 + stats stock 分支切新表 | done ✅ |
| 9 | stocks.py 个股强度查询 + fallback 切新表 | done ✅ |
| 10 | strength.py _build_strength_data stock 分支切新表 | done ✅ |
| 11 | 验证个股接口返回新表数据 | done ✅ |
| 12 | 验证空窗期呈现（ma240 缺失返回空） | done ✅ |

## 3. 验收标准完成度（维度 3）

plan-03 §5 验收标准逐条核对（**独立验证，非仅读 subagent 报告**）：

### 功能验收

- [x] **AC-02 stocks/{id}/strength 读新表**：实测向 `stock_strength_scores` 插入测试行（stock_id=999950, score=55.5），模拟 stocks.py `strength_stmt = select(StockStrengthScoreModel).where(stock_id==stock.id)` → 命中 stock_id=999950, score=55.5000 ✅
- [x] **AC-02 rankings/v2/stocks 读新表**：实测 `select(StockStrengthScoreModel, StockModel).join(StockModel, stock_id==StockModel.id)` 返回 1 行 ✅；分页/排序逻辑（offset/limit/order_by score desc）与改造前一致
- [x] **AC-02 rankings/v2/stats?entity_type=stock 读新表**：代码核查 `get_strength_stats` 按 `is_stock` 分发，stock 用 StockStrengthScoreModel（无 entity_type/period），sector 用 StrengthScoreModel（保留 entity_type/period=='all'）✅
- [x] **AC-02 fallback 重算读新表**：实测 `ma_data_loader.load_ma_values(entity_type='stock')` 读 `stock_moving_average_data`（返回 [5,10,20]）；`load_current_price(entity_type='stock')` 读 `stock_daily_market_data`（无数据时返回 None，无报错）✅
- [x] **AC-07 ma240 缺失返回空**：实测向 `stock_moving_average_data` 插 5d/10d/20d（无 240d），`load_ma_values(stock, periods=[5,10,20,240])` 返回 `{5,10,20}`，**不含 240 键** ✅；调用方按既有"数据不足"方式呈现
- [x] **percentile setattr 写入新表无报错**：实测插 3 条 stock 强度（score=80/60/40）→ 调 `RankingService.calculate_rankings(entity_types=['stock'])` → success=True，回查 `stock_strength_scores.percentile` 非空（80→rank1→66.67%, 60→rank2→33.33%, 40→rank3→0%），无 "column does not exist" 错误 ✅
- [x] **响应字段名/类型/结构与改造前一致**（AC-06 前置）：代码核查 stocks.py 响应仍用 `StockStrengthResponse`，硬填 `entity_type="stock"` / `period="all"` / `change_rate_5d=None`；rankings.py 响应用 `StrengthRankingResponse`（entity_type="stock"）。正式 diff 在 plan-04
- [x] **板块所有读路径完全不受影响**（AC-03 前置）：57/57 板块测试零回归（详见 §5.1）

### 性能验收

- [x] **个股强度接口响应时间持平**：stock 查询去 entity_type/period 段（`StockStrengthScore.where(stock_id, date)` 比 `StrengthScore.where(entity_type, entity_id, period, date)` 更精简），符合架构 §8.1 预期不退化

### 后端边界场景

- [x] **MADataLoader 板块查询仍读旧表**：实测 `load_ma_values(entity_type='sector', entity_id=1)` 返回 []（无报错，读 `moving_average_data`），分发逻辑正确 ✅
- [x] **RankingService 板块排名仍读旧表**：代码核查 `calculate_rankings` 的 sector 分支保留 `Model.entity_type==entity_type` + `Model.period=='all'`；57/57 板块测试零回归

## 4. 实现规格符合度（维度 4）

对照 plan-03 §3 实现规格逐条核对：

| 规格点 | 实现 | 核对 |
|---|---|---|
| §3.1 load_ma_values 按 entity_type 选 MAData | `is_stock = entity_type=="stock"; MAData = StockMovingAverageData if is_stock else MovingAverageData; id_field = MAData.stock_id if is_stock else MAData.entity_id`；stock 无 entity_type 条件，sector 保留 | ✅ |
| §3.1 load_current_price / _get_available_days 分发 | 同上，stock→StockDailyMarketData，sector→DailyMarketData | ✅ |
| §3.1 缓存 key 不变 | `_make_cache_key` 返回 `f"{entity_type}:{entity_id}:{calc_date}"`（line 57，未改） | ✅ |
| §3.2 StockMAService 全部 select 切新表 | plan-02 已切；本期清理未用 import | ✅ |
| §3.3 StrengthServiceV2 读分支 | plan-02 已切 `_save_strength_score`/`calculate_and_update_change_rate`；`_get_symbol` 读 Stock/Sector 非三表；核对无其他直查 StrengthScore 读路径 | ✅ |
| §3.4 RankingService 循环内分发 + percentile setattr 不变 | `Model = StockStrengthScore if is_stock else StrengthScore`；stock 去 entity_type/period；`score.percentile = round((1 - rank/total)*100, 2)`（line 98）保持 | ✅ |
| §3.5 StrengthHistoryService stock 切新表 / sector 不动 | get_stock_history/get_history_stats/get_latest_score 切 StockStrengthScore；get_sector_history 完全不动（保留 entity_type=='sector' + period=='all'） | ✅ |
| §3.6 StrengthSnapshotService 分发 | _update_ranks_and_percentiles stock 段 StockStrengthScore + percentile setattr；sector 段 StrengthScore；get_snapshot_status 同分发 | ✅ |
| §3.7 data_quality 股票分支切新表 | `_check_invalid_strength_scores` 股票分支 `select(StockStrengthScore)`；sector 分支查 Sector 实体表不动 | ✅ |
| §3.8 rankings.py 个股排名 + stats 分发 | get_stock_rankings_v2 用 StockStrengthScoreModel join StockModel；get_strength_stats 分发；get_sector_rankings_v2 不动；响应 schema 不动 | ✅ |
| §3.9 stocks.py strength_stmt + fallback | strength_stmt 用 StockStrengthScoreModel.where(stock_id)；fallback 调已切的 calculate_stock_strength；响应 period='all'/change_rate_5d=None 硬填 | ✅ |
| §3.10 strength.py _build_strength_data 分发 | stock 分支用 StockMovingAverageDataModel.where(stock_id, period)；sector 分支不动 | ✅ |
| §3 响应 schema 不动 | 所有响应继续用 StockStrengthResponse/StrengthRankingResponse 等，entity_type 硬填 "stock" | ✅ |

实现规格全部满足，无超出范围实现（"不在范围"中的写入路径、测试修复、板块代码改动均未触碰）。

## 5. 验证命令执行（维度 5）

### 5.1 板块回归硬门禁（必须全通过）

```bash
pytest tests/services/test_sector_ma_service.py \
       tests/services/test_strength_snapshot_service.py \
       tests/services/test_strength_scatter_service.py \
       tests/services/test_sector_classification_service.py --no-cov -q
```

结果：**57 passed, 18 warnings in 5.14s** ✅

> 板块四文件 57/57 全通过，AC-03 硬门禁满足（baseline 21 + snapshot 13 + scatter 10 + classification 13）。

### 5.2 个股接口测试

```bash
pytest tests/test_strength_api.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_rankings_heatmap_api.py --no-cov -q
```

结果：**34 passed, 10 warnings in 0.71s** ✅（strength_api 17 + stocks_api 7 + rankings_heatmap 10）

### 5.3 全量 pytest

```bash
pytest --no-cov -q
```

结果：**1054 passed, 13 failed**（208.60s）

### 5.4 关键风险运行时验证（独立实测，非仅读 subagent 报告）

```python
# percentile 列写入（ADR-3 关键风险）
RankingService.calculate_rankings(entity_types=['stock'])
→ success=True; stock_strength_scores.percentile 非空（80→66.67%, 60→33.33%, 40→0%）→ PASS

# AC-07 ma240 缺失
load_ma_values('stock', periods=[5,10,20,240])
→ 返回 {5,10,20}，不含 240 键 → PASS

# AC-02 API 直查
stocks.py strength_stmt / rankings v2 stocks join → 命中 stock_strength_scores → PASS
```

## 6. 失败分类（13 failures，与 plan-02 baseline 完全一致）

| # | 失败用例 | 与 plan-03 关系 | 归属 |
|---|---|---|---|
| 1 | test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade | 间接（plan-02 已识别） | **plan-04**（测试 fixture 需适配新表） |
| 2 | test_classification_cache::test_cache_get_performance | 无关（缓存性能） | 既有失败 |
| 3-6 | test_fund_sync_filter（4 个） | 无关（基金同步过滤） | 既有失败 |
| 7-11 | test_scheduler_service::TestJobManager（5 个） | 无关（调度器任务注册） | 既有失败 |
| 12 | test_sector_classification_data_init_atdd::test_ac02_02_scheduled_task_at_16_00 | 无关（板块分类调度） | 既有失败 |
| 13 | （与上 12 共为 13，scheduler 5 个含 test_get_jobs） | 无关 | 既有失败 |

**关键判定**：
- **新增失败：0**（与 plan-02 green baseline `1054 passed, 13 failed` 完全一致）
- 唯一与本需求间接相关的 `test_cleanup_deletes_disappeared_with_cascade` 在 plan-02 review 时已归 plan-04 范围（测试 fixture 需向新表插数据），**plan-03 未新增任何失败，不构成 blocker**
- 个别既有失败均属 plan-04 范围的 fixture 适配或与本需求无关的既有问题

## 7. E2E 质量证据（维度 6）

| 证据 | 路径 | 结论 |
|---|---|---|
| Red | docs/e2e/evidence/plan-03-e2e-red-2026-07-07.md | 「预期失败，测试有效」✅ |
| Green | docs/e2e/evidence/plan-03-e2e-green-2026-07-07.md | 「通过」✅ |

两份证据均覆盖 plan-03，结论符合 contract。本功能为纯后端数据存储层读路径改造（无用户可观察界面），按 README §7.2 说明 red/green 对应 pytest 失败/通过证据（非 Playwright）。本 review 在 green 证据基础上**独立补做**了 percentile/AC-02/AC-07 三项运行时实测（见 §3、§5.4），不依赖 subagent 报告结论。

## 8. 边界与风险（维度 7）

plan-03 §8 后端边界场景 6 项全部 `done`：

| 场景 | 状态 |
|---|---|
| StockStrengthScore 查询带 period 条件报错 | done ✅（grep + 代码核查：所有 stock 分支去 period=='all'，0 命中真实查询条件） |
| MADataLoader 误伤板块 | done ✅（ADR-4 内部分发；57/57 板块测试零回归） |
| 新表无 ma240 数据（空窗期） | done ✅（实测 load_ma_values 返回 dict 不含 240 键） |
| percentile 列写入失败 | done ✅（实测 setattr 写入成功，ADR-3 落地） |
| 个股接口 fallback 重算循环 | done ✅（fallback 逻辑不变，仅切新表读写） |
| 板块查询误读新表 | done ✅（RankingService/StrengthHistoryService 等 sector 分支完全不动） |

无未关闭的边界场景。验证失败排查方向（period 报错 / 误伤板块 / percentile / 响应字段缺失）均未在实际执行中触发。

## 9. 契约对齐（维度 8）

| 契约点 | 核对 |
|---|---|
| 响应 schema（api/schemas/strength.py）不动 | StockStrengthResponse / StrengthRankingResponse / StrengthStatsResponse 字段不变 ✅ |
| entity_type 字段硬填 "stock" | stocks.py（line 279）/ rankings.py（line 229）/ get_strength_stats 透传参数 ✅ |
| period 硬填 "all" / change_rate_5d 硬填 None | stocks.py（line 283/311）✅ |
| 缓存 key 命名不变（含 entity_type 段） | ma_data_loader._make_cache_key（line 57）未改 ✅ |
| 板块旧表契约 | sector 路径仍读 strength_scores + moving_average_data + daily_market_data（含 entity_type/period）✅ |

无契约偏差。下游消费方 plan-04（测试修复）依赖本功能读路径完整切换；plan-05（板块回归）依赖共享方法未误伤板块——两者前置条件均满足。

## 10. 代码审查（维度 9）

### 阻塞项

无。所有 stock 读路径正确指向新表，板块路径零改动，无安全隐患/数据损坏风险/契约错误。

### 重点核查项（任务要求）

- [x] **ma_data_loader 按 entity_type 内部分发且 sector 分支不动**：`is_stock = entity_type=="stock"` 三处分发（load_ma_values / load_current_price / _get_available_days）；sector 分支保留 `entity_type`/`entity_id`/`DailyMarketData`/`MovingAverageData`，与改造前完全一致 ✅
- [x] **ranking_service.calculate_rankings 循环内选 StockStrengthScore 且去 period=='all'**：循环内 `Model = StockStrengthScore if is_stock else StrengthScore`；stock 分支 `base_conds = [date, score.isnot(None)]`（无 entity_type、无 period）；sector 分支追加 entity_type + period=='all' ✅
- [x] **stocks.py strength_stmt 查 StockStrengthScoreModel 且响应 entity_type 硬填 "stock"**：`select(StockStrengthScoreModel).where(stock_id==stock.id)`；响应 `entity_type="stock"`/`period="all"`/`change_rate_5d=None` ✅

### 建议项（非阻塞）

- `ranking_service` 的 `get_top_rankings` / `get_percentile` / `get_entity_ranking` 仍保留 entity_type 参数且只用旧表 StrengthScore（未做分发）。按 red 证据 §2.1 说明，这些方法在切换后 stock 调用方基本不再出现，方法签名保留属合理 brownfield 兼容；如后续确认无 stock 调用方，可在独立清理项收敛
- `stocks.py` 响应构建从 ORM 取大量字段（line 284-312）逐一映射到 StockStrengthResponse，可后续考虑用 model_dump / from_attributes 收敛；当前实现清晰、与改造前结构一致，无 bug
- `ma_data_loader` 内 `_ma_base_conditions` 闭包写法在 load_ma_values 中清晰，load_current_price / _get_available_days 又各自重复 `base_conds` 构建；可后续抽公共助手，当前实现无 bug

## 11. 执行验证 / task handler（维度 10）

**不适用**。plan-03 为纯读路径改造，不新增/不修改 task handler；写入路径（task handler 触发）在 plan-02 已验证。本 review 的运行时验证（§5.4）通过 SQLAlchemy 直查 + RankingService 实际调用确认读路径正确，覆盖了"读取"这一本功能核心动作的数据链路。

## 12. 状态变更记录

| 项 | 变更前 | 变更后 |
|---|---|---|
| plan-03 frontmatter status | review | **done** |
| README §7.2 开发状态机（plan-03 行） | （未按要求修改，保持不动） | — |

> 按任务要求，不修改 README §7.2 开发状态机表（该表由后续流程统一同步）。

## 13. 移交事项（移交 plan-04，非本功能 blocker）

- **test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade** 测试 fixture 需适配新表（plan-02 review 已移交，plan-03 未新增失败）
- **响应字段正式 diff**：AC-06 的正式接口响应字段 diff 核对在 plan-04 完成；本功能已确保 schema 不动 + 关键字段硬填，前置条件满足
- **ranking_service 遗留的 entity_type 参数方法**（get_top_rankings 等）：如确认无 stock 调用方，可在后续清理项收敛（非本功能 blocker）
