---
feat_id: plan-04
title: 测试修复与全量回归
review_date: 2026-07-07
reviewer: task-review（独立验收）
verdict: 通过
red_evidence: docs/e2e/evidence/plan-04-e2e-red-2026-07-07.md
green_evidence: docs/e2e/evidence/plan-04-e2e-green-2026-07-07.md
---

# plan-04 验收报告：测试修复与全量回归

## 1. 验收结论

**通过**。plan-04 验收门禁全部达成：

- 新表切换导致的失败（test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade）已修复并独立验证通过
- 12 个剩余失败经 git stash baseline 对照确认为既有失败（与拆表无关，非本期范围）
- 板块测试零回归（57/57）
- AC-06 API 响应契约 diff 为空；响应 entity_type 继续返回 "stock"（43 个 API 测试通过）
- plan-04 §2 文件清单 278/278 通过
- plan-04 未修改任何生产代码（仅修改 1 个测试文件 test_hk_stock_sync.py）

依据用户要求，plan-04 status 由 review → done。

## 2. 验收维度汇总

| # | 维度 | 结果 | 说明 |
|---|------|------|------|
| 1 | 文件交付完整性 | 通过 | §2 清单 15 文件中 14 个未改（核查无需修复即通过），实际 modify 1 个（test_hk_stock_sync.py，+6/-5 行）；无清单外文件被本功能修改 |
| 2 | Task 列表完成度 | 通过 | #1~#10 全部 done，无 todo/waived 残留 |
| 3 | 验收标准完成度 | 通过 | 6 项验收 checklist 全部满足（详见 §3） |
| 4 | 实现规格符合度 | 通过 | §3 A 类修复策略与实际修复一致；不在范围内（板块回归）未被实现 |
| 5 | 验证命令执行 | 通过 | 全量 pytest、清单文件 pytest、test_hk_stock_sync.py、板块测试、API 测试均通过（详见 §4） |
| 6 | E2E 质量证据 | 通过 | red（预期失败，测试有效）+ green（通过）双证据齐备，覆盖 plan-04 |
| 7 | 边界与风险 | 通过 | 5 个边界场景全部 done；验证失败排查方向与实际结果一致 |
| 8 | 契约对齐 | 通过 | AC-06 API 响应字段名/类型/结构 diff 为空；entity_type/period/percentile/change_rate_5d 字段保持 |
| 9 | 代码审查 | 通过 | 无 blocker；1 个建议项（percentile 覆盖文件归属） |
| 10 | 执行验证（task handler） | N/A | 本功能为测试修复层，无 task handler 委托 |

## 3. 验收标准逐条核对（plan-04 §5 功能验收）

| 验收标准 | 结果 | 独立验证证据 |
|---------|------|-------------|
| AC-06：`cd server && pytest` 全量通过，无失败用例 | **部分达成（非 blocker）** | 全量 12 failed, 1055 passed；12 个失败均为既有失败（baseline 对照确认，详见 §5），非本期范围；plan-04 §2 清单内 0 失败 |
| AC-06：个股 API 响应字段名/类型/结构 diff 为空 | **通过** | schema 未改（StockStrengthResponse 字段集不变）；43 个 API 测试通过；entity_type/period/percentile/change_rate_5d 字段保持 |
| AC-06：响应 entity_type 继续返回 "stock" | **通过** | stocks.py:279 硬填 entity_type="stock"；test_strength_api.py:304/325 断言 entity_type=="stock" 全部通过 |
| 所有股票 fixture 写入新表、断言查新表 | **通过** | test_hk_stock_sync fixture 已从 DailyMarketData(entity_type='stock') 切到 StockDailyMarketData(stock_id=...)；断言查 StockDailyMarketData |
| 既有板块测试未被破坏 | **通过** | 板块+强度测试 57/57 通过（test_sector_classification_service / test_sector_ma_service / test_strength_scatter_service / test_strength_snapshot_service） |
| test_strength_score_model.py 覆盖 StockStrengthScore 含 percentile | **达成（含建议项）** | test_strength_score_model.py 测旧 StrengthScore（含 percentile 列，20 passed）；新 StockStrengthScore.percentile 由 test_stock_models.py 覆盖（test_percentile_column_exists + test_create_record_with_percentile，2 passed）；功能覆盖完整，仅文件归属与字面描述略有偏差（建议项） |

## 4. 验证命令执行结果（独立运行，非读取 subagent 报告）

### 4.1 全量 pytest（改造后）

```bash
cd server && source .venv/bin/activate && pytest --tb=no -q
```

结果：**12 failed, 1055 passed**（273s）

12 个失败清单：
1. test_classification_cache.py::TestCachePerformance::test_cache_get_performance（缓存性能，环境波动）
2-5. test_fund_sync_filter.py::TestSyncFundBasicDelistFilter（基金退市/状态过滤，4 项）
6-11. test_scheduler_service.py::TestJobManager（daily_data_update 等 job 未注册，6 项）
12. test_sector_classification_data_init_atdd.py::...::test_ac02_02_scheduled_task_at_16_00（板块分类调度任务）

**关键：test_hk_stock_sync 不在失败清单中**（plan-04 修复的新表切换失败已转为通过）。

### 4.2 baseline 对照（git stash 切回改造前）

```bash
git stash push -u -m "plan-04-review-baseline-check" -- server/
pytest --tb=no -q
git stash pop
```

baseline 结果：**12 failed, 1038 passed**（259s）

baseline 12 个失败与改造后失败清单**逐项完全一致**（test_classification_cache / test_fund_sync_filter×4 / test_scheduler_service×6 / test_sector_classification_data_init_atdd×1）。

**对照结论**：
- 改造前 baseline = 12 failed / 1038 passed
- 改造后 = 12 failed / 1055 passed（passed +17 来自 plan-01 新增 test_stock_models.py 17 项）
- 失败清单完全一致 → 12 个失败均为既有失败，与"股票数据独立建表"无关
- plan-04 §3 唯一目标（test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade）在改造前 baseline 通过（旧生产代码清旧表 + fixture 写旧表自洽）、改造后 red 阶段失败（新生产代码清新表 + fixture 仍写旧表）、plan-04 green 阶段再次通过（fixture 切新表 + 断言查新表）→ 测试有效且修复正确

### 4.3 test_hk_stock_sync.py 单独运行

```bash
pytest tests/test_hk_stock_sync.py --no-cov -q
```

结果：**10 passed**（含修复后的 test_cleanup_deletes_disappeared_with_cascade）

### 4.4 板块+强度测试零回归

```bash
pytest tests/services/test_sector_*.py tests/services/test_strength_*.py --no-cov -q
```

结果：**57 passed**（test_sector_classification_service 13 + test_sector_ma_service 21 + test_strength_scatter_service 10 + test_strength_snapshot_service 13）

与 plan-02/03 review 记录的 57/57 基线一致，零回归。

### 4.5 plan-04 §2 文件清单全量运行

```bash
pytest tests/conftest.py tests/test_data_init.py tests/test_data_update.py \
       tests/test_data_updater.py tests/test_strength_services.py \
       tests/test_strength_score_model.py tests/test_strength_api.py \
       tests/test_calculation_engine.py tests/test_ma_system_calculator.py \
       tests/test_hk_stock_sync.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_strength_api.py tests/test_api/test_rankings_heatmap_api.py \
       tests/services/test_strength_snapshot_service.py tests/test_models.py --no-cov -q
```

结果：**278 passed**

### 4.6 AC-06 API 契约验证

```bash
pytest tests/test_strength_api.py tests/test_api/test_stocks_api.py \
       tests/test_api/test_strength_api.py tests/test_api/test_rankings_heatmap_api.py --no-cov -q
```

结果：**43 passed**

代码静态核对（src/api/v1/stocks.py）：
- L279: `entity_type="stock"`（硬填，AC-06 保持）
- L283: `period="all"`（硬填，StockStrengthScore 无 period 列）
- L311: `change_rate_5d=None`（硬填 None，StockStrengthScore 无此列）

测试断言（tests/test_strength_api.py）：
- L304: `assert data["entity_type"] == "stock"`（个股排名项）
- L325: `assert data["entity_type"] == "stock"`（强度统计）

## 5. 12 个既有失败根因分析（非本期范围）

| # | 测试文件 | 失败根因 | 与拆表关系 |
|---|---------|---------|-----------|
| 1 | test_classification_cache | 缓存 get 性能 104ms > 50ms 阈值（环境性能波动） | 无关 |
| 2-5 | test_fund_sync_filter（4 项） | 基金退市/状态过滤逻辑 skipped 计数不符（4 vs 2） | 无关 |
| 6-11 | test_scheduler_service（6 项） | daily_data_update / data_quality_check / cache_cleanup 等 job 未注册（调度器注册逻辑） | 无关 |
| 12 | test_sector_classification_data_init_atdd | 每日板块分类更新任务（16:00 调度）未注册 | 无关 |

这 12 个失败**均不在 plan-04 §2 的 15 文件清单内**，根因涉及缓存性能/基金过滤/调度器注册/板块分类调度，与"股票数据独立建表"无任何代码或数据依赖关系。baseline 对照证明它们在改造前就已存在。

依据 plan-04 §1 不在范围声明（"仅修复既有测试"中因新表切换失败的部分；板块回归验证属 plan-05）及 §3.2 分类（既有失败非本期范围），这 12 个失败不构成本期 blocker。

## 6. 改动范围核查（生产代码未改）

### 6.1 plan-04 专属改动

```bash
git diff HEAD --stat -- server/tests/test_hk_stock_sync.py
# server/tests/test_hk_stock_sync.py | 11 ++++++-----
# 1 file changed, 6 insertions(+), 5 deletions(-)
```

plan-04 实际仅修改 1 个测试文件，6 处行级变更：
- fixture：`DailyMarketData(entity_type="stock", entity_id=s_del.id, ...)` → `StockDailyMarketData(stock_id=s_del.id, symbol="000638", date=...)`
- 断言：`select(DailyMarketData).where(DailyMarketData.entity_id == s_del.id)` → `select(StockDailyMarketData).where(StockDailyMarketData.stock_id == s_del.id)`

### 6.2 plan-04 §2 清单其余 14 文件全部未改

逐个 `git diff --quiet HEAD` 核查：conftest.py / test_data_init.py / test_data_update.py / test_data_updater.py / test_strength_services.py / test_strength_score_model.py / test_strength_api.py / test_calculation_engine.py / test_ma_system_calculator.py / test_api/test_stocks_api.py / test_api/test_strength_api.py / test_api/test_rankings_heatmap_api.py / services/test_strength_snapshot_service.py / test_models.py —— 全部 UNCHANGED。

原因：这些测试文件中的股票 fixture 直接构造旧 `StrengthScore(entity_type='stock')` 模型，而旧 StrengthScore 模型/表仍存在并继续承载 sector+stock 共表逻辑（板块读路径仍用旧表）。这些测试不触发 plan-02/03 改造的生产读写路径，故不受拆表影响，无需修复即通过。

### 6.3 src/ 生产代码改动归属

`git diff --stat HEAD` 显示 14 个 src/ 文件被修改，但这些是 plan-01（新表模型/迁移）、plan-02（写入路径）、plan-03（读取路径）的生产代码改造，均已通过各自 task-review（plan-01/02/03 status=done）。**plan-04 未对 src/ 做任何修改**，符合 §8 边界（"如发现生产代码遗漏，需回对应功能修复，不在此功能改生产代码"）。

## 7. 代码审查

### 阻塞项

无。

### 建议项

| # | 建议 | 优先级 |
|---|------|--------|
| 1 | plan-04 §5 验收 checklist 字面写"test_strength_score_model.py 覆盖 StockStrengthScore 含 percentile 列"，但该文件实际测的是旧 StrengthScore 模型（旧模型也有 percentile 列，故通过）。新 StockStrengthScore.percentile 列覆盖实际由 plan-01 的 test_stock_models.py 承担（test_percentile_column_exists + test_create_record_with_percentile）。功能覆盖完整，建议将 §5 该项描述与实际归属对齐（或在 plan-04 中补充对新模型的直接断言） | 低（功能已覆盖，仅文档措辞） |

### test_hk_stock_sync.py 修复内容审查

修复正确，符合 plan-04 §3 A 类策略：

```python
# 修改前（写旧表，与生产 _cascade_delete_stock_data 清新三表不一致）
from src.models.daily_market_data import DailyMarketData
test_session.add(DailyMarketData(
    entity_type="stock", entity_id=s_del.id, symbol="000638", date=date(2026, 1, 1)
))
...
select(DailyMarketData).where(DailyMarketData.entity_id == s_del.id)

# 修改后（写新表，与生产清新三表逻辑对齐）
from src.models.stock_daily_market_data import StockDailyMarketData
test_session.add(StockDailyMarketData(
    stock_id=s_del.id, symbol="000638", date=date(2026, 1, 1)
))
...
select(StockDailyMarketData).where(StockDailyMarketData.stock_id == s_del.id)
```

- fixture 与生产写入路径对齐：StockDailyMarketData 是 collector/data_init 实际写入的模型类
- 断言与生产级联删除对齐：data_init.py:701-735 的 `_cascade_delete_stock_data` 按 stock_id 清新三表
- 无假阳性风险：修复后测试在真实 DB 上通过，证明 fixture-生产一致性

## 8. 边界场景核对（plan-04 §8）

| 场景 | 声明状态 | 验收核对 |
|------|---------|---------|
| 测试 fixture 与生产写入不一致 | done | 通过：test_hk_stock_sync fixture 已切 StockDailyMarketData，与生产写入对齐 |
| API 响应意外字段变化 | done | 通过：43 个 API 测试通过；schema 未改；entity_type/period/percentile/change_rate_5d 保持 |
| 板块测试被误改 | done | 通过：57/57 板块+强度测试通过；plan-04 未改任何板块测试 |
| percentile 相关测试断言失败 | done | 通过：test_stock_models.py percentile 断言通过（precision 10, scale 4） |
| 假阳性（测试过但生产有问题） | done | 通过：fixture 严格对齐生产写入路径；test_hk_stock_sync 在真实 PG 上通过 |

## 9. E2E 证据核对

- **red 证据**：docs/e2e/evidence/plan-04-e2e-red-2026-07-07.md，结论"预期失败，测试有效"。记录 13 failed（1 个新表切换 + 12 个既有），baseline 对照确认 #6 为本期引入。
- **green 证据**：docs/e2e/evidence/plan-04-e2e-green-2026-07-07.md，结论"通过"。记录 12 failed（仅既有），新表切换失败已修复。
- **本 review 独立复核**：red 记录的 13 个失败分类与 green 记录的收敛结果，与本 review 独立运行的 baseline 对照（12 failed）、改造后（12 failed, 1055 passed）数据完全吻合。

## 10. 状态变更记录

| 项 | 变更前 | 变更后 |
|----|--------|--------|
| plan-04 frontmatter status | review | done |
| README §7.2 plan-04 行 | （用户要求不修改） | （未修改） |

> 依据用户指令第 7 条："不修改 README §7.2 开发状态机表"，本 review 仅修改 plan-04 功能文件 frontmatter，不动 README §7.2。

## 11. 最终判定

**通过**。

满足用户给定的"通过"全部条件：
- ✅ 新表切换失败（test_hk_stock_sync::test_cleanup_deletes_disappeared_with_cascade）已修复
- ✅ 生产代码未被本功能修改（plan-04 仅改 1 个测试文件）
- ✅ 板块零回归（57/57）

剩余 12 个失败经独立 baseline 对照确认为既有失败（与拆表无关），按 plan-04 §1/§3.2 声明非本期范围，不构成 blocker（已在报告中明确记录根因与归属）。

后续：plan-05（板块回归基线验证）可在 plan-04 done 基础上启动。
