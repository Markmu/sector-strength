# plan-01 验收报告

- **功能 ID**: plan-01
- **标题**: 股票独立表模型与迁移
- **维度**: backend
- **验收日期**: 2026-07-07
- **验收人**: task-review（独立 agent）
- **验收结论**: ✅ **通过**

---

## 一、验收结论摘要

| 项目 | 结果 |
| --- | --- |
| 文件交付完整性 | ✅ 通过 |
| Task 列表完成度 | ✅ 通过（8/8 done） |
| 验收标准完成度 | ✅ 通过（8/8 + 性能 1） |
| 实现规格符合度 | ✅ 通过 |
| 验证命令执行 | ✅ 全部通过 |
| E2E 质量证据 | ✅ 豁免（DDL/模型层，无用户可观察路径，已在功能文件 §8 显式声明） |
| 边界与风险 | ⚠️ 4 个边界场景状态为 `todo`（非阻塞，详见维度 7） |
| 契约对齐 | ✅ 通过 |
| 代码审查 | ✅ 无 blocker，2 个建议项 |
| 执行验证（task handler） | N/A（本功能不含 task handler） |

**最终结论：通过。** 所有阻塞性维度全部满足，状态从 `review` → `done`。

> 维度 7（边界场景）的 4 个 `todo` 不构成 blocker：它们都是"已知风险被设计性接受"的陈述（旧表不迁移、新表 downgrade 丢数据可接受、stock_id 无外键、percentile 已覆盖），并非未完成的开发任务。建议 implementer 在后续阶段将其状态置为 `done` 或 `waived`（带原因），但不阻塞本期验收。

---

## 二、10 个维度详查

### 维度 1：文件交付完整性 — ✅ 通过

清单要求 6 个文件，全部交付且内容非空：

| 动作 | 路径 | 状态 |
| --- | --- | --- |
| create | `server/src/models/stock_daily_market_data.py` | ✅ 存在，39 行 |
| create | `server/src/models/stock_moving_average_data.py` | ✅ 存在，40 行 |
| create | `server/src/models/stock_strength_scores.py` | ✅ 存在，136 行 |
| modify | `server/src/models/__init__.py` | ✅ 已注册 3 新模型（import + `__all__`） |
| create | `server/alembic/versions/2026_07_07_0119-dd92f496dfaf_add_stock_independent_tables.py` | ✅ 存在，180 行 |
| create | `server/tests/test_stock_models.py` | ✅ 存在，274 行 |

`git status` 显示修改/新增文件**严格限定在清单内**，无越界文件。`__init__.py` 的 diff 经核对仅为追加 3 个 import 与 3 个 `__all__` 项，无无关改动。

### 维度 2：Task 列表完成度 — ✅ 通过

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 创建 stock_daily_market_data.py | done | ✅ |
| 2 | 创建 stock_moving_average_data.py | done | ✅ |
| 3 | 创建 stock_strength_scores.py（含 percentile） | done | ✅ percentile 已验证 |
| 4 | 注册三新模型 | done | ✅ |
| 5 | 创建 alembic 迁移 | done | ✅ down_revision 正确 |
| 6 | 创建 test_stock_models.py | done | ✅ 17 用例全过 |
| 7 | alembic upgrade head 验证 | done | ✅ 三表创建 |
| 8 | alembic downgrade -1 验证可逆 | done | ✅ 三表删除 |

8/8 done，无 `todo` / 非法状态。

### 维度 3：验收标准完成度 — ✅ 通过

逐条独立验证（详见维度 5 命令输出）：

| 验收标准 | 结果 | 证据 |
| --- | --- | --- |
| AC-01（部分）：upgrade head 成功，三表创建 | ✅ | information_schema 查询确认 3 表存在 |
| AC-05：StockStrengthScore 模型与迁移含 percentile | ✅ | 模型 `cols['percentile'].type=(10,4)`；迁移 line 118 `sa.Column('percentile', ...)` |
| AC-05：三新模型无 entity_type；SSS 无 period/板块字段 | ✅ | 模型 columns 断言全部通过 |
| 新表字段/精度/注释与旧表个股字段一一对应 | ✅ | 逐字段精度对比 **0 mismatch** |
| downgrade -1 干净回滚（旧表不受影响） | ✅ | downgrade 后新表 `[]`，旧表 3 表仍在；旧 `strength_scores` 仍 46 列 |
| `from src.models import` 三新模型成功 | ✅ | 输出 `import ok` |
| test_stock_models.py 全部通过 | ✅ | 17 passed |
| 旧表结构与既有板块数据零影响 | ✅ | git diff 旧三表模型为空；DB schema 未变 |

性能：upgrade 三张空表创建 < 1s（实测毫秒级）✅。

### 维度 4：实现规格符合度 — ✅ 通过

逐条核对实现规格：

**StockDailyMarketData**（规格 §3.1）
- ✅ `__tablename__ = "stock_daily_market_data"`
- ✅ 13 列：id/stock_id/symbol/date/open/high/low/close/volume/turnover/change/change_percent/created_at
- ✅ 精度与旧表逐字段一致（open~close=10,2；volume/turnover=15,2；change=10,2；change_percent=10,4）
- ✅ 无 entity_type
- ✅ UniqueConstraint `uq_stock_daily_market_data_stock_date(stock_id, date)`
- ✅ CheckConstraint `check_stock_dmd_high_low` / `check_stock_dmd_volume_positive`
- ✅ 3 个索引 `idx_stock_dmd_stock_date` / `idx_stock_dmd_date_range` / `idx_stock_dmd_symbol_date`

**StockMovingAverageData**（规格 §3.2）
- ✅ `__tablename__ = "stock_moving_average_data"`
- ✅ 9 列：保留 period 业务字段（'5d'/'10d' 等）
- ✅ 精度与旧表一致（ma_value=10,2；price_ratio=10,4；trend=5,2）
- ✅ 无 entity_type
- ✅ UniqueConstraint `uq_stock_moving_average_data_stock_date_period(stock_id, symbol, date, period)`
- ✅ 6 个索引全部就位（含 `idx_stock_mad_date_desc`）

**StockStrengthScore**（规格 §3.3，最关键）
- ✅ `__tablename__ = "stock_strength_scores"`
- ✅ 41 列（含 percentile），与规格字段清单完全对应
- ✅ entity_id → stock_id 重命名，无外键约束
- ✅ percentile 列 `Numeric(10,4)` 存在（ADR-3）
- ✅ 个股死字段（ma5_score/ma10_score/ma20_score/volume_score/momentum_score）照搬保留
- ✅ **去除** entity_type、period、avg_stock_score、strong_stock_ratio、up_stock_ratio、volume_ratio
- ✅ CheckConstraint `chk_stock_strength_score_range` + 8 个 `price_above_ma*` 约束
- ✅ **新增** UniqueConstraint `uq_stock_strength_scores_stock_date(stock_id, date)`
- ✅ 5 个索引（symbol_date 用 text DESC、score_desc 用 text DESC，符合规格"参照旧表去掉 period 段"）

**注册**（规格 §3.4）：`__init__.py` import + `__all__` 已加 ✅

**alembic 迁移**（规格 §3.5）
- ✅ revision=`dd92f496dfaf`，down_revision=`687ec547d98e`
- ✅ 手写 `op.create_table`（非 autogenerate，ADR-6）
- ✅ StockStrengthScore 的 create_table 含 `sa.Column('percentile', sa.Numeric(10,4), nullable=True)`（line 118）
- ✅ downgrade 逆序 drop_index + drop_table，与 upgrade 严格对称
- ✅ 约束/索引名全部带 `stock_*` 前缀，与旧表 `*_scores_*` 零冲突（ADR-1 物理拆表）

**单元测试**（规格 §3.6）：覆盖 tablename/percentile/entity_type 缺失/period 缺失/板块字段缺失/死字段保留/唯一约束/范围约束/repr ✅

### 维度 5：验证命令执行 — ✅ 全部通过

以下命令均由 task-review 独立运行（非读 subagent 报告），工作目录 `server/`，已激活 `.venv`。

**1. 模型导入**
```bash
python -c "from src.models import StockDailyMarketData, StockMovingAverageData, StockStrengthScore; print('import ok')"
# 输出：import ok
#       tablenames: stock_daily_market_data stock_moving_average_data stock_strength_scores
```
退出码 0 ✅

**2. percentile 列存在性 + 字段裁剪**
```bash
python -c "from src.models import StockStrengthScore; ..."
```
输出：
```
percentile ok: precision= 10 scale= 4
no entity_type ok
no period / no sector fields ok
individual dead fields kept ok
mad period kept ok
col counts: dmd= 13 mad= 9 sss= 41
```
退出码 0 ✅

**3. 单元测试**
```bash
pytest tests/test_stock_models.py -v
```
输出：`17 passed, 10 warnings in 1.64s`（覆盖率 34% 未达 80% 项目默认阈值属 warning，不阻塞——单模型测试本就不覆盖全代码库）✅

**4. 迁移可逆性**
```bash
alembic current       # → dd92f496dfaf (head)
alembic downgrade -1  # → 687ec547d98e（drop 三表）
alembic upgrade head  # → dd92f496dfaf（重建三表）
alembic current       # → dd92f496dfaf (head)
```
全部退出码 0，迁移双向通过 ✅

**5. DB schema 核对（information_schema，via SQLAlchemy engine）**
- downgrade 后：新表 `[]`（已删），旧表 `[daily_market_data, moving_average_data, strength_scores]`（仍在）
- upgrade 后列数：`stock_daily_market_data: 13` / `stock_moving_average_data: 9` / `stock_strength_scores: 41`
- `percentile` 行：`('percentile', 'numeric', 10, 4)` ✅
- 旧 `strength_scores` 列数：`46`（迁移前后不变）✅

**6. 迁移链线性**
```
687ec547d98e -> dd92f496dfaf (head)
alembic heads → dd92f496dfaf (单一 head，无分叉)
```

**7. 旧三表模型未被修改**
```bash
git diff -- src/models/daily_market_data.py src/models/moving_average_data.py src/models/strength_score.py
# 输出：空
```

### 维度 6：E2E 质量证据 — ✅ 豁免

本功能为纯数据基础设施层（ORM 模型 + DDL 迁移），无 API、无前端、无用户可观察行为。功能文件 §8 已显式声明 E2E 不适用，并提供理由：
> "本功能是数据基础设施层（ORM 模型 + DDL 迁移），无用户可观察行为，无 API/前端交互。验收以模型测试 + 迁移可逆性 + DB schema 检查为主，不适用 E2E。但下游 plan-02/03 完成后会有完整执行验证。"

符合 task-review 维度 6 豁免条件（纯内部、无用户可观察路径）。README §7.2 状态机已将 red_e2e / green_e2e 标记为 waived。下游 plan-02/03 将补齐执行验证（维度 10）。

### 维度 7：边界与风险 — ⚠️ 非阻塞

`风险与边界` 的 4 个后端边界场景状态均为 `todo`：

| 场景 | 实际情况 | 影响 |
| --- | --- | --- |
| 迁移 upgrade 时旧表已有数据 | 新表全新创建，旧表零影响（已实测 git diff 空 + schema 不变） | 风险已消解，状态应置 done |
| 迁移 downgrade 时新表已有数据 | 本阶段新表无业务数据，drop 可接受 | 风险已接受，状态应置 done/waived |
| stock_id 无外键约束 | 与 top10_float_holders/broker_recommend 风格一致 | 设计决策，状态应置 done |
| percentile 列遗漏 | 已显式建模 + 测试覆盖 + DB 列存在 | 风险已消解，状态应置 done |

**判定**：4 个场景虽状态字段为 `todo`，但本质均为"已知风险被设计性接受"的陈述，验证命令已确认其物理结果与描述一致。它们不是未完成的开发任务，不构成 release 门阻塞。**建议** implementer 将状态修正为 `done`/`waived`，但不阻塞本期验收。

### 维度 8：契约对齐 — ✅ 通过

- 模型类名 `StockDailyMarketData` / `StockMovingAverageData` / `StockStrengthScore` 与交接上下文及下游 plan-02/03 期望一致
- 表名 `stock_daily_market_data` / `stock_moving_average_data` / `stock_strength_scores` 与架构 §7.2 推荐最小 schema 一致
- 字段语义（stock_id 替代 entity_id、percentile 显式建模、个股死字段照搬）与 ADR-3 字段裁剪规则一致
- percentile 列存在保证下游 `ranking_service.py:91` setattr 与 `strength_snapshot_service.py:342/363` 写库不会因 DB 无列报错

### 维度 9：代码审查 — ✅ 无 blocker

**阻塞项**：无。

**建议项**（非阻塞，可在后续阶段优化）：

1. **测试 DB 连接复用**：`test_stock_models.py` 的 `db_session` fixture 每个 schema 用 `create_engine` 创建了 3 个 engine（admin + main + cleanup），略重。可改为单 engine 复用，减少连接开销。属可维护性建议。
2. **percentile 列注释**：模型 `percentile` 列有 `comment='百分位'`，迁移亦有 comment，一致性良好。建议在 comment 中补充 ADR-3 引用（"ADR-3：ranking_service 写个股排名时需要"），便于后人理解为何模型层显式建模一个"看似死字段"的列。属可维护性建议。

### 维度 10：执行验证（task handler） — N/A

本功能不含异步 task handler。下游 plan-02 将引入 collector 写入路径，届时由其 task-review 负责维度 10 执行验证。

---

## 三、验证命令输出摘要

| 命令 | 退出码 | 关键输出 |
| --- | --- | --- |
| `python -c "from src.models import ...; print('import ok')"` | 0 | import ok |
| `python -c "...percentile...assert..."` | 0 | percentile ok (10,4)；3 模型无 entity_type；SSS 无 period/板块字段；死字段保留；col counts 13/9/41 |
| `pytest tests/test_stock_models.py -v` | 0 | 17 passed, 10 warnings in 1.64s |
| `alembic current` | 0 | dd92f496dfaf (head) |
| `alembic downgrade -1` | 0 | → 687ec547d98e |
| `alembic upgrade head` | 0 | → dd92f496dfaf |
| information_schema 查询（downgrade 后） | 0 | 新表 []，旧表 3 表在 |
| information_schema 查询（upgrade 后） | 0 | 新表 13/9/41 列；percentile=numeric(10,4)；旧表 strength_scores 仍 46 列 |
| `alembic heads` | 0 | dd92f496dfaf（单一 head） |
| `git diff -- 旧三表模型` | 0 | 空（未修改） |
| `git diff -- src/models/__init__.py` | 0 | 仅追加 3 import + 3 `__all__` |

---

## 四、Blocker 列表

无。

## 五、建议项列表

1. （可维护性）将 4 个边界场景状态从 `todo` 修正为 `done`/`waived`（带原因）。
2. （可维护性）`test_stock_models.py` fixture 可复用单 engine 降低连接开销。
3. （可维护性）percentile 列 comment 补充 ADR-3 引用说明建模意图。

---

## 六、状态变更记录

| 项目 | 变更前 | 变更后 |
| --- | --- | --- |
| `plan-01-*.md` frontmatter `status` | `review` | `done` |
| README §7.2 开发状态机 | （由主 agent 维护，本 review 不修改） | — |

验收通过。plan-01 已完成基础设施交付，可解锁 plan-02（股票写入路径切换）。
