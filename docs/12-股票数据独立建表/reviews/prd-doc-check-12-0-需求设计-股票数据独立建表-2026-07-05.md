# 需求设计文档检查报告：股票数据独立建表

> 检查对象：`12-0-需求设计-股票数据独立建表.md`
> 检查日期：2026-07-05
> 检查依据：`.zcode/skills/prd-doc-check`（checklist.md / logic-quality-rules.md / report-template.md），权威 contract 以 `.claude/contracts/workflow-schema.json` 的 `prd.sections` 为准
> 风格对照：`docs/03-数据源替换Tushare/03-0-需求设计-数据源替换Tushare.md`（同属"纯后端/基础设施改造、无 UI 变化"类型）

---

## 一、总览

| 部分 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| 文档头部 | ✅ | 2 | 0 | 0 | 100% |
| 第一部分：需求分析与确认 | ⚠️ | 3 | 1 | 0 | 75% |
| 第二部分：用户旅程和用户故事 | ✅ | 6 | 0 | 0 | 100% |
| 第三部分：UX/UE 设计 | ⚠️ | 2 | 1 | 0 | 67% |
| 第四部分：验收标准 | ⚠️ | 2 | 1 | 0 | 67% |
| 第五部分：决策记录 | ✅ | 1 | 0 | 0 | 100% |
| **合计** | — | **16** | **3** | **0** | **84%** |

### 逻辑质检概要（详见第四章）

| 维度 | 状态 |
|------|------|
| L.1 路径连通性 | ✅ |
| L.2 断点检测 | ✅ |
| L.5 故事-旅程映射 | ⚠️ |

---

## 二、逐部分详细检查

### 文档头部

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 0.1 | 文档标题 | ✅ | "12-0 需求设计：股票数据独立建表"，含需求编号(12)、文档编号(0)、文档类型(需求设计)、需求名称，符合命名规范 |
| 0.2 | 文档说明 | ✅ | 含"产品核心目标"与"本需求定位"两段，并补充了相关文档（上游决策、后续架构文档） |

### 第一部分：需求分析与确认

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 1.1 | 项目背景 | ✅ | "产品当前状态"一句话概括 + 4 条具体问题（量级、字段语义、软关联、表结构演进）+ 本期目标一句话，结构完整 |
| 1.2 | 核心问题定义 | ✅ | 4 行表格，"问题 / 用户损失"两列齐备 |
| 1.3 | 关键设计决策 | ⚠️ | 汇总表四列齐全（决策维度/最终选择/核心理由/否决方案），未拆为独立对比表，符合"去重"要求；**扣分点**：决策与"否决方案"栏混入大量技术术语（"union 合并""实体类型""物理隔离""读路径""外键"），违反 Q.1，详见问题汇总 |
| 1.4 | 产品范围 | ⚠️ | "本期范围 / 明确不做"齐备，范围与决策记录未重复；**扣分点**：本期范围条目直接写"含完整字段、约束、索引""级联删除""缓存键命名""死代码 Repository 层的激活重写"等技术实现层内容，超出 PRD 边界 |

### 第二部分：用户旅程和用户故事

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 2.1 | 核心用户旅程 | ✅ | `text` 代码块线性主旅程 + 异常旅程子段落，结构正确，未拆成多小节，无独立异常矩阵 |
| 2.1.1 | 主旅程起点明确性 | ✅ | 起点"数据维护者触发个股数据采集"为用户可自然进入的起点 |
| 2.1.2 | 主旅程终点明确性 | ✅ | 终点"板块相关功能继续从原表读取，行为完全不变"为明确终态 |
| 2.1.3 | 异常旅程存在性 | ✅ | 含 2 个异常场景（采集失败、读路径新表无数据），描述充分 |
| 2.2 | 用户故事 | ✅ | 核心 + 延伸两档，全部符合"作为 X，我希望 Y，以便 Z"三段式 |
| 2.2.1 | 核心用户故事覆盖充足 | ✅ | 2 条核心 + 2 条延伸，对纯改造类需求覆盖合理 |
| 2.2.2 | 用户故事格式规范 | ✅ | 4 条均符合三段式 |

### 第三部分：UX/UE 设计

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 3.1 | 线框图与说明 | ⚠️ | 文档以"本需求无用户可见界面变化"开篇，与模板 3.1"必须包含 ASCII 线框图"的形式要求不一致。**注**：同类型参考文档 `03-0` 同样无线框图并以一句话声明跳过，属可接受的行业惯例，故判⚠️而非❌ |
| 3.2 | 关键状态 | ✅ | 三列表格（状态 / 用户可观察结果 / 用户下一步）齐备，3 行覆盖采集进行中、采集完成、板块功能使用 |
| 3.3 | 业务规则 | ✅ | 三条业务语言规则（数据隔离、行为不变、空窗期呈现），整体为业务表述 |

### 第四部分：验收标准

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 4.1 | 功能验收标准 | ✅ | AC-01 ~ AC-06，稳定编号，无数据指标/暂停放量/验证命令等过度工程内容 |
| 4.2 | AC 结构完整性 | ✅ | 每条 AC 均含"前置 / 动作 / 结果"三要素 |
| 4.3 | AC 覆盖完整性 | ⚠️ | 覆盖核心故事（隔离、读路径、字段语义、级联删除、API 契约）与板块零回归，但**未覆盖关键异常/恢复路径**：第二部分 2.1 异常旅程明确列出"读路径在新表无数据 → 表现为相应个股指标为空"的空窗期场景，且 3.3 业务规则也定义了"空窗期呈现规则"，却没有一条 AC 落地校验该空窗期呈现是否如约定（按既有"数据不足"方式呈现、前端无需新增降级逻辑） |

### 第五部分：决策记录

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| 5.1 | 决策记录 | ✅ | 五列齐全（决策日期/结论/首版目标/核心假设/下一步），未重复验收标准或范围，无"推进结论/下一阶段前置"等冗余段落 |

### 跨部分质量检查

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| Q.1 | 使用业务语言，无技术实现细节 | ⚠️ | 多处技术术语泄漏，详见问题汇总表 |
| Q.2 | 无重复内容 | ✅ | 范围、旅程、AC、决策各司其职，无重复列举 |
| Q.3 | 无过度工程 | ✅ | 无数据指标阈值、暂停放量、feature flag、合规底线等内容 |

---

## 三、问题汇总

### 缺失项（需补充）

| 编号 | 所属部分 | 缺失内容 | 修复建议 |
|------|---------|---------|---------|
| — | — | 无结构性缺失项 | — |

### 部分完成项（需完善）

| 编号 | 所属部分 | 当前状态 | 改进建议 |
|------|---------|---------|---------|
| 1.3 / 1.4 | 第一部分 | 决策表与产品范围混入技术实现层表述（实体类型、union、外键、索引、Repository 层等） | 将技术细节下沉到架构文档，PRD 侧改用业务/数据语义表述（见 Q.1 替换表） |
| 3.1 | 第三部分 | 无 ASCII 线框图，仅以"无界面变化"声明带过 | 与参考文档 03 一致，可保留现状；若想满分，可补一句"无页面状态变化，故无线框图"作为显式声明即可，**不强制** |
| 4.3 | 第四部分 | 缺少覆盖"空窗期/异常恢复路径"的 AC | 新增 1 条 AC（见下方建议） |

**新增 AC 建议（针对 4.3）**：
```
- AC-07：长周期指标空窗期呈现
  前置：股票专用表上线初期，部分个股数据尚未采集满
  动作：访问依赖长历史积累的个股指标（如 240 日均线）
  结果：相应指标呈现为空或"数据不足"，呈现方式与改造前首次上线未采集完成时一致，前端无需新增降级逻辑
```

### Q.1 技术术语替换建议

| 位置 | 泄漏内容 | 建议替换为 |
|------|---------|-----------|
| 1.1 项目背景 第3条（第24行） | "实体类型字段区分""索引体积与查询成本都受拖累" | "用同一个'数据类别'标记区分""查询时需要额外过滤，效率受影响" |
| 1.1 项目背景 第4条（第25行） | "共用表无法建立真实外键关系，只能在业务层用'实体类型+实体ID'软关联" | "两类数据共用一张表，无法保证数据间的关联完整性，只能靠程序代码临时拼接，存在脏数据风险" |
| 1.1 本期目标（第27行） | "落入独立的股票专用表""物理隔离" | "存入独立的股票数据存储""两类数据彻底分开" |
| 1.3 历史数据处理 行（第44行） | "读路径只读新表""读时合并新旧表""读路径复杂度永久提升" | "读取时只读股票新表""读取时同时合并新旧表""读取逻辑永久变复杂" |
| 1.3 读路径策略 行（第45行） | "股票读写只走新表""union 合并新旧表""违背隔离初衷" | "股票数据的读写都只走股票新表""读取时合并新旧表""违背分开存储的初衷" |
| 1.3 字段裁剪 行（第46行） | "去掉实体类型、废弃的周期字段" | "去掉'数据类别'标记和不再使用的周期相关字段" |
| 1.4 本期范围 第1条（第51行） | "含完整字段、约束、索引" | "包含个股所需的全部字段与数据校验规则"（约束/索引属于架构实现） |
| 1.4 本期范围 第3条（第53行） | "级联删除" | "随个股删除一并清理其相关数据" |
| 1.4 明确不做 第4条（第60行） | "缓存键命名调整" | "缓存命名调整"或干脆删除该条（缓存策略本属实现层） |
| 1.4 明确不做 第5条（第61行） | "死代码 Repository 层的激活重写" | 删除该条，或改为"暂不重写已废弃的数据访问代码"（"Repository 层"是明确的架构实现术语） |
| 2.1 主旅程（第73行） | "写入股票专用表""读路径从股票专用表读取" | "存入股票数据存储""从股票数据存储读取"（"表/读路径"可降级为"存储/读取"） |
| AC-04（第134行） | "股票专用表中对应个股数据被正确清理" | "对应个股的相关数据被正确清理"（其余描述已够清晰，"专用表"非必须） |
| AC-06（第142-143行） | "API 契约""响应字段名、类型、结构" | "对外接口约定""返回的字段名、类型、结构"（"API 契约"偏技术，但可接受；建议软化为"接口约定"） |

> 说明：作为"数据库结构改造"类需求，文档**不可避免**会涉及"表/字段"这类半业务半技术词汇，Q.1 不要求把它们全部删除。重点整改的是**纯实现层术语**：union、读路径、Repository 层、外键、索引体积、API 契约、缓存键命名、物理隔离等——这些应下沉到 `12-1-架构文档`。表格/字段名等"领域对象"层级词汇可酌情保留。

---

## 四、逻辑质检

> 前置门控：第二部分结构检查不为「缺失」，执行三维逻辑质检。

### 4.1 逻辑质检总览

| 维度 | 编号 | 状态 | 发现问题数 |
|------|------|------|-----------|
| 路径连通性 | L.1 | ✅ | 0 |
| 断点检测 | L.2 | ✅ | 0 |
| 故事-旅程映射 | L.5 | ⚠️ | 1（轻微） |

### 4.2 L.1 路径连通性详情

| 旅程名称 | 起点 | 终点 | 步骤数 | 状态 | 备注 |
|----------|------|------|--------|------|------|
| 主旅程（数据维护者视角） | 数据维护者触发个股数据采集 | 板块相关功能继续从原表读取，行为完全不变 | 4 | ✅ | 起点为用户可自然进入状态；终点"行为完全不变"为明确终态；相邻转换（写入新表 → 读新表 → 板块读原表）逻辑连贯 |
| 异常旅程①采集失败 | 采集触发 | 新表不写入，重试机制不变 | — | ✅ | 与改造前一致，路径闭合 |
| 异常旅程②读路径新表无数据 | 读路径访问 | 表现为指标为空，随采集积累恢复 | — | ✅ | 终态为"逐步恢复"，属可接受的恢复态描述 |

### 4.3 L.2 断点检测结果

未发现断点。

- 无非法终态：主/异常旅程终点均为终态或恢复态
- 无不可达步骤：所有步骤前置可从前序推导
- 无分支悬空：决策表中的"否决方案"虽是分支，但已注明否决理由，非悬空
- 无依赖断裂：旅程未引用未定义的前序产物

### 4.4 L.5 故事-旅程映射

> 文档未显式标注 P0/P1，按惯例将"核心用户故事"视作 P0，"延伸用户故事"视作 P1。

#### 核心用户故事覆盖表

| 用户故事 | 核心动作 | 覆盖的旅程步骤 | 覆盖状态 |
|----------|---------|--------------|---------|
| 核心 US-1：个股与板块数据分表存储 | 我希望个股数据和板块数据存储在各自独立的表中 | 主旅程 step1→2（写入股票新表）+ step4（板块继续读原表） | ✅ |
| 核心 US-2：个股数据准确完整 | 我希望个股的行情、均线、强度数据准确完整 | 主旅程 step2→3（写入/读取新表） | ✅ |

**延伸用户故事（P1，仅提醒级）**

| 用户故事 | 覆盖状态 |
|----------|---------|
| 延伸 US-3：板块功能行为不变 | ✅ 主旅程 step4 + AC-03 明确覆盖 |
| 延伸 US-4：个股表只含个股字段 | ⚠️ 旅程未直接体现，但 AC-05（字段语义收敛）已覆盖。建议在主旅程或关键状态中补一句"个股新表仅含个股相关字段"以形成显式映射，非阻塞 |

**覆盖率统计**：2/2 条核心用户故事已被旅程覆盖（100%）；延伸故事 1/2 在旅程中有显式映射。

#### 发现问题

| # | 旅程 | 步骤 | 类型 | 说明 | 建议 |
|---|------|------|------|------|------|
| 1 | 主旅程 | — | 映射弱关联（轻微） | 延伸 US-4"个股表只含个股字段"在旅程中无对应步骤，仅在 AC-05 体现，故事-旅程映射存在弱关联 | 可选：在主旅程 step1 后补一行"→ 个股新表只保留个股相关字段，板块专属字段不入新表"。非阻塞，仅作提醒 |

---

## 五、总结

- **整体完成度**：84%（16 通过 / 3 部分完成 / 0 缺失）
- **逻辑质检评分**：**A-（良好）** —— L.1、L.2 通过，L.5 仅 1 处轻微弱关联
- **整体评价**：
  - ⚠️ **需小幅补充后可进入下一阶段**
  - 5 大部分无 ❌，⚠️ 部分 3 个（略超"≤2 个"的可直接放行阈值），逻辑质检无 ❌ 维度。完成上述小修补后即可进入架构文档阶段。
- **重点关注（按优先级）**：
  1. **Q.1 技术术语下沉**（影响面最大）：1.3 决策表、1.4 产品范围中混入 union / 读路径 / Repository 层 / 外键 / 索引体积 / API 契约 / 缓存键 等实现层术语，应改写为业务/数据语义表述，技术细节留给 `12-1-架构文档`。
  2. **补 1 条空窗期 AC**：2.1 异常旅程与 3.3 业务规则都定义了"空窗期呈现规则"，但第四部分没有对应 AC 落地校验，建议新增 AC-07（见问题汇总）。
  3. **L.5 弱关联（可选）**：延伸 US-4 在旅程中无显式步骤，可在主旅程补一行映射，非阻塞。

> 备注：本报告遵循 skill 默认行为写入被检查文档同级 `reviews/` 子目录；如不需要留存文件，可删除。本次检查**未修改** PRD 原文。
