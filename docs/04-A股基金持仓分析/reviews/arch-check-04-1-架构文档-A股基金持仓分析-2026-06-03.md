# 架构文档检查报告：A 股基金持仓分析

> 检查对象：`04-1-架构文档-A股基金持仓分析.md`
> 需求基准：`04-0-需求设计-A股基金持仓分析.md`
> 检查日期：2026-06-03

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | 架构 frontmatter `input_documents` 唯一指向 `04-0-需求设计-A股基金持仓分析.md`，文件存在 | 无需处理 |
| Frontmatter contract | ✅ | 4 个必填字段齐全（`workflow_type=arch-gen` / `status=review_ready` / `input_documents` / `open_questions`），`status` 取值合法 | 无需处理 |
| `open_questions` 同步性 | ✅ | `open_questions: []` 与正文 `5.x 待确认问题: 无` 同步 | 无需处理 |
| 实现影响的未决问题 | ✅ | 无未决问题 | 无需处理 |

> 阻塞检查全部通过，可进入后续维度评估。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6/6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3/3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5/5 | 0 | 0 | 100% |
| 3. 关键架构决策（ADR） | ✅ | 6/6 | 0 | 0 | 100% |
| 4. 用户流程与状态 | ✅ | 3/3 | 0 | 0 | 100% |
| 5. 系统上下文与模块职责 | ✅ | 3/3 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3/3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6/6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6/6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2/2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2/2 | 0 | 0 | 100% |
| **合计** | — | **45/45** | **0** | **0** | **100%** |

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖（6 项 P0 功能） | 6 | 0 | 0 | 100% |
| 3. 用户故事满足（6 个故事） | 6 | 0 | 0 | 100% |
| 4. 业务规则执行（10 条规则） | 10 | 0 | 0 | 100% |
| 5. 异常与边界覆盖（3 个异常旅程） | 3 | 0 | 0 | 100% |
| 6. 成功标准可达性（7 条 AC） | 7 | 0 | 0 | 100% |
| 7. 风险与降级对齐（3 风险 + 3 降级） | 6 | 0 | 0 | 100% |
| 8. UX 设计支撑（6 个交互） | 6 | 0 | 0 | 100% |
| 9. ADR 需求溯源（6 条 ADR） | 6 | 0 | 0 | 100% |
| 10. 范围合规性（8 个不做项） | 8 | 0 | 0 | 100% |
| 11. 一致性检查（4 子项） | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 7（均为合理） | 0 | 0 | 100% |
| **合计** | **69** | **0** | **0** | **100%** |

## 三、结构完整性详细检查

全部章节均为 `✅`，关键证据摘要如下：

- **Frontmatter**：`workflow_type: arch-gen`、`status: review_ready`、`input_documents` 唯一指向 PRD、`open_questions: []` 与正文 `5.x 待确认问题: 无` 同步。
- **系统摘要**：1.1 概括系统能力；1.2 核心闭环 `Fund → Portfolio → Query`（≤5 词）；1.3 首版 MVP 定位明确。
- **范围/非目标/成功标准**：§2.1 6 项 P0 功能；§2.2 8 项明确不做；§2.3 6 条成功标准（功能项定性、性能项数值化）；§2.4 AC-01~AC-07 承接矩阵完整。
- **ADR**：ADR-1~ADR-6 共 6 条（在 4-8 范围内），每条含「选择/理由/风险与对策/演进余地」四要素。
- **用户流程与状态**：§3.1 三主流程；§3.2 5 个关键分支；§3.3 状态机 mermaid 图（AsyncTask 复用）。
- **系统上下文与模块职责**：§4.1 数据流向图；§4.2 6 个模块表（在 ≤6 临界）；§4.3 6 条「避免过度设计」说明。
- **运行链路**：6.1~6.4 四条链路，每条含实现原则段落，覆盖所有 P0 功能端到端。
- **领域对象与契约**：§7.2 Schema（嵌套 ≤2 层）；§7.3 6 个 API 端点（在 ≤10 范围）；§7.4 状态流转引用 §3.3。
- **非功能/风险/运行策略**：§8.1 性能目标数值化合理；§8.2 降级 L1-L4；§8.3 安全 4 项；§8.4 成本 3 项；§8.5 可观测性；§8.6 风险 5 条。
- **实施方案**：Phase A/B/C 分阶段，每 Phase 含文件路径 + 验证目标。
- **架构结论**：4 条核心判断 + 演进方向。

**质量检查项**：
- Q.1 实施细节：§9 含文件路径是实施方案必要内容，未堆砌 DDL/环境变量 ✅
- Q.2 重复内容：未发现显著重复 ✅
- Q.3 一致性：详见维度 11 ✅
- Q.4 量化约束：ADR=6（合规）/模块=6（临界合规）/API=6（合规）/Schema 嵌套≤2（合规） ✅
- Q.5 frontmatter/正文一致：open_questions、input_documents 均同步 ✅

## 四、需求满足度详细检查

### 维度 2：范围覆盖

| PRD 需求 | 架构覆盖 | 状态 | 证据 |
|----------|---------|------|------|
| F-01 基金基本信息库 | ✅ | §2.1 + §7.2 Fund Schema（11 字段）+ §6.1 列表 |
| F-02 基金列表与搜索 | ✅ | §6.1（search 前缀+包含，market/fundType 过滤，分页） |
| F-03 基金详情页 | ✅ | §6.2（基本信息 + 最新持仓 + LEFT JOIN 取名） |
| F-04 股票反查 | ✅ | §6.3（symbol → MAX report_period + 1% 阈值 + 排序） |
| F-05 管理端采集入口 | ✅ | §6.4（admin API → AsyncTask → TaskHandler） |
| F-06 数据缺失透明呈现 | ✅ | §3.2 分支 + §6.2 step 6-7 + §8.2 L1 降级 |

### 维度 3：用户故事满足

| 用户故事 | 架构流程/链路 | 状态 | 证据 |
|----------|-------------|------|------|
| US-01 搜索基金看持仓 | 流程 A + 6.1/6.2 | ✅ | 完整操作路径均覆盖 |
| US-02 股票反查 | 流程 B + 6.3 | ✅ | 完整操作路径均覆盖 |
| US-03 场内/场外+类型过滤 | 6.1 | ✅ | market(E/O) + fundType 多选参数 |
| US-04 管理员一键同步 | 流程 C + 6.4 | ✅ | 手动同步 + 进度展示 + 统计 |
| US-05 列表关键信息 | 7.2 Schema + 6.1 | ✅ | name/management/fundType/benchmark/foundDate 字段齐全 |
| US-06 反查按占净值比降序 | 6.3 | ✅ | "按 stk_mkv_ratio DESC 排序" |

### 维度 4：业务规则执行

| 业务规则 | 执行机制 | 状态 | 证据 |
|----------|---------|------|------|
| BR-01 搜索匹配 | 6.1 实现原则：ts_code 前缀 OR name 包含 | ✅ | 后端 WHERE 子句强制 |
| BR-02 过滤 AND | 6.1：market/fundType 精确匹配，AND 组合 | ✅ | 后端强制 |
| BR-03 排序 | 6.1 ts_code 升序 / 6.2-6.3 stk_mkv_ratio DESC | ✅ | ORDER BY 强制 |
| BR-04 前 20 条 | 6.2：分页 pageSize=20 默认 | ✅ | API 分页参数 |
| BR-05 仅最新一期 | 6.2：子查询 MAX(report_period) | ✅ | DB 子查询强制 |
| BR-06 数据缺失标注 | 3.2 + 6.2 + 8.2 L1 | ✅ | 三处呼应 |
| BR-07 反查阈值 1% | 6.3：stk_mkv_ratio >= 1.0 在后端 SQL | ✅ | 后端强制 |
| BR-08 同步权限 | 8.3：管理员 RBAC | ✅ | 复用 RBAC |
| BR-09 货币/单位 | 7.6 术语映射 + Schema 注释 | ✅ | 单位在字段定义中标注 |
| BR-10 跟踪标的 | 7.2 Fund.benchmark + 7.6 术语映射 | ✅ | benchmark 字段映射 |

### 维度 5：异常与边界覆盖

| PRD 异常场景 | 架构错误处理 | 状态 | 证据 |
|-------------|-------------|------|------|
| J-04 数据缺失（基金无持仓） | §3.2 分支 + §6.2 step 6 + §8.2 L1 | ✅ | 触发条件→处理→用户操作三要素齐全 |
| J-05 同步失败 | §3.2 分支 + §6.4 step 8 + §8.2 L3 | ✅ | 弹窗+保留旧数据+error_message 记录 |
| J-06 股票无任何基金重仓 | §3.2 分支 + §6.3 step 6 | ✅ | 空结果前端文案 |

### 维度 6：成功标准可达性

| AC | 架构承接 | 状态 | 证据 |
|----|---------|------|------|
| AC-01 列表展示与搜索 | §2.4 矩阵 + §6.1 | ✅ | 搜索词、字段、列表项均覆盖 |
| AC-02 过滤（市场+类型） | §2.4 矩阵 + §6.1 | ✅ | AND 关系在 WHERE 子句 |
| AC-03 详情页最新持仓 | §2.4 矩阵 + §6.2 | ✅ | 最新报告期 + 排序 + 字段列 |
| AC-04 股票反查 | §2.4 矩阵 + §6.3 | ✅ | 1% 阈值 + 排序 + 字段列 |
| AC-05 数据缺失透明 | §2.4 矩阵 + §3.2 + §6.2 | ✅ | 信息卡 + 标注文案 + 返回按钮 |
| AC-06 管理员手动同步 | §2.4 矩阵 + §6.4 | ✅ | 异步任务 + 统计 + 同步记录表 |
| AC-07 同步失败可见 | §2.4 矩阵 + §6.4 step 8 + §8.2 L3 | ✅ | 弹窗 + 错误原因 + 旧数据保留 |

**性能目标**（PRD §2.3 vs 架构 §8.1）：
- 列表 < 2s：架构 < 2s ✅
- 持仓 < 3s：架构 < 3s ✅
- 同步任务：架构补充了 < 10 分钟（基本信息）/ < 30 分钟（持仓）的额外性能目标，PRD 未给出具体数值，属于合理补强 ✅

### 维度 7：风险与降级对齐

| PRD 风险/降级 | 架构对应 | 状态 | 证据 |
|--------------|---------|------|------|
| R-01 Tushare 收录不全 | §8.6 + L1 降级 | ✅ | 风险表 + 降级策略 |
| R-02 Tushare 积分要求 | §8.6 + ADR-1 对策 | ✅ | 管理端提前检测提示 |
| R-03 反查阈值合理性 | §6.3 阈值 + ADR | ✅ | 阈值在后端 SQL |
| D-01 数据缺失标注 | §8.2 L1 | ✅ | 列表标注 + 详情页提示 |
| D-02 同步失败 | §8.2 L3 | ✅ | 弹窗 + 保留旧数据 |
| D-03 股票无基金重仓 | §3.2 + §6.3 | ✅ | 前端空状态文案 |

### 维度 8：UX 设计支撑

| PRD UX 交互 | 架构状态/接口 | 状态 | 证据 |
|------------|-------------|------|------|
| 列表搜索 + 过滤 | 6.1 + 7.3 | ✅ | 搜索+过滤参数+API 端点 |
| 反查入口（顶部搜索框） | 6.3 + Phase C 页面 | ✅ | reverse-lookup 端点 + 页面规划 |
| 详情页信息卡+持仓表 | 6.2 + Phase C 组件 | ✅ | 详情页端点 + 组件规划 |
| 管理端同步面板+进度 | 6.4 + Phase C | ✅ | admin API + FundSyncPanel |
| 同步中按钮禁用 | 3.1 流程 C step 4 | ✅ | 流程描述明确 |
| 同步失败弹窗 | 3.1 流程 C + 6.4 | ✅ | 错误提示在前端响应处理 |

### 维度 9：ADR 需求溯源

| ADR | 溯源需求 | 判定 | 说明 |
|-----|---------|------|------|
| ADR-1 数据源 Tushare | 1.3 决策 + 5 决策记录 | 需求驱动 | PRD §1.3、§5 决策记录第二行明确 |
| ADR-2 两表平铺 | F-01/F-03/F-04 | 需求驱动 | 服务范围 6 项功能 |
| ADR-3 复用异步任务框架 | F-05 + §1.3 数据采集方式 | 需求驱动 | MVP 手动 + 复用决策 |
| ADR-4 持仓按报告期全量入库 | F-03/F-04 + BR-05 | 需求驱动 | 持仓数据快照语义 |
| ADR-5 占净值比近似 | 范围 P0 持仓展示 + BR-07 | 合理扩展 | MVP 权衡，Tushare 字段限制 |
| ADR-6 前端独立路由 | US-01/US-02 + UX 设计 | 需求驱动 | 独立模块定位 |

### 维度 10：范围合规性

| PRD 不做项 | 架构出现情况 | 状态 |
|-----------|-------------|------|
| NF-01 实时净值/日K | §2.2 明确不做 | ✅ |
| NF-02 持仓变动提醒 | §2.2 明确不做 | ✅ |
| NF-03 历史多期趋势图 | §2.2 明确不做 | ✅ |
| NF-04 基金经理分析 | §2.2 明确不做 | ✅ |
| NF-05 持仓行业聚合 | §2.2 明确不做 | ✅ |
| NF-06 自动化定时同步 | §2.2 明确不做 | ✅ |
| NF-07 自选基金/股票 | §2.2 明确不做 | ✅ |
| NF-08 第三方登录/社交 | §2.2 明确不做 | ✅ |

### 维度 11：一致性检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | 6 条 ADR 各自独立，无矛盾（数据源/表/任务/同步/字段/路由） |
| Schema 与 API 一致性 | ✅ | §7.2 Schema 字段（tsCode/name/management/...）与 §7.3 API 端点参数（search/market/fundType/...）对齐 |
| 状态机与流程一致 | ✅ | §3.3 状态机（pending→running→completed/failed/cancelled）与 §3.1 流程 C、§6.4 步骤描述一致 |
| 范围与设计一致 | ✅ | §2.1 范围 6 项均映射到具体 ADR 或运行链路 |

### 维度 13：过度设计检测

| 架构设计 | 对应需求 | 判定 | 说明 |
|----------|---------|------|------|
| 不引入独立缓存层 | 全部 | 合理扩展 | 季度数据查询频率低 |
| 不引入消息队列 | 全部 | 合理扩展 | 复用 AsyncTask 足够 |
| 不引入净值计算引擎 | 全部 | 合理扩展 | MVP 范围外 |
| 不引入 AI 模块 | 全部 | 合理扩展 | 本需求纯数据展示 |
| 不引入独立快照表 | 全部 | 合理扩展 | 持仓直接存 fund_portfolio |
| 不引入基金分类服务 | 全部 | 合理扩展 | 直接用 Tushare fund_type |
| 6 个模块、6 个 API 端点 | 全部 | 合理 | 处于 Q.4 量化约束范围内 |

## 五、深度质量检查

### 维度 14：闭环完整性

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| 6.1 基金列表 | ✅ | 无 | 6 步：HTTP→API→Repository→DB→响应→渲染。`funds.ts_code` 唯一索引 + `name` 索引（建议实施时确认 LIKE 查询索引方案） |
| 6.2 基金详情页 | ✅ | 无 | 并行 2 请求；MAX(report_period) 子查询；LEFT JOIN stocks 取名（兜底 NULL→"—"） |
| 6.3 股票反查 | ✅ | 无 | stocks 表验证 symbol；MAX(report_period) 子查询；1% 阈值 SQL 过滤；`fund_portfolio(stock_symbol, report_period)` 索引支撑 |
| 6.4 管理员同步 | ✅ | 无 | admin API → AsyncTask → TaskExecutor → FundTaskHandler → Tushare → upsert/insert+delete → 统计写 result；进度通过 progress callback |

### 维度 15：安全底线

| PRD 合规底线 | 架构安全设计 | 状态 | 证据 |
|-------------|-------------|------|------|
| C-01 数据源沿用 Tushare（项目 03 决策） | ADR-1 | ✅ | 明确选择 Tushare，不引入爬虫 |
| C-02 同步入口仅管理员 | §8.3 | ✅ | 复用 RBAC |
| C-03 Tushare Token 不暴露前端 | §8.3 | ✅ | 仅服务端持有 |
| C-04 SQL 注入防护 | §8.3 | ✅ | SQLAlchemy ORM 参数化 |
| C-05 API 认证 | §8.3 | ✅ | 复用 JWT 认证 |

### 维度 16：复用可行性验证

| 复用声明 | 数据源 | 写入目标 | 前置依赖 | 判定 |
|---------|--------|---------|---------|------|
| AsyncTask 框架（TaskRegistry + TaskExecutor） | AsyncTask 表 | AsyncTask 状态 | 无（已有） | ✅ 可行 |
| TushareDataSource 基类（`_enforce_rate_limit` / `_execute_with_retry`） | Tushare pro API | 无（只读） | Tushare token | ✅ 可行 |
| BaseRepository | SQLAlchemy 模型 | 对应表 | AsyncSession | ✅ 可行 |

**潜在前置依赖（⚠️ 需补强）**：

链路 6.3 步骤 2 描述"先查询 stocks 表确认股票代码有效，获取 stock_name"。架构文档 §4.2 复用声明未显式验证 **stocks 表已覆盖所有 A 股股票**这一前置依赖。虽然 §8.6 风险表提到了"股票代码格式不一致"（已涵盖 format 问题），但未覆盖"stocks 表覆盖度"。这属于合理补强点，详见 §六 修复建议。

## 六、问题汇总与修复建议

### 前置阻塞项

无。

### 未满足的需求

无（所有 P0 需求均为 ✅）。

### 部分满足的需求（中优先级）

| 编号 | 维度 | 需求/观察 | 当前状态 | 完善建议 |
|------|------|---------|---------|---------|
| 1 | 维度 16 复用可行性 | 链路 6.3 依赖 `stocks` 表 JOIN 取名，架构未显式验证 stocks 表覆盖度 | §8.6 风险表仅覆盖"格式不一致"，未覆盖"覆盖度" | 在 §4.2 模块职责"FundRepository 反查方法"或 §8.6 风险表中补充：「前置依赖：stocks 表已覆盖 A 股全市场股票代码（依赖项目 03 的 stock_basic 同步），未覆盖的股票反查会得到 stock_name 为 NULL 但仍能反查持仓」 |

### 存疑设计（需评估）

无。

### 可选优化建议（低优先级，非阻塞）

| 编号 | 章节 | 建议 |
|------|------|------|
| 1 | §6.1 实现原则 | 建议明确"名称包含"是否使用不区分大小写（PRD BR-01 写明"不区分大小写"，但架构未强调） |
| 2 | §6.2 | 建议在"实现原则"中说明"最新一期"在「该基金无任何持仓」时如何回退——当前仅说明返回空列表，未说明无任何数据时前端行为是否区分"无任何持仓"与"该期未披露"（PRD 3.2 状态表对此有区分"暂无持仓数据"统一文案） |
| 3 | §8.5 可观测性 | 建议补充前端错误监控埋点方案（MVP 可暂不实现） |

---

## 八、修复记录

**修复日期**：2026-06-03

以下 3 项建议已在架构文档中修复，原检查建议全部关闭：

| 编号 | 修复位置 | 修复内容 |
|------|---------|---------|
| 1 | 架构 §4.2 复用声明后追加"外部表依赖"小节 | 明确 `stocks` 表覆盖度前置依赖（依赖项目 03 的 stock_basic 同步），未覆盖股票在反查时 `stock_name` 为 NULL 不影响核心功能，详见 §8.6 风险表 |
| 2 | 架构 §6.1 实现原则 | 明确"search 参数匹配 ts_code 和 name 时**均不区分大小写**（使用 ILIKE 或 LOWER() 统一处理）" |
| 3 | 架构 §6.2 实现原则 | 区分两种"无持仓"场景：场景 A（Tushare 未收录）返回 `isPortfolioEmpty: true`，场景 B（最新期未披露）返回 `latestPeriodExists: false`，Repository 返回空列表时附带字段供前端差异化展示 |

**修复后状态**：
- 维度 16 复用可行性：100%（4/4，含 stocks 覆盖度显式声明）
- 维度 6 成功标准可达性：100%（7/7 AC 完整覆盖）
- 整体评价保持：**✅ 架构充分满足需求，可进入开发计划阶段**

## 七、总结

- **结构完整度**：100%（45/45 检查项通过）
- **需求满足度**：100%（69/69 需求项通过）
- **前置阻塞**：无
- **关键维度（6/11/14/15/16）**：
  - 维度 6 成功标准可达性：100%（7/7 AC 全部承接，无语义漂移）
  - 维度 11 一致性：100%（4/4 子项）
  - 维度 14 闭环完整性：100%（4 条链路全部可走通）
  - 维度 15 安全底线：100%（5/5 合规底线有对应设计）
  - 维度 16 复用可行性：3/3 复用声明已验证 + 1 项补强建议
- **整体评价**：✅ **架构充分满足需求，可进入开发计划阶段**
  - 范围内所有 P0 需求均有充分架构覆盖（✅/✅/✅/✅/✅/✅）
  - 关键维度无 ❌ 项
  - 仅有 1 项非关键的 `⚠️` 部分满足建议（stocks 表覆盖度前置依赖未显式声明）
  - 3 项可选低优先级优化建议，不影响进入下一阶段
- **优先修复建议**：
  1. （可选）在 §4.2 或 §8.6 补充 stocks 表覆盖度前置依赖说明
  2. （可选）在 §6.1 实现原则中明确搜索"不区分大小写"
  3. （可选）在 §6.2 区分"无任何持仓"与"该期未披露"的前端处理差异
