# 架构文档检查报告：板块成分股列表

> 检查对象：`11-1-架构文档-板块成分股列表.md`
> 需求基准：`11-0-需求设计-板块成分股列表.md`
> 检查日期：2026-07-05

## 一、阻塞检查

| 项目 | 状态 | 结论 | 处理建议 |
|------|------|------|---------|
| PRD / 架构配对可靠性 | ✅ | 架构 frontmatter `input_documents` 唯一指向 `docs/11-板块成分股列表/11-0-需求设计-板块成分股列表.md`，文件存在，配对可靠 | — |
| Frontmatter contract | ✅ | 含全部 required_frontmatter_fields（workflow_type=arch-gen / status=review_ready / input_documents / open_questions），status 取值合法（wip/review_ready/done 之一） | — |
| `open_questions` 同步性 | ✅ | frontmatter `open_questions: []` 为空；正文 §5.x 待确认问题写"无。Q1 已决策：…"，已收敛为决策记录，二者同步 | — |
| 实现影响的未决问题 | ✅ | 唯一历史未决项 Q1（getStock 返回字段与落地页渲染）已在 §5.x 明确决策并附后端代码锚点（stocks.py:201-212），不再改变模块边界/API/Schema/状态机/降级/验收口径 | — |

> 四项全部通过，无前置阻塞。

## 二、总览

### 2.1 结构完整性

| 章节 | 状态 | 通过项 | 部分完成 | 缺失项 | 完成度 |
|------|------|--------|----------|--------|--------|
| Frontmatter | ✅ | 6 | 0 | 0 | 100% |
| 1. 系统摘要 | ✅ | 3 | 0 | 0 | 100% |
| 2. 范围、非目标与成功标准 | ✅ | 5 | 0 | 0 | 100% |
| 5. 关键架构决策（ADR） | ✅ | 6 | 0 | 0 | 100% |
| 3. 用户流程与状态 | ✅ | 3 | 0 | 0 | 100% |
| 4. 系统上下文与模块职责 | ✅ | 3 | 0 | 0 | 100% |
| 6. 运行链路 | ✅ | 3 | 0 | 0 | 100% |
| 7. 领域对象与关键契约 | ✅ | 6 | 0 | 0 | 100% |
| 8. 非功能需求、风险与运行策略 | ✅ | 6 | 0 | 0 | 100% |
| 9. 实施建议与技术选型 | ✅ | 2 | 0 | 0 | 100% |
| 10. 架构结论 | ✅ | 2 | 0 | 0 | 100% |
| **合计** | — | **45** | **0** | **0** | **100%** |

### 2.2 需求满足度

| 维度 | 满足 | 部分满足 | 未满足 | 完成度 |
|------|------|---------|--------|--------|
| 2. 范围覆盖 | 7 | 0 | 0 | 100% |
| 3. 用户故事满足 | 5 | 0 | 0 | 100% |
| 4. 业务规则执行 | 6 | 0 | 0 | 100% |
| 5. 异常与边界覆盖 | 3 | 0 | 0 | 100% |
| 6. 成功标准可达性 | 7 | 0 | 0 | 100% |
| 7. 风险与降级对齐 | 4 | 0 | 0 | 100% |
| 8. UX 设计支撑 | 5 | 0 | 0 | 100% |
| 9. ADR 需求溯源 | 5 | 0 | 0 | 100% |
| 10. 范围合规性 | 5 | 0 | 0 | 100% |
| 11. 一致性检查 | 4 | 0 | 0 | 100% |
| 13. 过度设计检测 | 0 | 0 | 0 | 100%（无过度设计） |
| **合计** | **51** | **0** | **0** | **100%** |

## 三、结构完整性详细检查

### Frontmatter（全绿，6/6）

F.1 workflow_type=arch-gen ✅；F.2 status=review_ready（合法取值）✅；F.3/F.5 input_documents 唯一指向现存 PRD ✅；F.4 open_questions 为空数组 ✅；F.6 与正文 §5.x 决策记录同步 ✅。

### 1. 系统摘要（全绿，3/3）

1.1 核心能力描述（3 句、约 130 字，<200）✅；1.2 核心闭环锚点"**Sector → Stocks → Stock**"（3 词，≤5）✅；1.3 首版定位明确（纯前端接入 + 补齐个股分析页最小落地页）✅。

### 2. 范围、非目标与成功标准（全绿，5/5）

2.1 范围 7 项与 PRD §1.4 一一对应 ✅；2.2 明确不做 6 项完整承接 PRD §1.4 不做列表 ✅；2.3 成功标准 5 项中 4 项功能性用定性描述、1 项响应时间用数值（<500ms），分类正确 ✅；2.4 AC 承接矩阵逐条承接 AC-01~AC-07（AC-ID + PRD 原文摘要 + 承接模块 + 链路/状态 + 风险/降级），未改写原意 ✅；2.5 隐含于 2.4 ✅。

### 5. 关键架构决策（ADR）（全绿，6/6）

5.1 五条 ADR 均有编号 ADR-1~5 与描述性标题 ✅；5.2 每条明确"选择了什么" ✅；5.3 每条含"为什么这样选"+"为什么不用更重方案"（如 ADR-3 不用通用 FilterTable、ADR-1 不本地排序）✅；5.4 每条有风险与对策或演进余地 ✅；5.5 数量 5 条（在 4-8 区间），每条正文≤5 行 ✅；5.6 待确认问题已收敛为"Q1 已决策"一行记录，未删除章节 ✅。

### 3. 用户流程与状态（全绿，3/3）

3.1 主流程端到端覆盖（进入→并行加载→渲染→排序/分页/点击→跳转）✅；3.2 关键分支表 5 行（排序/分页/加载失败/空数据/下钻），含入口+架构处理方式 ✅；3.3 状态机 mermaid（Loading/Success/Empty/Error 四态 + 转换）附关键规则 ✅。

### 4. 系统上下文与模块职责（全绿，3/3）

4.1 系统上下文 mermaid 框图含系统边界、依赖、数据流 ✅；4.2 模块职责表 6 个模块（≤6），含职责+上游输入+下游输出 ✅；4.3 明确列出 5 项刻意避免的过度设计（二级筛选/虚拟滚动/趋势图框架/新增后端表/本地排序缓存）✅。

### 6. 运行链路（全绿，3/3）

6.1 两条链路 L1/L2 均编号步骤（L1 六步、L2 四步）✅；6.2 每条附"实现原则"段落（L1 四条、L2 两条）✅；6.3 覆盖全部 P0 功能（成分股加载/排序/分页/三态/下钻）✅。

### 7. 领域对象与关键契约（全绿，6/6）

7.1 核心对象表 4 项（对象名+SoT+Owner+用途）✅；7.2 最小 Schema（SectorStockItem/SectorStocksData/SectorStocksResponse/SectorStocksTableState），嵌套≤2 层 ✅；7.3 API 边界表 2 个接口 + 契约约定（响应包裹/命名边界/序列化/解包）✅；7.4 状态流转指向 §3.3，说明无后端状态机 ✅；7.5 数据边界三层（DB/接口/浏览器）✅；7.6 命名与标识规则（id vs symbol、白名单、trend 映射、市值简写）✅。

### 8. 非功能需求、风险与运行策略（全绿，6/6）

8.1 性能目标表（成分股<500ms / 个股<500ms）✅；8.2 错误处理与降级按影响分级（空态>排序分页失败>整块失败）✅；8.3 安全策略表 3 项（鉴权/sort_by 注入防护/越权）✅；8.4 成本控制（无新增计费）✅；8.5 可观测性（复用 console.error + 既有日志）✅；8.6 主要风险表 3 项含缓解方式 ✅。

### 9. 实施建议与技术选型（全绿，2/2）

9.1 Phase A/B/C 三阶段，每条明确到文件路径 + 具体实现内容（api.ts:191 / types / hook / 组件 / page.tsx:313 / stock-analysis 落地页）✅；9.2 每 Phase 末尾有独立验证目标（Phase A 类型可引用、Phase B AC-01~06、Phase C AC-07）✅。

### 10. 架构结论（全绿，2/2）

10.1 核心判断三点（后端零改动/独立性优先/复用而非重造）+ 设计原则 ✅；10.2 演进方向明确（通用表格组件可后续抽取、个股分析页可扩展、本期预留路由入口）✅。

### 质量检查项（全绿，5/5）

| 编号 | 检查项 | 状态 | 备注 |
|------|--------|------|------|
| Q.1 | 无实施细节 | ✅ | 无 DDL/目录树/环境变量/代码片段；§9 文件路径属允许的"文件路径"；§7.2 Schema interface 属明确例外 |
| Q.2 | 无重复内容 | ✅ | 三态处理在 §3.2/§8.2 各有侧重（状态机视角 vs 降级分级视角），不视为重复 |
| Q.3 | 一致性 | ✅ | ADR 间无矛盾（ADR-1 后端驱动 ↔ ADR-4 参数对齐 ↔ ADR-2 独立 hook 互为支撑）；Schema 与 API 对齐；状态机与流程一致 |
| Q.4 | 量化约束 | ✅ | ADR 5 条（4-8 ✓）；模块 6 个（≤6 ✓）；API 2 个（≤10 ✓）；Schema 嵌套≤2 层 ✓ |
| Q.5 | frontmatter 与正文一致 | ✅ | input_documents/open_questions/status 均与正文及 §5.x 一致 |

## 四、需求满足度详细检查

### 维度 2：范围覆盖（全绿，7/7）

PRD §1.4 的 7 项 P0 功能（成分股区块/六列/排序/分页/三态/跳转/个股落地页）在架构中均有明确 ADR + 链路 + 模块覆盖，不仅出现在范围列表：成分股区块→§4.2 模块职责 + §6.1 L1；六列→§7.2 Schema；排序/分页→ADR-1 + §6.1；三态→§3.3 状态机 + §8.2；跳转/落地页→ADR-5 + §6.2 L2。

### 维度 3：用户故事满足（全绿，5/5）

3 条核心故事（看成分股/排序/下钻）+ 2 条延伸故事（分页/加载状态）拆解的操作步骤，在 §3.1 主流程与 §6.1 L1、§6.2 L2 均有完整路径与状态/API 支撑。

### 维度 4：业务规则执行（全绿，6/6）

逐条核对 PRD §3.3 业务规则与架构执行机制（区分"提到"与"执行方案"）：

| 业务规则 | 执行机制 | 状态 | 证据 |
|----------|---------|------|------|
| BR 排序规则（初始强度降序/可排序列强度+市值/升降序切换/箭头指示） | 后端 sort_by/sort_order 透传 + 前端白名单锁定；§6.1 实现原则规定切换排序列重置 page=1 | ✅ | ADR-1、§6.1 实现原则、§8.3 sort_by 注入防护（前端白名单 strength_score/market_cap + 后端 getattr 兜底） |
| BR 分页规则（默认20/可切50,100/分页器展示/不足一页不显示分页器但显示总数/翻页滚动顶部） | page/page_size 透传后端；§7.2 SectorStocksTableState.page_size 字面量联合 `20\|50\|100` 类型强制；§3.2 翻页滚动到区块顶部 | ✅ | §7.2 Schema、§6.1 实现原则、§3.2 关键分支 |
| BR 数据呈现-趋势红涨绿跌 | §7.6 命名规则明确 trend_direction 映射 + UI 渲染为红▲/绿▼/灰▬（A 股红涨绿跌惯例） | ✅ | §7.6 |
| BR 数据呈现-市值量级简写 | §7.6 明确"万亿/亿，前端按量级格式化展示" | ✅ | §7.6 |
| BR 数据呈现-最新价两位小数/强度分整数 | §7.2 Schema 字段为 number（含 null）；具体小数位属 UI 渲染细节，架构正确未在契约层固化（保留实现弹性） | ✅ | §7.2（渲染精度留实现层合理） |
| BR 数据呈现-总数始终显示 | §3.1 主流程"显示总数"；§3.2 空数据分支"不渲染表格/分页器"但 §6.1 步骤6 仅 total=0 才走空态——总数在表格场景始终显示；§2.4 AC-01/AC-04 承接"显示总数" | ✅ | §3.1、§2.4 承接矩阵 |
| BR 跳转规则（用代码不用symbol/表头分页器不触发跳转） | ADR-5 + §7.6 明确"下钻跳转用 id 不用 symbol（后端 isdigit 校验）"；§6.2 L2 步骤1"非表头非分页器" | ✅ | ADR-5、§6.2、§7.6（注：PRD §3.3 跳转参数原文写"股票代码"，架构基于后端 isdigit 校验事实修正为"数据库主键 id"，见下注） |
| BR 降级呈现（成分股与图表独立/失败重试仅重载成分股） | ADR-2 独立 SWR hook；§8.2 成分股失败不影响图表；AC-05 承接"SWR mutate 重发独立于图表 hook" | ✅ | ADR-2、§8.2、§2.4 AC-05 |

> **跳转参数语义说明（非缺陷，已正确收敛）**：PRD §3.3 跳转规则原文"跳转参数使用股票代码"，但后端 `/stocks/{stock_id}` 实际以数据库 id 查询并 `isdigit()` 校验（stocks.py:170-172）。架构 Q1 决策据此将跳转参数修正为"成分股项的 `id`（数据库主键）"而非 symbol，并在 §7.6、§6.2、ADR-5 三处一致说明"不要用 symbol（会被 isdigit 拒绝）"。这是基于代码事实的必要修正，避免实现期踩坑，不构成对 PRD 的弱化。

### 维度 5：异常与边界覆盖（全绿，3/3）

PRD §2.1 异常旅程三场景均覆盖"触发→处理→用户后续"三要素：

| PRD 异常场景 | 架构处理 | 状态 | 证据 |
|-------------|---------|------|------|
| 成分股加载失败 | 触发：请求异常（§3.3 Error）；处理：失败态+重试按钮，独立 hook 不影响图表（§8.2）；后续：点击重试→mutate→L1（§3.2） | ✅ | §3.2、§3.3、§8.2、AC-05 |
| 板块无成分股 | 触发：total=0（§3.3 Empty）；处理：空态引导文案，不渲染表格/分页器（§3.2）；后续：引导数据管理页同步（文案提示，不强制跳转，与 PRD 一致） | ✅ | §3.2、§3.3、AC-06 |
| 个股分析页加载失败 | 触发：getStock 异常；处理：落地页页内失败态不回退（§6.2 实现原则、§8.2）；后续：页内呈现（PRD 明确本期不细化，与 PRD §2.1 一致） | ✅ | §6.2、§8.2 |

### 维度 6：成功标准可达性（全绿，7/7，关键维度）

逐条核对 AC 承接矩阵（§2.4）：

| AC | 架构承接 | 定性/数值 | 状态 | 证据 |
|----|---------|----------|------|------|
| AC-01 区块位置+降序+六列+总数 | SectorStocksTable+useSectorStocks，链路 L1→成功态；后端默认 strength_score desc | 定性 | ✅ | §2.4、§6.1 |
| AC-02 强度分升降序 | 排序状态切换→L1 重载；参数透传后端 | 定性 | ✅ | §2.4、§6.1 |
| AC-03 市值降序+不可排序列不响应 | sort_by 白名单（strength_score/market_cap）防任意穿透 | 定性 | ✅ | §2.4、§8.3 |
| AC-04 翻页+切每页条数+总页数更新+滚动顶部 | SectorStocksTable+Pagination，page_size 上限100（后端约束）；翻页滚动顶部 | 定性 | ✅ | §2.4、§6.1 |
| AC-05 失败重试+图表不受影响 | useSectorStocks SWR mutate 重发，独立于图表 hook | 定性 | ✅ | §2.4、ADR-2 |
| AC-06 无成分股空态不显示表格分页器 | 空态 total=0 不渲染表格/分页器 | 定性 | ✅ | §2.4、§3.2 |
| AC-07 点击跳转+落地页不空白 | StockAnalysisPage 新建，stocksApi.getStock 取数；失败页内处理 | 定性 | ✅ | §2.4、ADR-5、§6.2 |

功能性 AC 全部用定性描述（无百分比量化，符合规范）；性能指标（成分股<500ms）在 §2.3 与 §8.1 用数值量化。AC 原意均未被弱化或改写。

### 维度 7：风险与降级对齐（全绿，4/4）

PRD §1.3 关键设计决策含隐含风险（个股页未实现→跳转失败），架构 ADR-5 + §8.6 直接对应；PRD 降级（成分股与图表独立）→ ADR-2 + §8.2。架构 §8.6 风险表三项（getStock 字段不一致/旧 SectorStock 类型误导/sort_by 穿透）均与 PRD 场景对齐且有缓解。

### 维度 8：UX 设计支撑（全绿，5/5）

PRD §3.1/§3.2 五种交互（首次进入骨架屏/加载成功/排序切换/翻页滚动/三态）在架构 §3.3 状态机 + §6.1 实现原则 + §4.2 模块职责（SectorStocksTable 含三态/排序/分页/行点击）均有状态管理与接口支撑。

### 维度 9：ADR 需求溯源（全绿，5/5）

| ADR | 溯源需求 | 判定 | 说明 |
|-----|---------|------|------|
| ADR-1 后端驱动排序/分页 | PRD §1.3 列表初始排序/可排序列/分页方式 + §3.3 排序分页规则 | 需求驱动 | 直接满足排序分页需求，理由含"不用本地排序（与分页冲突）" |
| ADR-2 成分股与图表独立加载 | PRD AC-05 + §3.3 降级呈现规则 | 需求驱动 | 直接满足"互不阻塞"验收 |
| ADR-3 复用 CrowdRankingTable 范式 | PRD §1.3 分页方式（与基金扎堆排行榜风格一致） | 需求驱动 | 溯源到 PRD 显式提到的风格一致性 |
| ADR-4 修正 getSectorStocks 参数 | PRD 隐含（前端从未真正使用，参数 skip/limit 与后端 page/page_size 不符） | 合理扩展 | brownfield 对齐，非 PRD 显式需求但为满足排序分页所必需，且风险可控（无调用方） |
| ADR-5 个股分析页最小落地页 | PRD AC-07 + §1.3 个股页处理决策 + §1.4 范围7 | 需求驱动 | 直接满足 AC-07，理由含 RankingItem `/stock/${id}` 悬空引用事实 |

无孤儿决策，无存疑设计。

### 维度 10：范围合规性（全绿，5/5）

PRD §1.4 明确不做 5 项（个股深度功能/权重占比/二级筛选/导出收藏自选/迷你走势图）在架构全文均未引入；§4.3 还反向显式列出不引入项，与不做列表对齐。架构额外列"后端成分股接口新增/改造"为不做（PRD 未列但合理补充）。

### 维度 11：一致性检查（全绿，4/4，关键维度）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ADR 间一致性 | ✅ | ADR-1（后端驱动）↔ ADR-4（参数对齐后端）↔ ADR-2（独立 hook）互为支撑无矛盾；ADR-3 复用范式与 ADR-1 不冲突 |
| Schema 与 API 一致性 | ✅ | §7.2 SectorStockItem 字段与后端 sectors.py:309-318 实际返回逐字段对齐（id/symbol/name/current_price/market_cap/strength_score/trend_direction）；§7.3 API 边界与 §7.2 Schema 入参/返回对齐 |
| 状态机与流程一致性 | ✅ | §3.3 状态机（Loading/Success/Empty/Error）与 §6.1 L1 步骤6 的分支判断（total>0/=0/异常）一致；Error→Loading 经 mutate 与 §3.2 一致 |
| 范围与设计一致性 | ✅ | §2.1 范围 7 项均有 ADR/链路/模块支撑；§2.2 不做项在 §4.3 反向确认 |

### 维度 13：过度设计检测（全绿，无过度设计）

扫描模块（6 个，与 PRD 7 项功能匹配）、API（2 个，均为既有）、架构层级（纯前端接入，无新增中间件）、技术选型（SWR + 既有组件，无过重方案）。§4.3 显式避免 5 项过度设计。ADR-4（修正既有方法）属 brownfield 对齐非新设计。未发现存疑设计。

## 五、深度质量检查

### 维度 14：闭环完整性（全绿，关键维度）

| 运行链路 | 完整性 | 悬空步骤 | 说明 |
|----------|--------|---------|------|
| §6.1 L1 成分股加载与交互 | ✅ | 无 | 六步闭环：路由取 sectorId → SWR key → apiClient GET → 后端 JOIN/排序/count/分页 → 前端解包 .data.data → 按 total 分支渲染。每步输入输出明确，后端接口已验证存在（sectors.py:254） |
| §6.2 L2 点击下钻个股页 | ✅ | 无 | 四步闭环：点击行（非表头/分页器）→ router.push 用 id → 个股页 getStock(id) → 渲染。跳转参数 id 与后端 isdigit 校验匹配，无悬空依赖；stocksApi.getStock 已验证存在（api.ts:160） |

两条链路均可独立走通，无悬空步骤、无未定义依赖。

### 维度 15：安全底线（不适用/通过，关键维度）

本 PRD 为 MVP 需求，未设独立"合规底线"章节（§5 仅有决策记录，无 §5.x 合规底线）。架构 §8.3 仍提供基础安全设计（接口鉴权复用 get_current_user / sort_by 注入防护白名单+getattr 兜底 / 越权只读公开数据），覆盖合理。该维度按规则视为**不适用/通过**。

### 维度 16：复用可行性验证（全绿，关键维度）

对 §4.2 模块职责表与 §4.1 上下文图中所有"复用/调用既有 X"声明，逐一验证数据源/写入目标/前置依赖：

| 复用声明 | 被复用方法/组件 | 数据源验证 | 写入目标/前置依赖验证 | 状态 |
|---------|---------------|-----------|---------------------|------|
| 复用后端 `get_sector_stocks` | server/src/api/v1/sectors.py:254 | ✅ 从 StockModel JOIN SectorStockModel（stock_code==symbol, sector_code==sector.code）查成分股——与"板块成分股"场景完全匹配 | ✅ 只读查询，无写入；返回 items 含 id/symbol/name/current_price/market_cap/strength_score/trend_direction，与架构 §7.2 SectorStockItem 逐字段一致；参数为 page/page_size/sort_by/sort_order（与架构 ADR-4 待修正方向一致） | ✅ 可行 |
| 复用 `Pagination` | web/src/components/ui/Pagination.tsx | ✅ props 含 currentPage/pageSize/onPageChange/onPageSizeChange/pageSizeOptions，支持翻页+每页条数切换+跳页 | ✅ CrowdRankingTable.tsx:292-296 已用相同模式验证可行 | ✅ 可行 |
| 复用 `SimpleSelect` | web/src/components/ui/SimpleSelect.tsx | ✅ 用作每页条数下拉 | ✅ CrowdRankingTable.tsx:148 已验证使用 | ✅ 可行 |
| 复用 `stocksApi.getStock` | web/src/lib/api.ts:160 | ✅ 参数为 stockId（数据库 id），调用 `/stocks/${stockId}` | ✅ 后端 stocks.py:160 get_stock_detail 以 id 查询 + isdigit 校验，返回 StockDetail（stock.py:46）含 strength_score/trend_direction/current_price/market_cap，落地页可完整渲染；与架构 §5.x Q1 决策一致。注：当前 getStock 返回类型为 `any`，架构 §9 Phase A 已规划补类型 | ✅ 可行 |
| 复用 useSectorStrengthHistory SWR 范式 | web/src/hooks/useSectorStrengthHistory.ts | ✅ 范式：useMemo queryKey + useSWR + 返回 data/isLoading/isError/mutate | ✅ §9 Phase B 明确仿此范式建 useSectorStocks；queryKey 含全部排序/分页参数，状态变化自动重载与 ADR-1 一致 | ✅ 可行 |

未发现"方法签名匹配但 entity_type/数据源/写入目标不匹配"的典型错误。所有复用声明的数据源、写入目标、前置依赖均与目标场景匹配。

## 六、问题汇总与修复建议

### 前置阻塞项（必须先修）

无。

### 未满足的需求（高优先级）

无。

### 部分满足的需求（中优先级）

无。

### 存疑设计（需评估）

无。

> 说明：架构文档质量较高，唯一需留意的是 PRD §3.3 跳转参数原文为"股票代码"，而架构基于后端 `isdigit()` 校验事实将其修正为"数据库主键 id"。此修正是实现必需且已在 §5.x/§6.2/§7.6/ADR-5 四处一致说明，属正确收敛而非缺陷；建议在交付时向需求方同步该口径修正，避免验收时对"跳转参数"表述产生歧义。

## 七、总结

- **结构完整度**：100%（45/45 检查项全部通过）
- **需求满足度**：100%（51/51 需求项全部充分满足，含 4 个关键维度 6/11/14/15/16 全绿）
- **整体评价**：✅ **可进入下一阶段**
  - 无前置阻塞（配对可靠、frontmatter 合规、open_questions 同步、Q1 已决策）
  - 范围内 7 项 P0 需求、5 条用户故事、6 类业务规则、3 个异常场景、7 条 AC 全部 ✅
  - 关键维度（成功标准可达性/一致性/闭环完整性/安全底线/复用可行性）无 ❌
  - 非关键维度 ⚠️ 项为 0（≤3）
  - 复用可行性经源码逐项验证（get_sector_stocks/getStock/Pagination/SimpleSelect/SWR 范式数据源与契约均匹配）
- **优先修复建议**：
  1. （非阻塞，建议同步口径）PRD §3.3 跳转参数"股票代码"在实现中实为"数据库主键 id"（后端 isdigit 校验所致），架构已正确修正并多处说明，建议交付时向需求方/验收方同步该口径，避免 AC-07 验收表述歧义。
  2. （实现期注意，非文档问题）§9 Phase A 需补 `stocksApi.getStock` 的返回类型（当前为 `any`），与 SectorStockItem 同源的 StockDetail 字段对齐，落地页类型安全才有保障。
  3. （可选优化）§7.2 可在 Schema 注释中补一句"最新价两位小数/强度分整数"为 UI 渲染约定，使 PRD §3.3 数据呈现精度在契约层留痕（当前正确地未在类型层固化，仅留实现弹性）。
