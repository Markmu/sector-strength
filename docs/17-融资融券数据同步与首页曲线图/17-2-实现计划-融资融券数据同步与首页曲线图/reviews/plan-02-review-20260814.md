# 功能验收报告

## 基本信息

- **功能**: plan-02: 融资融券采集适配器
- **维度**: backend
- **阶段**: Phase 1
- **依赖**: 无
- **验收时间**: 2026-08-14
- **验收依据**: plan-02 §验收标准 checklist + §风险与边界边界场景 + §验证命令全量重跑 + 代码审查（对照实现规格与 16 期采集范式）；E2E 按 plan-02 §5 与 README §7.2 声明 waived（纯采集层，见维度六）
- **验收结论**: 通过

## 一、文件交付完整性

| 动作 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| modify | `server/src/services/data_acquisition/tushare_client.py` | 通过 | `git diff` 显示 **+93 行纯新增、零删改**（L2044-2129）：`MARGIN_DECIMAL_FIELDS` 类常量 + `get_margin`（L2056）+ `_build_margin_row`（L2087），插入位置在 `get_market_daily_quotes` 方法族之后、`_suspend_row_date` 之前，符合"方法族旁"定位 |
| create | `server/tests/services/data_acquisition/test_tushare_margin.py` | 通过 | 文件存在非空（359 行），22 个测试函数，mock DataApi 不依赖网络 |

**对 16 期既有方法零改动声明复核**：`git diff server/src/services/data_acquisition/tushare_client.py` 输出仅含一个连续 `+` 块（margin 三成员），无任何 `-` 行或上下文修改，`_execute_with_retry` / `_df_to_rows` / `_parse_tushare_date` / `_decimal_field` / `_build_market_daily_quote` / `get_market_daily_quotes` / suspend 系列全部原样。声明成立。

**工作区其他改动核查（git status）**：

| 路径 | 归属 | 说明 |
| --- | --- | --- |
| `server/src/models/__init__.py`、`server/src/models/market_margin_daily.py`、`server/alembic/versions/2026_08_15_0002-63164af1c44c_*.py`、`server/tests/models/` | plan-01（已 done） | plan-01 §文件清单内交付物，见 reviews/plan-01-review-20260814.md |
| `docs/FEAT-0002-market-margin.md`、`docs/17-*/` | 计划文档本身 | spec 与实现计划目录 |

plan-02 自身未引入任何文件清单外改动（风险与边界允许列表为"无"，实际也为无：`base.py` 与 `data_acquisition/models.py` 均未触碰）。通过。

## 二、Task 列表完成度

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 实现 get_margin 主流程 | done | `pro.margin(trade_date=)` 包在 `_execute_with_retry` 的 `_fetch` 闭包内 + `_df_to_rows`，无分页（mock 断言 call_count==1） |
| 2 | 实现 _build_margin_row 校验 | done | 七字段 `_decimal_field` Decimal + 逐字段非负复验 + 日期一致 + exchange_id 非空/strip |
| 3 | 编写 test_tushare_margin.py | done | 22 用例，重跑 22 passed |

**结论**: 3/3 done，无 todo/waived 遗留。通过。

## 三、验收标准完成度

| 验收项 | 状态 | 复核说明 |
| --- | --- | --- |
| 正常三行（SSE/SZSE/BSE，裁定口径）：3 个 dict、七数值字段全 `Decimal` 实例、trade_date 与入参一致；本层不强制行数 | 通过 | `test_normal_three_rows_sse_szse_bse`：`type(row[field]) is Decimal` 严格断言 + `{SSE,SZSE,BSE}` 集合 + 键集合与原生蛇形 schema 完全一致 + 原始值 Decimal 精确相等；`test_two_rows_returned_as_is` / `test_single_row_returned_as_is` 覆盖"不强制行数照常返回"。真实冒烟同口径复验（维度五） |
| 空结果（空 DataFrame/None）：返回空列表不抛错 | 通过 | `test_empty_dataframe_returns_empty_list`（columns-only 空 df）+ `test_none_response_returns_empty_list`（None 直通 `_df_to_rows` 归空） |
| 字段缺失 / NaN / Infinity / 负值 / 日期不符 / exchange_id 为空六类场景均抛 `MarketDataIntegrityError` 且错误信息含 exchange_id 与字段值 | 通过 | 对应 `test_missing_field_raises` / `test_nan_field_raises` / `test_infinity_field_raises` / `test_negative_field_raises` / `test_wrong_trade_date_raises` / `test_empty_exchange_id_raises`，全部断言 `match=` 关键字 + `"SSE"/"SZSE" in str(exc)`；另有 `test_missing_exchange_id_raises`（列级缺失）、`test_each_negative_decimal_field_raises`（七字段逐一负值）、`test_unparsable_trade_date_raises`（日期不可解析）、`test_zero_values_allowed`（0 值合法）加固 |
| 单日仅发起 1 次 `margin` 调用（无分页循环） | 通过 | `test_single_call_no_pagination` 断言 `call_count == 1`；`test_request_params_exact` 断言 `assert_called_once_with(trade_date="20260813")`（同时锁定"不传 fields"规格） |
| `Decimal(str(...))` 精度保持：科学计数法无 float 精度损失 | 通过 | `test_scientific_notation_no_float_precision_loss`：float 列 `1.0e12` 与字符串 `"9.87654321e+11"` 双路断言，`Decimal("987654321000")` 精确相等；`test_string_numeric_values_accepted` 补字符串数值列路径 |
| 既有采集方法回归通过（现有测试不受影响） | 通过 | 目录回归 67 passed（45 个既有 `test_tushare_market_daily.py` 用例全绿，16 期采集行为无回归）；且 tushare_client.py 对既有方法零 diff（维度一） |
| E2E 不适用：纯采集层功能，无用户界面；用户可见效果由 plan-04 执行验证与 plan-07 面板间接验证 | 通过（豁免） | 详见维度六 |

**结论**: checklist 全部完成或合理豁免，覆盖功能概要"无直接承接（AC-1 聚合原料供给）"定位。通过。

## 四、实现规格符合度

| 规格要求 | 状态 | 说明 |
| --- | --- | --- |
| #1 `get_margin(trade_date)` 主流程 | 通过 | `pro.margin(trade_date=YYYYMMDD)` **不传 fields**（mock 精确断言）；`_execute_with_retry(_fetch)` 闭包范式；`_df_to_rows` NaN→None；**无 offset/limit 分页循环**；空结果（None/空 df）→ 空列表（失败判定归 plan-03，采集层不吞不判） |
| #2 `_build_margin_row` 行构建与校验 | 通过 | `exchange_id = str(...).strip()` 为空/缺失抛完整性错误（endpoint='margin'）；`_parse_tushare_date(ts_code=exchange_id, endpoint='margin')` + `row_date != trade_date` 防串日；七字段经 `_decimal_field(row, field, ts_code=exchange_id)`（内部 `Decimal(str(value))` + `is_finite()`）；非负复验在本方法追加（`value < 0` 抛错，含 exchange_id 与字段值），与 16 期 `_build_market_daily_quote`"范围复验归调用方"惯例同构；返回蛇形键 dict |
| #2 可观测性 | 通过 | 结束日志 `"[Tushare] margin %s 获取 %d 行（交易所: %s）"` 含 trade_date/行数/交易所集合；六类完整性错误均含 exchange_id 与字段值 |
| #2 rqyl/rzrqye 去向 | 通过 | 两字段原样保真透传（无删改、无换算），`MARGIN_DECIMAL_FIELDS` 常量注释明示 spec D1/D2 约束，聚合口径留给 plan-03 |
| #4 明确不做 | 通过 | `base.py` 未加抽象方法、`data_acquisition/models.py` 未加 Pydantic 模型（git status 两文件均无改动）；无 exchange_id 白名单过滤（Provider 原生全部行透传）；margin_detail 未引入 |

**"不在范围"核查**: 聚合与落库（plan-03）零实现、`base.py`/`data_acquisition/models.py` 零改动、margin_detail 零引入。无超范围实现。

**裁定同步核查（2026-08-14 三行口径）**: 测试正常场景已按裁定改写为 SSE/SZSE/BSE 三行（文件头 docstring 与 `margin_df` 默认值一致），两行/单行保留为"本层不强制行数"边界用例；§6 冒烟断言三行全存在。**唯一残留**：`get_margin` 生产代码 docstring 仍写"单日仅 SSE+SZSE 两行"（裁定前旧口径，见建议项 W1），行为与测试均不受影响。

**结论**: 实现规格全部满足，无超范围实现。通过。

## 五、验证命令执行

全部命令本次验收全量重跑（venv：`server/.venv`）：

| 命令 | 退出码 | 状态 | 输出摘要 |
| --- | --- | --- | --- |
| `pytest tests/services/data_acquisition/test_tushare_margin.py -v --no-cov` | 0 | 通过 | **22 passed**（10 warnings，均为既有 pydantic class-based config deprecation 等依赖告警，非本功能引入） |
| `pytest tests/services/data_acquisition/ -q --no-cov` | 0 | 通过 | **67 passed**（22 margin + 45 既有 market_daily，1.03s） |
| 真实冒烟 `python -c "... DataSourceFactory.create().get_margin(date(2026, 8, 13)) ..."` | 0 | 通过 | **SMOKE OK**：3 行 SSE/SZSE/BSE，七字段全 Decimal；BSE rzye=8,371,680,293 与 plan-02 裁定记录逐位一致；SZSE/SSE 万亿级量级正常 |

冒烟环境备注：token/API URL 实际位于仓库根 `.env`（非 `server/.env`），`TushareDataSource.__init__` 经 `os.getenv` 读取，导出后冒烟成功；根 `.env` L52/L56 两行存在 shell 兼容噪音（`source` 时报 command not found，不影响变量导出），属仓库环境文件既有小瑕疵，与本功能无关。

## 六、E2E 质量证据

| 项 | 值 |
| --- | --- |
| 用例文档 / Spec | 不适用 |
| Red / Green 证据 | 不适用 |
| 资源证据 | 不适用（无 UI 资源） |
| 豁免判定 | **豁免成立** |

**豁免理由核对**：

- plan-02 §验收标准第 7 条显式声明「E2E 不适用：纯采集层功能，无用户界面；用户可见效果由 plan-04 同步任务执行验证与 plan-07 面板间接验证」。
- README §7.2 开发状态机注记 plan-01~06 为纯后端功能，red/green 浏览器 E2E 步骤按 `waived` 处理，质量门为各 §6 验证命令 + task-review；本次验收指令亦明确按此口径执行、无需寻找 red/green evidence 文件。
- 符合 task-review 维度六规则「只有纯内部、无用户可观察路径的功能可以豁免 E2E」——本功能为 tushare margin 接口适配与行级校验，无任何用户可观察路径。
- 间接验证链已规划：plan-04（同步任务执行验证：触发→等待→查库）+ plan-07（首页面板 E2E red/green）；真实冒烟（维度五命令 3）已提供对真实 Provider 的行为证据。

## 七、风险与边界

| 场景 | 状态 | 复核说明 |
| --- | --- | --- |
| Provider 返回空 DataFrame | done | `_df_to_rows` 空表→`[]`，`get_margin` 返回空列表不抛错；`test_empty_dataframe_returns_empty_list` 锁定 |
| 某数值字段为 NaN/None | done | `_df_to_rows` NaN→None 后 `_decimal_field`（allow_none=False）抛完整性错误；`test_nan_field_raises` / `test_missing_field_raises` 锁定 |
| 行 trade_date 与入参不符 | done | 抛完整性错误防串日（错误信息含期望/实际日期与 exchange_id）；`test_wrong_trade_date_raises` 锁定 |
| exchange_id 为空串/缺失 | done | 抛完整性错误；`test_empty_exchange_id_raises`（空串含纯空白）/ `test_missing_exchange_id_raises`（列缺失）锁定；`test_exchange_id_whitespace_stripped` 锁定 strip 归一化 |
| 返回仅 1 行（如上游截断） | done | 本层照常返回 1 行不强制行数（两行/单行各有用例）；行数异常观察与口径归 plan-03 |

**暂停条件裁定核查（2026-08-14）**: 「实测 margin 单日超两行则暂停上报」已触发并完成人工裁定——全市场合计口径 = 对接口返回全部交易所行求和（SSE/SZSE/BSE，含北交所）。plan-02 按"保真透传、无白名单过滤"执行：代码无行过滤逻辑，冒烟三行全过校验（BSE 行七字段全部合法），与裁定完全一致。

**风险备注核查**: 「积分不足时 `_execute_with_retry` 对非可重试关键字立即失败、不要吞错」——`test_non_retryable_error_fails_immediately` 以真实积分错误文案断言 `DataFetchError`（match="不可恢复"）且 `call_count == 1`（不重试），符合预期行为。

**结论**: 5/5 边界场景全 done，暂停条件闭环。通过。

## 八、契约对齐

| 契约项 | 方向 | 状态 | 说明 |
| --- | --- | --- | --- |
| `get_margin(trade_date: date) -> List[dict]` 签名 | 下游提供（plan-03 `MarginService.sync_date`） | 通过 | 签名逐字一致；`DataSourceFactory.create()` 路径冒烟实测可达 |
| 返回行 `{trade_date: date, exchange_id: str, rzye/rzmre/rzche/rqye/rqmcl/rqyl/rzrqye: Decimal}`（非 Pydantic） | 下游提供（plan-03 聚合原料） | 通过 | 测试断言键集合与 tushare 原生蛇形 schema 完全一致（无多余键）、七字段 `type is Decimal`、`trade_date` 为 `date`；rqyl/rzrqye 保真透传（rqyl 不入库、rzrqye 仅供排查，聚合重算归 plan-03——spec D2 不变） |
| 元/股原始口径不换算 | 下游提供 | 通过 | 冒烟实测万亿级原值（SSE rzye=1,356,308,222,032）与测试断言值均未做 ÷1e8 等换算（展示层换算归 plan-07） |
| `_decimal_field` endpoint 文案固定 "daily" | 16 期既有契约（交接上下文已知并接受） | 通过 | 未为改文案动 16 期方法签名，错误信息经 ts_code=exchange_id 与字段值仍可定位到 margin 行 |
| 既有采集方法行为不变 | 16 期既有契约 | 通过 | 零 diff（维度一）+ 目录回归 67 passed |

**结论**: 契约无偏差。通过。

## 九、代码审查

### 阻塞项

无。get_margin/_build_margin_row 逻辑正确（校验次序：exchange_id → 日期一致 → 七字段 Decimal+非负，任何一步失败即抛完整性错误不产脏行）；纯采集层无数据写入，无安全与数据损坏风险；对下游 plan-03 的契约（签名/键名/Decimal 口径）无阻断性错误。

### 维度十：执行验证（task handler）

**不适用**。plan-02 为纯采集适配器，不含异步任务处理器；同步任务 handler 属 plan-04。真实执行链路已由本 review 冒烟直接验证（`DataSourceFactory.create().get_margin()` 对真实 Provider 三行往返）。

### 改进建议（非阻塞）

| # | 位置 | 类别 | 建议 |
| --- | --- | --- | --- |
| W1 | `server/src/services/data_acquisition/tushare_client.py` L2061（get_margin docstring） | 文档陈旧 | docstring 写「单日仅 SSE+SZSE 两行」，与 2026-08-14 裁定后口径（SSE/SZSE/BSE 三行全量）不符（测试与功能文件均已按裁定更新，此处漏改）。代码行为不受影响（无白名单过滤，保真透传）。建议后续顺手改为「单日返回全部交易所行（实测 SSE/SZSE/BSE 三行）」，避免误导维护者 |
| W2 | `server/src/services/data_acquisition/tushare_client.py` L2120 | 健壮性（理论边缘） | `assert value is not None` 做类型收窄——`_decimal_field` 默认 allow_none=False 时该断言恒真，但 Python 以 `-O` 运行时 assert 被剥除、若未来 `_decimal_field` 语义变化 None 会流向 `value < 0` 抛 TypeError 而非完整性错误。当前无实际风险（服务不以 -O 运行），仅记录 |

## 十、总结

**验收结论**: 通过

- 功能 frontmatter `status` 已从 `review` 更新为 `done`
- 验收 checklist 已按仓库惯例由 task-review 勾选（`[ ]` → `[x]`）
- 验收报告已写入 `reviews/plan-02-review-20260814.md`
- blocker：0；改进建议：2 项（W1 docstring 陈旧、W2 assert 理论边缘，均非阻塞）
- README §7.2 开发状态机表按验收指令**未修改**（状态以 plan-02 frontmatter 为准，plan-03 正在并行 implement）
- README frontmatter `status` 维持 `in_execution`（其余功能未达 done）

### 状态变更记录

| 项目 | 变更前 | 变更后 |
| --- | --- | --- |
| plan-02 frontmatter status | review | done |
