# 功能验收报告

## 基本信息

- **功能**: plan-01: 数据模型与迁移
- **维度**: backend
- **阶段**: Phase 1
- **依赖**: 无
- **验收时间**: 2026-08-14
- **验收依据**: plan-01 §验收标准 checklist + §风险与边界边界场景 + §验证命令复核（只读口径，见维度五）+ 代码审查（对照实现规格与 16 期范式母本）
- **验收结论**: 通过

## 一、文件交付完整性

| 动作 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| create | `server/src/models/market_margin_daily.py` | 通过 | 文件存在非空（88 行），列/约束/索引/comment/`__repr__` 与实现规格 #1 逐项一致 |
| modify | `server/src/models/__init__.py` | 通过 | L34 `from .market_margin_daily import MarketMarginDaily` 紧邻 L33 `MarketDailyMetric`；L75 `__all__` 追加紧邻 L74，符合注册惯式 |
| create | `server/alembic/versions/2026_08_15_0002-63164af1c44c_add_market_margin_daily.py` | 通过 | revision=`63164af1c44c`、down_revision=`a7d2e9f4c1b8`，单表 create/drop 对称（文件名偏离 spec 见裁定 B） |
| create | `server/tests/models/test_market_margin_daily_model.py` | 通过 | 9 个元数据断言用例，覆盖规格 #4 全部要求 |

**工作区其他改动核查（git status）**：

| 路径 | 归属 | 说明 |
| --- | --- | --- |
| `server/src/services/data_acquisition/tushare_client.py`（+87 行） | plan-02 并行工作 | plan-02 §文件清单内（get_margin 采集方法）；README §7.2 显示 plan-02 implement running，execution_order 首组即 `[plan-01, plan-02]` 并行、文件不相交。非 plan-01 清单外改动 |
| `server/tests/services/data_acquisition/test_tushare_margin.py` | plan-02 并行工作 | 同上 |
| `docs/FEAT-0002-market-margin.md`、`docs/17-*/` | 计划文档本身 | spec 与实现计划目录 |

plan-01 自身未引入任何文件清单外改动（风险与边界允许列表为"无"，实际也为无）。通过。

## 二、Task 列表完成度

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 创建 MarketMarginDaily 模型 | done | 六指标 Numeric(20,2) + 唯一约束 + 索引 + 双时间戳 |
| 2 | models/__init__.py 注册导出 | done | import + `__all__` |
| 3 | 编写 Alembic 迁移（down_revision=a7d2e9f4c1b8） | done | 单表 create/drop 对称 |
| 4 | 编写模型元数据测试 | done | 9 用例，重跑 9 passed |

**结论**: 4/4 done，无 todo/waived 遗留。通过。

## 三、验收标准完成度

| 验收项 | 状态 | 复核说明 |
| --- | --- | --- |
| `alembic upgrade head` 成功创建 `market_margin_daily`（唯一约束+索引+comment 齐全）；downgrade -1 可回退、再 upgrade 恢复（revision 63164af1c44c） | 通过 | 只读实测：`alembic current` = `63164af1c44c (head)`；information_schema/pg_constraint/pg_indexes 逐项核对——10 列（id/trade_date/六指标/双时间戳）、`uq_market_margin_daily_trade_date`（contype=u）、`idx_market_margin_daily_trade_date`、10 列 comment 齐全。回退能力依据 implement 阶段 upgrade/downgrade/upgrade 往返证据（已知事实）+ 本次 downgrade 代码静态审查（drop index → drop table 逆序对称）；本次按验收指令未重跑往返，避免影响并行工作 |
| `alembic check` 无新 drift（drift 操作集合与基线逐项一致） | 通过 | 实测 exit 255 FAILED（基线即 FAILED）。FAILED 操作集合全部为存量：`sector_classification` 表移除（2025-01 旧表）+ `broker_recommend.trade_date`/`funds.status`/`sector_fund_flow.*`/`stock_daily_market_data.*`/`stock_moving_average_data.*`/`stock_strength_scores.stock_id` 的 comment drift 与 ix_* 索引差异——均早于本功能。`market_margin_daily` 仅出现一条无害 INFO（"Detected sequence … assuming SERIAL and omitting"，与其他 35 张表同款），**无任何新增 drift 操作** |
| `from src.models import MarketMarginDaily` 成功；`trading_calendar_days` 未被触碰（迁移往返后列数仍为 6） | 通过 | 实测导入成功（tablename: market_margin_daily）；只读查库 `trading_calendar_days` 列数 = 6；本期迁移仅含一张新表，无日历 DDL |
| 模型元数据测试通过（9 passed） | 通过 | 重跑 `pytest tests/models/test_market_margin_daily_model.py -v --no-cov` → **9 passed** |
| E2E 不适用：纯数据层功能，无用户可见界面 | 通过（豁免） | 详见维度六 |

**结论**: checklist 全部完成或合理豁免。通过。

## 四、实现规格符合度

| 规格要求 | 状态 | 说明 |
| --- | --- | --- |
| #1 模型列定义 | 通过 | id Integer PK autoincrement；trade_date Date nullable=False；六指标全部 `Numeric(precision=20, scale=2)` nullable=True 且列名与 tushare margin 同名（rzye/rqye/rzmre/rzche/rqmcl/rzrqye）；created_at/updated_at `DateTime(timezone=True)` server_default=func.now()（updated_at 另带 onupdate）；全部列带中文 comment；`from .base import Base`；`__repr__` 存在 |
| #1 约束与索引 | 通过 | `UniqueConstraint('trade_date', name='uq_market_margin_daily_trade_date')` + `Index('idx_market_margin_daily_trade_date', 'trade_date')`，命名与规格逐字一致 |
| #1 rqyl 不入库 | 通过 | 模型无 rqyl 列；测试 `test_rqyl_not_stored` 断言锁定 |
| #2 models/__init__.py 注册 | 通过 | import 与 `__all__` 追加位置紧邻 MarketDailyMetric（L34/L75），惯式满足 |
| #3 迁移范式（照抄 c4b9e2a7f813） | 通过 | `op.create_table` + `op.create_index`、列带 comment、downgrade 逆序 drop index → drop table、不夹带无关 drift、不建任何日历表；单表迁移与规格一致 |
| #3 down_revision | 通过 | `'a7d2e9f4c1b8'` 与规格一致，实测 `alembic current` 显示 `63164af1c44c (head)` 单 head，链条无分叉 |
| #4 模型元数据测试 | 通过 | 表名/Base.metadata 注册/唯一约束名/索引名/trade_date 非空/六指标 Numeric(20,2)/rqyl 不入库/双时间戳 timezone-aware/id 主键，9 用例全覆盖；`server/tests/models/` 无 `__init__.py` 与全库 tests 惯例一致（`find server/tests -name __init__.py` 为空，pytest rootdir 收集正常） |

**范围核查**: "不在范围"三项均未越界——无 get_margin 采集方法（plan-02）、无汇总/upsert 逻辑（plan-03）、日历表零改动（复用 16 期）。

### 实现者已声明决策裁定

| # | 决策 | 裁定 | 依据 |
| --- | --- | --- | --- |
| A | `updated_at` 迁移列直接带 `server_default=sa.text('now()')`（16 期原迁移无此 default） | **接受** | 事实链完整：16 期 c4b9e2a7f813 的 `market_daily_metrics.updated_at` 确实无 server_default（迁移文件 L55 可证），后由补丁迁移 `2026_08_14_0002-e5c1f3a90b2d_add_updated_at_serverdefault_market_daily_metrics.py` 补齐（文件在迁移链中）。本期一步到位避免重复补丁；与模型定义（server_default=func.now()）一致，且若迁移不带 default，新表会立即产生一条 modify 列 drift、反而违反"drift 集合与基线一致"验收口径。属正确的教训固化，非擅自偏离 |
| B | 迁移文件名 `2026_08_15_0002-*` 替代 spec 代码地图的 `2026_08_14_XXXX-*` | **接受** | 迁移链 head 已推进至 `2026_08_15_0001-a7d2e9f4c1b8`（实测存在），沿用 `2026_08_14` 前缀会破坏文件名单调递增惯例；plan-01 §3 与交接上下文均已预先声明该偏离，链条事实以 `down_revision='a7d2e9f4c1b8'` 为准且正确 |

**结论**: 实现规格全部满足，无超范围实现，两处声明决策均接受。通过。

## 五、验证命令执行

按验收指令采用只读复核口径（不重跑 downgrade 往返，库内有 plan-02 等并行工作）：

| 命令 | 退出码 | 状态 | 输出摘要 |
| --- | --- | --- | --- |
| `pytest tests/models/test_market_margin_daily_model.py -v --no-cov` | 0 | 通过 | **9 passed**（10 warnings，均为既有依赖告警） |
| `python -c "from src.models import MarketMarginDaily; print(...)"` | 0 | 通过 | `tablename: market_margin_daily` / `import OK` |
| `alembic current`（只读，替代 upgrade head 重跑） | 0 | 通过 | `63164af1c44c (head)`——implement 阶段往返后库处于本迁移 head，与已知事实一致 |
| `alembic check` | 255（FAILED） | 通过（按声明口径） | 基线即 FAILED；本次 FAILED 操作集合逐项核对全部为存量 drift（sector_classification 表 + broker_recommend/funds/sector_fund_flow/stock_*/stock_strength_scores 的 comment/索引），`market_margin_daily` 仅一条 "assuming SERIAL and omitting" INFO 行、无新增 drift 操作。与验收标准第 2 条声明口径完全一致 |
| （附加）只读库结构核查 | 0 | 通过 | `market_margin_daily` 10 列：六指标均 `numeric(20,2) nullable`、trade_date `NOT NULL`、created_at/updated_at `timestamptz DEFAULT now()`、10 列 comment 齐全、约束/索引三条（pkey/uq/idx）；`trading_calendar_days` 列数 = 6 |

## 六、E2E 质量证据

| 项 | 值 |
| --- | --- |
| 用例文档 / Spec | 不适用 |
| Red / Green 证据 | 不适用 |
| 豁免判定 | **豁免成立** |

**豁免理由核对**：

- plan-01 §验收标准第 5 条显式声明「E2E 不适用：纯数据层功能，无用户可见界面；其用户可见效果由 plan-07 面板与 plan-08 同步面板间接验证」。
- README §7.2 开发状态机注记 plan-01~06 为纯后端功能，red/green 浏览器 E2E 步骤按 `waived` 处理，质量门为各 §6 验证命令 + task-review；本次验收指令亦明确以此口径执行。
- 符合 task-review 维度六规则「只有纯内部、无用户可观察路径的功能可以豁免 E2E」——本功能为模型/迁移/注册导出，无任何用户可观察路径。
- 间接验证链已规划：plan-07（首页面板 E2E red/green）+ plan-08（同步面板 E2E red/green）+ plan-04 执行验证（查库断言）。

**备注（非阻塞）**: 豁免声明位于 §验收标准 而非 task-read 惯例提到的 §风险与边界；内容完整且 README §7.2 有同级声明，不构成偏差（16 期同类功能验收亦按此口径接受）。

## 七、风险与边界

| 场景 | 状态 | 复核说明 |
| --- | --- | --- |
| 迁移前库中已有同名表 | done | 处理方式为人工核对不吞错，无需代码；本次实测无同名表冲突 |
| downgrade 后残留索引 | done | downgrade 先 `drop_index` 再 `drop_table`，与 upgrade 逆序对称 |
| 六指标全为 NULL 的行 | done | 模型 nullable=True 允许；守卫职责在 plan-03 聚合层，本期不越界实现 |

**结论**: 3/3 边界场景全 done。通过。

## 八、契约对齐

| 契约项 | 方向 | 状态 | 说明 |
| --- | --- | --- | --- |
| 六指标列名 rzye/rqye/rzmre/rzche/rqmcl/rzrqye（与 tushare margin 同名） | 下游提供（plan-03 写入） | 通过 | 模型与库表逐列一致，rqyl 不入库 |
| `Numeric(20,2)` 存储口径 | 下游提供（plan-03 Decimal 写入 / plan-06 float 输出） | 通过 | 库表实测 numeric(20,2)×6 |
| trade_date 唯一约束 + 索引（upsert on_conflict 锚点） | 下游提供（plan-03 `on_conflict_do_update(trade_date)`） | 通过 | `uq_market_margin_daily_trade_date` + `idx_market_margin_daily_trade_date` 在库 |
| `from src.models import MarketMarginDaily` | 下游提供（plan-03/04/06） | 通过 | 实测导入成功 |
| 迁移链 `a7d2e9f4c1b8 → 63164af1c44c` | 下游（后续迁移的 down_revision 锚点） | 通过 | 单 head，无分叉（暂停条件未触发） |
| `trading_calendar_days` 复用不重建 | 16 期既有契约 | 通过 | 列数 6 未变，本期迁移零日历 DDL |

**结论**: 契约无偏差。通过。

## 九、代码审查

### 阻塞项

无。模型与迁移逐列比对一致（列名/类型/精度/nullable/comment/约束/索引），upgrade/downgrade 对称，无安全与数据损坏风险，无不阻断下游的契约错误。

### 维度十：执行验证（task handler）

**不适用**。plan-01 为纯数据层（模型/迁移/注册导出），不含异步任务处理器；同步任务 handler 属 plan-04。

### 改进建议（非阻塞）

| # | 位置 | 类别 | 建议 |
| --- | --- | --- | --- |
| 1 | `server/tests/models/test_market_margin_daily_model.py` L30 | 可维护性 | `test_trade_date_unique_constraint` 用 `c.__class__.__name__ == "UniqueConstraint"` 字符串判型，改用 `isinstance(c, UniqueConstraint)`（sqlalchemy 已在文件头 import 区可见）更直接、抗重命名 |
| 2 | `server/src/models/market_margin_daily.py` L44-76 | 风格对照注记 | created_at/updated_at 未显式声明 nullable（默认 True，库中 nullable=YES），与 16 期 market_daily_metric 范式完全一致，非问题；仅作对照记录，供后续若引入"时间戳非空"规范时统一处理 |

## 十、总结

**验收结论**: 通过

- 功能 frontmatter `status` 已从 `review` 更新为 `done`
- 验收报告已写入 `reviews/plan-01-review-20260814.md`
- blocker：0；改进建议：2 项（均非阻塞）
- README §7.2 开发状态机表按验收指令**未修改**（状态以 plan-01 frontmatter 为准）
- README frontmatter `status` 维持 `in_execution`（其余 7 个功能未达 done）

### 状态变更记录

| 项目 | 变更前 | 变更后 |
| --- | --- | --- |
| plan-01 frontmatter status | review | done |
