# plan-05 验收报告：admin同步触发端点

- **功能**: plan-05 admin同步触发端点（feat_id: plan-05，dimension: backend，phase: 2）
- **验收日期**: 2026-08-14
- **验收结论**: **通过**（0 blocker / 3 warning）
- **状态变更**: frontmatter `status: review → done`（本次 review 执行；README §7.2 状态机表按并行验收约定由主 agent 维护，本 review 不改）
- **E2E**: 浏览器 E2E waived（README §7.2 + plan-05 §5 验收标准第 7 条已声明：后端路由功能无浏览器界面，浏览器侧触发交互由 plan-08 E2E 覆盖；plan §5 声明的**路由级执行验证不可豁免**——implement 阶段已完成，本报告维度 10 只读复核其记录与代码/数据库一致性，未新建任务）

---

## 一、验收维度结果

| # | 维度 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 文件交付完整性 | 通过 | 清单 3 个文件全部交付且内容非空；`admin/__init__.py` diff 恰两行对称挂载；无清单外越界（详见下） |
| 2 | Task 列表完成度 | 通过 | 5/5 Task 全部 `done` |
| 3 | 验收标准完成度 | 通过 | 7/7 checklist 已勾选，覆盖 AC-4（主）/AC-3（端点侧）/执行验证/RESERVED 联动/16 期回归/E2E 豁免 |
| 4 | 实现规格符合度 | 通过 | 五项校验链 / require_admin / create_exclusive_task / 挂载 / 可观测日志逐项落地；16 期母本逐行对照无行为差异（详见二） |
| 5 | 验证命令执行 | 通过 | 本功能 8 用例 + admin/api 回归 53 用例 + RESERVED 联动 16 用例全绿（详见四） |
| 6 | E2E 质量证据 | 豁免（waived） | 纯后端路由无浏览器可观察路径；质量门为路由级执行验证（已做并复核）+ §6 pytest |
| 7 | 边界与风险 | 通过 | 7 个后端边界场景全部 `done`，逐一有对应单测 |
| 8 | 契约对齐 | 通过 | 路径 / 请求 snake_case body / 响应 `data={"task_id"}` / task_type 字符串 / RESERVED hint 与交接上下文及 plan-04 全一致 |
| 9 | 代码审查 | 通过（3 warning） | 无 blocker；W1~W3 见五 |
| 10 | 执行验证（task handler 链路必检） | 通过 | 只读查库复核：任务终态 / params / created_by / result / 幂等数据与端点代码链路完全一致（详见三） |

### 文件清单交付核对

| 动作 | 路径 | 核对结果 |
| --- | --- | --- |
| create | `server/src/api/admin/init_margin.py` | ✓ 193 行非空；五项校验链 + require_admin + create_exclusive_task + INFO/WARNING 日志，逐行对照 16 期母本（见二） |
| modify | `server/src/api/admin/__init__.py` | ✓ git diff HEAD 恰两行：L25 import + L48 include_router，对称挂于 16 期行旁，16 期行零改动 |
| create | `server/tests/api/admin/test_init_margin.py` | ✓ 8 用例：合法创建接线 / 五项校验逐一 / 互斥 / 403 |

**清单外改动归属核实**（不判越界）：工作区其余变更均属并行功能——`api/v1/margin.py` + `api/v1/__init__.py` + `tests/api/test_margin.py`（plan-06，另有 agent 并行验收）、`task_manager.py`/`task_executor.py`/`task_fence.py`/`task_handlers.py`/`admin/tasks.py`（plan-04 已验收）、`tushare_client.py`（plan-02）、`models/__init__.py` + 迁移 + `market_margin_daily.py`（plan-01）、`margin_service.py`（plan-03）。`tests/api/admin/conftest.py` 经 git 历史核实为 **16 期提交 59a1c29 既有文件**（其 docstring 中 "(plan-05)" 指 16 期 plan-05），与交接上下文"16 期建立、无需新建"声明一致，非本期新增。

---

## 二、实现规格符合度：16 期母本逐行对照（重点复核项）

`init_margin.py` vs `init_market_metrics.py`（16 期 plan-05 成品，L1-154）逐段比对：

| 段落 | 16 期母本（行号） | plan-05 实现（行号） | 一致性 |
| --- | --- | --- | --- |
| router 声明 | :42 `prefix="/init"` | :49 `prefix="/init"`，仅 tags 差异（Admin - Margin） | ✓ |
| `_MAX_BACKSPAN_DAYS` | :45 `= 3650` | :52 `= 3650` 照搬 | ✓ |
| Payload | :48-58 双 `date` Field | :55-65 `MarginRangePayload` 同结构 | ✓ |
| 端点签名 | :61-66 `require_admin` + `get_session` + `ApiResponse[dict]` | :68-73 同签名 | ✓ |
| 校验 1 起止倒置 | :80-85 "开始日期不能晚于结束日期" | :87-97 同 message（新增 INFO 日志，规格 §1 可观测性要求） | ✓ |
| 校验 2 end>today（AC-4） | :86-91 "结束日期不能晚于今天" | :98-108 同 message | ✓ |
| 校验 3 跨度>3650 天 | :94-99 f"日期范围不能超过 10 年（3650 天）" | :111-121 同 | ✓ |
| 校验 4 日历刷新 | :102-112 `refresh_range` 异常 → `session.rollback()` + WARNING + "交易日历刷新失败，未创建任务：{e}" | :124-134 同（日志前缀 [margin]） | ✓ 不降级旧批次 |
| 校验 5 零交易日 | :115-123 "所选范围内没有交易日，未创建任务" | :137-150 同 | ✓ |
| 延迟导入 | :126 `from src.services.task_manager import TaskManager` | :153 同（避免循环依赖） | ✓ |
| 互斥创建 | :129-136 `create_exclusive_task(task_type=..., params={ISO 起止}, created_by=_admin.id)` | :156-163 `task_type="sync_market_margin"` 其余同构 | ✓ 不透传 max_retries（固定 0） |
| 互斥 None 分支 | :138-144 "已有市场量价同步任务正在运行…"（HTTP 200） | :165-176 "已有融资融券同步任务正在运行…"（HTTP 200） | ✓ 与 16 期锚点一致 |
| 成功响应 | :146-153 `data={"task_id":...}` + "…任务已创建（起 ~ 止，交易日 N 个）" | :185-192 同构 | ✓ |

差异仅为规格明确允许的三类：tag 名、任务类型字符串、margin 文案，以及规格 §1"可观测性"要求的 INFO/WARNING 日志。**16 期 `init_market_metrics.py` 本身零改动**（不在 git modified 列表），16 期行为语义不变。

**重点复核项逐条结论**：

1. **五项校验逐项拒绝且不建任务**：五用例各自断言 `success=False` + 明确 message + `mock_create.assert_not_awaited()`（`create_exclusive_task` 是 `async_tasks` 表唯一插入入口，mock 断言与"表无新行"语义等价且隔离共享库，与 16 期 test_init_market_metrics.py 同款范式）——全部通过。
2. **互斥 BLOCKED 响应**：`create_exclusive_task` 返回 None（plan-04 margin advisory lock 9001003 互斥命中）→ HTTP 200 + `success=False` + "已有融资融券同步任务正在运行，请等待当前任务完成"，专测覆盖，与 16 期锚点行为一致。
3. **403 非 admin**：`_admin: User = Depends(require_admin)`；`test_non_admin_returns_403` 在 conftest autouse admin override 之上再覆盖为 `role="user"`，断言 403 通过。
4. **16 期 init_market_metrics.py 逐行对照**：见上表，无行为改变。
5. **与 plan-04 RESERVED 封堵联动**：`tasks.py` L30 `_RESERVED_ENDPOINT_HINTS["sync_market_margin"] = "POST /api/v1/admin/init/margin"` 与本端点实际路径一致；`test_admin_create_rejects_margin_reserved_task_type`（plan-04 交付用例）断言拒绝消息含该路径，复跑通过（见四）。

**路径链核实**：admin 主路由（无统一前缀）→ `router.py` L29 `/v1/admin` → `main.py` L111 `/api` = `/api/v1/admin/init/margin`；main.py L113 另有无 `/api` 前缀挂载（16 期既有双挂载结构），故单测走 `/v1/admin/init/margin`、路由级执行验证走 `/api/v1/admin/init/margin` 均可达，无矛盾。

**"不在范围"核查**：handler/fencing 未重复实现（正确委托 plan-04 `create_exclusive_task`）；查询 API（plan-06）与前端面板（plan-08）未触碰。✓

---

## 三、维度 10 执行验证复核（只读查库，未 POST 新任务）

implement 阶段执行验证记录：任务 `task_5275a0b49f6b`（幂等重同步 2026-08-11 ~ 2026-08-13）completed。本 review 经 `docker exec sector-strength-postgres-1 psql -U sector_user -d sector_strength` 只读复核：

| 检查项 | 库中实测 | 判定 |
| --- | --- | --- |
| 任务终态 | `task_5275a0b49f6b` / `sync_market_margin` / **completed** / progress **3/3** / created 2026-08-14 11:03:04 / completed 11:03:11 | ✓ 与记录一致 |
| params（端点写入） | `start_date="2026-08-11"`、`end_date="2026-08-13"`（async_task_params，ISO snake_case） | ✓ 与 `init_margin.py` L158-161 写入代码一致 |
| created_by | `707d0102-…` → users 表 `muchao1303@gmail.com`，`role=admin`（真实管理员） | ✓ 与偏差裁定（见六）一致 |
| result（plan-04 handler 契约） | `{"successCount":3,"skippedCount":0,"failedCount":0,"dateResults":[3×success],"unprocessedDates":[]}`（camelCase） | ✓ |
| 数据写入 | `market_margin_daily` 恰 **3 行**（2026-08-11/12/13，六指标齐全）——幂等重同步后无新增行 | ✓ AC-2 幂等（upsert 覆盖） |
| rzrqye 重算复算 | 08-11: 2637920218091+25408914989=2663329133080 ✓；08-12: 2647383275810+25789571175=2673172846985 ✓；08-13: 2649740550850+25977668669=2675718219519 ✓ | ✓ D2 口径 |

调用链路（端点 → `create_exclusive_task`（lock key 映射 `_EXCLUSIVE_TASK_LOCK_KEYS["sync_market_margin"]=9001003`）→ plan-04 `sync_market_margin` handler → plan-03 `MarginService.sync_date` → `market_margin_daily`）数据源、写入目标、前置依赖逐级正确，且有真实执行终态背书。**维度 10 通过。**

---

## 四、验证命令输出摘要（本次 review 重跑，venv=server/.venv，--no-cov）

| 命令 | 结果 |
| --- | --- |
| `pytest tests/api/admin/test_init_margin.py -v --no-cov`（§6 第 1 条） | **8 passed**（合法创建 / 起止倒置 / end>today / 跨度>10 年 / 日历刷新失败 / 零交易日 / 互斥 / 403），10 warnings（既有 deprecation，退出码 0） |
| `pytest tests/api/admin/ tests/api -q --no-cov`（§6 第 2 条，16 期回归硬门槛） | **53 passed**（含 16 期 `test_init_market_metrics.py` 8 用例——init/market-metrics 行为不变；含并行 plan-06 `test_margin.py` 13 用例） |
| `pytest tests/services/test_task_manager.py -k "reserved or margin" -v --no-cov`（RESERVED 联动，§5 第 5 条） | **16 passed**——关键用例 `test_admin_create_rejects_margin_reserved_task_type` 断言拒绝消息含 `POST /api/v1/admin/init/margin`，通过 |
| `pytest tests/api/admin/ tests/api -k "reserved or margin"`（联动用例的 api 侧范围） | **21 passed**（8 init_margin + 13 test_margin；与"16 passed"的数字差源于命令范围：RESERVED 用例位于 `tests/services/test_task_manager.py`，且 plan-06 并行 agent 在 implement 之后将其 test_margin.py 用例数增至 13；数量向更多覆盖方向偏差，非回归） |
| §6 第 3 条路由级执行验证 | implement 阶段已完成（任务 `task_5275a0b49f6b`）；本 review 只读查库复核（见三），按约定不新建任务 |
| §6 第 4 条全量 `pytest tests/ -q --no-cov` | implement 阶段 1275 passed 全绿；按并行验收约定不重跑 |

---

## 五、blocker / warning

**blocker：无。**

**warning（不阻塞验收）**：

- **W1（验收标准第 1 条断言形式）**："断言 async_tasks 表无新行"在测试中实现为 mock `create_exclusive_task` + `assert_not_awaited()`，而非直查表。`create_exclusive_task` 为该表插入唯一入口，语义等价、隔离性更佳，且与 16 期已验收通过的 test_init_market_metrics.py 完全同款范式。接受，仅记录。
- **W2（执行验证脚本偏差，裁定：接受）**：实现者声明 §6 脚本的 `User(...)` mock（随机 uuid）被 `async_tasks.created_by` 外键（`REFERENCES users(id)`，已从 `\d async_tasks` 实测确认）拒绝，改用真实 admin id。裁定接受：FK 为真实库约束非代码缺陷；真实 id 使验证更贴近生产路径（created_by 可追溯），验证强度不降；偏差仅涉脚手架，不影响被验证的校验链/建任务/执行/数据写入行为。
- **W3（E2E 豁免记录位置）**：豁免理由写在功能文件"验收标准"第 7 条与 README §7.2，而非 SKILL 严格要求的"风险与边界"节。理由内容充分（纯后端路由、plan-08 覆盖浏览器侧、路由级执行验证为质量门且已做），与 plan-01~04 已验收先例一致。接受，建议后续 plan 文件模板把豁免理由同时落"风险与边界"节。

---

## 六、结论

plan-05 交付物完整、规格逐项落地、16 期母本对照无行为漂移、验证命令全绿、路由级执行验证记录与代码及数据库实测一致、契约与 plan-04/plan-08 上下游对齐、代码审查无 blocker。**验收通过**，frontmatter `status: review → done`。
