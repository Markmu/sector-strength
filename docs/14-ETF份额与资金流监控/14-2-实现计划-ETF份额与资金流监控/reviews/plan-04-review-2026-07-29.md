---
feat_id: plan-04
title: 前端基础设施
review_date: 2026-07-29
reviewer: task-review skill
status_before: review
status_after: done
---

# plan-04 验收报告：前端基础设施

## 验收结论

**通过（无 blocker）。** plan-04 状态 `review → done`。

功能概要的实现目标已全部达成：ETF 监控前端基础设施就绪——`etfMonitorApi`（4 方法）、`etfMonitorTypes.ts`（camelCase 业务对象）、`useEtfMonitor.ts`（4 SWR hooks）、路由壳 `etf-monitor/page.tsx`（'use client' + DashboardLayout 占位）、`DashboardLayout.tsx` 导航入口（板块资金流之后，TrendingUp）。侧边栏"ETF 监控"可达，点击跳转 `/dashboard/etf-monitor` 空载渲染。API 契约四件套（endpoint 不带 /api/v1、query snake_case、{success,data} 包裹、响应 camelCase）与架构 §7.6/§7.3 一致。`npm run build` 退出码 0，E2E green（TC-4.1~4.6 全 6 passed，12 skipped 为 plan-05 骨架）。

## 10 维度验收结果

| # | 维度 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 文件交付完整性 | 通过 | 5 个清单文件全部交付且非空（2 modify + 3 create）；无清单外文件 |
| 2 | Task 列表完成度 | 通过 | 6 个 Task 全 done |
| 3 | 验收标准完成度 | 通过 | §5 前端 checklist 全部勾选；覆盖关联 AC（基础设施为 plan-05 提供支撑，AC 交互验证在 plan-05）；lint 项标注豁免原因（192/272 pre-existing，新增零错误） |
| 4 | 实现规格符合度 | 通过 | §3 实现规格逐条落地（api/types/hooks/路由壳/导航）；"不在范围"（业务组件 plan-05）未越界实现 |
| 5 | 验证命令执行 | 通过 | `npm run build` 退出码 0；E2E 6 passed/12 skipped；lint 退出码非 0 但属 pre-existing（非 plan-04 引入），按 brief 不作为 blocker |
| 6 | E2E 质量证据 | 通过 | red + green 证据均覆盖 plan-04；red 结论"预期失败，测试有效"，green 结论"通过（TC-4.1~4.6，6 passed/12 skipped）" |
| 7 | 边界与风险 | 通过 | §8 前端边界场景 5 项全 done |
| 8 | 契约对齐 | 通过 | API 契约四件套与架构 §7.2/§7.3/§7.6 一致；下游 plan-05 所需 hooks/types/api 契约已暴露 |
| 9 | 代码审查 | 通过（无 blocker，0 建议） | apiClient 单例/endpoint 拼接/SWR 解包/条件 hook null key/'use client'/导航项 全部正确；无安全隐患、无数据损坏风险 |
| 10 | 执行验证（task handler） | 不适用 | plan-04 纯前端基础设施，无 task handler |

## 维度 1：文件交付完整性

逐一核对 §2 文件清单：

| 动作 | 路径 | 核对结果 |
| --- | --- | --- |
| modify | `web/src/lib/api.ts` | 存在；末尾新增 `etfMonitorApi`（4 方法，api.ts:1358-1419）+ 类型 re-export（api.ts:1346-1352），含相关注释 |
| create | `web/src/types/etfMonitorTypes.ts` | 存在（126 行，EtfCategory/EtfSortBy/EtfTrendMetric/EtfTargetType/EtfTrendDays + 6 业务对象接口），非空 |
| create | `web/src/hooks/useEtfMonitor.ts` | 存在（219 行，4 SWR hooks + SWR_OPTIONS），非空 |
| create | `web/src/app/dashboard/etf-monitor/page.tsx` | 存在（28 行，'use client' + DashboardLayout + 占位 "ETF 监控（建设中）"），非空 |
| modify | `web/src/components/dashboard/DashboardLayout.tsx` | 存在；baseSidebarItems 新增 `{ title: 'ETF 监控', href: '/dashboard/etf-monitor', icon: TrendingUp }`（:32-36），置于板块资金流之后；lucide-react import 含 TrendingUp（:5） |

清单外文件：无。Next.js build 路由输出确认 `/dashboard/etf-monitor` 已注册（Static prerender）。

## 维度 2：Task 列表完成度

| # | Task | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 新增 etfMonitorApi（4 方法） | done | api.ts 末尾，endpoint 不带 /api/v1 |
| 2 | 新建 etfMonitorTypes.ts | done | 全 camelCase 业务对象 |
| 3 | 新建 useEtfMonitor.ts（4 SWR hooks） | done | 仿 useSectorFundFlow，深路径导入 |
| 4 | 新建 etf-monitor/page.tsx 路由壳 | done | 'use client' + DashboardLayout 占位 |
| 5 | DashboardLayout.tsx 新增导航入口 | done | baseSidebarItems 加 ETF 监控 |
| 6 | type-check + build 验证 | done | npm run build 通过 |

无遗留 todo 或 waived。

## 维度 3：验收标准完成度

### 前端验收

- [x] etfMonitorApi 4 方法类型正确，apiClient.get 泛型为 {success, data} — **通过**：4 方法泛型均为 `apiClient.get<{ success: boolean; data: XxxData }>`（api.ts:1368/1386/1403/1413）
- [x] etfMonitorTypes.ts 类型与后端响应 camelCase 字段一致 — **通过**：字段名 hasData/tradeDate/indexName/etfCount/totalShare/totalShareChange/totalNetInflow/tsCode/unitNav/share/shareChange/netInflow/changePercent 全 camelCase，与后端 `_dict_to_camel` 输出一致
- [x] 4 SWR hooks 正确解包（.then(res=>res.data)），条件 hook 传 null key 不请求 — **通过**：useEtfIndexRankings/useEtfIndexDetail/useEtfTrend/useEtfLatestDate 均 `.then(res => res.data)`；useEtfIndexDetail（indexName null → key null）、useEtfTrend（targetCode null → key null）条件 hook 正确
- [x] 侧边栏出现"ETF 监控"，点击跳转 /dashboard/etf-monitor 渲染占位页 — **通过**：TC-4.1/4.3/4.4 E2E green
- [x] 现有导航项与 active 高亮不受影响（降级回归） — **通过**：TC-4.6 E2E green

### 降级回归验收（架构 §8.2）

- [x] 新增导航项不遮挡/破坏现有布局结构 — **通过**：TC-4.6 + build 全路由正常

### E2E / 验收

- [x] E2E-TDD 导航可点击跳转 — **通过**：TC-4.1~4.6 green（6 passed）
- [x] `npm run build` 通过 — **通过**：退出码 0
- [ ] `npm run lint` 通过 — **豁免**：仓库存在 272 个 pre-existing lint errors（brief 记 192，实际 272，均位于测试/helper 文件与 api.ts 既有 `any` 代码），plan-04 新增文件零新增 lint 错误（grep etfMonitorTypes/useEtfMonitor/etf-monitor/page 无命中）；按 brief 约定 lint 非 blocker，权威验证为 build + E2E

## 维度 6：E2E 质量证据

- red 证据：`docs/e2e/evidence/plan-04-e2e-red-2026-07-29.md`，结论"预期失败，测试有效"（5 failed/1 passed/12 skipped）
- green 证据：`docs/e2e/evidence/plan-04-e2e-green-2026-07-29.md`，结论"通过（TC-4.1~4.6，6 passed/12 skipped）"
- 两份证据均覆盖 plan-04 feat_id，spec 文件同一份（`web/tests/e2e/etf-monitor.spec.ts`），red→green 反转由实现驱动

## 维度 7：边界与风险

| 场景 | 处理方式 | 状态 |
| --- | --- | --- |
| apiClient baseURL 已含 /api/v1 | endpoint 不带 /api/v1，避免双前缀 | done（endpoint 全部 `/etf-monitor/*`，无 /api/v1） |
| 条件 hook 无 indexName/targetCode | 传 null key，SWR 不请求 | done（useEtfIndexDetail/useEtfTrend null key 实现） |
| 后端 {success,data} 包裹 | hook 内 .then(res=>res.data) 解包再取 .data | done（4 hooks 均解包，组件读 data?.data） |
| 未登录访问 | middleware 路由守卫拦截（现有机制） | done（无 middleware.ts，客户端 AuthContext 拦截，现有机制） |
| 导航 icon 缺失 | lucide-react 取 TrendingUp | done（DashboardLayout.tsx:5 import + :35 使用） |

风险备注（严禁 lib/fetcher.ts、严禁 Redux slice）已遵守：useEtfMonitor.ts 仅用 apiClient + SWR。

## 维度 8：契约对齐

API 契约四件套（架构 §7.3/§7.6）逐项核对：

| 项 | 契约 | 实现 | 结果 |
| --- | --- | --- | --- |
| endpoint | 不带 /api/v1（baseURL 已含） | `/etf-monitor/index-rankings`、`/etf-monitor/index-detail`、`/etf-monitor/trend`、`/etf-monitor/latest-date` | 通过（拼接后 `/api/v1/etf-monitor/*`，无双前缀） |
| HTTP 方法 | apiClient.get（带鉴权） | 4 方法均 `apiClient.get`（ApiClient.get 经 getAuthHeaders） | 通过 |
| query 命名 | snake_case | category/trade_date/sort_by/index_name/target_type/target_code/metric/days/end_date/page/page_size/order | 通过（query 名全 snake_case） |
| query 值 | sort_by/metric 用 camelCase（§7.6 特例） | sort_by: netInflow/shareChange/share；metric: share/netInflow | 通过（值 camelCase，未下划线化） |
| 响应字段 | {success,data} 包裹，data camelCase | 泛型 `{ success: boolean; data: XxxData }`；types 全 camelCase | 通过 |

下游消费方（plan-05）所需契约已暴露：etfMonitorApi（4 方法）、4 hooks（返回 camelCase 业务对象 + isLoading/isValidating/isError/mutate）、types（EtfIndexRankingsData 等）。无契约偏差。

## 维度 9：代码审查

### 阻塞项（blocker）

无。

### 正确性核对

1. **apiClient 单例复用**：`useEtfMonitor.ts:15` `import { etfMonitorApi } from '@/lib/api'`，etfMonitorApi 内部用具名导出的 `apiClient` 单例（api.ts:140），baseURL = `${NEXT_PUBLIC_API_URL}/api/v1`。endpoint 拼接正确，无双前缀。
2. **SWR 解包层级**：fetcher `.then(res => res.data)`——res 是 `ApiResponse<T>`（{ data?: T }），.data 取其 body `{ success, data }`；hook 返回 `data?.data` 取业务对象。与 useSectorFundFlow 一致。注：实现用 `res.data as unknown as {...}` 类型断言处理 ApiResponse 泛型擦除，运行时正确，类型安全。
3. **条件 hook null key**：useEtfIndexDetail（indexName null → key null）、useEtfTrend（targetCode null → key null），SWR 收到 null key 不发请求。useEtfIndexRankings/useEtfLatestDate 始终启用（hasData=false 时返回空态供判断）。符合 §3 实现规格。
4. **'use client' 缺失风险**：page.tsx:13 `'use client'` 已加（/dashboard 路由必须，否则 build error 污染 E2E）；DashboardLayout.tsx:1 已有 'use client'。build 退出码 0 证实无 use client 缺失问题。
5. **未用 lib/fetcher.ts**：grep 确认 etfMonitor 三个新文件 + api.ts etfMonitorApi 均未 import lib/fetcher.ts（仅 useEtfMonitor.ts 注释提及"不直接用"）。严格遵守风险备注。
6. **复用范式**：etfMonitorApi 仿 sectorFundFlowApi（api.ts:1086）；useEtfMonitor 仿 useSectorFundFlow（SWR 数组 key + .then(res=>res.data) + SWR_OPTIONS = revalidateOnFocus:false/revalidateOnReconnect:true/dedupingInterval:30000 + 深路径导入）；types 仿 fundFlowTypes（全 camelCase）。深路径导入未改 hooks/index.ts，符合 §3 #3。

### 安全审查

无安全隐患：apiClient.get 经 getAuthHeaders 注入鉴权；无 SSRFS/注入面；无敏感信息泄露。

### 建议项（非阻塞）

无。代码结构清晰、注释充分、范式与现有 sector-fund-flow 一致，可维护性良好。

## 维度 5：验证命令输出摘要

```bash
cd /Users/muchao/code/sector-strength/web

# 1. npm run build（plan-04 §6 验证命令）
$ npm run build
→ 退出码 0
→ 路由输出含 `/dashboard/etf-monitor`（○ Static prerender）

# 2. E2E（plan-04 §5）
$ npx playwright test tests/e2e/etf-monitor.spec.ts --project=admin
→ 6 passed (TC-4.1~4.6), 12 skipped (TC-5.1~5.12 plan-05 骨架)
→ 耗时 1.6s

# 3. npm run lint（plan-04 §6 验证命令）
$ npm run lint
→ 退出码 1（272 problems，全 pre-existing：测试/helper 文件 + api.ts 既有 any）
→ plan-04 新增文件（etfMonitorTypes/useEtfMonitor/etf-monitor/page）零新增 lint 错误
→ 按 brief 约定非 plan-04 blocker（权威验证 = build 退出码 0 + E2E green）
```

## blocker 列表

无。

## 状态变更记录

- plan-04 frontmatter `status`: `review → done`
- README §7.2 开发状态机 plan-04 行同步（按 brief 不修改 README §7.2 状态机表，状态机由 auto-dev 维护；本 review 仅改功能文件 frontmatter）
