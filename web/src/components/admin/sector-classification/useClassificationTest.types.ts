import type { ClassificationTestResult, UseClassificationTestReturn } from '@/types/admin-test'

/**
 * useClassificationTest Hook 类型定义
 *
 * @description
 * 导出共享的类型定义，避免循环依赖
 */

// 重新导出类型
export type { ClassificationTestResult, UseClassificationTestReturn }
